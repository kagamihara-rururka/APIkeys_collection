# 開發日誌

最後更新：2026-06-03

這份文件從 2026-05-21 起持續記錄開發歷史，並已依 GitHub Actions push run 反推回補 2026-05-17 以後的流水帳。它不是取代 `PROJECT_GTD.md` 或 `AGENT_HANDOFF.zh-TW.md`：GTD 管目前進度與下一步，handoff 管接力狀態，開發日誌管「每個版本怎麼走到現在、哪個點可當 checkpoint、還有什麼風險」。

時間以 GitHub Actions `createdAt` 轉為 Asia/Taipei (UTC+8) 分組。
日期區塊與同日內時間都採倒序排列：最近日期在最上方，同一天內最新 run 也在最上方。

## 標記規則

- `**CHECKPOINT**`：該 push 的 GitHub Actions 結論為 success，可作為回溯穩定點。
- `**CI 失敗**`：該 push 的 GitHub Actions 結論為 failure；保留在流水帳中，用來看見修復脈絡。
- 每筆使用表格欄位：`時間`、`開發階段`、`標記`、`SHA`、`Run`、`原始標題`、`中文說明`。
- `開發階段` 是粗粒度階段標籤，用來讓人一眼分辨當前工作屬於 `MVP Demo Closure`、`MVP Hardening`、`Database / Repair`、`Discovery / Crawler`、`Docs / Workflow` 等哪一段；新 checkpoint 必須填寫，不要只藏在中文說明裡。
- 日期區塊與同日內時間都倒序，讓最近期 checkpoint 一打開就能看到。

### 2026-06-03

| 時間 | 階段 | 狀態 | SHA | Run | Commit | 變更與驗證 |
| --- | --- | --- | --- | --- | --- | --- |
| 00:50 | Core Readiness / Manifest Reference | **FULL SMOKE PASS** | `pending` | `pending` | pending | 新增 `--core-manifest-reference-report-json` / `core_manifest_reference_report.v1`，把 download sidecar manifest、Visual/Skin manifest reference、registry persistence projection、ready-event manifest context 與 readiness bridge 收成 Core-only diagnostic。這不新增 manifest schema、不建立正式 user DB persistence、不做 payload health check、不讀 renderer payload / `.npz`、不 import displaytools / visual-compressor、不做 cross-repo manifest consumer contract；status 保守維持 `partial`。已驗證：focused `py_compile` OK；focused tests 19 tests 通過；broader related tests 73 tests 通過；JSON stdout 可由 downstream `json.load(sys.stdin)` 解析，輸出 `schema_version=core_manifest_reference_report.v1`、`status=partial`；完整 smoke `state\logs\pre_push_smoke_20260603_005322.log` 通過 1115 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。 |
| 00:27 | Core Readiness / Lifecycle | **CI PASS** | `ccd4ece` | `26833987071` | Add lifecycle audit report JSON | 新增 `--core-lifecycle-audit-json` / `core_lifecycle_audit_report.v1`，把 lifecycle vocabulary、status display profiles、ready / terminal / review / construction status classification、ready-event guard 與 missing runtime state machine 收成 Core-only audit。這不新增 lifecycle status、不改 lifecycle schema、不定義 transition persistence、不實作 runtime state machine、不 import displaytools / visual-compressor、不讀 `.npz` / renderer payload、不做 cross-repo builder lifecycle adapter；status 保守維持 `partial`。已驗證：focused `py_compile` OK；focused tests 15 tests 通過；broader related tests 44 tests 通過；JSON stdout 可由 downstream `json.load(sys.stdin)` 解析，輸出 `schema_version=core_lifecycle_audit_report.v1`、`status=partial`、`status_count=7`；完整 smoke `state\logs\pre_push_smoke_20260603_003047.log` 通過 1111 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26833987071` 通過 Ubuntu、Windows 與 real DB smoke。 |
| 00:23 | Core Readiness / Job Status | **CI PASS** | `95439ee` | `26833115060` | Add job-status report JSON | 新增 `--core-job-status-report-json` / `core_job_status_report.v1`，把 Visual/Skin lifecycle statuses、auto event disabled guard、explicit event writer contract、Tk background job policy registry、single-flight start result、SQLite write gate、missing unified bounded scheduler 與 `o_1` triggers 收成 Core-only diagnostic。這不新增 scheduler schema、不改 lifecycle schema/status、不啟用 auto lifecycle event、不 import displaytools / visual-compressor、不讀 `.npz` / renderer payload、不做 RendererSkinAsset / SkinAsset 或 compression integration；status 保守維持 `partial`。已驗證：focused `py_compile` OK；focused tests 11 tests 通過；broader related tests 50 tests 通過；JSON stdout 可由 downstream `json.load(sys.stdin)` 解析，輸出 `schema_version=core_job_status_report.v1`、`status=partial`、`bounded_tk_policy_count=11`；完整 smoke `state\logs\pre_push_smoke_20260603_001447.log` 通過 1107 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26833115060` 通過 Ubuntu、Windows 與 real DB smoke。 |

### 2026-06-02

| 時間 | 階段 | 狀態 | SHA | Run | Commit | 變更與驗證 |
| --- | --- | --- | --- | --- | --- | --- |
| 23:51 | Core Readiness / Review Gate | **CI PASS** | `96fa464` | `26832005970` | Add review-required report JSON | 新增 `--core-review-required-report-json` / `core_review_required_report.v1`，把 content review rules、unknown fallback、visual `review_required` lifecycle surface、missing review queue persistence、blocked unsupported payload format 與 `o_1` triggers 收成 Core-only diagnostic。這不新增 review queue schema、不改 lifecycle schema/status、不改 review workflow、不 import displaytools / visual-compressor、不讀 `.npz` / renderer payload、不做 SkinAsset/RendererSkinAsset 或 compression integration；status 保守維持 `partial`。已驗證：focused `py_compile` OK；focused tests 11 tests 通過；broader related tests 52 tests 通過；JSON stdout 可由 downstream `json.load(sys.stdin)` 解析，輸出 `schema_version=core_review_required_report.v1`、`status=partial`；GitHub Actions manual run `26832005970` 通過 Ubuntu、Windows 與 real DB smoke。 |
| 23:40 | Core Readiness / Planning | **CI PASS** | `41a043e` | `26831097725` | Add core integration readiness audit | 新增 `docs/CORE_INTEGRATION_PLANNING_GATE_READINESS.zh-TW.md`，把 `core_readiness_report.v1` 的 registry / lifecycle / manifest reference / review_required / job-status / asset-lineage evidence 整理成 Integration Planning Gate readiness gap table、`o_1` review triggers、JSON quality check 與下一批 Core-only slices。這是 docs-only planning / audit note，不改產品碼、不改 lifecycle schema/status、不 import displaytools / visual-compressor、不讀 `.npz` / renderer payload、不做 RendererSkinAsset / SkinAsset 或 compression integration，也不把 `partial` gate 改成 ready。已驗證：`--core-readiness-report-json` JSON parse OK，輸出 `schema_version=core_readiness_report.v1`、gate `partial`；changed docs mojibake scan OK；`git diff --check` OK；GitHub Actions manual run `26831097725` 通過 Ubuntu、Windows 與 real DB smoke。 |
| 22:14 | Core Readiness / Diagnostics | **CI PASS** | `0ead886` | `26825598254` | Add readiness report JSON diagnostic | 新增 `--core-readiness-report-json`，由 `api_launcher/core_readiness_report.py` / `api_launcher/cli_core_readiness.py` 輸出 `core_readiness_report.v1`，聚合 crawler registry、content registry、dataset adapter、lifecycle contract、manifest reference、review_required、job status 與 asset lineage evidence；`integration_planning_gate.status` 保守維持 `partial`，並明確列出 missing / blocked / contract_only / planned surfaces。這是 RRKAL Core control-plane evidence aggregation，不是 renderer/compressor integration，不實作 RendererSkinAsset / SkinAsset、不讀 `.npz` / renderer payload、不改 lifecycle schema/status、不跨 repo。已驗證：focused compile OK；focused tests 8 tests 通過；broader related tests 53 tests 通過；JSON stdout 可由下游 `json.load(sys.stdin)` 解析；完整 smoke `state\logs\pre_push_smoke_20260602_215026.log` 通過 1101 tests / 4 skipped；GitHub Actions manual run `26825598254` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：本輪同步 GTD / handoff / development log，並修補 `WORKFLOW`、`DOCS_INDEX`、`VISUAL_SKIN_ASSET_CONTRACT` 中仍把 `L:\AGENT_EXCHANGE` 寫成 active route 的漂移；Notion `Agents討論區` 是協調 dashboard，GitHub commits/tests/smoke/CI 才是產品證據。 |
| 19:41 | CLI / Consolidation | **CI PASS** | `afbbd36` | `26817283123` | Split handoff CLI helper | 新增 `api_launcher/cli_handoff.py`，把 `--handoff-report` 與 `--handoff-report-json` 的 argparse、active check、JSON stdout 判斷、Markdown 寫檔與 dispatch 從 `core.py` 抽成專責 helper；`core.py` 只保留 orchestration 呼叫，`cli_flags.command_requested()` 也改用同一個 helper 判斷。這是 consolidation slice，用來防止 `core.py` 繼續吸收 handoff report CLI 責任；不改 handoff payload、不改 JSON schema、不改 Web/Tk、crawler、download/import、Visual/Skin lifecycle、RendererSkinAsset / SkinAsset integration，也不跨 repo。已驗證：不寫 pyc 的 `compile()` 檢查通過；`py -3 -B -m unittest tests.test_handoff tests.test_cli_flags tests.test_cli_json -v` 通過 24 tests；實跑 `--handoff-report-json` 可被下游 Python `json.load(sys.stdin)` 解析，主要 key 包含 `generated_at`、`git_head`、`git_status`、`mvp_readiness`；實跑 `--handoff-report PATH` 會寫出非空 Markdown；完整 smoke `state\logs\pre_push_smoke_20260602_185926.log` 通過 1096 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26817283123` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log，並把 `AGENT_START_HERE` 的跨 agent 協調入口從已廢止的 `L:\AGENT_EXCHANGE` 改為 Notion `Agents討論區`；本輪不需要 user guide。 |
| 18:40 | CLI / Consolidation | **CI PASS** | `d5dc92c` | `26814370211` | Split MVP CLI helper | 新增 `api_launcher/cli_mvp.py`，把 `--write-mvp-demo-flow`、`--run-mvp-demo-smoke-json`、`--mvp-readiness-json`、`--write-mvp-readiness-json` 的 argparse、active check、JSON stdout 判斷、MVP flow 寫檔、offline smoke 與 readiness JSON dispatch 從 `core.py` 抽成專責 helper；`core.py` 只保留 orchestration 呼叫。這是 consolidation slice，用來防止 `core.py` 持續吸收 MVP diagnostic/smoke CLI 責任；不改 MVP demo flow payload、不改 offline smoke 行為、不改 readiness payload、不改 Web/Tk、crawler、download/import、Visual/Skin 或 CI workflow。已驗證：不寫 pyc 的 `compile()` 檢查通過；`py -3 -B -m unittest tests.test_handoff tests.test_mvp_demo_flow tests.test_ingestion_pipeline tests.test_cli_flags tests.test_cli_json -v` 通過 32 tests；實跑 `--mvp-readiness-json` 可被下游 Python `json.load(sys.stdin)` 解析；實跑 `--write-mvp-demo-flow` 與 `--write-mvp-readiness-json` 會寫出 flow / readiness 檔；完整 smoke `state\logs\pre_push_smoke_20260602_183340.log` 通過 1096 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26814370211` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 18:24 | CLI / Consolidation | **CI PASS** | `f549dc5` | `26813587586` | Split project maturity CLI helper | 新增 `api_launcher/cli_project_maturity.py`，把 `--project-maturity-json`、`--write-project-maturity-json`、`--project-maturity-markdown` 的 argparse、active check、JSON / Markdown / file output dispatch 從 `core.py` 抽成專責 helper；`core.py` 只保留 orchestration 呼叫，`cli_flags.command_requested()` 也改用同一個 helper 判斷。這是 consolidation slice，用來防止 `core.py` 持續吸收診斷報表責任；不改 maturity payload、不改 markdown render、不改 Web/Tk、crawler、download/import、Visual/Skin 或 CI workflow。已驗證：不寫 pyc 的 `compile()` 檢查通過；`py -3 -B -m unittest tests.test_project_maturity tests.test_cli_flags tests.test_cli_json -v` 通過 9 tests；實跑 `--project-maturity-json` 可被下游 Python `json.load(sys.stdin)` 解析；實跑 `--write-project-maturity-json` 與 `--project-maturity-markdown` 會寫出 JSON / Markdown 檔；完整 smoke `state\logs\pre_push_smoke_20260602_181653.log` 通過 1096 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26813587586` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 18:09 | CLI / Consolidation | **CI PASS** | `8efdd9a` | `26812924715` | Split registry report CLI helpers | 新增 `api_launcher/cli_registry_reports.py`，把 crawler/content/dataset-adapter 三個 registry report JSON 旗標、active check 與 dispatch 從 `core.py` 抽成專責 helper；`core.py` 只保留 orchestration 呼叫，`cli_flags.command_requested()` 也改用同一個 helper 判斷。這是 consolidation slice，用來防止 `core.py` 持續吸收 registry report 責任；不改三個 report payload、不改 source crawler、content registry、adapter report、download/import、Web/Tk 或 maturity 計算。已驗證：不寫 pyc 的 `compile()` 檢查通過；`py -3 -B -m unittest tests.test_developer_diagnostics tests.test_content_registry tests.test_dataset_adapters tests.test_cli_flags tests.test_cli_json -v` 通過 26 tests；實跑 `--crawler-registry-report-json` / `--content-registry-report-json` / `--dataset-adapter-report-json` 皆可被下游 Python `json.load(sys.stdin)` 解析，分別輸出 14 / 6 / 3；完整 smoke `state\logs\pre_push_smoke_20260602_180235.log` 通過 1096 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26812924715` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 17:54 | CLI / Deep Adapter Diagnostics | **CI PASS** | `cabdbe0` | `26812174056` | Add dataset adapter report CLI JSON | 新增 `--dataset-adapter-report-json`，讓 `dataset_adapter_report()` 可直接由 CLI 輸出 agent-readable JSON，列出現有 provider-specific deep adapter inventory、adapter ids、provider ids、registered adapter metadata 與 coverage boundary；同時納入 `json_stdout_mode()` 與 `command_requested()`，確保 `--init-db --seed --dataset-adapter-report-json` stdout 是純 JSON。這是 developer/agent 診斷入口，不新增 adapter、不改 adapter discovery、不改 source crawler、download/import、Web/Tk 或 maturity 計算邏輯。已驗證：不寫 pyc 的 `compile()` 檢查通過；`py -3 -B -m unittest tests.test_dataset_adapters tests.test_cli_json -v` 通過 6 tests；實跑 `--dataset-adapter-report-json` 可被下游 Python `json.load(sys.stdin)` 解析；第一次完整 smoke `state\logs\pre_push_smoke_20260602_174437.log` 在 MVP demo CLI 讀 `APIkeys_collection.py` 時遇到 L 槽 transient `[Errno 13] Permission denied`，立即重跑 `state\logs\pre_push_smoke_20260602_174739.log` 通過 1096 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26812174056` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 17:37 | Maturity Matrix / Deep Adapters | **CI PASS** | `9a3c767` | `26811334953` | Expose dataset adapter maturity evidence | `api_launcher.dataset_adapters` 新增 `DatasetAdapterRegistryEntry`、`dataset_adapter_registry_entries()` 與 `dataset_adapter_report()`，讓 provider-specific deep adapter coverage 變成可查 payload；`--project-maturity-json` 的 `provider_specific_deep_adapters` row 現在列出 `gebco_topography`、`hyg_star_catalog`、`yfinance_market_data` 的 provider id、class/module、delivery boundary、supported formats、status 與 coverage boundary，並明確標示 deep adapter coverage 不等於 14 個 source crawler type 覆蓋。這不新增 adapter、不改 `adapters_for_provider()` 行為、不改下載 / 匯入 / Web / Tk / source crawler handler。已驗證：不寫 pyc 的 `compile()` 檢查通過；`py -3 -B -m unittest tests.test_dataset_adapters tests.test_project_maturity -v` 通過 9 tests；`--project-maturity-json` 可見 `registered_adapters` 與 `coverage_not_equal_to_supported_source_types=true`；完整 smoke `state\logs\pre_push_smoke_20260602_173017.log` 通過 1095 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26811334953` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 17:19 | Maturity Matrix / Crawler Asset | **CI PASS** | `a7521ec` | `26810453332` | Expose crawler asset maturity evidence | `--project-maturity-json` 的 `crawler_asset_download_import` row 已補上可查 metrics：formal asset / seed download-import service、recommended seed closure service、shared display payload contract、agent-readable CLI surfaces、Web/Tk surfaces、credential-blocking flag 與 partial 原因。這是成熟度矩陣證據 surface，不改 Web/Tk/CLI 操作、不改下載或匯入行為、不新增 provider adapter、不新增 content parser，也不把此 row 升級成 100%。已驗證：`py -3 -B -m py_compile api_launcher\project_maturity.py tests\test_project_maturity.py` OK；`py -3 -B -m unittest tests.test_project_maturity -v` 通過 6 tests；`--project-maturity-json` 可見 `crawler_asset_download_import.metrics`；完整 smoke `state\logs\pre_push_smoke_20260602_171323.log` 通過 1092 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26810453332` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 17:06 | Content Parser / Import Contract | **CI PASS** | `543e510` | `26809788678` | Add content registry report CLI JSON | 新增 `--content-registry-report-json`，讓 `api_launcher/content_registry.py` 的 `content_registry_report()` 可直接由 CLI 輸出 agent-readable JSON，列出 direct SQLite importer、resolver-backed format、review rule 與 unknown fallback。這是 content parser/import contract 的診斷 surface，不新增 NetCDF / GeoTIFF / Parquet parser、不改 CSV/JSON/Socrata bounded sample 匯入行為、不改下載、匯入、Web/Tk 顯示、adapter review 或 maturity 計算。已驗證：`py -3 -B -m py_compile api_launcher\core.py api_launcher\cli_flags.py tests\test_content_registry.py` OK；`py -3 -B -m unittest tests.test_content_registry tests.test_cli_json -v` 通過 17 tests；PowerShell pipe 實測 `--content-registry-report-json` 可被下游 Python `json.load(sys.stdin)` 解析；完整 smoke `state\logs\pre_push_smoke_20260602_165908.log` 通過 1092 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26809788678` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 16:42 | Content Parser / Import Contract | **CI PASS** | `def2e02` | `26808977279` | Declarativize content registry review rules | `api_launcher/content_registry.py` 新增 `ContentReviewRule` / `CONTENT_REVIEW_RULES` / `content_registry_report()`，把 archive、scientific grid、geospatial asset、columnar table、database snapshot、document/markup 的 review lane 收斂為可查 declarative contract；`--project-maturity-json` 的 content parser/import metrics 也輸出 review rule count、resolver-backed format count 與 registry report。這不新增 NetCDF / GeoTIFF / Parquet parser，不改 CSV/JSON/Socrata bounded sample 的可匯入行為，不改下載、匯入、Web/Tk 顯示或 adapter review 決策。已驗證：focused `py_compile` OK；`py -3 -B -m unittest tests.test_content_registry tests.test_project_maturity -v` 通過 20 tests；完整 smoke `state\logs\pre_push_smoke_20260602_164342.log` 通過 1091 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26808977279` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 16:24 | Scheduler / Tk Background Jobs | **CI PASS** | `c640092` | `26808107355` | Add Tk background job start result contract | `frontends/tk/background_jobs.py` 新增 `TkBackgroundJobStartResult` 與 `start_single_flight_thread_result()`，讓 single-flight helper 可結構化回報 `started` / `duplicate` / `capacity`、active job count 與 capacity limit；既有 `start_single_flight_thread()` 仍維持 bool 相容回傳。`--project-maturity-json` 的 background scheduler metrics 也暴露 `single_flight_start_result_contract` 與 guard test。這是 scheduler diagnostics hardening，不是統一 bounded job scheduler，不改 Tk workflow 行為、不改 UI 文案、不改 crawler/download/import/credential/OAuth/SQLite write gate，也不做 asyncio 重寫。已驗證：focused `py_compile` OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_project_maturity -v` 通過 16 tests；完整 smoke `state\logs\pre_push_smoke_20260602_162611.log` 通過 1090 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26808107355` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 16:07 | OpenSpec / Visual Asset Persistence | **CI PASS** | `6f0bc21` | `26807302644` | Archive visual registry persistence OpenSpec | 已完成 `visual-asset-registry-persistence` OpenSpec 歸檔：active change 移至 `openspec/changes/archive/2026-06-02-visual-asset-registry-persistence/`，delta spec 同步成正式 `openspec/specs/visual-asset-registry-persistence/spec.md`。這是 governance/spec archival，不改產品碼、Visual/Skin registry runtime、owned test DB helper、ready event writer、UI、crawler/download/import 或 renderer integration。已驗證：OpenSpec status 顯示 artifacts complete；tasks 全部 `[x]`；`npx.cmd -y @fission-ai/openspec@latest validate --all --no-interactive` 通過 2 specs；完整 smoke `state\logs\pre_push_smoke_20260602_160845.log` 通過 1089 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26807302644` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 15:52 | Tests / CLI Guardrail | **CI PASS** | `3489d00` | `26806558103` | Guard CLI JSON stdout helper usage | `tests/test_cli_json.py` 新增 repository-level guard，掃描 `api_launcher/**/*.py` 並禁止 `print(json.dumps(...))` 這種直接 stdout JSON 輸出，避免未來 agent-readable JSON 繞過 `print_cli_json()`；寫檔 JSON 使用 `json.dumps(..., ensure_ascii=False)` 仍允許。這是測試護欄，不改產品行為、payload shape、Web/Tk、crawler/download/import/repair/visual registry。已驗證：`py -3 -B -m unittest tests.test_cli_json tests.test_developer_diagnostics tests.test_project_maturity tests.test_handoff -v` 通過 33 tests；`py_compile` focused test OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260602_155337.log` 通過 1089 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26806558103` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 15:34 | CLI / JSON Encoding | **CI PASS** | `8555d26` | `26805931030` | Use CLI JSON helper in subcommands | `api_launcher.cli_json.print_cli_json()` 已從 `core.py` 主入口延伸到子 CLI JSON stdout path：dataset discovery、crawler assets、visual asset registry、manual import、download plan、database repair、crawler run summary 皆改用同一個 ASCII-escaped stdout helper；`api_launcher` 內不再有直接 `print(json.dumps(...))`。寫檔 JSON 仍保留 UTF-8 / `ensure_ascii=False`。這只改 agent-readable stdout encoding safety，不改 payload shape、Web/Tk 顯示、crawler/download/import/repair/visual registry 語意。已驗證：focused `py_compile` OK；`git diff --check` OK；`rg` 確認無直接 JSON stdout print；focused unittest 通過 272 tests；PowerShell pipe 實測 dataset discovery handler smoke、crawler run summary、正式 crawler asset listing、visual registry summary JSON 可由下游 Python `json.load(sys.stdin)` parse；完整 smoke `state\logs\pre_push_smoke_20260602_153808.log` 通過 1088 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26805931030` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 15:12 | CLI / JSON Encoding | **CI PASS** | `3559c72` | `26804806195` | Centralize CLI JSON stdout escaping | 新增 `api_launcher.cli_json.print_cli_json()` / `cli_json_dumps()`，把 `core.py` 主要 agent-readable JSON stdout mode 收斂成同一個 ASCII-escaped stdout helper；寫檔 JSON 繼續使用 UTF-8 與 `ensure_ascii=False`，因此人類可讀檔案不退化。已接入口包含 verify downloads、MVP demo smoke、MVP readiness、project maturity、crawler registry report、adapter review、adapter resolver summary、handoff、heartbeat、library actions、data-store test 與 database self-check JSON。這只改 stdout encoding safety，不改 payload 語意、Web/Tk、crawler/download/import 或 renderer 行為。已驗證：`py -3 -B -m unittest tests.test_cli_json tests.test_developer_diagnostics tests.test_project_maturity tests.test_handoff tests.test_heartbeat tests.test_repair tests.test_dataset_download_plan tests.test_adapter_plan_resolver tests.test_library_actions tests.test_data_store_connections tests.test_database_self_check -v` 通過 208 tests；`py_compile` focused files OK；`git diff --check` OK；PowerShell pipe 實測 project maturity / crawler registry / MVP readiness / handoff JSON 可由下游 Python parse；完整 smoke `state\logs\pre_push_smoke_20260602_151343.log` 通過 1088 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26804806195` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 08:40 | CLI / Project Maturity | **CI PASS** | `5351820` | `26791299700` | Guard project maturity JSON stdout for Windows pipelines | `--project-maturity-json` stdout 改為 ASCII-escaped JSON，避免 Windows PowerShell legacy pipeline / redirection 把成熟度矩陣的中文與 emoji label 轉成 `????`；寫檔輸出仍保留 UTF-8 中文。這不改成熟度矩陣語意、不改 UI、不改 crawler/download/import/renderer 行為。已驗證：`py -3 -B -m unittest tests.test_project_maturity -v` 通過 6 tests；`py -3 -B -m py_compile api_launcher\core.py tests\test_project_maturity.py` OK；PowerShell pipeline `py -3 -B APIkeys_collection.py --project-maturity-json | py -3 -B -c "import sys,json; ..."` 可正常 parse 並輸出 Unicode escape；完整 smoke `state\logs\pre_push_smoke_20260602_084206.log` 通過 1087 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26791299700` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 08:31 | OpenSpec / Visual Asset Persistence | **LOCAL DOCS PASS** | `pending` | `not needed` | Mark visual registry persistence OpenSpec ready to archive | `openspec/changes/visual-asset-registry-persistence/tasks.md` 已全數勾選完成，表示 Visual/Skin registry persistence change 已完成 dry-run schema、owned test persistence、read-side summary、CLI JSON/debug endpoint、explicit ready-event boundary、duplicate-event guard、ordinary upsert no-auto-event guard，以及 docs/checks/smoke/CI 證據。這是 OpenSpec task state 收尾，不改產品行為、不接 UI、不接正式 user DB repository、不讀 renderer payload、不 import displaytools / visual-compressor / vis_2_dis；尚未執行 OpenSpec archive。Docs drift check：已同步 handoff / development log；本輪不需要 user guide。 |
| 08:16 | Control Plane / Visual Asset Persistence | **CI PASS** | `830bd3d` | `26790548355` | Add explicit visual registry ready-event CLI | 新增 `log_visual_asset_ready_from_owned_test_database()` 與 `--visual-registry-emit-ready-event-json`，完成 OpenSpec task 4.1-4.3。此入口只從明確 acknowledged 的 RRKAL owned test DB 讀取指定 persisted `ready` registry entry，然後顯式寫入 `visual_asset_ready` event；預設 duplicate policy 拒絕同一 registry entry / skin asset 的重複 ready event，必須明確 allow-duplicate 才能重複。ordinary table write/upsert 仍不會自動發 lifecycle event，persistence module 不 import event writer。這不是 UI integration、不是正式 user DB repository，不做 migration、不讀 manifest / `.npz` / renderer payload、不 import displaytools / visual-compressor / vis_2_dis。已驗證：source compile OK；`py -3 -B -m unittest tests.test_visual_asset_event_logging tests.test_visual_asset_registry_cli tests.test_visual_asset_registry_persistence tests.test_project_maturity tests.test_visual_asset_contracts -v` 通過 51 tests；完整 smoke `state\logs\pre_push_smoke_20260602_081900.log` 通過 1087 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26790548355` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 Visual/Skin contract doc / maturity matrix / GTD / handoff / OpenSpec tasks / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 08:10 | Control Plane / Visual Asset Persistence | **CI PASS** | `8e14f81` | `26789781759` | Add visual registry summary CLI JSON | 新增 `api_launcher.cli_visual_asset_registry` 並接上 `--visual-registry-summary-json` / `--visual-registry-summary-db` / `--visual-registry-owned-test-db`，完成 OpenSpec task 3.3。CLI 只讀明確 acknowledged 的 RRKAL owned test DB，輸出 lifecycle summary JSON；沒有 visual DB 時回傳空 summary 且不建檔。這不是 UI integration、不是正式 user DB repository，不做 migration、不讀 manifest / `.npz` / renderer payload、不 import displaytools / visual-compressor / vis_2_dis、不發 lifecycle event。已驗證：`py -3 -B -m unittest tests.test_visual_asset_registry_cli tests.test_visual_asset_registry_persistence tests.test_project_maturity tests.test_visual_asset_contracts -v` 通過 42 tests；完整 smoke `state\logs\pre_push_smoke_20260602_075755.log` 通過 1082 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26789781759` 通過 Ubuntu、Windows 與 real DB smoke；Windows subprocess test 明確使用 UTF-8 capture，避免 cp950 破壞含中文/emoji 的 JSON。Docs drift check：已同步 GTD / handoff / OpenSpec tasks / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 07:50 | Control Plane / Visual Asset Persistence | **CI PASS** | `b2d639f` | `26789141598` | Add owned visual registry summary helper | 新增 `visual_asset_registry_summary_for_owned_test_database()`，完成 OpenSpec task 3.1-3.2。helper 必須明確傳入 `allow_owned_test_database=True`；DB 不存在時只回傳空 summary 且不建檔，DB 存在時必須有 RRKAL owned marker，否則拒絕。payload 只含 lifecycle counts、status display profiles、review count、renderer target counts、DB ownership/table flags 與 safety flags；不讀 manifest / `.npz` / renderer payload、不 import displaytools / visual-compressor / vis_2_dis、不發 `visual_asset_ready` event。已驗證：`py -3 -B -m unittest tests.test_visual_asset_registry_persistence tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 40 tests；完整 smoke `state\logs\pre_push_smoke_20260602_074220.log` 通過 1080 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26789141598` 通過 Ubuntu、Windows 與 real DB smoke；`--project-maturity-json` 可見 owned-test summary contract 且 renderer row 仍是 `contract_only`。Docs drift check：已同步 Visual/Skin contract doc / maturity matrix / GTD / handoff / OpenSpec tasks / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 07:35 | Control Plane / Visual Asset Persistence | **CI PASS** | `4a8dfb8` | `26788569680` | Add owned visual registry rollback preview | 新增 `visual_asset_registry_owned_test_drop_preview()`，完成 OpenSpec task 2.4。helper 必須明確傳入 `allow_owned_test_database=True` 並確認既有 SQLite DB 有 RRKAL owned marker；回傳可審閱 DROP INDEX / DROP TABLE SQL preview。這是 dry-run，不執行 DROP、不刪 DB、不改 registry row、不發 `visual_asset_ready` event、不讀 renderer payload、不 import displaytools / visual-compressor / vis_2_dis，也會拒絕非 owned SQLite DB。已驗證：`py -3 -B -m unittest tests.test_visual_asset_registry_persistence tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 36 tests；完整 smoke `state\logs\pre_push_smoke_20260602_072740.log` 通過 1076 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26788569680` 通過 Ubuntu、Windows 與 real DB smoke；maturity payload 可見 owned-test drop preview contract。Docs drift check：已同步 Visual/Skin contract doc / maturity matrix / GTD / handoff / OpenSpec tasks / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 07:20 | Control Plane / Visual Asset Persistence | **CI PASS** | `5cf29d2` | `26787944543` | Add owned visual registry write/read helpers | 新增 `write_visual_asset_registry_entry_for_owned_test_database()`、`read_visual_asset_registry_entry_payload_for_owned_test_database()`、`list_visual_asset_registry_entry_payloads_for_owned_test_database()`，完成 OpenSpec task 2.2-2.3。write helper 只在明確 `allow_owned_test_database=True` 的 RRKAL owned test SQLite DB 中 upsert registry row，且 row shape 來自 `visual_asset_registry_entry_persistence_record()`；read/list 回傳 `RendererSkinAssetRegistryEntry` 相容 control-plane payload。這不是正式 migration 或 user DB persistence，不讀 renderer payload、不 import displaytools / visual-compressor / vis_2_dis、不自動發 `visual_asset_ready` event，也會拒絕非 owned SQLite DB。已驗證：`py -3 -B -m unittest tests.test_visual_asset_registry_persistence tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 33 tests；`--project-maturity-json` 可見 owned-test write/read/list helper contract 且 renderer row 仍是 `contract_only`；完整 smoke `state\logs\pre_push_smoke_20260602_071210.log` 通過，1073 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26787944543` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 Visual/Skin contract doc / maturity matrix / GTD / handoff / OpenSpec tasks / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 06:52 | Control Plane / Visual Asset Persistence | **CI PASS** | `5143e9e` | `26787163638` | Add owned test visual registry table helper | 新增 `api_launcher.visual_asset_registry_persistence.create_visual_asset_registry_table_for_owned_test_database()`，完成 OpenSpec task 2.1。helper 必須明確傳入 `allow_owned_test_database=True`，會建立 RRKAL owned marker table，再 materialize `visual_skin_asset_registry` table/index；未 opt-in 不建檔，既有非 owned SQLite DB 會被拒絕。這不是正式 migration 或 user DB persistence，不寫 registry row、不讀 renderer payload、不發 ready event。已驗證：focused compile OK；`py -3 -B -m unittest tests.test_visual_asset_registry_persistence tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 29 tests；`--project-maturity-json` 可見 helper contract；完整 smoke `state\logs\pre_push_smoke_20260602_065359.log` 通過，1069 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26787163638` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 Visual/Skin contract doc / maturity matrix / GTD / handoff / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 06:38 | Control Plane / Visual Asset Persistence | **CI PASS** | `3a5c956` | `26786612078` | Add visual registry DDL dry-run preview | 新增 `visual_asset_registry_sqlite_ddl_preview()`，由 `visual_asset_registry_persistence_schema()` 產生可審閱 SQLite `CREATE TABLE` / `CREATE INDEX` dry-run SQL，並把 contract 暴露到 project maturity renderer metrics；OpenSpec task 1.1-1.3 與本輪 docs task 已勾選。這不是 DB persistence：不連 SQLite、不建表、不寫檔、不自動發 ready event、不讀 renderer payload，renderer row 仍是 `contract_only`。已驗證：focused compile OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 26 tests；`--project-maturity-json` 抽查可見 `visual_asset_registry_sqlite_ddl_preview` 與 `sqlite_ddl_dry_run`；完整 smoke `state\logs\pre_push_smoke_20260602_064026.log` 通過，1066 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26786612078` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 Visual/Skin contract doc / maturity matrix / GTD / handoff / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 06:31 | OpenSpec / Visual Asset Persistence | **LOCAL SPEC PASS** | `pending` | `not needed` | Propose visual registry persistence OpenSpec | 建立 `openspec/changes/visual-asset-registry-persistence/`，把 Visual/Skin registry persistence 實作前契約收成 proposal / design / spec / tasks：repository / migration 層必須消費既有 schema contract 與 row projection，persistence 只保存 control-plane row，不讀 `.npz`、GPU buffer、renderer project 或 payload bytes；table write 不得自動發 ready event，未來 event emission 必須走 explicit workflow。已驗證：`openspec status --change visual-asset-registry-persistence` 完整；`openspec validate --all --no-interactive` 通過；OpenSpec mojibake scan 通過。Docs drift check：已同步 GTD / handoff / development log；這不是 persistence 實作，不改產品碼、UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 06:24 | Docs / Workflow | **LOCAL DOCS PASS** | `pending` | `not needed` | Correct OpenSpec L workspace boundary | 修補 `openspec/specs/development-workflow/spec.md` 的 workspace drift：canonical cloud workspace 從舊 `K:\APIkeys_collection` 更新為 `L:\RRKAL_project`，並明確標示舊 K 槽只作 historical/read-only reference。這是 workflow/spec contract 修補，不改產品碼、crawler、download/import、Visual/Skin contract、UI 或 GitHub workflow。Docs drift check：已同步 GTD / handoff / development log；本輪不需要 user guide。 |
| 06:17 | Control Plane / Visual Asset Contract | **CI PASS** | `d59b32f` | `26785426976` | Add visual registry persistence row projection | 新增 `visual_asset_registry_entry_persistence_record()`，把 `RendererSkinAssetRegistryEntry` 投影成一筆符合 `visual_asset_registry_persistence_schema()` 的扁平 row。`renderer_targets` 與 bounded metadata 會以 JSON 字串保存，metadata 會過濾明顯 `payload` / `secret` / `token` / `api_key` / `.npz` / GPU buffer 類 key。這仍不寫資料庫、不建表、不做 migration、不發 lifecycle event、不讀 renderer payload，也不 import displaytools / visual-compressor / vis_2_dis。已驗證：source compile check OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 25 tests；`--project-maturity-json` 抽查 `visual_asset_registry_entry_persistence_record_contract=visual_asset_registry_entry_persistence_record`；完整 smoke `state\logs\pre_push_smoke_20260602_061359.log` 通過，1065 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26785426976` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Visual/Skin contract doc / maturity matrix / docs registry / development log；本輪不改 crawler、download/import、credential、renderer I/O 或 user guide。 |
| 06:05 | Control Plane / Visual Asset Contract | **CI PASS** | `39a458f` | `26784919740` | Add visual asset registry persistence schema | 新增 `VisualAssetRegistryColumn` 與 `visual_asset_registry_persistence_schema()`，先把未來 `visual_skin_asset_registry` 的欄位、index、allowed lifecycle status 與 migration guard 做成 machine-readable contract。這是 `schema_contract_only`：不自動建表、不連 SQLite / MySQL、不寫 registry persistence、不自動 lifecycle emission，也不讀 `.npz`、GPU buffer 或 renderer payload；若未來要落地 DB persistence，仍需 OpenSpec / migration guard。已驗證：source compile check OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 24 tests；`--project-maturity-json` 抽查 `visual_asset_registry_persistence_schema_contract=visual_asset_registry_persistence_schema`，且 migration guard 為 `create_table_automatically=false`、`auto_event_emission=false`；完整 smoke `state\logs\pre_push_smoke_20260602_060226.log` 通過，1064 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26784919740` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Visual/Skin contract doc / maturity matrix / docs registry / development log；本輪不改 crawler、download/import、credential、renderer I/O 或 user guide。 |
| 05:46 | Control Plane / Visual Asset Contract | **CI PASS** | `8f5276f` | `26784156831` | Include display profiles in visual registry summary | `visual_asset_registry_summary()` 現在會在 `status_counts` 旁輸出 `status_display_profiles`，讓 dashboard / future UI 能直接使用後端定義的 icon / tone / label / next_action 呈現 lifecycle count，不需要自己維護 status 對照表。summary 仍只彙總 registry control-plane rows，不掃 manifest、不讀 `.npz`、不做 payload health check，也不 import displaytools / visual-compressor / vis_2_dis。已驗證：source compile check OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 23 tests；`--project-maturity-json` 抽查 empty registry summary 會輸出 `status_display_profiles`；完整 smoke `state\logs\pre_push_smoke_20260602_054647.log` 通過，1063 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26784156831` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Visual/Skin contract doc / maturity matrix / docs registry / development log；本輪不改 crawler、download/import、credential、renderer I/O 或 user guide。 |
| 05:36 | Control Plane / Visual Asset Contract | **CI PASS** | `b996203` | `26783664461` | Include display profile in skin build results | `SkinBuildResult.to_dict()` 現在即使沒有 `skin_asset` 也會輸出 `lifecycle_status_display_profile`，讓 failed / review-required build result 可以直接交給 UI 顯示 tone / label / next_action，不需要 Tk/Web/Qt 自行依狀態分支判斷。這不是 builder 執行、不是 renderer integration、不是 payload reader；它只是 control-plane result payload 的顯示契約。已驗證：source compile check OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 23 tests；完整 smoke `state\logs\pre_push_smoke_20260602_053645.log` 通過，1063 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26783664461` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Visual/Skin contract doc / development log；本輪不改 crawler、download/import、credential、renderer I/O 或 user guide。 |
| 05:28 | Control Plane / Visual Asset Contract | **CI PASS** | `3f00721` | `26783277810` | Add visual asset lifecycle display profile | 新增 `skin_asset_status_display_profile()`，把 Visual/Skin lifecycle status 轉成 UI-neutral payload：`status_icon`、`display_tone`、`display_label`、`next_action`、`is_ready`、`is_terminal`、`review_required`、`construction`。`planned` / `building` / `review_required` 會明確標成施工中或 review 類顯示狀態，讓 Tk/Web/未來 Qt 不必自己推論，也避免把 contract-only renderer 能力顯示成已交付。這仍是 control-plane display contract，不讀 renderer payload、不 import displaytools / visual-compressor / vis_2_dis。已驗證：source compile check OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 22 tests；`--project-maturity-json` 抽查 `skin_asset_lifecycle_display_profile_contract=skin_asset_status_display_profile`；完整 smoke `state\logs\pre_push_smoke_20260602_052828.log` 通過，1062 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26783277810` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Visual/Skin contract doc / maturity matrix / architecture / docs index / docs registry / development log；本輪不改 crawler、download/import、credential、renderer I/O 或 user guide。 |
| 05:15 | Control Plane / Visual Asset Contract | **CI PASS** | `5fb7a9e` | `26782746568` | Add registry entry visual asset event logger | 新增 `api_launcher.visual_asset_event_logging.log_visual_asset_ready_registry_entry()`，讓未來 registry persistence 或 explicit workflow 可把 `ready` `RendererSkinAssetRegistryEntry` 轉成 `VisualAssetReadyEvent`，再透過同一份 bounded writer 寫入 `visual_asset_ready` event。非 `ready` / review / failed entry 會被 ready-event factory 拒絕；這不是 registry persistence，不是自動 lifecycle emission，不在 import 時寫 log，不 import displaytools / visual-compressor / vis_2_dis，也不讀 `.npz` 或 renderer payload。已驗證：source compile check OK；`py -3 -B -m unittest tests.test_visual_asset_event_logging tests.test_project_maturity -v` 通過 10 tests；`--project-maturity-json` 抽查 `visual_asset_ready_registry_entry_log_writer_contract=log_visual_asset_ready_registry_entry`；完整 smoke `state\logs\pre_push_smoke_20260602_051514.log` 通過，1061 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26782746568` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Visual/Skin contract doc / maturity matrix / architecture / docs index / docs registry / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 05:06 | Control Plane / Visual Asset Contract | **CI PASS** | `84b3acb` | `26782033964` | Add visual asset ready event logger | 新增 `api_launcher.visual_asset_event_logging.log_visual_asset_ready_event()`，用 bounded `visual_asset_ready_event_log_context()` 顯式寫入 `visual_asset_ready` JSONL event，並支援 `log_event_func` 注入與 `log_path`。這不是自動 lifecycle emission，不在 import 時寫 log，不接 registry persistence，不 import displaytools / visual-compressor / vis_2_dis、不讀 renderer payload；event context 仍只含 bounded manifest reference、lineage、status、renderer targets、safety flags 與白名單 metadata。已驗證：source compile check OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_visual_asset_event_logging tests.test_project_maturity -v` 通過 23 tests；`--project-maturity-json` 抽查 `visual_asset_ready_event_log_writer_contract=log_visual_asset_ready_event`、`control_plane_only=true`、`payload_loading=false` OK；完整 smoke `state\logs\pre_push_smoke_20260602_050142.log` 通過，1059 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26782033964` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Visual/Skin contract doc / maturity matrix / architecture / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 04:55 | Control Plane / Visual Asset Contract | **CI PASS** | `3ed3a31` | `26781404396` | Add visual asset ready event log context | 新增 `visual_asset_ready_event_log_context()`，把 `VisualAssetReadyEvent` 投影成 bounded event-log context。context 只輸出 manifest reference、lineage、status、renderer targets、asset format、checksum、size、generated_by、emitted_at 與 safety flags；任意 metadata、secret、token、payload bytes、renderer internals 不會進入 context，只保留白名單 `registry_entry_id` 與 `projection_type`。這不是 runtime event log writer，不呼叫 `log_event()`、不 import displaytools / visual-compressor / vis_2_dis、不讀 renderer payload。已驗證：source compile check OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 21 tests；`--project-maturity-json` 抽查 `visual_asset_ready_event_log_context_contract=visual_asset_ready_event_log_context`、`control_plane_only=true`、`payload_loading=false` OK；完整 smoke `state\logs\pre_push_smoke_20260602_044955.log` 通過，1057 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26781404396` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Visual/Skin contract doc / maturity matrix / architecture / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 04:35 | Control Plane / Visual Asset Contract | **CI PASS** | `8ac89d8` | `26780838475` | Add visual asset ready event factory | 新增 `visual_asset_ready_event_from_registry_entry()`，從 `RendererSkinAssetRegistryEntry` 安全產生 `VisualAssetReadyEvent`：只接受 `ready` registry entry，source request lineage 由 `skin_asset.source_request_id` 自動帶入，metadata 補 `registry_entry_id` 與 projection type。這只是 event object factory，不寫 runtime event log、不呼叫 renderer、不 import displaytools / visual-compressor / vis_2_dis、不讀 renderer payload；下一步若接 event log，仍只能寫 manifest reference。已驗證：source compile check OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 20 tests；`--project-maturity-json` 抽查 factory contract、`control_plane_only=true`、`payload_loading=false` OK；完整 smoke `state\logs\pre_push_smoke_20260602_043651.log` 通過，1056 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26780838475` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Visual/Skin contract doc / maturity matrix / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 04:24 | Control Plane / Visual Asset Contract | **CI PASS** | `24225c4` | `26780195487` | Add visual skin manifest projection | 新增 `renderer_skin_asset_manifest_projection()`，讓 RRKAL Core 可把 `RendererSkinAssetRegistryEntry` 投影成 compact cross-project manifest reference envelope；projection 只輸出 registry entry id、skin asset id、manifest path、lifecycle status/label、renderer targets、checksum、lineage 與 safety flags，不輸出完整 source request internals、不讀 renderer payload、不 import displaytools / visual-compressor / vis_2_dis。Project maturity renderer row 新增 `visual_skin_asset_manifest_projection_contract=renderer_skin_asset_manifest_projection`，矩陣版本更新為 `2026-06-02`。已驗證：focused source compile check OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 18 tests；`--project-maturity-json` 抽查 projection contract、`control_plane_only=true`、`payload_loading=false` OK；完整 smoke `state\logs\pre_push_smoke_20260602_042601.log` 通過，1054 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26780195487` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Visual/Skin contract doc / maturity matrix / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 04:18 | Docs / Visual Asset Contract | **LOCAL DOCS PASS** | `pending` | `not needed` | Document visual skin asset contract | 新增 `docs/VISUAL_SKIN_ASSET_CONTRACT.zh-TW.md`，把 Visual/Skin Asset control-plane contract 從 handoff/GTD/architecture 的分散描述收成正式專題文件：列出 `CuratedDataAssetReference`、`SkinBuildRequest`、`RendererSkinAssetReference`、`SkinBuildResult`、`VisualAssetReadyEvent`、`RendererSkinAssetRegistryEntry`、`visual_asset_registry_summary()`、lifecycle status、lineage guard、已驗證能力、尚未實作能力與下一步。同步 `DOCS_INDEX`、`DOCS_REGISTRY`、GTD 與 handoff。Docs drift check：本輪只修文件入口，不改產品碼、UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 04:06 | Control Plane / Visual Asset Contract | **CI PASS** | `bbf74ca` | `26779213359` | Guard visual asset registry lineage | 補強 `RendererSkinAssetRegistryEntry` lineage guard：source request id、source curated asset id、latest build result request id 與 result skin asset id 若和 registry entry 的 `skin_asset` 對不上，會在建立時 fail-fast，避免未來 visual asset registry 記錄錯誤 lineage。這仍是 control-plane guard，不接資料庫 persistence、不 import displaytools / visual-compressor / vis_2_dis、不讀 renderer payload，也不改 renderer row 的 `contract_only` 定位。已驗證：`py -3 -B -m py_compile api_launcher\visual_asset_contracts.py api_launcher\project_maturity.py tests\test_visual_asset_contracts.py tests\test_project_maturity.py` OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 17 tests；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260602_040723.log` 通過，1053 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26779213359` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |
| 03:51 | Control Plane / Visual Asset Contract | **CI PASS** | `0bcd4fb` | `26778639797` | Add visual asset registry contract entry | 延續 Visual/Skin Asset control-plane contract，新增 `RendererSkinAssetRegistryEntry` 與 `visual_asset_registry_summary()`，讓 RRKAL Core 可登錄 renderer-ready manifest reference 與 lifecycle summary，但仍不讀 `.npz`、GPU buffer、renderer project file 或 payload bytes，也不 import displaytools、visual-compressor、vis_2_dis、Taichi/PyQt。Project maturity renderer row 新增 `visual_skin_asset_registry_entry_contract=RendererSkinAssetRegistryEntry` 與 `empty_visual_asset_registry_summary`，明確標示目前只是 registry contract surface、沒有假裝已有 renderer asset records。已驗證：`py -3 -B -m py_compile api_launcher\visual_asset_contracts.py api_launcher\project_maturity.py tests\test_visual_asset_contracts.py tests\test_project_maturity.py` OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 14 tests；`--project-maturity-json` 抽查 registry contract 與 empty summary OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260602_035442.log` 通過，1050 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26778639797` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不改 UI、crawler、download/import、credential、renderer I/O 或 user guide。 |

### 2026-06-01

| 時間 | 階段 | 狀態 | SHA | Run | Commit | 變更與驗證 |
| --- | --- | --- | --- | --- | --- | --- |
| 21:56 | Control Plane / Visual Asset Contract | **CI PASS** | `20aabed` | `26759592378` | Add visual asset control-plane contracts | 新增 `api_launcher/visual_asset_contracts.py` 與 `tests/test_visual_asset_contracts.py`，先把未來 RendererSkinAsset 的 `SkinBuildRequest`、`SkinBuildResult`、`RendererSkinAssetReference`、`VisualAssetReadyEvent` 與 lifecycle status 收成 RRKAL Core control-plane contract。這只是 manifest reference / lifecycle / lineage / job-status 草案，不 import displaytools、visual-compressor、vis_2_dis，不讀 `.npz`、GPU buffer、Qt/Taichi payload 或 renderer 專案檔；project maturity renderer row 仍標為 `contract_only` / `🚧`，並輸出 `visual_skin_asset_contract_schema=api_launcher.visual_asset_contracts`。已驗證：`py -3 -B -m py_compile api_launcher\visual_asset_contracts.py api_launcher\project_maturity.py tests\test_visual_asset_contracts.py tests\test_project_maturity.py` OK；`py -3 -B -m unittest tests.test_visual_asset_contracts tests.test_project_maturity -v` 通過 12 tests；完整 smoke `state\logs\pre_push_smoke_20260601_215243.log` 通過，1048 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26759592378` 通過 Ubuntu、Windows 與 real DB smoke。 |
| 21:43 | Docs / Workflow | **PUSHED DOCS** | `77d2b53` | `not needed` | Clarify active L workspace boundary | 使用者明確收緊工作區邊界：`L:\RRKAL_project` 是 RRKAL active workspace / commit source；`K:` 只作歷史紀錄、舊狀態查詢與必要資料參考，不再主動掃描、治理或寫入。本輪同步 `AGENT_START_HERE`、`WORKFLOW`、`DOCS_INDEX`、`PROJECT_GTD` 與 `AGENT_HANDOFF`；交換區檢查無新的 RRKAL `Status: new` entry。這是 docs drift 修補，不改產品碼、Web/Tk、crawler、download/import 或 credential flow。 |

### 2026-05-31

| 時間 | 階段 | 狀態 | SHA | Run | Commit | 變更與驗證 |
| --- | --- | --- | --- | --- | --- | --- |
| 15:44 | UI Contract / Web Display | **SMOKE PASS** | `pending` | `local smoke` | pending | Web Preview `assetInitials()` 只使用 `asset.display_name`；缺顯示名稱時使用中性 `RR`，不再從 `provider_id` / `asset_id` 取可見 initials。route key、button action、search/debug/provenance、provider display fallback、crawler/download/import 行為不變。已驗證：`node --check frontends\web\static\display_contract.js` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過 65 tests；完整 smoke `state\logs\pre_push_smoke_20260531_154516.log` 通過，1042 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：已同步 GTD / handoff / development log；本輪不改 user guide。 |
| 15:36 | UI Contract / Tk Seed | **SMOKE PASS** | `pending` | `local smoke` | pending | Recommended-seed closure payload 補 `recommended_seed_display_label`，後端會從 `seed_page.seeds` 找回推薦 seed 的人類標題；Tk closure 完成 / 未完成訊息顯示 seed title 或「seed 待確認」，不再把 `recommended_seed_uid` 當主要文案。closure failure status 也改用 `closure_stage_label`，不再顯示 raw `closure_stage`。raw uid 仍保留在 route、event context、payload 與 provenance；closure 選 seed 規則、formal seed download/import、Web 路由與 event summary 不變。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_closure.py frontends\tk\crawler_asset_ui_helpers.py tests\test_crawler_asset_download.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_crawler_asset_download tests.test_tk_ui_helpers -v` 通過 38 tests；完整 smoke `state\logs\pre_push_smoke_20260531_153712.log` 通過，1042 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：已同步 GTD / handoff / development log；本輪不改 user guide。 |
| 15:22 | UI Contract / Tk Seed | **SMOKE PASS** | `pending` | `local smoke` | pending | Formal seed download/import payload 補 `dataset_title` / `seed_display_label`，shared display payload 也轉出這兩個欄位；Tk Seed 清單 dialog、sidebar preview 與 seed download/import 完成訊息改用 `crawler_seed_dialog_display_title()`，主要 seed 標題不再 fallback 到 raw `dataset_uid` / `dataset_id`。raw id 仍保留在 Seed ID 欄位、action payload、event context、route key 與 provenance；seed ownership validation、favorite key、schema probe entry、resolved plan、SQLite import、Web JS、crawler registry 不變。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_download.py api_launcher\crawler_asset_service.py api_launcher\crawler_asset_display.py frontends\tk\crawler_asset_seed_dialog.py frontends\tk\crawler_asset_ui_helpers.py tests\test_crawler_asset_download.py tests\test_tk_dialogs.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_crawler_asset_download tests.test_tk_dialogs tests.test_tk_ui_helpers -v` 通過 182 tests；完整 smoke `state\logs\pre_push_smoke_20260531_152543.log` 通過，1041 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；`git diff --check` OK；checkpoint 前交換區無新的 RRKAL `Status: new`。Docs drift check：已同步 GTD / handoff / development log；本輪不改 user guide。 |
| 15:00 | UI Contract / Web Consolidation | **CI PASS** | `ac9b1b2` | `26706046879` | Log Web runtime helper consolidation | Web Preview 繼續把純 runtime/filter/progress display helper 收斂到 `frontends/web/static/display_contract.js`：`serverRuntimeLabel()`、`serverRuntimeTitle()`、`sourceTypeFilterLabel()`、`boundedPercent()` 從 `app.js` 移出（code commit `e247aeb`），`app.js` 只傳入 health / assets 等狀態並保留互動 orchestration。這是 Web 顯示 helper ownership cleanup，不改 Web API、crawler/download/import/credential policy、event payload、route/search/debug raw id、Tk 顯示或後端 display contract。交換區：已評審 `a_1` 的跨專案數據-渲染配置合約草案，決策為 `backlogged`；零導入 JSON bridge 方向可採納，但未來必須以 RRKAL 既有 `*.manifest.json` sidecar contract 為基礎做版本化投影，不把 displaytools renderer config 混進 ingestion。已驗證：`node --check frontends\web\static\display_contract.js` OK；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v tests.test_web_preview` 通過 65 tests；Browser reload 驗證 `http://127.0.0.1:8765/` 顯示 23 個 crawler asset、13 個來源範式且無 console error；完整 smoke `state\logs\pre_push_smoke_20260531_145744.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26706046879` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log；本輪不改 user guide。 |
| 14:39 | UI Contract / Web Consolidation | **CI PASS** | `6f6d86a` | `26705678488` | Move Web display state helpers | Web Preview 繼續把純 display state helper 收斂到 `frontends/web/static/display_contract.js`：`toneClass()`、`flowStatusClass()`、`credentialDisplayProfile()` 從 `app.js` 移出；`tests.test_web_preview` 補 ownership guard，避免這批 CSS tone / flow status / credential display-profile 投影 helper 回流到互動 orchestration 檔。這是 Web 顯示 helper ownership cleanup，不改 CSS class 名稱、Web API、crawler/download/import/credential policy、event payload、route/search/debug raw id、Tk 顯示或後端 display contract。已驗證：`node --check frontends\web\static\display_contract.js` OK；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v tests.test_web_preview` 通過 65 tests；完整 smoke `state\logs\pre_push_smoke_20260531_144050.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26705678488` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log，並回補 CI run id。 |
| 14:26 | UI Contract / Web Consolidation | **CI PASS** | `65daaa7` | `26705462035` | Move remaining Web display helpers | Web Preview 延續 display-contract helper consolidation，把閉環比例文案、能力位址 / 能力膠囊摘要與 bounds preset label 等純顯示 helper 從 `app.js` 移到 `frontends/web/static/display_contract.js`；`tests.test_web_preview` 補 ownership guard，確認這批 helper 不回流 `app.js`，並確認 `index.html` 先載 `display_contract.js` 再載 `app.js`。這是 Web 顯示 helper ownership cleanup，不改 Web API、crawler/download/import/credential policy、event payload、route/search/debug raw id、Tk 顯示或後端 display contract。已驗證：`node --check frontends\web\static\display_contract.js` OK；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v tests.test_web_preview` 通過 65 tests；完整 smoke `state\logs\pre_push_smoke_20260531_142859.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26705462035` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log，並回補 CI run id。 |
| 14:09 | CI / Web Guardrail | **CI PASS** | `6ca8553` | `26705184272` | Add Web static syntax CI guard | GitHub Actions CI workflow 新增 Web static syntax step：`node --check frontends/web/static/display_contract.js` 與 `node --check frontends/web/static/app.js` 會在 Python tests 前執行，讓 Web Preview JS syntax regression 不只被本地 pre-push smoke 擋住。這是 CI/workflow hardening，不改 Web/Tk/crawler/download/import/credential 行為。已驗證：本地 `node --check` 兩檔 OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260531_141215.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26705184272` 通過 Ubuntu、Windows 與 real DB smoke，且新增的 `Web static syntax` step 已在 CI 執行。Docs drift check：已同步 GTD / handoff / development log；修正 amend 後 commit SHA 與 CI run 記錄，避免後續接手誤讀成未驗證。 |
| 13:53 | UI Contract / Web Consolidation | **CI PASS** | `949d587` | `26704873947` | Extract more Web display helpers | Web Preview 延續 display-contract helper extraction，把下載 / 匯入 stage/next-action、asset/seed display text、flow/plan/review/stale passport label、content review summary、status/capability/bounds field label/help、asset initials 與 source type/surface helper 從 `app.js` 移到 `frontends/web/static/display_contract.js`；新增靜態 ownership guard，避免這批純顯示 helper 回流到 `app.js`。這是 Web 顯示 helper ownership cleanup，不改 Web API、crawler/download/import/credential policy、event payload、route/search/debug raw id、Tk 顯示或後端 display contract。已驗證：`node --check frontends\web\static\display_contract.js` OK；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v tests.test_web_preview` 通過 65 tests；`frontends\web\static\display_contract.js` mojibake scan OK；`git diff --check` 無 whitespace error（Git 仍提示 Web static files line-ending warning）；完整 smoke `state\logs\pre_push_smoke_20260531_135629.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26704873947` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log。 |
| 13:36 | UI Contract / Web Consolidation | **LOCAL PASS** | `77515c5` | `not run yet` | Extract Web display contract helpers | Web Preview 新增 `frontends/web/static/display_contract.js`，集中 `displayTextOrFallback()`、backend-token guard、event context label、content review / pipeline lane / parser label 與 provider display helper；`index.html` 先載 helper 再載 `app.js`。這是 Web 顯示 helper 解耦，不改 Web API、crawler/download/import/credential policy、event payload、route/search/debug raw id 或 Tk 顯示。交換區已回覆 displaytools 的 ViewModel producer contract 建議，決策為 `backlogged`，並在 GTD 留下未來 OpenSpec/ADR backlog。已驗證：`node --check frontends\web\static\display_contract.js` OK；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v tests.test_web_preview` 通過 65 tests；docs / `display_contract.js` mojibake scan OK；`git diff --check` 無 whitespace error（Git 仍提示 Web static files line-ending warning）；完整 smoke `state\logs\pre_push_smoke_20260531_133812.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log。 |
| 13:26 | UI Contract / Web Display | **LOCAL PASS** | `1cdcb45` | `not run yet` | Label Web asset card providers | Web Preview asset card slot 副標改走 `providerDisplayText(asset)`，不再直接以 `asset.provider_id` 當 provider 顯示名稱；provider id 仍保留在 payload、route/search/debug、credential flow、download/import、crawler registry 與 event context。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v tests.test_web_preview` 通過 65 tests；docs mojibake scan OK；`git diff --check` 無 whitespace error（Git 仍提示 `frontends/web/static/app.js` line-ending warning）；完整 smoke `state\logs\pre_push_smoke_20260531_132658.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log。 |
| 13:17 | UI Contract / Web Display | **LOCAL PASS** | `42e9ae9` | `not run yet` | Label Web event context chips | Web Preview 事件紀錄 context chip 加上 display adapter：context key 顯示「資產 ID」「執行紀錄」「下一步」等人類欄位名，`next_action` / `user_next_action` scalar value 缺 label 時顯示「下一步待確認」，不再直接把 raw event key 或 raw action id 當可見 chip 文案。event payload、event context summary、JSON/debug、recent-events API、event log storage、crawler/download/import/Tk 流程不變。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v tests.test_web_preview` 通過 65 tests；docs mojibake scan OK；`git diff --check` 無 whitespace error（Git 仍提示 `frontends/web/static/app.js` line-ending warning）；完整 smoke `state\logs\pre_push_smoke_20260531_131800.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log。 |
| 13:06 | UI Contract / Web Display | **LOCAL PASS** | `1104369` | `not run yet` | Hide Web download context tokens | Web Preview 下載 / 匯入結果列的 context chip 改顯示「爬蟲資產路徑」「下載 / 匯入管線」，不再把 `crawler_asset_path` / `download_import_pipeline` backend trace token 直接放進使用者可見 chip。download/import payload、artifacts、route key、event/provenance、plan outcome、callback diagnostics、Tk 顯示、crawler registry 與正式下載 / 匯入流程不變。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v tests.test_web_preview` 通過 65 tests；docs mojibake scan OK；`git diff --check` 無 whitespace error（Git 仍提示 `frontends/web/static/app.js` line-ending warning）；完整 smoke `state\logs\pre_push_smoke_20260531_130753.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log。 |
| 12:56 | UI Contract / Web Display | **LOCAL PASS** | `8c4b51c` | `not run yet` | Hide Web seed row title ids | Web Preview seed row 主標題改走 `seedDisplayText()`，不再以 `dataset_id` / `uid` 作主標題 fallback；dataset id 仍保留在小字追溯欄，明確標為 `Dataset ID：...`，缺追溯值時顯示「Seed ID 待確認」。favorite key、route key、download uid、seed ownership validation、seed download/import endpoint 與本機 seed catalog 不變。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v tests.test_web_preview` 通過 65 tests；docs mojibake scan OK；`git diff --check` 無 whitespace error（Git 仍提示 `frontends/web/static/app.js` line-ending warning）；完整 smoke `state\logs\pre_push_smoke_20260531_125801.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log。 |
| 12:47 | UI Contract / Web Display | **LOCAL PASS** | `4142389` | `not run yet` | Hide Web downloader title raw ids | Web Preview 下載器結果列與推薦 seed 面板標題改走 `assetDisplayText()` / `seedDisplayText()`，缺 label 時顯示「爬蟲資產待確認」「seed 待確認」，不再把 `payload.asset_id`、`result.asset_id`、`recommended_seed_uid` 當主要標題 fallback。raw ids 仍保留在 payload、route key、favorite key、writeJson/debug 與 provenance；Web API、seed recommendation service、download/import、crawler registry、credential flow、Tk 顯示與 project maturity 不變。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v tests.test_web_preview` 通過 65 tests；docs mojibake scan OK；`git diff --check` 無 whitespace error（Git 仍提示 `frontends/web/static/app.js` line-ending warning）；完整 smoke `state\logs\pre_push_smoke_20260531_124839.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log。 |
| 12:31 | UI Contract / Web Display | **CI PASS** | `2c68f2e` | `26703440667` | Hide Web mission raw ids | Web Preview mission queue 的資產 / seed detail 已改用 `assetDisplayText()` / `seedDisplayText()`：asset-level download/import、seed download/import、recommended-seed closure、seed schema probe、seed listing、credential-blocked build-plan 與 credential-save mission 都不再把 `assetId`、`datasetUid` 或 `recommended_seed_uid` 當主要使用者文案。raw ids 仍保留在 route key、JSON/debug、writeJson 與 provenance；Web API shape、crawler registry、seed enumeration、schema probe service、download/import service、credential storage、Tk 顯示與 project maturity 不變。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v tests.test_web_preview` 通過 65 tests；docs mojibake scan OK；`git diff --check` 無 whitespace error（Git 仍提示 `frontends/web/static/app.js` line-ending warning）；完整 smoke `state\logs\pre_push_smoke_20260531_123324.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26703440667` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log。 |
| 12:21 | Docs / Workflow | **LOCAL DOCS** | `docs-only` | `not needed` | Document agent exchange workflow | 將 `L:\AGENT_EXCHANGE` 跨 agent 交換區規則寫回 RRKAL repo 的工作流文件：`AGENT_START_HERE` 增加 session start / checkpoint close 收信與回覆義務，`WORKFLOW` 增加開工命令、工作區邊界與 checkpoint close 檢查步驟，`DOCS_INDEX` 增加「檢查跨 agent 建議」閱讀路線。邊界維持：交換區不上 GitHub、不是 source of truth、原始信件不複製進公開 repo；採納建議後才轉成 RRKAL 的 GTD / handoff / docs / OpenSpec / code slice。本輪不改產品碼、Web/Tk、crawler、download/import 或 credential flow；交換區檢查無新的 RRKAL `Status: new` 收信。 |
| 11:56 | UI Contract / Web Display | **CI PASS** | `2878b35` | `26702672659` | Require Web source type display labels | Web Preview `sourceTypeDisplayText()` 的最後 raw fallback 已移除：source type filter、asset card、Passport 與 selected hero 只顯示後端 `source_type_label` / capability profile label，缺 label 時顯示「來源範式待確認」，不再用 `shortPattern(source_type)` 把 raw source id 美化成假人類文案。raw `source_type` 仍保留在 payload、filter key、route key、search haystack、JSON/debug 與 developer diagnostics；crawler registry、source type dispatch、capability profile、download/import、credential flow、Tk 顯示與 project maturity 不變。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v "tests.test_web_preview"` 通過 65 tests；第一次完整 smoke `state\logs\pre_push_smoke_20260531_115255.log` 在 L 槽雲端 transient `Unable to read current working directory` 失敗，立即重跑 `state\logs\pre_push_smoke_20260531_115307.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26702672659` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log；本輪只調整 Web source-type visible fallback，不改 backend payload shape、crawler dispatch、download/import、credential flow 或 Tk。 |
| 11:45 | UI Contract / Web Display | **CI PASS** | `f498178` | `26702495054` | Hide Web label helper fallback ids | Web Preview 的登入設定完成 mission、capability label 與 bounds field label helper 再收斂：登入狀態、能力與欄位只顯示後端 label、本地已知 label map 或中性 fallback，不再把 `status.status`、`capability_id`、`field_id` 當可見文案候選。raw ids 仍保留在 payload、JSON/debug、路由/表單 key 與追溯資料裡；credential save、capability metadata、bounds form schema、download/import、crawler registry、Tk 顯示與 project maturity 不變。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v "tests.test_web_preview"` 通過 65 tests；完整 smoke `state\logs\pre_push_smoke_20260531_114156.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26702495054` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log；本輪只調整 Web visible label fallback，不改 backend payload shape、credential save、capability schema、bounds form schema、crawler/download/import/credential flow 或 Tk。 |
| 11:32 | UI Contract / Web Display | **CI PASS** | `a081fdb` | `26702264196` | Hide Web content display fallback ids | Web Preview 的 flow step label、event object context、content review bucket、content pipeline lane 與 parser summary 都只消費後端 display label / short label / stage label / status label 或中性 fallback，不再把 `step_id`、`review_bucket`、`pipeline_lane`、`parser_id`、`source_format`、raw `stage/status` 當可見文案候選。raw ids 仍保留在 payload、JSON/debug、event context 與搜尋/追溯資料裡；adapter review、content parser/import 判斷、event log shape、download/import、crawler registry、Tk 顯示與 project maturity 不變。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v "tests.test_web_preview"` 通過 65 tests；完整 smoke `state\logs\pre_push_smoke_20260531_112945.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26702264196` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log；本輪只調整 Web visible fallback display，不改 backend payload shape、event schema、adapter review、content parser/import、crawler/download/import/credential flow 或 Tk。 |
| 11:19 | UI Contract / Web Crawler | **CI PASS** | `30419dc` | `26702034041` | Hide Web next action fallback ids | Web Preview 使用者可見 next-action fallback 再收斂：Downloader row、seed 枚舉 mission、Crawler Passport、credential badge、plan preview 狀態 / mission、selected hero、Plan Passport 與 stale passport 都只顯示後端 `*_next_action_label` 或中性 fallback，不再把 raw `next_action` / `stale_next_action` snake_case id 放進畫面文案。raw ids 仍保留在 payload、JSON/debug、search haystack 或控制流程 comparison 給 agent 與程式使用；crawler registry、capability profile、download/import、credential flow、Tk 顯示與 project maturity 不變。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest -v "tests.test_web_preview"` 通過 65 tests；完整 smoke `state\logs\pre_push_smoke_20260531_111535.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26702034041` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log；本輪只調整 Web visible fallback display，不改 backend payload shape、next-action ids、route behavior、crawler/download/import/credential flow 或 Tk。 |
| 11:00 | UI Contract / Crawler Capability | **CI PASS** | `47b779c` | `26701734284` | Label crawler capability dimensions | Crawler capability profile 補上後端 display labels：`source_family_label`、`transport_label`、`auth_mode_label`、`terms_risk_label`、`result_shape_label` 會隨 `CrawlerCapabilityProfile.to_dict()` 輸出；Web capability capsule summary 改吃這些 label，不再把 `catalog_search / json / public_or_review / dataset_list` 類 raw backend token 放進使用者可見摘要。raw `source_family`、`transport`、`auth_mode`、`terms_risk`、`result_shape` 仍保留在 payload 給 agent/debug/filter；crawler registry、capability address、seed enumeration、download/import、credential flow、Tk 顯示與 project maturity 都不變。已驗證：`node --check frontends\web\static\app.js` OK；第一次 `py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview -v` 僅在已知 Windows localhost short-response 測試遇到 transient `WinError 10053`，該單測立即重跑通過；`py -3 -B -m unittest -v "tests.test_crawler_assets" "tests.test_web_preview"` 通過 112 tests；完整 smoke `state\logs\pre_push_smoke_20260531_105851.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26701734284` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log；本輪只補後端 capability dimension display label 與 Web 消費方式，不改 crawler handler、download/import、credential storage、Tk 操作或 user guide。 |
| 10:42 | UI Contract / Tk Crawler | **CI PASS** | `d81ee45` | `26701397722` | Hide Tk seed scope fallback ids | Tk crawler asset `crawler_asset_seed_scope_label()` 改吃後端 `seed_scope_display_label()`：dict / object profile 都能讀 `seed_scope_label`，缺 label 且遇到未知 `seed_scope` / `current_seed_scope` 時顯示「Seed 範式待確認」，不再把 `new_seed_scope` 類 raw backend token 放進表格或 Passport。raw `capability_profile.seed_scope` 與 `current_seed_scope` 仍保留在 payload / asset object 給 agent/debug；crawler registry、seed enumeration、Web 顯示、download/import、credential flow 與 project maturity 都不變。已驗證：`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs tests.test_crawler_assets -v` 通過 221 tests；同時執行的 `py_compile` 在 L 槽 `__pycache__` rename 遇到已知 `WinError 5` 雲端鎖，完整 smoke `state\logs\pre_push_smoke_20260531_104000.log` 通過，1039 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26701397722` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk seed scope display fallback，不改後端 payload shape、crawler handler、download/import、credential storage 或 user guide。 |
| 10:28 | UI Contract / Web Crawler | **CI PASS** | `5e20250` | `26701162075` | Hide Web seed scope fallback ids | Web Preview 的 Crawler Passport 與 capability capsule summary 再收斂：`Seed 範式` 只消費後端 `capability_profile.seed_scope_label`，缺 label 時顯示「Seed 範式待確認」，不再把 raw `seed_scope` 作為使用者文案候選。raw `capability_profile.seed_scope` 仍保留在 payload 給 agent/debug；crawler registry、seed enumeration、Tk 顯示、download/import、credential flow 與 project maturity 都不變。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過 65 tests；完整 smoke `state\logs\pre_push_smoke_20260531_102546.log` 通過，1038 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26701162075` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Web seed scope display fallback，不改後端 payload shape、crawler handler、download/import、credential storage 或 user guide。 |
| 10:16 | UI Contract / Project Maturity | **CI PASS** | `ec03262` | `26700904557` | Hide maturity fallback ids | Project maturity 的 row fallback 再收斂：Web maturity card 缺 `area_label` 時顯示「成熟度面向待確認」，不再以 `row.area_id` 當使用者標題；Markdown render 缺 `area_label` / `maturity_label_zh_TW` / `display_label` 時顯示「成熟度面向待確認」與 maturity display profile 的「未分類」，不再把 `new_backend_area` / `new_backend_level` 類 raw id 當人類文案。`area_id`、`maturity_level`、display profile 與 metrics 仍保留在 payload 給 agent/debug；project maturity matrix 計算、Web route、Tk dialog、download/import/crawler 行為不變。已驗證：`py -3 -B -m unittest tests.test_project_maturity tests.test_web_preview -v` 通過 71 tests；完整 smoke `state\logs\pre_push_smoke_20260531_100946.log` 通過，1038 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26700904557` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 maturity row fallback display，不改 project maturity 判斷邏輯、crawler asset payload、download/import、credential storage 或 user guide。 |
| 10:01 | UI Contract / Project Maturity | **CI PASS** | `63e231a` | `26700613871` | Label project maturity delivery status | Project maturity 的 canonical delivery scope 補上後端 `status_label`，`ready_for_mvp_demo` 顯示「可展示小閉環」；Tk 成熟度矩陣 dialog 改吃這個 label，缺 label 時顯示「交付狀態待確認」，不再把 raw delivery status id 當主要使用者文案。Markdown render 也輸出 `status_label`，Web 既有 `displayTextOrFallback()` 會自動消費同一欄位。`canonical_delivery_scope.status` raw 欄位仍保留給 agent/debug；maturity row 計算、MVP readiness 判斷、Web route、Tk dialog 開啟流程、crawler/download/import 不變。已驗證：`py -3 -B -m unittest tests.test_project_maturity tests.test_tk_dialogs tests.test_web_preview -v` 通過 214 tests；完整 smoke `state\logs\pre_push_smoke_20260531_095829.log` 通過，1037 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26700613871` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只補 maturity delivery status display label，不改 project maturity 判斷邏輯、crawler asset payload、download/import、credential storage 或 user guide。 |
| 09:49 | UI Contract / Crawler Capability | **CI PASS** | `aced815` | `26700405770` | Hide crawler capability status fallbacks | Crawler asset 能力狀態顯示改吃 `status_label_or_fallback()`：後端 `CrawlerAsset.capability_summary`、Tk 表格三個能力欄、右側 Passport 能力清單遇到未知 capability status 時顯示「需檢查能力狀態」，不再把 `new_capability_status` 類 raw backend token 直接露到使用者文案。`CrawlerAssetCapability.status` raw 欄位、`capability_status()`、能力判斷、crawler health、registry、download/import、Web route 與 credential flow 不變；這只是 display-contract fallback 收斂。已驗證：`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_crawler_assets -v` 通過 76 tests；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs tests.test_crawler_assets -v` 通過 220 tests；完整 smoke `state\logs\pre_push_smoke_20260531_094645.log` 通過，1037 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26700405770` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整能力狀態 display fallback，不改 crawler asset capability raw payload、Web route、download/import、credential storage 或 user guide。 |
| 09:30 | UI Contract / Tk Health | **CI PASS** | `54cb206` | `26700154616` | Reuse crawler health labels in Tk | Tk crawler asset 表格 / Passport 的 compact state label 改成共用後端 `health_status_label_or_fallback()`；未知 `asset.health.status_code` 會顯示「未知」，不再把 `new_backend_health` 類 raw status token 直接露到 UI。`asset.health.status_code` raw 欄位、`status_tone`、`status_gate`、crawler health evaluation、Web asset card 與 download/import/crawler 行為不變；這只是 Tk compact state label 的 display-contract 收斂。已驗證：第一次 `py_compile` 在 L 槽 `__pycache__` rename 遇到雲端碟 `WinError 5`，後續改用 `PYTHONDONTWRITEBYTECODE=1` / `-B` 路線驗證；`py -3 -B -m unittest tests.test_tk_ui_helpers -v` 通過 28 tests；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 通過 172 tests；完整 smoke `state\logs\pre_push_smoke_20260531_093004.log` 通過，1036 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26700154616` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk compact health state label fallback，不改 crawler asset health payload、Web route、download/import、credential storage 或 user guide。 |
| 06:50 | UI Contract / Crawler Capability | **CI PASS** | `7b85048` | `26697139842` | Hide unknown seed scope labels | `seed_scope_display_label()` 的未知 seed scope fallback 改成「Seed 範式待確認」，已知 `entry_listing` / `paginated_catalog` / `unknown` 仍顯示「入口列表」「分頁 catalog」「未知」，避免 registry 未來新增 seed scope token 時直接露出 raw backend id。raw `seed_scope` 仍保留在 payload 給 agent/debug，crawler registry、handler dispatch、seed enumeration、Web/Tk 讀取 `capability_profile.seed_scope_label` 的方式不變。已驗證：`py -3 -B -m unittest tests.test_web_preview.WebPreviewApiTest.test_server_rejects_oversized_discard_body_before_diagnostic_handler -v` 通過，前一次 Windows localhost short-response 問題為 transient；`py -3 -B -m unittest tests.test_crawler_assets -v` 通過 47 tests；`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview tests.test_tk_dialogs -v` 通過 256 tests；完整 smoke `state\logs\pre_push_smoke_20260531_065021.log` 通過，1035 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26697139842` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整後端 display label fallback，不改 crawler handler、seed enumeration、download/import、credential storage、Web route、Tk 操作或 user guide。 |
| 06:40 | UI Contract / Tk Discovery | **CI PASS** | `d6210f1` | `26696880389` | Hide Tk crawler action fallback ids | Tk crawler audit 的未知 `next_action` fallback 改成「查看 crawler 審核結果。」；已知 `repair_crawler_query_or_parser` 等 action 仍顯示原本的人類指引，未知 action 不再把 raw backend id 直接露到 discovery/audit 訊息。crawler audit payload、source audit result、promotion/upsert、warning code、problem source grouping、CLI/JSON 與 Web route 不變；這只是 Tk audit next-step display fallback。已驗證：`py_compile` for `frontends\tk\ui_labels.py` / `tests\test_tk_ui_labels.py` OK；`py -3 -B -m unittest tests.test_tk_ui_labels tests.test_launcher_ui tests.test_tk_dialogs -v` 通過 182 tests；完整 smoke `state\logs\pre_push_smoke_20260531_063741.log` 通過，1034 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26696880389` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk crawler audit next-action fallback，不改 crawler audit result、candidate promotion、download/import、credential storage、Web route 或 user guide。 |
| 06:28 | UI Contract / Tk Provider | **CI PASS** | `b07a5e6` | `26696686112` | Label Tk provider status fallbacks | Tk `ProviderRow.update_label` / `local_label` 改由 `provider_update_status_label()` / `provider_local_status_label()` 產生；已知狀態仍顯示「有更新」「未納管」，未知 backend status 改顯示「更新狀態待確認」「本地狀態待確認」，避免新 provider status token 直接露到 Tk 主表格欄位。ProviderCatalogEntry、download eligibility、repository row、search haystack、download/import/crawler/credential flow 不變；這只是 Tk provider view-model 的 display-safe fallback。已驗證：`py_compile` for `frontends\tk\provider_models.py` / `tests\test_tk_provider_models.py` OK；`py -3 -B -m unittest tests.test_tk_provider_models tests.test_tk_dialogs -v` 通過 147 tests；完整 smoke `state\logs\pre_push_smoke_20260531_062818.log` 通過，1034 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26696686112` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk provider table status fallback，不改 provider schema、crawler、download/import、credential storage、Web route 或 user guide。 |
| 06:14 | UI Contract / Tk Import | **CI PASS** | `72af949` | `26696368474` | Label Tk import plan statuses | Tk 匯入狀態 fallback 改由 `import_plan_status_label()` 產生；已知 `manual_review_required` 顯示「需內容 Parser review」，未知 `import_plan.status` 顯示「匯入狀態待確認」，避免新 backend status 直接露到下載器匯入欄。CSV/JSON importer、download-plan runner、manual-review payload、content parser review payload、SQLite write gate、manifest 與 Web route 不變；這只是 Tk 匯入欄位的 display-safe fallback。已驗證：`py_compile` for `frontends\tk\import_workflows.py` / `tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 144 tests；完整 smoke `state\logs\pre_push_smoke_20260531_061218.log` 通過，1032 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26696368474` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk 匯入欄位 fallback，不改 importer、download queue、manifest、crawler、credential storage、Web route 或 user guide。 |
| 06:03 | UI Contract / Tk Downloader | **CI PASS** | `3962d96` | `26696145938` | Label Tk download skip buckets | Tk 下載器略過摘要的 bucket 顯示改由 `download_skip_bucket_label()` 產生；已知 bucket 顯示「需 Adapter」「缺下載 URL」等文案，未知 bucket 只顯示「其他待處理」，避免未登錄 backend bucket 直接洩漏成 raw id。`download_entry_skip_bucket()`、`download_skip_summary()`、download plan runner、CLI JSON、queue、manifest/import 行為不變；這只是 Tk 下載略過摘要的人類文案 fallback。已驗證：`py_compile` for `frontends\tk\download_workflows.py` / `tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 142 tests；完整 smoke `state\logs\pre_push_smoke_20260531_060120.log` 通過，1030 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26696145938` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk 下載略過摘要 fallback，不改 download plan runner、download queue、manifest、import、crawler、credential storage、Web route 或 user guide。 |
| 05:52 | UI Contract / Tk Downloader | **CI PASS** | `5766f6c` | `26695927338` | Label Tk download job statuses | Tk 下載器 runtime status 顯示改由 `download_job_status_label()` 產生；表格與失敗/取消狀態列顯示「已規劃」「下載中」「失敗」等人類文案，不再把 raw `planned` / `failed` 直接當主要使用者文字。download queue stable `JobStatus`、`download_status_by_provider` tuple、plan key、provider id lookup、manifest/register/import 與 `download_job_problem` event context 不變，raw status/id 仍保留給 agent/debug。已驗證：`py_compile` for `frontends\tk\download_workflows.py` / `tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 141 tests；完整 smoke `state\logs\pre_push_smoke_20260531_055004.log` 通過，1029 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26695927338` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk 下載器使用者可見 runtime status label，不改 queue、download execution、manifest、import、crawler、credential storage、Web route 或 user guide。 |
| 05:36 | UI Contract / Tk Repair | **CI PASS** | `40dae91` | `26695645884` | Label Tk repair provider ids | Tk 修復/驗證面板的下載 manifest table、database issue table、repair suggestion、requeue status、reimport confirmation、dry-run SQL confirmation、database connection title 與 unmanage confirmation 改用 `repair_provider_label()` / `repair_asset_title()`；使用者可見處顯示 `Provider ID：...`，detail pane 的 `provider_id:` trace key/value 與 event context 保留 raw id。已驗證：`py_compile` for `frontends\tk\repair_workflows.py` / `tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 139 tests；完整 smoke `state\logs\pre_push_smoke_20260531_053625.log` 通過，1027 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26695645884` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk repair UI provider provenance display，不改 manifest scan、database self-check、requeue、SQLite reimport、dry-run SQL、connection metadata、unmanage、credential storage、Web route 或 user guide。 |
| 05:25 | UI Contract / Tk Review | **CI PASS** | `afd20d3` | `26695394542` | Label Tk adapter review providers | Adapter Review dialog 表格 provider 欄改用 `provider_display_label(None, provider_id)`，顯示 `Provider ID：...`，避免 raw `nyc_open_data` 被當成人類資料源名稱；detail pane 的 `provider_id:` key/value 仍保留 raw id，因為那裡是 agent / 人類可複製的追溯區。已驗證：`py_compile` for `frontends\tk\adapter_review_dialog.py` / `tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 138 tests；完整 smoke `state\logs\pre_push_smoke_20260531_052210.log` 通過，1026 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26695394542` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk adapter review 表格 provider provenance display，不改 detail key/value、adapter resolver、download/import、credential storage、Web route 或 user guide。 |
| 05:12 | UI Contract / Tk Review | **CI PASS** | `ce27d37` | `26695103614` | Label Tk candidate review providers | Dataset Candidate Review dialog 的 Provider 欄與 detail provider line 改用 `provider_display_label(None, provider_id)`，候選資料只知道 provider id 時顯示 `Provider ID：...`，不再把裸 `example_provider` 放在「提供商」欄位裡像人類名稱；candidate status、evidence JSON、source URL、approve/reject/add-to-plan、repository schema、download/import/crawler flow 不變。已驗證：`py_compile` for `frontends\tk\dataset_candidate_review_dialog.py` / `tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 138 tests；完整 smoke `state\logs\pre_push_smoke_20260531_050944.log` 通過，1026 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26695103614` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk candidate review provider provenance display，不改候選審核行為、crawler、download/import、credential storage、Web route 或 user guide。 |
| 05:01 | UI Contract / Tk | **CI PASS** | `88d126b` | `26694878725` | Use Tk provider display labels in details | Tk 主表格 row title 與 detail panel title/description 改用共用 `provider_display_label()`，讓 `ProviderRow.name=""` 時顯示 `Provider ID：...`，不再出現空白表格標題或以空白 provider name 組出不完整描述；search haystack、provider repository row、detail owner/category/auth/website、download/import/crawler/credential flow 不變。已驗證：`py_compile` for `frontends\tk\table_data_workflows.py` / `detail_panel_workflows.py` / `tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 138 tests；完整 smoke `state\logs\pre_push_smoke_20260531_045819.log` 通過，1026 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26694878725` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk 主表格與詳情面板 provider display fallback，不改 provider schema、crawler、download/import、credential storage、Web route 或 user guide。 |
| 04:42 | UI Contract / Tk | **CI PASS** | `0f39122` | `26694502805` | Reuse Tk provider display labels | 延續 Tk provider display helper 收斂：AI summary 產生/寫入狀態與下載完成狀態改用 `provider_display_label()`，避免 `row.name=""` 時把空白名稱或 raw provider id 當主要使用者文案；下載/AI worker 的 stable provider id、plan key、repository 行為不變。已驗證：`py_compile` for `frontends\tk\ai_summary_workflows.py` / `download_workflows.py` / `tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 136 tests；完整 smoke `state\logs\pre_push_smoke_20260531_044010.log` 通過，1024 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26694502805` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk AI summary 與 downloader status label fallback，不改 AI profile/token flow、download queue、manifest/import、crawler、Web route 或 user guide。 |
| 04:28 | UI Contract / Tk | **CI PASS** | `b1e15b6` | `26694215394` | Share Tk provider display labels | Tk provider 顯示名稱 fallback 收斂到 `frontends/tk/provider_display.py::provider_display_label()`；`source_action_workflows.py` 保留相容 wrapper，`plan_workflows.py` 的 provider toggle、加入下載計畫、version plan 與 cart label 改吃同一份 helper，避免空白 `row.name` 在不同 workflow 各自退化成空白或 raw id；stable provider id 仍用於 repository、plan key 與 background job key。已驗證：`py_compile` for `frontends\tk\provider_display.py` / `source_action_workflows.py` / `plan_workflows.py` / `tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 135 tests；完整 smoke `state\logs\pre_push_smoke_20260531_042609.log` 通過，1023 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26694215394` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk display helper ownership 與 plan/source-action label fallback，不改 provider repository schema、crawler、download/import、credential storage、Web route 或 user guide。 |
| 04:14 | UI Contract / Tk | **CI PASS** | `2198b22` | `26693884337` | Guard Tk blank provider labels | Tk source action workflow 在已有 `ProviderRow` 但 `row.name` 為空白時，選取、metadata row action、解除納管、標記移除與開官方文件頁都改用 `source_action_provider_label()`，顯示 `Provider ID：...` 或既有 provider name，不再出現空白 provider 名稱；stable provider id 仍用於 repository、metadata crawl 與 background job key。已驗證：`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m py_compile frontends\tk\source_action_workflows.py tests\test_tk_dialogs.py` OK；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 134 tests；完整 smoke 第一次 `state\logs\pre_push_smoke_20260531_041138.log` 在 early `git diff --check staged` 遇到雲端碟 transient `Unable to read current working directory`；同一工作區立即確認 `pwd` / `git status` / `git diff --check` 正常，第二次完整 smoke `state\logs\pre_push_smoke_20260531_041156.log` 通過，1022 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26693884337` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk source action status/confirmation/open-doc label fallback 與 regression tests，不改 provider repository schema、metadata crawler、managed install/uninstall semantics、credential storage、download/import、Web route 或 user guide。 |
| 04:03 | UI Contract / Tk | **CI PASS** | `d0c715a` | `26693645745` | Label Tk source action provider ids | Tk source action workflow 的置頂、metadata 檢查、本地資產驗證與納管狀態若找不到 `ProviderRow.name`，會明確顯示 `Provider ID：...`，不再把 raw provider id 偽裝成 provider 顯示名稱；repository call、active provider id 與 background job key 仍保留 stable id。已驗證：`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m py_compile frontends\tk\source_action_workflows.py tests\test_tk_dialogs.py` OK；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 133 tests；完整 smoke `state\logs\pre_push_smoke_20260531_040016.log` 通過，1021 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26693645745` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk source action status helper 與 regression tests，不改 provider repository schema、metadata crawler、asset verification、managed install、credential storage、download/import、Web route 或 user guide。 |
| 03:49 | UI Contract / Tk Credential | **CI PASS** | `60a5b09` | `26693340352` | Label Tk credential subtitle ids | Tk crawler asset credential/login dialog 的副標改由 `crawler_asset_credential_subtitle()` 產生，Provider、Source、Provider ID、Asset ID 都有明確標籤，不再把 `provider / source_type / asset_id` 裸串成主要使用者文案；raw ids 仍可作為支援與追溯資訊。已驗證：`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m py_compile frontends\tk\crawler_asset_credential_dialog.py tests\test_tk_dialogs.py` OK；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 131 tests；完整 smoke `state\logs\pre_push_smoke_20260531_034638.log` 通過，1019 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26693340352` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk credential dialog header helper 與 regression test，不改 credential storage、OAuth/API key flow、crawler handler、download/import、Web route 或 user guide。 |
| 03:36 | UI Contract / Tk | **CI PASS** | `1b60382` | `26693093030` | Label Tk listing status names | Tk crawler asset listing 的狀態列與 duplicate guard 改顯示 `asset.display_name`，但 single-flight key 與 listing worker 參數仍保留 raw `asset.asset_id`，讓邏輯可追溯而使用者文字不再露出 `demo_index` 類內部 id。已驗證：`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 130 tests；完整 smoke `state\logs\pre_push_smoke_20260531_033255.log` 通過，1018 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26693093030` 通過 Ubuntu、Windows 與 real DB smoke。交換區新信 `c_3_RRKAL_project` 已回覆並標為 `backlogged`，territory / EEZ / maritime-boundary manifest 留待未來 OpenSpec/ADR。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk listing status 文案與 regression test，不改 active provider、listing worker、crawler handler、download/import、credential storage、Web route 或 user guide。 |
| 03:21 | UI Contract / Tk | **CI PASS** | `aa92271` | `26692761185` | Label Tk metadata crawl status | Tk 既有 metadata crawl 狀態列改顯示 `asset.display_name`，但仍保留 raw `provider_id` 作為 `active_provider_id` 邏輯輸入；使用者可見狀態不再把 provider id 當主要文案。已驗證：`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 129 tests；第一次完整 smoke `state\logs\pre_push_smoke_20260531_031658.log` 在 `unittest discover` 起點遇到雲端碟 transient `Start directory is not importable: 'tests'`；同一環境立即確認 `tests\__init__.py` 存在且手動 `py -3 -B -m unittest discover -s tests -p "test*.py" -v` 通過 1017 tests / 4 skipped；第二次完整 smoke `state\logs\pre_push_smoke_20260531_031911.log` 通過，1017 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26692761185` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk metadata crawl 狀態文字與 regression test，不改 active provider 邏輯、crawler handler、download/import、credential storage、Web route 或 user guide。 |
| 03:05 | UI Contract / Tk | **CI PASS** | `bc775da` | `26692492110` | Label Tk profile provenance ids | Tk Crawler Asset profile editor 副標改由 `crawler_asset_profile_subtitle()` 產生，顯示來源範式與明確標籤化的 `Provider ID` / `Asset ID`，不再把裸 `provider_id / source_type_label / asset_id` 串成主要使用者文案。已驗證：`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m py_compile frontends\tk\crawler_asset_profile_dialog.py tests\test_tk_dialogs.py` OK；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 128 tests；完整 smoke `state\logs\pre_push_smoke_20260531_030524.log` 通過，1016 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26692492110` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk profile editor 顯示 helper 與 regression test，不改 profile 保存、crawler handler、download/import、credential storage、Web route 或 user guide。 |
| 02:55 | UI Contract / Web | **CI PASS** | `3b40128` | `26692222235` | Label Web provider preset seed fallbacks | Web Crawler Passport provider 文字改吃 `providerDisplayText(card)`，credential editor title 改走 `credentialProviderTitle()`，bounds preset 顯示改走 `boundPresetLabel()`，seed row 空 metadata 改顯示「資料摘要待確認」；Web 不再直接把 `card.provider_id`、`status.provider_id`、`assetId`、`preset_id` 或英文 `metadata pending` 當主要使用者文案。已驗證：`node --check frontends\web\static\app.js` OK；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_web_preview -v` 通過 65 tests；第一次完整 smoke `state\logs\pre_push_smoke_20260531_025000.log` 的 unittest 已通過 1015 tests / 4 skipped，但 MVP demo CLI 遇到雲端碟 transient `Permission denied`；同一條 MVP demo CLI 立即重試成功 `download_import_completed` / `row_count=3`；第二次完整 smoke `state\logs\pre_push_smoke_20260531_025242.log` 通過，1015 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26692222235` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log；本輪只調整 Web 顯示 fallback 與靜態 guard，不改後端 payload、crawler handler、download/import service、credential storage 或 Tk 操作。 |
| 02:40 | UI Contract / Web | **CI PASS** | `7161ab5` | `26691886415` | Guard Web id label fallbacks | Web Parser Registry chips、後端 flow step label、capability label 與 bounds field label 改用 `displayTextOrFallback()` 類 helper，缺 label 時顯示「Parser 線索待確認」「流程步驟待確認」「能力待確認」「欄位待確認」，不再直接 fallback 到 `parser_id`、`step_id`、`capability_id` 或 `field_id`。已驗證：`node --check frontends\web\static\app.js` OK；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_web_preview -v` 通過 65 tests；完整 smoke `state\logs\pre_push_smoke_20260531_023814.log` 通過，1015 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26691886415` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log；本輪只調整 Web 顯示 helper fallback 與靜態 guard，不改後端 payload、crawler handler、download/import service、credential storage 或 Tk 操作。 |
| 02:31 | UI Contract / Web | **CI PASS** | `6116d4b` | `26691697057` | Hide Web maturity area ids | Web 成熟度工作區 row heading 改用 `displayTextOrFallback("成熟度面向待確認", row.area_label, row.area_id)`，缺 label 時不再顯示英文 `maturity area` 或 raw `area_id`。已驗證：`node --check frontends\web\static\app.js` OK；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_web_preview -v` 通過 65 tests；完整 smoke `state\logs\pre_push_smoke_20260531_022904.log` 通過，1015 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26691697057` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log；本輪只調整 Web maturity display fallback 與靜態 guard，不改後端 maturity payload、crawler handler、download/import service、credential storage 或 Tk 操作。 |
| 02:17 | UI Contract / Web | **CI PASS** | `1fe48ac` | `26691488253` | Label Web provider fallback | Web Downloader row 與 selected hero 的 provider 文字改由 `providerDisplayText()` 產生，空值顯示「Provider 待確認」，不再出現英文開發者 fallback `provider unknown`；provider id 暫時保留為可追溯顯示值，待後端 provider display label 完整化後再替換。已驗證：`node --check frontends\web\static\app.js` OK；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_web_preview -v` 通過 65 tests；完整 smoke `state\logs\pre_push_smoke_20260531_021933.log` 通過，1015 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26691488253` 通過 Ubuntu、Windows 與 real DB smoke；`git diff --check` OK（僅既有 `frontends/web/static/app.js` CRLF warning）。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log；本輪只調整 Web 顯示 fallback 與靜態 guard，不改後端 payload、crawler handler、download/import service、credential storage 或 Tk 操作。 |
| 02:05 | UI Contract / Plan Outcome | **CI PASS** | `70a9a5f` | `26691178190` | Label blocked plan reasons | 本輪做 display-contract 小收斂：`api_launcher.crawler_plan_outcome_display` 新增 blocked reason display label/fallback，`plan_outcome_display_profile()` 的 blocked summary / short label 不再把 raw `missing_credentials`、`crawler_asset_disabled` 類 backend id 放進使用者文案；Tk 下載計畫 blocked dialog 也改吃同一個 label。已驗證：`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview -v` 通過 192 tests；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m py_compile api_launcher\crawler_plan_outcome_display.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_dialogs.py tests\test_web_preview.py` OK；完整 smoke `state\logs\pre_push_smoke_20260531_020551.log` 通過，1015 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26691178190` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只改 plan outcome display payload 與 Tk 顯示消費方式，不改 crawler handler、download/import service、credential storage 或 Web route shape。 |
| 01:49 | UI Contract / Tk Credential | **CI PASS** | `733ff0b` | `26690911510` | Label Tk AI profile OAuth status | Tk AI 摘要 profile login status 改吃同一個 OAuth display helper：`AiSummaryWorkflowMixin.ai_profile_login_status()` 在 OAuth ready 時顯示「OAuth 已登入：已登入」/ `OAuth signed in: Signed in`，不再把 raw `ready` 放進 AI 模型選擇表格或狀態摘要。已驗證：`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_tk_dialogs -v` 通過 127 tests；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m py_compile frontends\tk\ai_summary_workflows.py tests\test_tk_dialogs.py` OK；完整 smoke `state\logs\pre_push_smoke_20260531_014855.log` 通過，1015 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26690911510` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 Tk AI profile login status display label 消費方式，不改 token exchange/storage、credential flow、AI summary generation、crawler handler、download/import service 或 Web 行為。 |
| 01:40 | UI Contract / Tk Bounds | **CI PASS** | `b1710f0` | `26690639138` | Label Tk schema probe failures | Tk 下載計畫界域欄位探測失敗訊息改吃後端 schema probe display helper：`api_launcher.schema_probe` 新增 `schema_probe_status_label()` / `schema_probe_failure_detail()`，`PlanWorkflowMixin._finish_plan_bounds_probe()` 失敗 dialog 顯示「缺少可探測資料端點」「欄位探測發生錯誤」等人類文案，必要時附技術細節，不再把 raw `unavailable` / `error` / 未知 `probe.status` 直接放進使用者可見訊息。已驗證：`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_source_download tests.test_tk_dialogs -v` 通過 138 tests；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m py_compile api_launcher\schema_probe.py frontends\tk\plan_workflows.py tests\test_source_download.py tests\test_tk_dialogs.py` OK；完整 smoke `state\logs\pre_push_smoke_20260531_014010.log` 通過，1014 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26690639138` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 schema probe status display helper 與 Tk plan bounds 消費方式，不改 schema probe fetch 行為、bounds form schema、download/import service、crawler handler、credential storage 或 Web 行為。 |
| 01:29 | UI Contract / Tk Credential | **CI PASS** | `1e98417` | `26690438010` | Label Tk OAuth token statuses | Tk Google/Gemini 連線與 OAuth device-flow 的 raw token/status id 收回後端 display helper：新增 `oauth_token_status_label()`，Google/Gemini 設定視窗顯示「尚未登入」「已登入」「登入請求失敗」等人類文案；OAuth device polling 未完成時顯示「等待授權」「登入請求失敗」等 label，不再把 raw `missing`、`ready`、`request_failed` 或未知 status id 放進使用者可見 status bar / connection message。已驗證：`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_oauth_device tests.test_tk_dialogs -v` 通過 138 tests；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m py_compile api_launcher\oauth_device.py frontends\tk\ai_settings_dialogs.py frontends\tk\oauth_workflows.py tests\test_oauth_device.py tests\test_tk_dialogs.py` OK；完整 smoke `state\logs\pre_push_smoke_20260531_012951.log` 通過，1012 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26690438010` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 OAuth token/status display helper 與 Tk 消費方式，不改 token exchange/storage、credential flow、crawler handler、download/import service 或 Web 行為。 |
| 01:19 | UI Contract / Tk Showcase | **CI PASS** | `9e45de2` | `26690162318` | Label Tk showcase status stages | Tk 展示模式的 seed coverage 與 showcase download 顯示文案改吃後端 / shared display label：seed coverage report 新增 `showcase_status_label`，dialog 顯示「所有入口都有完整 seed 嘗試路徑」等文案；showcase download dialog / status bar 重用 `download_import_stage_display_label()`，顯示「下載 / 匯入完成」；未知 progress stage 顯示「展示流程狀態待確認」，不再把 raw `all_sources_have_complete_seed_attempt_path`、`download_import_completed` 或未知 stage id 放進使用者可見文字。已驗證：`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_showcase_workflows tests.test_dataset_discovery -v` 通過 65 tests；`$env:PYTHONDONTWRITEBYTECODE='1'; py -3 -B -m unittest tests.test_tk_dialogs tests.test_showcase_workflows -v` 通過 130 tests；完整 smoke `state\logs\pre_push_smoke_20260531_011655.log` 通過，1010 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26690162318` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 showcase display label payload 與 Tk showcase 消費方式，不改展示下載流程、crawler handler、download/import service 或 credential storage。 |
| 01:06 | UI Contract / Tk-Web | **CI PASS** | `70f01d7` | `26689937366` | Label closure stage messages | Recommended-seed closure 與 Tk seed download/import 的 stage 顯示契約補齊：`CrawlerAssetRecommendedSeedClosureResult.to_dict()` 新增 `closure_stage_label`，Tk seed download/import 與 recommended closure message 顯示「下載 / 匯入完成」「下載前需處理」「沒有推薦 seed」等人類文案，不再把 raw `download_import_completed`、`blocked_before_download`、`no_recommended_seed` 放進 message body 或 status message。已驗證：`PYTHONDONTWRITEBYTECODE=1 py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_crawler_asset_download -v` 通過 33 tests；`PYTHONDONTWRITEBYTECODE=1 py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview tests.test_crawler_asset_download -v` 通過 195 tests；docs mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260531_010544.log` 通過，1008 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26689937366` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 stage display payload 與 Tk message 消費方式，不改 closure 執行流程、download/import、crawler handler 或 credential storage。 |
| 00:52 | UI Contract / Tk-Web | **CI PASS** | `aedd95b` | `26689631897` | Label crawler asset surface access | Crawler Asset 的入口表面 / 存取邊界文案收回後端 display payload：`CrawlerAsset.to_dict()` 新增 `source_surface_label` / `access_requirement_label`，Tk 右側 Passport 與 Web asset card / Passport / selected hero 改顯示「資料目錄」「API 入口」「需登入 / API key」等人類文案，不再由 Web 維護 `file_index` / `map_service` 本地翻譯表，也不在 Tk detail 顯示 raw `crawler_managed_auth`。已驗證：`node --check frontends\web\static\app.js` OK；`PYTHONDONTWRITEBYTECODE=1 py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets tests.test_tk_ui_helpers -v` 通過 138 tests；`PYTHONDONTWRITEBYTECODE=1 py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview -v` 通過 189 tests；docs mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260531_005312.log` 通過，1007 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26689631897` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX / development log；本輪只調整 display payload 與 UI 消費方式，不改 crawler handler、download/import、credential storage 或 source access 判斷。 |
| 00:38 | UI Contract / Tk | **CI PASS** | `be3f5bd` | `26689307021` | Label Tk crawler asset maturity risk | Tk Crawler Asset 右側 Passport 原本直接顯示 raw `asset.maturity` / `asset.risk_tier`，例如 `unbuilt`、`needs_handler`；本輪改為消費後端 `crawler_asset_maturity_label()` / `crawler_asset_risk_tier_label()`，顯示「待補 handler」「待審核」等人類文案。新增 `test_crawler_asset_detail_text_uses_maturity_and_risk_labels()` 鎖住 raw token 不再進 detail text。已驗證：`PYTHONDONTWRITEBYTECODE=1 py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 通過 151 tests；docs mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260531_003914.log` 通過，1007 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26689307021` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk 顯示投影，不改 crawler handler、download/import、credential storage 或 Web 行為。 |
| 00:22 | Project Maturity / Encoding Guard | **CI PASS** | `751b64c` | `26689055642` | Guard maturity matrix encoding | 針對成熟度矩陣補 mojibake 防回歸測試：先用 Python UTF-8 / `ascii()` 確認 `api_launcher/project_maturity.py` 與 `tests/test_project_maturity.py` 實體內容乾淨，PowerShell `Get-Content` 看到的亂碼屬 console code page 顯示問題；新增 `test_project_maturity_payload_and_markdown_do_not_contain_mojibake()`，掃 `--project-maturity-json` payload 與 markdown render，禁止 U+FFFD、常見 mojibake 片段與 private-use 亂碼進成熟度矩陣輸出。已驗證：`PYTHONDONTWRITEBYTECODE=1 py -3 -B -m unittest tests.test_project_maturity tests.test_web_preview tests.test_tk_dialogs -v` 通過 194 tests；docs mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260531_002749.log` 通過，1006 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26689055642` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只新增測試 guardrail，不改產品行為、Web/Tk 操作、crawler、download/import 或 credential storage。 |
| 00:10 | Import Pipeline / Maturity Evidence | **CI PASS** | `571d727` | `26688687472` | Expose importer shim maturity metrics | 延續 23:57 的 scoped importer compatibility shim，新增 `ImporterCompatibilityShimProfile` / `importer_compatibility_shim_report()`，並把 shim profile 接進 `--project-maturity-json` 的 `content_parser_and_import.metrics`；payload 會輸出 `supported_sqlite_importers`、`compatibility_shim_count`、`compatibility_shim_runtime_scope=scoped_importer_boundary`、`global_monkeypatch=false` 與 shim 明細。這是讓元編程 / compatibility shim 可觀測化的 declarative evidence，不是新的全域 monkeypatch，也不改下載或匯入主流程。已驗證：`--project-maturity-json` 實際輸出 `compatibility_shim_count=1`、`runtime_scope=scoped_importer_boundary`、`global_monkeypatch=false`；`PYTHONDONTWRITEBYTECODE=1 py -3 -B -m unittest tests.test_importer_compatibility_shims tests.test_project_maturity tests.test_csv_importer tests.test_json_importer -v` 通過 20 tests；`git diff --check` OK；docs mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260531_001121.log` 通過，1005 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26688687472` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不改 UI、crawler handler、download/import service 或 credential storage。 |

### 2026-05-30

| 時間 | 階段 | 狀態 | SHA | Run | Commit | 變更與驗證 |
| --- | --- | --- | --- | --- | --- | --- |
| 23:57 | Import Pipeline / Compatibility Shim | **CI PASS** | `db66df6` | `26688452891` | Add importer compatibility shim | 新增 `api_launcher.importers.compatibility_shims`，把使用者提出的「劫持外部混亂來源」收斂成 importer 邊界的 scoped normalization：CSV 欄位會攤平 tuple/list/MultiIndex-like label 與字串 repr，`Unnamed:*_level_*` 會落到安全 fallback；CSV/JSON importer 會把 dict/list/tuple cell 穩定 JSON 化，`None` / NaN 保持空字串。這不是全域 monkeypatch，不常駐覆寫 `builtins.print`、`sys.modules` 或 pandas。已驗證：`PYTHONDONTWRITEBYTECODE=1 py -3 -B -c "import ..."` OK；`py -3 -B -m unittest tests.test_importer_compatibility_shims tests.test_csv_importer tests.test_json_importer -v` 通過 15 tests；`git diff --check` OK；docs mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260530_235939.log` 通過，1004 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26688452891` 通過 Ubuntu、Windows 與 real DB smoke。`py_compile` 在 L 槽雲端 `__pycache__` replace 時遇到 `WinError 5`，本輪以不寫 pyc 的 import check 與完整 smoke 補驗證。Docs drift check：已同步 GTD / handoff / user guide / development log；本輪不改下載流程、crawler handler、UI 操作或 credential storage。 |
| 23:41 | Crawler Closure / Tk | **CI PASS** | `a3141d9` / `4591bb5` | `26688043642` | Wire recommended seed closure into Tk | Tk Seed 清單 dialog 新增「驗證閉環」入口，會使用後端 `recommended_seed_uid` 重跑 bounded listing、讀回本機 seed page，再呼叫同一個 `run_recommended_seed_closure()` 做正式推薦 seed 下載 / 匯入；Tk 不自行挑 seed，也不重寫 Web/CLI 的 closure 規則。新增 Tk helper `crawler_asset_recommended_seed_closure_target_paths()`、`crawler_asset_recommended_seed_closure_ui_message()`、`crawler_asset_recommended_seed_closure_event_context()`，事件 payload 只記 compact seed page summary，不把整頁 seed list 寫進 event log。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_seed_dialog.py frontends\tk\crawler_asset_ui_helpers.py frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs tests.test_crawler_asset_download tests.test_crawler_seed_registry -v` 通過 179 tests；`git diff --check` OK；frontends/tk 與 tests mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260530_234119.log` 通過，1001 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26688043642` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / user guide / development log；本輪不改 Web endpoint、CLI command、crawler handler、credential storage 或 import policy。另記錄 compat/shim guardrail：可借鑑 `sys.modules` / monkeypatch 作測試或 adapter 相容層，但不要讓產品主線常駐覆寫全域 import / print。 |
| 23:18 | Crawler Closure / Web | **CI PASS** | `3ccb3ff` | `26687624870` | Wire recommended seed closure into Web | Web Preview 新增 `POST /api/crawler-assets/{asset_id}/recommended-seed-closure` 與 seed 推薦面板「驗證閉環」按鈕，會把目前 bounds form values 交給後端 `run_recommended_seed_closure()`，固定走 listing -> backend recommended seed -> formal seed download/import；缺憑證時先回 shared credential-blocked payload，不呼叫 live closure service。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_closure.py frontends\web\preview_api.py frontends\web\server.py tests\test_web_preview.py` OK；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_asset_download tests.test_crawler_seed_registry -v` 通過 94 tests；`git diff --check` OK；Browser 實測 `http://127.0.0.1:8765/` 可載入 Web Preview，畫面可見「下載推薦 seed」與「驗證閉環」；完整 smoke `state\logs\pre_push_smoke_20260530_232325.log` 通過，993 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26687624870` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / user guide / Web Preview UIUX / development log；本輪不改 crawler handler、CLI closure command、credential storage、Tk 操作或 import policy。 |
| 22:54 | Crawler Closure / CLI | **CI PASS** | `5bf15ca` | `26687064209` | Add recommended seed closure CLI | 新增 `api_launcher/crawler_asset_closure.py` 與 CLI `--run-crawler-asset-recommended-seed-closure ASSET_ID --crawler-asset-closure-json`，把 existing listing、seed page、backend recommended seed 與 formal seed download/import 串成單一 agent-readable closure artifact。已用 DataSF live public source `sf_open_data_socrata_catalog` 驗證：枚舉 4 筆 seed，推薦 `ds_c0ebed9866e8c58b72784bff`，`closure_stage=download_import_completed`、`succeeded=true`、`imported=1`，stdout 為純 JSON。驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_closure.py api_launcher\cli_crawler_assets.py` OK；`py -3 -B -m unittest tests.test_crawler_asset_download tests.test_crawler_seed_registry -v` 通過 29 tests；live closure subprocess JSON parse OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_225806.log` 通過，990 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26687064209` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / user guide / development log；本輪不改 Web/Tk UI，只新增 CLI/後端 closure artifact。 |
| 22:30 | Docs / Workflow | **LOCAL DOCS** | `docs-only` | `not needed` | Initialize Codex Cloud handoff workflow | 初始化 RRKAL 專屬 Codex Cloud / 新 thread 接手與對話備份規則：新增 `docs/CODEX_CLOUD_HANDOFF.zh-TW.md` 與 `docs/WORKFLOW.zh-TW.md`，同步 `AGENT_START_HERE`、`DOCS_INDEX`、`DOCS_REGISTRY.csv`、`PROJECT_GTD` 與 `AGENT_HANDOFF`。規則明確要求公開 repo 只保存蒸餾後 handoff/workflow/decision/log，raw transcript 只放 private `Kagamihara-Ruruka/dialogue-save`，不得貼回公開 repo。本輪是 docs/workflow 初始化，不改產品碼、不需要 RRKAL 測試；已用 `git diff --check` 與 docs mojibake scan 驗證。 |
| 19:55 | UI Contract / Tk | **CI PASS** | `859fc5a` | `26683225294` | Use source type labels in Tk | Tk Crawler Asset 表格、右側 Passport 詳情、flow step、crawler profile dialog 與 credential dialog 改顯示後端 `source_type_label`，例如「CKAN package search」「HTML file index」或「來源範式待確認」，不再把 raw `ckan_package_search` / `html_file_index` 當主要使用者文字。驗證：in-memory compile 相關 Tk/backend/test 檔 OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_tk_ui_helpers tests.test_crawler_assets -v` 通過，188 tests OK；完整 smoke `state\logs\pre_push_smoke_20260530_195239.log` 通過，987 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26683225294` 通過 Ubuntu、Windows 與 real DB smoke。`py_compile` 曾被 L 槽雲端 `__pycache__` replace lock 擋住，已用不寫 pyc 的 compile 與完整 smoke 補驗證。Docs drift check：已同步 GTD / handoff / Web Preview UIUX contract / development log；本輪只調整 Tk 顯示投影與對話框副標，不改 crawler registry dispatch、source type id、download/import service、credential storage 或 Web 行為。 |
| 19:43 | UI Contract / Backend Display | **CI PASS** | `a0d71be` | `26682992419` | Label crawler source types for Web | `CrawlerCapabilityProfile` 與 `CrawlerAsset.to_dict()` 現在輸出 `source_type_label`，Web source-type filter、Downloader row、Crawler Passport 與 selected hero 改顯示 label 或「來源範式待確認」，不再直接把 `stac_collections` / `html_file_index` 類 raw registry id 當人類文案。驗證：`py -3 -B -m py_compile api_launcher\crawler_capability_profiles.py api_launcher\crawler_assets.py tests\test_crawler_assets.py tests\test_web_preview.py` OK；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview -v` 通過，108 tests OK；完整 smoke `state\logs\pre_push_smoke_20260530_194021.log` 通過，987 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26682992419` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX contract / development log；本輪只增加 source type display label payload 與 Web 消費方式，不改 crawler registry dispatch、source type id、download/import service、credential storage 或 Tk 操作。 |
| 19:24 | UI Contract / Web Consolidation | **CI PASS** | `50559e7` | `26682740605` | Hide raw maturity status in Web | Web「成熟度」工作區的 Delivery Scope 摘要改用 `deliveryClosureText()`，優先顯示後端 `status_zh_TW` / display label；maturity row label 缺值時顯示「成熟度待確認」，不再把 raw `ready_for_mvp_demo`、`maturity_level` 或 `unknown` 放進使用者可見摘要。驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，62 tests OK；完整 smoke `state\logs\pre_push_smoke_20260530_192430.log` 通過，987 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26682740605` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX contract / development log；本輪只調整 Web maturity workspace 顯示 fallback，不改後端 maturity payload、crawler registry、download/import service、credential storage 或 Tk 操作。 |
| 19:14 | UI Contract / Web Consolidation | **CI PASS** | `85f8ecd` | `26682454145` | Use stage labels in Web missions | Web mission queue 的 asset-level 與 seed-level 下載 / 匯入完成訊息改用 `downloadImportStageText()`，顯示「下載 / 匯入完成」這類後端 display label，不再把 raw `download_import_completed` / `stage` id 放進互動紀錄。驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，62 tests OK；完整 smoke `state\logs\pre_push_smoke_20260530_191232.log` 通過，987 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26682454145` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX contract / development log；本輪只調整 Web mission queue 顯示 fallback，不改後端 payload、download/import service、crawler registry、credential storage 或 Tk 操作。 |
| 19:01 | UI Contract / Backend Display | **CI PASS** | `2c786bf` | `26682250910` | Label crawler asset maturity risk | `CrawlerAsset.to_dict()` 現在輸出 `maturity_label` / `risk_tier_label`，Web Passport 顯示後端 label，缺值時落到「成熟度待確認」「風險層級待確認」，能力膠囊缺摘要時顯示「能力膠囊待確認」，不再顯示 raw `unknown` 或 tier token。驗證：`py -3 -B -m py_compile api_launcher\crawler_assets.py tests\test_crawler_assets.py tests\test_web_preview.py` OK；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview -v` 通過，108 tests OK；完整 smoke `state\logs\pre_push_smoke_20260530_190104.log` 通過，987 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26682250910` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX contract / development log；本輪只增加 Crawler Asset display labels 與 Web 消費方式，不改 maturity/risk 判斷、crawler registry、download/import service、credential storage 或 Tk 操作。 |
| 18:49 | UI Contract / Web Consolidation | **CI PASS** | `6f20da2` | `26682035145` | Guard Web capability fallbacks | Web asset card 的「能力位址」缺值時顯示「待確認」，不再 fallback 到 `source_type` 短碼；能力膠囊摘要的 Seed 範式也改走 `displayTextOrFallback()`，缺 label 時顯示「Seed 範式待確認」而不是 raw `entry_listing` / `paginated_catalog`。驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，62 tests OK；完整 smoke `state\logs\pre_push_smoke_20260530_184921.log` 通過，987 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26682035145` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX contract / development log；本輪只調整 Web 顯示 fallback 與靜態 guard，不改後端 payload、crawler registry、download/import service、credential storage 或 Tk 操作。 |
| 18:37 | UI Contract / Web Consolidation | **CI PASS** | `c972bdd` | `26681755877` | Remove Web stage label map | Web `downloadImportStageText()` 已移除 download/import stage 翻譯表，只消費後端 `download_import.stage_label` / display label；缺 label 時顯示「下載狀態待確認」。測試也禁止 `download_import_completed` / `blocked_before_download` 的中文 stage map 回到 JS。驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，62 tests OK；完整 smoke `state\logs\pre_push_smoke_20260530_183533.log` 通過，987 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26681755877` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX contract / development log；本輪只移除 Web 端重複 stage 翻譯表與補靜態 guard，不改後端 payload、download/import 執行語意、crawler registry、credential storage 或 Tk 操作。 |
| 18:28 | UI Contract / Backend Display | **CI PASS** | `cb94da4` | `26681570087` | Add download import stage labels | `crawler_asset_download_import_display_payload()` 現在會輸出 `download_import.stage_label`，Web download/import credential-blocked payload 也會輸出同一類 label，讓 Web/Tk/未來 Qt 顯示「下載 / 匯入完成」「下載前需處理」而不必自行翻譯 `download_import_completed` / `blocked_before_download`。驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_display.py frontends\web\preview_payloads.py tests\test_crawler_asset_download.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_crawler_asset_download tests.test_web_preview -v` 通過，66 tests OK；完整 smoke `state\logs\pre_push_smoke_20260530_182547.log` 通過，987 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26681570087` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX contract / development log；本輪只增加 shared display payload 欄位與 Web blocked response label，不改 download/import 執行語意、crawler registry、credential storage 或 Tk 操作。 |
| 18:12 | UI Contract / Web | **CI PASS** | `c319655` | `26681344585` | Guard Web stage and review fallbacks | Web Preview 下載 / 匯入結果列的 Stage 改用 `downloadImportStageText()` 顯示人類文案；content review bucket、import lane、credential save mission、event context chips 與 Seed 範式 fallback 都透過 display-safe helper，不再把 `download_import_completed`、`content_parser_required`、`content_parser_review`、`entry_listing` 等 raw backend token 當作使用者文字。驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，62 tests OK；完整 smoke `state\logs\pre_push_smoke_20260530_181251.log` 通過，987 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26681344585` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX contract / development log；本輪只調整 Web 顯示 fallback 與靜態防回歸測試，不改後端 payload、crawler registry、download/import service、credential storage 或 Tk 操作。 |
| 17:55 | Database / Hardening | **CI PASS** | `7eb4e9a` | `26681030452` | Gate SQLite import writes | 新增 `api_launcher.sqlite_write_gate`，以 process-local / per-SQLite-path `RLock` 序列化同一 Python process 內的 SQLite writer；CSV/JSON importer 與 download-plan import policy decision 都已進入 `sqlite_write_gate()`，避免同一 curated SQLite path 被同 process 多個 import worker 同時寫入或同時做 rename/replace 決策。`--project-maturity-json` 也會輸出 `sqlite_write_gate_available=true` 與 gate profile。驗證：`py -3 -B -m py_compile api_launcher\sqlite_write_gate.py api_launcher\importers\csv_importer.py api_launcher\importers\json_importer.py api_launcher\downloads\plan_runner.py api_launcher\project_maturity.py tests\test_sqlite_write_gate.py tests\test_project_maturity.py` OK；`py -3 -B -m unittest tests.test_sqlite_write_gate tests.test_csv_importer tests.test_json_importer tests.test_project_maturity -v` 通過，20 tests OK；`py -3 -B -m unittest tests.test_ingestion_pipeline tests.test_crawler_asset_download tests.test_download_jobs tests.test_project_maturity tests.test_sqlite_write_gate -v` 通過，19 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_175518.log` 通過，987 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26681030452` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / project maturity matrix / development log；限制是這仍不是跨 process lock 或 unified bounded scheduler。 |
| 17:39 | UI Contract / Tk | **CI PASS** | `a223c62` | `26680645293` | Guard Tk next-action fallbacks | Tk crawler asset helper 裡的 blocked download-plan summary、Plan Passport stale summary、credential guard message、credential summary 不再在 label 缺失時回退顯示 raw `next_action` / `stale_reason`；都會使用 `_ui_next_action_text()` 產生的人類文案或中性 fallback。驗證：`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 通過，142 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_173622.log` 通過，983 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26680645293` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不改 backend payload、crawler registry、download/import service、credential storage 或 Web 操作。 |
| 17:32 | Docs / Workflow | **LOCAL DOCS** | `docs-only` | `not needed` | Web UIUX display contract doc sync | 小型 consolidation / docs drift 修補：`docs/WEB_PREVIEW_UIUX.zh-TW.md` 已把 Plan Passport freshness guard 從 `stale_next_action` raw token 校正成 `stale_next_action_label`，並補上 Web 使用者可見文字不得 fallback 到 snake_case / raw backend token 的規則。驗證：docs mojibake scan OK；`git diff --check` OK。Docs drift check：本輪就是 drift 修補；只更新 Web UIUX contract，不改 Web/Tk/後端行為，也不需要跑 RRKAL 測試。 |
| 17:24 | UI Contract / Web | **CI PASS** | `64f1fed` | `26680390399` | Guard Web display text fallbacks | Web Preview 新增 `displayTextOrFallback()` / `looksLikeBackendToken()`，讓 Downloader row、Crawler Passport、Credential badge、Plan Preview mission、Seed import badge、Hero next action、Plan Passport stale/next action 都拒絕 snake_case / raw token 當人類文案；raw ids 仍保留在 API / debug JSON。驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，62 tests OK；`git diff --check` 無 whitespace error（Git 仍提示 `frontends/web/static/app.js` line-ending warning）；完整 smoke `state\logs\pre_push_smoke_20260530_172115.log` 通過，983 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26680390399` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不改後端 payload、crawler registry、download/import service、credential storage 或 Tk 操作。 |
| 17:12 | UI Contract / Web | **CI PASS** | `2f0586a` | `26680156433` | Hide Web download action ids | Web Preview 的 asset-level / seed-level 正式下載與匯入未完成訊息改用 `downloadImportNextActionText()`，只消費後端 `next_action_label` 類文案；download/import result row 的 plan chip 也不再 fallback 到 raw `outcome_bucket`，避免把 `next_action` / `stage` / `outcome_bucket` raw id 顯示給使用者。驗證：`py -3 -B -m unittest tests.test_web_preview -v` 通過，62 tests OK；`git diff --check` 無 whitespace error（Git 仍提示 `frontends/web/static/app.js` line-ending warning）；本切片 UTF-8/mojibake check OK；完整 smoke `state\logs\pre_push_smoke_20260530_170812.log` 通過，983 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26680156433` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪不改 download/import service、resolver、crawler registry、credential storage 或 Tk 操作。 |
| 17:01 | UI Contract / Web | **CI PASS** | `61fb8d1` | `26679912192` | Add crawler asset health labels | Crawler Asset health payload 現在輸出 `status_label`、`status_tone` 與 `next_action_label`；Web asset passport 與 selected hero 的 state pill 改讀 `card.health.status_label`，未知 status fallback 也不再直接顯示 raw id。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_health.py tests\test_crawler_assets.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview -v` 通過，108 tests OK；`git diff --check` 無 whitespace error（Git 仍提示 `frontends/web/static/app.js` line-ending warning）；health label slice UTF-8/mojibake check OK；完整 smoke `state\logs\pre_push_smoke_20260530_165759.log` 通過，983 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26679912192` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 health display payload 與 Web state pill 消費方式，不改 health evaluation、crawler registry、download/import、credential storage 或 Tk 操作。 |
| 16:48 | UI Contract / Consolidation | **CI PASS** | `85485b6` | `26679666184` | Label crawler asset capability rows | `crawler_asset_card_capabilities()` 現在會輸出 capability `status_label` 與 `next_action_label`，Web asset passport 的 capability row 只顯示人類文案，不再直接組 `capability.status / capability.next_action`。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_capabilities.py api_launcher\crawler_asset_flow_display.py tests\test_crawler_assets.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview -v` 通過，108 tests OK；同一組測試前一次遇到 Windows localhost oversized-body socket flake，單測與全組 rerun 均通過；`git diff --check` 無 whitespace error（Git 仍提示 `frontends/web/static/app.js` line-ending warning）；capability label slice UTF-8/mojibake check OK；完整 smoke `state\logs\pre_push_smoke_20260530_164446.log` 通過，983 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26679666184` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 asset card capability display payload 與 Web passport 消費方式，不改 capability status 判斷、crawler registry、download/import、credential storage 或 Tk 操作。 |
| 16:33 | UI Contract / Web | **CI PASS** | `0255727` | `26679378203` | Use bound form labels in Web UI | Web Preview 的 seed schema probe 完成 mission 不再 fallback 到 `payload.schema_probe.status`，bounds form 狀態 pill 改吃後端 `spec.display_label`，不再直接顯示 `spec.status`。已驗證：`py -3 -B -m unittest tests.test_web_preview -v` 通過，62 tests OK；`git diff --check` 無 whitespace error（Git 仍提示 `frontends/web/static/app.js` line-ending warning）；Web slice UTF-8/mojibake check OK；完整 smoke `state\logs\pre_push_smoke_20260530_162942.log` 通過，982 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26679378203` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Web 顯示 fallback，不改 schema probe service、bounds form schema、download/import、credential storage、crawler registry 或 Tk 操作。 |
| 16:21 | UI Contract / Tk | **CI PASS** | `c71c830` | `26679162441` | Label remote pagination statuses | `crawler_remote_pagination_payload()` 現在會輸出遠端分頁狀態的 display payload；Tk seed enumeration note 優先顯示「仍有下一頁線索」「已列完」「遠端狀態待確認」等人類文案，不再把未知 remote status raw id 直接放進使用者可見內容。已驗證：`py -3 -B -m py_compile api_launcher\crawler_seed_display.py api_launcher\crawler_asset_listing_payloads.py frontends\tk\crawler_asset_ui_helpers.py tests\test_crawler_assets.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_tk_dialogs tests.test_web_preview -v` 通過，226 tests OK；同一組測試前一次遇到 Windows localhost oversized-body socket flake，單測與全組 rerun 均通過；`git diff --check` OK；`api_launcher` / `frontends` / `tests` mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260530_161735.log` 通過，982 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26679162441` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 remote pagination display payload 與 Tk seed enumeration note fallback，不改 crawler pagination 行為、token storage、seed enumeration limits、download/import、Web 操作或 user guide。 |
| 16:03 | UI Contract / Tk | **CI PASS** | `418efa9` | `26678879682` | Label account provider support status | `api_launcher.account_links` 新增帳號登入模式與支援狀態 label helper；Tk Google/Gemini 連線視窗的帳號支援表格會顯示「OAuth 登入」「規劃中」「🚧 施工中」等人類文案，不再把 `oauth` / `planned` / `skeleton` raw contract id 當成使用者文字。已驗證：`py -3 -B -m py_compile api_launcher\account_links.py frontends\tk\ai_settings_dialogs.py tests\test_account_links.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_account_links tests.test_tk_dialogs -v` 通過，123 tests OK；`git diff --check` OK；`api_launcher` / `frontends` / `tests` mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260530_160327.log` 通過，981 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26678879682` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整帳號支援表格顯示文案，不改 OAuth/API key storage、credential flow、account provider contract、crawler registry、download/import、Web 操作或 user guide。 |
| 15:49 | UI Contract / Tk | **CI PASS** | `d567e7d` | `26678612556` | Label dataset candidate review status | 新增 `api_launcher.dataset_candidate_display`，把 Dataset Candidate Review 的 `needs_review` / `approved` / `planned` / `rejected` / `all` 狀態顯示文案集中到後端 helper；Tk review dialog 的表格、detail 與 filter 下拉只顯示「待審核」「可用」「已排入」「已拒絕」「全部」等人類文案，送 repository 查詢或更新時才轉回 raw status id。已驗證：`py -3 -B -m py_compile api_launcher\dataset_candidate_display.py frontends\tk\dataset_candidate_review_dialog.py frontends\tk\table_data_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview -v` 通過，180 tests OK；`git diff --check` OK；`api_launcher` / `frontends` / `tests` mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260530_154916.log` 通過，980 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26678612556` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Dataset Candidate Review 狀態顯示與 Tk filter mapping，不改 candidate repository schema、crawler registry、download/import、Web 操作或 user guide。 |
| 15:39 | UI Contract / Consolidation | **CI PASS** | `3d179a7` | `26678335312` | Centralize adapter review item labels | 新增 `adapter_review_item_display_payload()`，把 Adapter Review item 的 required action、outcome bucket、content import status、content review bucket、content pipeline lane、content next action label 集中到後端 display helper；Tk dialog 只消費這份 payload，不再自行翻譯多組 enum。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_review_display.py api_launcher\crawler_asset_display.py frontends\tk\adapter_review_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview -v` 通過，180 tests OK；`git diff --check` OK；`api_launcher` / `frontends` mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260530_153556.log` 通過，980 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26678335312` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只收斂 Adapter Review item 顯示投影 ownership，不改 adapter review item JSON、resolver、download/import、crawler registry、credential storage、Web 操作或 user guide。 |
| 15:26 | UI Contract / Tk | **CI PASS** | `e4e3ca1` | `26678102571` | Label adapter review detail buckets | Adapter Review detail text 內的 `outcome_bucket`、`content_import_status`、`content_review_bucket`、`content_pipeline_lane` value 改用後端共用 display helper，顯示「來源解析待辦」「需內容 Parser review」「內容 Parser 待辦」等人類文案，不再把 `source_resolution_required` / `content_parser_required` / `content_parser_review` 直接放進使用者可見 detail。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_review_display.py api_launcher\crawler_asset_display.py frontends\tk\adapter_review_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview -v` 通過，180 tests OK；`git diff --check` OK；`api_launcher` / `frontends` / `tests` mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260530_152157.log` 通過，980 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26678102571` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Adapter Review detail display labels，不改 adapter review item JSON、resolver、download/import、crawler registry、credential storage、Web 操作或 user guide。 |
| 15:12 | UI Contract / Tk | **CI PASS** | `d5fba12` | `26677811389` | Label adapter review actions in Tk | Tk Adapter Review 對話框的 `required_action` / `content_next_action` 現在會透過 shared `next_action_display_label_or_fallback()` 顯示人類文案，例如「解析 API，產生可下載 resources」與「新增內容 Parser 或保留原始檔」，不再把 `resolve_api` / `add_content_parser_or_keep_raw_artifact` raw backend id 直接放進使用者可見表格或 detail text。已驗證：`py -3 -B -m py_compile api_launcher\crawler_next_action_display.py frontends\tk\adapter_review_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_crawler_next_action_display tests.test_tk_dialogs tests.test_web_preview -v` 通過，183 tests OK；`git diff --check` OK；`api_launcher` / `frontends` / `tests` mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260530_150912.log` 通過，980 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26677811389` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk Adapter review 顯示文案與共用 action label 表，不改 adapter review resolver、download/import、crawler registry、credential storage、Web 操作或 user guide。 |
| 14:54 | UI Contract / Tk-Web | **CI PASS** | `8e9788c` | `26677496659` | Hide unknown next action ids from UI | 新增 `next_action_display_label_or_fallback()`，讓 Tk/Web/後端 display payload 遇到未知 snake_case `next_action` 時回到安全人類提示；已登錄 action id 仍顯示共用 label，診斷層仍可保留 raw id。Tk credential guard、credential summary、seed download/import message、crawler asset row fallback、Web asset card / listing payload、crawler download/import display payload 都改吃這個 UI-safe helper。已驗證：`py -3 -B -m py_compile api_launcher\crawler_next_action_display.py api_launcher\crawler_asset_download.py api_launcher\crawler_asset_display.py api_launcher\crawler_plan_outcome_display.py frontends\web\preview_assets.py frontends\web\preview_payloads.py frontends\tk\crawler_asset_ui_helpers.py tests\test_crawler_next_action_display.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_crawler_next_action_display tests.test_tk_dialogs tests.test_crawler_assets tests.test_web_preview tests.test_crawler_asset_download -v` 通過，232 tests OK；`git diff --check` OK；`api_launcher` / `frontends` / `tests` mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260530_145227.log` 通過，980 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26677496659` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 UI/display fallback，不改 crawler asset listing、download/import 執行語意、credential storage、crawler registry 或 user guide。 |
| 14:35 | UI Contract / Tk | **CI PASS** | `d4f924d` | `26677141154` | Label crawler asset row fallback actions | Tk Crawler Asset 表格在沒有最近 plan outcome 時，最後欄位會透過 shared `next_action_display_label(asset.next_action)` 顯示人類文案，不再把 `run_full_crawl_or_export_candidates` raw id 直接顯示給使用者；新增 regression test 鎖住此 fallback。已驗證：`py -3 -B -m py_compile api_launcher\crawler_next_action_display.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過，115 tests OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_tk_dialogs tests.test_web_preview -v` 通過，222 tests OK；`git diff --check` OK；`api_launcher` / `frontends` mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260530_143559.log` 通過，974 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26677141154` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk table display fallback，不改 crawler asset profile、listing、download/import、Web 操作、credential storage、crawler registry 或 user guide。 |
| 14:24 | UI Contract / Tk-Web | **CI PASS** | `6a0c27c` | `26676890389` | Label stale plan passport actions | Stale Plan Passport payload 現在輸出 `stale_next_action_label`，Tk/Web 顯示 stale plan 下一步時優先使用人類文案，不再把 `enable_before_building_download_plan` 這類 raw action id 放進使用者可見摘要；raw id 仍保留給 agent/debug。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_profiles.py api_launcher\crawler_next_action_display.py frontends\tk\crawler_asset_ui_helpers.py tests\test_crawler_assets.py tests\test_tk_dialogs.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_tk_dialogs tests.test_web_preview -v` 通過，221 tests OK；`git diff --check` OK；`api_launcher` / `frontends` mojibake scan OK；完整 smoke `state\logs\pre_push_smoke_20260530_142419.log` 通過，973 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26676890389` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 stale Plan Passport display contract，不改 plan staleness 判斷、profile storage、download/import、Web/Tk 操作流程、credential storage、crawler registry 或 user guide。 |
| 14:09 | UI Contract / Tk | **CI PASS** | `55a180d` | `26676617783` | Hide bounds warning codes in Tk | Tk Crawler Asset 界域表單的警示預覽現在由 `crawler_asset_bound_warning_text()` 產生人類文案，說明欄位探測建議或探測結果已套用但仍需人工確認，不再把 `warning_codes` / `schema_probe_*` raw id 直接顯示給使用者。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_bound_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_crawler_assets tests.test_web_preview -v` 通過，221 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_140930.log` 通過，973 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26676617783` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只調整 Tk warning display contract，不改 schema probe service、bounds payload、download/import、Web 操作、credential storage、crawler registry 或 user guide。 |
| 13:55 | UI Contract / Tk | **CI PASS** | `4abe249` | `26676323224` | Show download bound labels in Tk | `download_bound_status` 現在由後端輸出 `applied_labels.zh_TW/en`，Tk plan bounds status 優先顯示「樣本上限、時間範圍、下載大小上限」等人類文案，不再把 `sample_limit` / `max_bytes_enforced` raw code 直接丟給使用者。已驗證：`py -3 -B -m py_compile api_launcher\source_download.py frontends\tk\plan_workflows.py tests\test_source_download.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_source_download tests.test_tk_dialogs -v` 通過，123 tests OK；`py -3 -B -m unittest tests.test_source_download tests.test_tk_dialogs tests.test_tk_ui_helpers tests.test_crawler_asset_download -v` 通過，150 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_135528.log` 通過，971 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26676323224` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 backend display labels 並讓 Tk plan status 消費它，不改 download/import 行為、Web 操作、credential storage、crawler registry 或 user guide。 |
| 13:42 | Download / Payload Contract | **CI PASS** | `3ff00fc` | `26676072708` | Clarify max bytes bound status | `api_launcher/source_download.py` 的 `download_bound_status.applied` 現在把 `SourceDownloadBounds.max_bytes` 標成 `max_bytes_enforced`，對齊 `HTTPDownloadAdapter` 已實際執行 byte cap 的現況，避免 Tk/Web/agent 把它看成 review-only metadata。已驗證：`py -3 -B -m py_compile api_launcher\source_download.py tests\test_source_download.py` OK；`py -3 -B -m unittest tests.test_source_download -v` 通過，11 tests OK；`py -3 -B -m unittest tests.test_source_download tests.test_http_downloader tests.test_download_plan_runner tests.test_crawler_asset_download tests.test_tk_ui_helpers -v` 通過，60 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_134217.log` 通過，971 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26676072708` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只修正 backend payload label，不改 Web/Tk 操作流程、實際 download/import 行為、credential storage、crawler registry 或 user guide。 |
| 11:44 | Metadata / Hardening | **CI PASS** | `3bea35b` | `26673700933` | Expose metadata probe truncation flag | `api_launcher/core.py` 的 `safe_fetch_metadata()` 保留既有 bounded excerpt 行為，但成功 response 超過 `max_bytes` 時會在 payload 回傳 `truncated=true`，讓 agent/UI 知道 excerpt 不是完整頁面。已驗證：`py -3 -B -m py_compile api_launcher\core.py tests\test_metadata_probe.py` OK；`py -3 -B -m unittest tests.test_metadata_probe -v` 通過，3 tests OK；`api_launcher` mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_114424.log` 通過，971 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26673700933` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 handoff / development log；本輪只新增 metadata probe JSON 欄位，不改 provider discovery、crawler registry、download/import、Web/Tk 操作或 user guide。 |
| 11:34 | Download / Hardening | **CI PASS** | `a0ee7bc` | `26673503912` | Enforce download max byte bounds | `api_launcher/downloads/http.py` 現在會讀 plan entry 的 `download_bounds.max_bytes`；有 `Content-Length` / `Content-Range` 時提前拒絕超量下載，未知總長度時在 chunk 累計超過 budget 前 fail-fast，避免 source bounds 的 `max_bytes` 只停留在 review metadata。已驗證：`py -3 -B -m py_compile api_launcher\downloads\http.py tests\test_http_downloader.py` OK；`py -3 -B -m unittest tests.test_http_downloader tests.test_download_plan_runner tests.test_source_download -v` 通過，33 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_113427.log` 通過，970 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26673503912` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只讓既有 `max_bytes` bounds 在 HTTP download layer 生效，不改 Web/Tk 表單操作、crawler registry、adapter resolver、credential storage 或 user guide。 |
| 11:19 | Bounds / Hardening | **CI PASS** | `53ac5bd` | `26673225198` | Reject oversized schema probe responses | `api_launcher/schema_probe.py` 的 `fetch_probe_bytes()` 現在會讀 `max_bytes + 1`，若 schema/head probe response 超過 budget 會明確 raise `ValueError`，避免用截斷 CSV/JSON 推論欄位並誤導 Web/Tk 動態界域表單。已驗證：`py -3 -B -m py_compile api_launcher\schema_probe.py tests\test_source_download.py` OK；`py -3 -B -m unittest tests.test_source_download tests.test_tk_dialogs tests.test_web_preview -v` 通過，185 tests OK；`api_launcher` mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_111958.log` 通過，969 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26673225198` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只加固 schema/head probe response budget，不改 Web/Tk 操作流程、crawler registry、download/import、credential storage 或 user guide。 |
| 11:08 | Project Maturity / Crawler Registry | **CI PASS** | `8ce74e7` | `26672959016` | Expose crawler registry maturity metrics | `--project-maturity-json` 的 `source_pattern_and_crawler_handlers.metrics` 現在輸出 `registry_matrix_cell_count`、`capability_address_width`、`capability_address_group_count`、`seed_scope_counts`、`dispatch_owner` 與相容 surface，讓成熟度矩陣明確表示 crawler source handler 宣告式 registry/matrix 已落地，但不把 deep adapter 覆蓋誤算進這一格。已驗證：`py -3 -B -m py_compile api_launcher\project_maturity.py tests\test_project_maturity.py` OK；`py -3 -B -m unittest tests.test_project_maturity tests.test_dataset_discovery -v` 通過，63 tests OK；`py -3 -B APIkeys_collection.py --project-maturity-json` 可看到 source handler row 的 registry metrics；`api_launcher` mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_110838.log` 通過，968 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26672959016` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只擴充成熟度 JSON 證據，不改 crawler handler、registry dispatch、Web/Tk 操作、download/import、credential storage 或 user guide。 |
| 10:58 | Web / Hardening | **CI PASS** | `461c749` | `26672739923` | Bound Web preview POST bodies | `frontends/web/server.py` 新增 `DEFAULT_WEB_PREVIEW_POST_BODY_MAX_BYTES=1024 * 1024`，`read_json_body()` 與 `discard_request_body()` 都會先檢查 `Content-Length`；oversized POST body 會回 400 並停在 crawler asset route handler 或 developer diagnostic handler 前，避免本機 Web Preview API 被錯誤 client 或過大 payload 拖住。已驗證：`py -3 -B -m py_compile frontends\web\server.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，62 tests OK；`frontends\web` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_105811.log` 通過，968 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26672739923` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / Web Preview UIUX notes / development log；本輪只加固本機 Web Preview POST body budget，不改 Web UI 操作、crawler、download/import、credential storage、Tk 操作或 user guide。 |
| 10:46 | Tk / Hardening | **CI PASS** | `491b711` | `26672465125` | Reject oversized favicon responses | `download_favicon_png()` 現在會讀 `max_bytes + 1` 並拒絕 oversized response，讓異常大的 favicon payload fail-fast，不再交給 Pillow 解析；正常小 icon 仍會轉成 Tk 用 PNG cache。已驗證：`py -3 -B -m py_compile api_launcher\favicons.py tests\test_favicons.py` OK；`py -3 -B -m unittest tests.test_favicons tests.test_tk_dialogs -v` 通過，118 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_104606.log` 通過，965 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26672465125` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只加固 favicon response budget，不改 favicon URL 正規化、cache path、Tk/Web 操作、crawler、download/import、credential 或 user guide。 |
| 10:37 | Discovery / Hardening | **CI PASS** | `fe6fa58` | `26672279172` | Reject oversized source pattern probes | `fetch_pattern_probe()` 現在會讀 `max_bytes + 1` 並拒絕 oversized response，讓 source pattern detector 遇到過大 payload 時安全降級成 `unknown` / review，而不是用截斷 JSON/HTML 誤判來源範式。已驗證：`py -3 -B -m py_compile api_launcher\crawlers\source_patterns.py tests\test_source_patterns.py` OK；`py -3 -B -m unittest tests.test_source_patterns tests.test_source_pattern_drafts -v` 通過，41 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_103738.log` 通過，964 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26672279172` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / dataset discovery notes / development log；本輪只加固 detector probe budget，不改 detector registry、source draft review gate、crawler handlers、download/import、credential、Tk/Web 操作或 user guide。 |
| 10:28 | Project Maturity / Scheduler | **CI PASS** | `a6ee50e` | `26672090649` | Expose Tk scheduler guard metrics | `--project-maturity-json` 的 `background_jobs_and_scheduler.metrics` 現在除了 policy count / max-active table，也輸出 `capacity_policy_call_site_guarded=true`、`direct_thread_spawn_guarded=true`、`direct_thread_spawn_owner=frontends/tk/background_jobs.py` 與對應 guard test 名稱，讓 agent/UI 可從成熟度 payload 看見 scheduler hardening 的測試證據。已驗證：`py -3 -B -m py_compile api_launcher\project_maturity.py tests\test_project_maturity.py` OK；`py -3 -B -m unittest tests.test_project_maturity tests.test_tk_background_jobs -v` 通過，13 tests OK；`py -3 -B APIkeys_collection.py --project-maturity-json` 顯示 scheduler guard metrics；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_102820.log` 通過，963 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26672090649` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / project maturity matrix / development log；本輪只擴充成熟度 JSON 的 metrics，不改 Tk runtime 行為、policy 數量、crawler、download/import、credential、Web 操作或 user guide。 |
| 10:19 | Tk / Hardening | **CI PASS** | `8eae394` | `26671893236` | Guard Tk direct thread spawning | 新增第二道 scheduler 測試護欄：`tests/test_tk_background_jobs.py` 以 AST 掃描 `frontends/tk/*.py`，要求 Tk 模組除了 `frontends/tk/background_jobs.py` 以外不能直接呼叫 `threading.Thread` / `Thread(...)`；未來背景 worker 必須經由共用 helper 與 policy registry。已驗證：`py -3 -B -m py_compile tests\test_tk_background_jobs.py` OK；`py -3 -B -m unittest tests.test_tk_background_jobs -v` 通過，9 tests OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs -v` 通過，121 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_101939.log` 通過，963 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26671893236` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只新增測試 guardrail，不改 Tk runtime 行為、policy 數量、crawler、download/import、credential、Web 操作或 user guide。 |
| 10:10 | Tk / Hardening | **CI PASS** | `57ae939` | `26671701107` | Guard Tk background job capacity policy | 新增測試護欄：`tests/test_tk_background_jobs.py` 以 AST 掃描 `frontends/tk/*.py`，確保所有 `start_single_flight_thread(...)` call site 都明確傳入 `max_active_jobs`，避免未來新增 Tk 背景 worker 時繞過 `frontends/tk/background_job_policies.py`。已驗證：`py -3 -B -m py_compile tests\test_tk_background_jobs.py` OK；`py -3 -B -m unittest tests.test_tk_background_jobs -v` 通過，8 tests OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs -v` 通過，120 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_101011.log` 通過，962 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26671701107` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只新增測試 guardrail，不改 Tk runtime 行為、policy 數量、crawler、download/import、credential、Web 操作或 user guide。 |
| 09:59 | Tk / Hardening | **CI PASS** | `66ca52e` | `26671489455` | Cap remaining Tk background jobs | developer CLI subprocess、MVP demo smoke、showcase download 也納入 `frontends/tk/background_job_policies.py`，每個 UI instance 同時最多 1 個；`--project-maturity-json` 的 `background_jobs_and_scheduler.metrics.bounded_tk_policy_count` 現在為 11。已驗證：`py -3 -B -m py_compile frontends\tk\background_job_policies.py frontends\tk\developer_cli_dialog.py frontends\tk\mvp_demo_workflows.py frontends\tk\showcase_workflows.py tests\test_tk_background_jobs.py` OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs tests.test_project_maturity -v` 通過，123 tests OK；`py -3 -B APIkeys_collection.py --project-maturity-json` 顯示 `bounded_tk_policy_count=11`；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_095939.log` 通過，961 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26671489455` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / project maturity matrix / development log；本輪只補 Tk background job caps，不改 subprocess 命令語義、MVP demo/showcase 內容、crawler、download/import、credential、Web 操作或 user guide。 |
| 09:49 | Tk / Hardening | **CI PASS** | `4ba6576` | `26671260990` | Expose Tk background policy metrics | 修正文檔漂移：`PROJECT_MATURITY_MATRIX` 已說 Tk 背景工作上限有 typed policy registry，但 `--project-maturity-json` 的 background scheduler row 仍是舊描述、metrics 空白。現在 `api_launcher.project_maturity` 會輸出 `policy_registry_available=true`、`bounded_tk_policy_count=8` 與 `max_active_jobs_by_policy`，讓 Web/Tk/agent 能讀到 scheduler hardening 的已完成部分。已驗證：`py -3 -B -m py_compile api_launcher\project_maturity.py tests\test_project_maturity.py frontends\tk\background_job_policies.py` OK；`py -3 -B -m unittest tests.test_project_maturity tests.test_tk_background_jobs -v` 通過，11 tests OK；`py -3 -B APIkeys_collection.py --project-maturity-json` 已顯示 `background_jobs_and_scheduler.metrics.max_active_jobs_by_policy.sqlite_import=1`；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_094947.log` 通過，961 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26671260990` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 handoff / development log / project maturity matrix；本輪只修正 machine-readable maturity payload，不改 Tk/Web 操作、crawler、download/import、credential 或 user guide。 |
| 09:40 | Tk / Hardening | **CI PASS** | `b2bcdf9` | `26671011263` | Declare Tk background job policies | 新增 `frontends/tk/background_job_policies.py`，把 AI summary、crawler asset、discovery、OAuth、plan bounds probe、sidebar favicon、source action、SQLite import 的 `max_active_jobs` 與 active-job owner attrs 收斂成 typed policy registry；各 workflow 改為匯入 policy 常數，不再自行維護裸值。這是 declarative policy 表，不是全域 scheduler 或 asyncio 重寫。已驗證：`py -3 -B -m py_compile` 相關 Tk workflow / policy / tests OK；`py -3 -B -m unittest tests.test_tk_background_jobs -v` 通過，7 tests OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs tests.test_tk_ui_helpers -v` 通過，142 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_093807.log` 通過，961 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26671011263` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / project maturity matrix / development log；本輪只整理既有 Tk background job caps，不改 worker 行為、UI 操作、crawler、download/import、credential、Web 操作或 user guide。 |
| 09:23 | Credential / Hardening | **CI PASS** | `79dea32` | `26670676995` | Bound Tk OAuth background jobs | Tk Google browser login 與 device-code polling 仍用同一份 OAuth single-flight helper，但同一 UI 同時最多 2 個 OAuth background worker；queue 滿時走 capacity callback，不再開新的 callback server / token polling worker。已驗證：`py -3 -B -m py_compile frontends\tk\oauth_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過，112 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_092339.log` 通過，958 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26670676995` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 Tk OAuth 背景工作容量 guard，不改 token exchange/storage、credential UI、crawler、download/import、Web 操作或 user guide。 |
| 09:14 | Tk / Hardening | **CI PASS** | `20d110c` | `26670478264` | Bound Tk sidebar favicon jobs | Tk sidebar favicon 背景下載現在同一 UI 同時最多 4 個 provider favicon worker；重複 owner/favicon 仍維持 single-flight，queue 滿時跳過本輪下載並清掉 loading marker，下一次 sidebar 重繪仍可再試。已驗證：`py -3 -B -m py_compile frontends\tk\sidebar_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過，111 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_091441.log` 通過，957 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26670478264` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 Tk sidebar favicon 背景工作容量 guard，不改 favicon URL 推導、cache、下載 byte budget、crawler、download/import、credential、Web 操作或 user guide。 |
| 09:04 | Tk / Hardening | **CI PASS** | `fb301fa` | `26670244075` | Bound Tk plan bounds probe jobs | Tk 下載計畫的「界域欄位探測」現在有 capacity guard，同一 UI 同時最多 2 個 plan bounds/schema probe worker；同一 plan item 仍維持 single-flight，queue 滿時只更新狀態，不開新 thread。已驗證：`py -3 -B -m py_compile frontends\tk\plan_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過，110 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_090436.log` 通過，956 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26670244075` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 Tk plan bounds probe 背景工作容量 guard，不改 schema probe service、bounds form contract、download/import、crawler、credential、Web 操作或 user guide。 |
| 08:55 | Tk / Profile Display | **CI PASS** | `96b1850` | `26670001042` | Show Tk seed scope label from profile | Tk Crawler Asset 表格與右側 Crawler Passport 現在優先顯示後端 `capability_profile.seed_scope_label`，例如「入口列表」「分頁 catalog」，只有缺 label 時才 fallback 到 raw `seed_scope` / `current_seed_scope`。這讓 Tk 與 Web 一樣消費同一份 backend display contract，不再把 raw enum 當成主要使用者文案。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_tk_ui_helpers -v` 通過，132 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_085350.log` 通過，955 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26670001042` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / discovery notes / Web Preview UIUX / development log；本輪只改 Tk 顯示投影，不改 crawler handler、seed enumeration service、download/import、credential、Web 操作或 user guide。 |
| 08:37 | Web / Profile Display | **CI PASS** | `0dcc809` | `26669557147` | Add seed scope display label to profiles | `CrawlerCapabilityProfile.to_dict()` 現在輸出 `seed_scope_label`，Web Preview 的 Crawler Passport 與能力膠囊摘要優先顯示後端 label，再 fallback 到 raw `seed_scope`。這把使用者可讀文案收回 backend profile，避免 Web/Tk/未來 Qt 各自翻譯 entry-listing / paginated-catalog。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview -v` 通過，104 tests OK；`git diff --check` OK（僅既有 CRLF/LF warning）；完整 smoke `state\logs\pre_push_smoke_20260530_083705.log` 通過，954 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26669557147` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / discovery notes / Web Preview UIUX / development log；本輪只新增 UI-neutral display label，不改 seed scope registry、crawler handler、seed enumeration service、download/import、credential、Tk 操作或 user guide。 |
| 08:26 | Web / Profile Display | **CI PASS** | `c3ad3f5` | `26669287121` | Show seed scope in Web Preview passport | Web Preview 的 Crawler Passport 現在直接顯示 `capability_profile.seed_scope`，能力膠囊摘要也包含 seed scope，讓使用者/agent 可辨識 crawler seed 枚舉表面是 `entry_listing` 或 `paginated_catalog`。Web 只薄顯示後端 contract，不從 `source_type` 重建分流。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m py_compile tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，59 tests OK；`git diff --check` OK（僅既有 CRLF/LF warning）；完整 smoke `state\logs\pre_push_smoke_20260530_082633.log` 通過，954 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26669287121` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / discovery notes / Web Preview UIUX / development log；本輪只改 Web 顯示層與測試，不改 crawler handler、seed enumeration service、download/import、credential、Tk 操作或 user guide。 |
| 08:17 | Discovery / Profile | **CI PASS** | `38b4a8f` | `26668930811` | Expose seed scope in capability profile | `CrawlerCapabilityProfile` 新增 `seed_scope`，crawler asset payload 的 `capability_profile` 現在直接輸出 registry 宣告的 `entry_listing` / `paginated_catalog`，讓 Tk/Web/未來 Qt 不必從 raw `source_type` 或 `current_seed_scope` 反推 crawler 本身的 seed 枚舉語義。已驗證：in-memory compile `api_launcher\crawler_capability_profiles.py` / `tests\test_crawler_assets.py` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview -v` 通過，104 tests OK；sample `html_file_index` asset payload 輸出 `capability_profile.seed_scope=entry_listing`；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_081522.log` 通過，954 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26668930811` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / discovery notes / development log；本輪不改 crawler handler 行為、download/import、credential、Tk/Web 操作或 user guide。 |
| 08:06 | Discovery / Registry | **CI PASS** | `4052c5f` | `26668632330` | Declare crawler seed scope metadata | `CrawlerSpec` 新增 `seed_scope`，14 個 crawler handler decorator 明確宣告 `entry_listing` 或 `paginated_catalog`；`dataset_seed_coverage.py` 改由 registry metadata 產生 entry-listing / paginated-catalog source type set，不再自行維護平行硬編碼清單。`crawler_registry_report` 也輸出 `dimensions.seed_scope` / summary seed scopes，供 developer diagnostics 與未來 UI/Qt filter 使用。已驗證：in-memory compile 相關 registry / dataset seed coverage / diagnostics / tests OK；`py -3 -B -m unittest tests.test_dataset_discovery tests.test_developer_diagnostics -v` 通過，63 tests OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_crawler_audit_smoke tests.test_handoff tests.test_heartbeat -v` 通過，81 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_080433.log` 通過，954 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26668632330` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / discovery notes / development log；本輪不改 crawler handler 行為、download/import、credential、Tk/Web 操作或 user guide。 |
| 07:46 | Backend / Consolidation | **CI PASS** | `3e690f6` | `26668021259` | Name metadata error excerpt budget | 新增 `DEFAULT_HTTP_ERROR_EXCERPT_MAX_BYTES=8192`，讓 `core.safe_fetch_metadata()` 處理 HTTPError 時讀取的 error body excerpt budget 不再藏成裸值；metadata probe 正常 response 的 bounded excerpt 行為不變。已驗證：in-memory compile `api_launcher\core.py` / `tests\test_metadata_probe.py` OK；`py -3 -B -m unittest tests.test_metadata_probe -v` 通過，2 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_074401.log` 通過，952 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26668021259` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只命名 metadata probe HTTP error excerpt budget，不改 metadata CLI output shape、crawler、download/import、credential、Tk/Web 操作或 user guide。 |
| 07:36 | Backend / Consolidation | **CI PASS** | `6dbd7ee` | `26667734258` | Name event log tail block budget | 新增 `DEFAULT_EVENT_LOG_TAIL_BLOCK_BYTES=8192`，讓 `latest_events()` / `_tail_text_lines_seek()` 的 tail block size 不再藏成裸值；雲端碟 binary seek/read 失敗時 fallback 到 streaming tail 的行為不變。已驗證：in-memory compile `api_launcher\event_log.py` / `tests\test_event_log.py` OK；`py -3 -B -m unittest tests.test_event_log -v` 通過，4 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_073441.log` 通過，950 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26667734258` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只命名 event log tail block budget，不改 event JSONL shape、handoff output、UI/CLI 操作、crawler、download/import、credential 或 user guide。 |
| 07:29 | Discovery / CLI | **CI PASS** | `299a870` | `26667473612` | Expose provider discovery fetch budget | CLI 新增 `--provider-discovery-max-bytes`，預設讀 `DEFAULT_PROVIDER_DISCOVERY_FETCH_MAX_BYTES=120_000`，並把該值傳給 `discover_provider_candidates()`；大型官方 docs/homepage 可由命令調高 budget，不需要改產品碼。已驗證：in-memory compile `api_launcher\cli_discovery.py` / `tests\test_discovery.py` OK；`py -3 -B -m unittest tests.test_discovery -v` 通過，10 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_072640.log` 通過，950 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26667473612` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 provider discovery CLI budget 參數，不改 Tk/Web 操作、provider promotion、local discovery audit、crawler、download/import、credential 或 user guide。 |
| 07:20 | Discovery / Hardening | **CI PASS** | `7ee46e9` | `26667191598` | Bound provider discovery fetch size | 新增 `DEFAULT_PROVIDER_DISCOVERY_FETCH_MAX_BYTES=120_000`，並讓 `discovery.fetch_text()` 對 homepage/docs response 讀 `max_bytes + 1`；若遠端頁面超過 budget，會 fail-fast 而不是靜默用截斷頁面推斷 metadata、API base 或 auth hints。已驗證：in-memory compile `api_launcher\discovery.py` / `tests\test_discovery.py` OK；`py -3 -B -m unittest tests.test_discovery -v` 通過，9 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_071727.log` 通過，949 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26667191598` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 provider discovery metadata fetch size guard，不改 provider promotion、local discovery audit、crawler、download/import、credential 或 user guide。 |
| 07:10 | AI / Hardening | **CI PASS** | `7c594b4` | `26666910109` | Bound AI summary response size | 新增 `DEFAULT_AI_SUMMARY_RESPONSE_MAX_BYTES=2 * 1024 * 1024`，並讓 `integrations._post_json()` 接受可覆寫 `max_bytes`；AI summary 相關 OpenAI-compatible / Gemini / Ollama 類 JSON response 會讀 `max_bytes + 1` 並拒絕過大 payload，避免 summary 生成遇到異常 response 時無界讀取。已驗證：in-memory compile `api_launcher\integrations.py` / `tests\test_ai_summary_generation.py` OK；`py -3 -B -m unittest tests.test_ai_summary_generation -v` 通過，12 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_070812.log` 通過，947 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26666910109` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 AI summary POST JSON response size guard，不改 AI profile selection、credential storage、credential UI、crawler、download/import 或 user guide。 |
| 07:00 | Credential / Hardening | **CI PASS** | `52bae62` | `26666579322` | Bound Google OAuth form response size | 新增 `DEFAULT_GOOGLE_OAUTH_FORM_MAX_BYTES=512 * 1024`，並讓 `google_auth._post_form()` 接受可覆寫 `max_bytes`；legacy Google device/token POST response 會讀 `max_bytes + 1` 並拒絕過大 payload，對齊 `oauth_device` 的 bounded guard。已驗證：in-memory compile `api_launcher\google_auth.py` / `tests\test_google_auth.py` OK；`py -3 -B -m unittest tests.test_google_auth -v` 通過，6 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_065755.log` 通過，945 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26666579322` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 legacy Google OAuth POST form response size guard，不改 token storage、credential UI、crawler、download/import 或 user guide。 |
| 06:51 | Credential / Hardening | **CI PASS** | `0eb72a5` | `26666292682` | Bound OAuth form response size | 新增 `DEFAULT_OAUTH_FORM_MAX_BYTES=512 * 1024`，並讓 `oauth_device._post_form()` 接受可覆寫 `max_bytes`；device-code / browser-code token POST response 會讀 `max_bytes + 1` 並拒絕過大 payload。已驗證：in-memory compile `api_launcher\oauth_device.py` / `tests\test_oauth_device.py` OK；`py -3 -B -m unittest tests.test_oauth_device -v` 通過，12 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_064907.log` 通過，943 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26666292682` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 OAuth POST form response size guard，不改 token storage、credential UI、crawler、download/import 或 user guide。 |
| 06:42 | Adapter Review / Hardening | **CI PASS** | `66cfa31` | `26665963070` | Bound adapter metadata fetch size | 新增 `DEFAULT_ADAPTER_JSON_MAX_BYTES=8 * 1024 * 1024`，並讓 `fetch_json()` 接受可覆寫 `max_bytes`；外部 metadata lookup 會讀 `max_bytes + 1` 並拒絕過大 payload，避免 adapter review resolver 無界讀取 CKAN / CMR / DataCite / Dataverse 等 JSON。已驗證：in-memory compile `api_launcher\adapter_plan_resolver.py` / `tests\test_adapter_plan_resolver.py` OK；`py -3 -B -m unittest tests.test_adapter_plan_resolver -v` 通過，57 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_063947.log` 通過，941 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26665963070` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只增加 adapter metadata fetch size guard，不改 resolver output shape、download/import、Web/Tk 操作、credential 或 user guide。 |
| 06:32 | Tk / Hardening | **CI PASS** | `b5e3e58` | `26665627697` | Name favicon fetch byte budget | 新增 `DEFAULT_FAVICON_MAX_BYTES=128 * 1024`，並讓 `download_favicon_png()` 接受可覆寫 `max_bytes`，保留 Tk favicon cache 128 KiB 預設讀取上限，但不再把 byte budget 藏成裸值。已驗證：in-memory compile `api_launcher\favicons.py` / `tests\test_favicons.py` OK；`py -3 -B -m unittest tests.test_favicons -v` 通過，5 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_063016.log` 通過，939 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26665627697` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只命名並外顯 favicon fetch byte budget，不改 favicon URL 正規化、cache path、Tk/Web 操作、crawler、download/import、credential 或 user guide。 |
| 06:23 | Backend / Hardening | **CI PASS** | `8e0f123` | `26665313476` | Name source pattern probe byte budget | 新增 `DEFAULT_PATTERN_PROBE_MAX_BYTES=128 * 1024`，並讓 `fetch_pattern_probe()` 接受可覆寫 `max_bytes`，保留既有 source pattern detector 128 KiB 預設讀取上限，但不再把 byte budget 藏成裸值。已驗證：in-memory compile `api_launcher\crawlers\source_patterns.py` / `tests\test_source_patterns.py` OK；`py -3 -B -m unittest tests.test_source_patterns -v` 通過，26 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_062054.log` 通過，938 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26665313476` 通過 Ubuntu、Windows 與 real DB smoke。`py_compile` 曾被 L 槽雲端 `__pycache__` replace lock 擋住，已用不寫 pyc 的 compile 驗證語法。Docs drift check：已同步 GTD / handoff / development log；本輪只命名並外顯 source pattern detector probe byte budget，不改 detector scoring、crawler dispatch、Web/Tk 操作、download/import、credential 或 user guide。 |
| 06:12 | Backend / Hardening | **CI PASS** | `241a3c7` | `26664906622` | Name schema probe byte budget | 新增 `DEFAULT_SCHEMA_PROBE_MAX_BYTES=128 * 1024`，並讓 `fetch_probe_bytes()` 接受可覆寫 `max_bytes`，保留既有 schema/head probe 128 KiB 預設讀取上限，但不再把 byte budget 藏成裸 `1024 * 128`。已驗證：`py -3 -B -m py_compile api_launcher\schema_probe.py tests\test_source_download.py` OK；`py -3 -B -m unittest tests.test_source_download -v` 通過，10 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_060957.log` 通過，937 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26664906622` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只命名並外顯 schema/head probe byte budget，不改 Web/Tk 欄位探測操作、crawler、download/import、credential 或 user guide。 |
| 06:02 | Backend / Hardening | **CI PASS** | `ff56909` | `26664558279` | Bound resource metadata traversal depth | `resource_mappings_from_candidate()` 新增 `MAX_RESOURCE_MAPPING_DEPTH=12` 與可覆寫 `max_depth` 參數，讓 Adapter review resolver 在攤平 JSON-LD/DCAT/Schema.org 巢狀 resources 時保留正常行為，但遇到異常深層 metadata 會安全停止，避免無限制遞迴。已驗證：`py -3 -B -m py_compile api_launcher\adapter_plan_resolver.py tests\test_adapter_plan_resolver.py` OK；`py -3 -B -m unittest tests.test_adapter_plan_resolver -v` 通過，55 tests OK；`api_launcher` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_055953.log` 通過，936 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26664558279` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只新增 adapter resolver metadata traversal 防護，不改 crawler、download/import service、CLI/Tk/Web 操作或 user guide。 |
| 05:50 | Backend / Consolidation | **CI PASS** | `6460eaa` | `26664108955` | Extract direct resource resolver lane | 新增 `direct_resource_entry_from_resource()`，把 `adapter_plan_resolver.py` 中單一 resource summary 是否能 promotion 成 bounded direct file 的判斷抽成獨立 helper；`direct_resource_entries_for_plan_entry()` 保留原 pipeline 順序：bounded API resolver -> resource summaries -> Socrata/NCEI fallback -> metadata lookup -> ERDDAP sample。已驗證：in-memory compile `api_launcher\adapter_plan_resolver.py` / `tests\test_adapter_plan_resolver.py` OK；`py -3 -B -m unittest tests.test_adapter_plan_resolver -v` 通過，54 tests OK；`api_launcher` mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_054828.log` 通過，935 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26664108955` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 adapter resolver 內部 helper 邊界，不改 crawler、download/import service、CLI/Tk/Web 操作或 user guide。 |
| 05:40 | Web / Consolidation | **CI PASS** | `7628c7d` | `26663712241` | Bundle Web plan preview result payload | 新增 `WebPlanPreviewResultResponse` 與 `web_plan_preview_result_payload()`，讓 Web plan preview route 在取得 response fragment 時同時取得後續 profile passport persist / event log 需要的 compact `plan_outcome` 與 `plan_passport`；舊 `web_plan_preview_result_response()` 保留 dict response 相容入口。已驗證：in-memory compile `frontends\web\preview_api.py` / `frontends\web\preview_payloads.py` / `tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，59 tests OK；`frontends\web` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_053838.log` 通過，935 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26663712241` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Web plan-preview route 內部 payload handoff，不改 Web 操作流程、crawler、download/import、credential、storage policy 或 user guide。 |
| 05:28 | Web / Consolidation | **CI PASS** | `ec6e995` | `26663264563` | Move Web download import result payload | 新增 `WebDownloadImportResultResponse` 與 `web_download_import_result_response()`，由 `frontends/web/preview_payloads.py` 集中建立 Web download/import completion response fragment，並一次回傳 route 後續要持久化與寫 event 的 compact `plan_outcome` / `plan_passport`；`preview_api.py` 的 asset-level 與 seed-level download/import route 不再各自呼叫 display helper 後重複拆 payload。已驗證：in-memory compile `frontends\web\preview_api.py` / `frontends\web\preview_payloads.py` / `tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，59 tests OK；`frontends\web` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_052640.log` 通過，935 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26663264563` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Web download/import 成功 response fragment 與 route 內部解包邊界，不改 Web 操作流程、crawler、download/import service、credential、storage policy 或 user guide。 |
| 05:11 | Web / Consolidation | **CI PASS** | `dffb31b` | `26662713557` | Move Web plan-preview result response payload | 新增 `web_plan_preview_result_response()`，由 `frontends/web/preview_payloads.py` 集中建立已執行 plan preview 的 response fragment；`preview_api.py` 的 `crawler_asset_plan_preview()` 不再自行組 `plan_result`、`plan_outcome`、`plan_passport`、`adapter_review` 或 plan next-action。已驗證：in-memory compile `frontends\web\preview_api.py` / `frontends\web\preview_payloads.py` / `tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，58 tests OK；`frontends\web` / tests / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_051449.log` 通過，934 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26662713557` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Web plan-preview 成功 response fragment，不改 Web 操作流程、crawler、download/import、credential 或 user guide。 |
| 04:58 | Web / Consolidation | **CI PASS** | `c0024af` | `26662122047` | Move Web listing result response payload | 新增 `web_crawler_asset_listing_result_response()`，由 `frontends/web/preview_payloads.py` 集中建立 crawler listing 成功 response fragment；`preview_api.py` 的 `crawler_asset_listing()` 不再自行拆 `listing_result`、`audit_summary` 或 listing `next_action`。已驗證：in-memory compile `frontends\web\preview_api.py` / `frontends\web\preview_payloads.py` / `tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，57 tests OK；`frontends\web` / tests / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_050046.log` 通過，933 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26662122047` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Web listing 成功 response fragment，不改 Web 操作流程、crawler、download/import、credential 或 user guide。 |
| 04:48 | Web / Consolidation | **CI PASS** | `2518c4c` | `26661656999` | Move Web listing credential block payload | 新增 `web_crawler_asset_listing_credential_blocked_response()`，由 `frontends/web/preview_payloads.py` 集中建立 crawler listing 缺憑證 blocked response；`preview_api.py` 的 `crawler_asset_listing()` 不再直接建立 `CrawlerAssetListingResult` 或自行套 credential next-action。已驗證：in-memory compile `frontends\web\preview_api.py` / `frontends\web\preview_payloads.py` / `tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，56 tests OK；`frontends\web` / tests / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_045032.log` 通過，932 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26661656999` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Web listing 缺憑證 payload 組裝邊界，不改 credential storage policy、Web 操作流程、crawler、download/import 或 user guide。 |
| 04:37 | Web / Consolidation | **CI PASS** | `ef84f38` | `26661212407` | Move Web plan-preview credential block payload | 新增 `web_plan_preview_credential_blocked_response()`，由 `frontends/web/preview_payloads.py` 集中建立 Web plan preview 缺憑證 blocked response；`preview_api.py` 的 `crawler_asset_plan_preview()` 不再自行組 `plan_outcome` / `plan_passport` / credential next-action。已驗證：in-memory compile `frontends\web\preview_api.py` / `frontends\web\preview_payloads.py` / `tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，55 tests OK；`frontends\web` / tests / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_044005.log` 通過，931 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26661212407` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Web plan-preview 缺憑證 payload 組裝邊界，不改 credential storage policy、Web 操作流程、crawler、download/import 或 user guide。 |
| 04:25 | Web / Consolidation | **CI PASS** | `9660752` | `26660663183` | Move Web credential event payload | 新增 `web_crawler_asset_credentials_event_context()`，由 `frontends/web/preview_payloads.py` 集中建立 Web credential update 的 safe event context；`preview_api.py` 的 `save_crawler_asset_credentials()` 不再自行組 event context。event 只記錄 asset/provider、status、configured/field counts、env var 名稱與 next_action，不寫入 token/password 或 value preview。已驗證：in-memory compile `frontends\web\preview_api.py` / `frontends\web\preview_payloads.py` / `tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 通過，54 tests OK；`frontends\web` / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_042738.log` 通過，930 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26660663183` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Web credential event payload 組裝邊界，不改 credential storage policy、Web 操作流程、crawler、download/import 或 user guide。 |
| 04:14 | Tk / Consolidation | **CI PASS** | `0ce2825` | `26660167246` | Move crawler plan built event payload | 新增 `crawler_asset_download_plan_built_event_context()`，由 `frontends/tk/crawler_asset_ui_helpers.py` 集中建立 `crawler_asset_download_plan_built` compact event context；`crawler_asset_workflows.py` 的 `_crawler_asset_download_plan_worker()` 不再自行組 `asset_id`、direct/review counts 與 resolved plan path，worker 只負責呼叫 backend plan builder、寫 plan artifacts 與記錄 event。已驗證：in-memory compile `frontends\tk\crawler_asset_workflows.py` / `frontends\tk\crawler_asset_ui_helpers.py` / `tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 通過，131 tests OK；`frontends\tk` / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_041635.log` 通過，929 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26660167246` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Tk crawler plan built event payload 組裝邊界，不改 UI 操作、crawler、download-plan builder、download/import、credential 或 user guide。 |
| 04:01 | Tk / Consolidation | **CI PASS** | `ea20633` | `26659664434` | Move source pattern draft event payload | 新增 `source_pattern_draft_written_event_context()` 與 `source_pattern_draft_blocked_event_context()`，由 `frontends/tk/source_pattern_draft_ui_helpers.py` 集中建立 source URL detector / local source draft 的 compact event context；`crawler_asset_workflows.py` 的 `_source_pattern_draft_worker()` 不再自行組 success / blocked event payload，只負責呼叫後端 detector/source-draft service、寫 event 與顯示 success / review 訊息。已驗證：in-memory compile `frontends\tk\crawler_asset_workflows.py` / `frontends\tk\source_pattern_draft_ui_helpers.py` / `tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 通過，108 tests OK；`frontends\tk` / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_040505.log` 通過，928 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions manual run `26659664434` 通過 Ubuntu、Windows 與 real DB smoke。`py_compile` 曾被 L 槽雲端 `__pycache__` replace lock 擋住，已用不寫 pyc 的 compile 驗證語法。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Tk source pattern draft event payload 組裝邊界，不改 source pattern detector、local source draft 行為、UI 操作、crawler、download/import、credential 或 user guide。 |
| 03:51 | Tk / Consolidation | **LOCAL SMOKE** | `5d7295f` | `not pushed` | Move crawler seed download import event payload | 新增 `crawler_seed_download_import_event_context()`，由 `frontends/tk/crawler_asset_ui_helpers.py` 集中建立 seed download/import 的 compact event context；`crawler_asset_workflows.py` 的 `_crawler_asset_seed_download_import_worker()` 不再自行拆 `result.pipeline.stage`、`result.succeeded`、`pipeline.to_dict()` 與 artifacts，worker 只負責呼叫正式 seed download/import service、寫 event、handoff 到 Tk completion message。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 通過，128 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_035311.log` 通過，926 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Tk seed download/import event payload 組裝邊界，不改 UI 操作、crawler、download/import、credential 或 user guide。 |
| 03:44 | Tk / Consolidation | **LOCAL SMOKE** | `97c8ac6` | `not pushed` | Move crawler seed schema probe event payload | 新增 `crawler_seed_schema_probe_event_context()`，由 `frontends/tk/crawler_asset_ui_helpers.py` 集中建立 seed schema probe 的 compact event context；`crawler_asset_workflows.py` 的 `_crawler_asset_seed_schema_probe_worker()` 不再自行組 `probe.to_dict()` / `schema_probe_required_count` / `warning_codes` event payload，worker 只負責呼叫後端 probe service、寫 event、handoff 到 Tk dialog。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 通過，127 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_034629.log` 通過，925 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Tk schema probe event payload 組裝邊界，不改 UI 操作、crawler、download/import、credential 或 user guide。 |
| 03:37 | Tk / Consolidation | **LOCAL SMOKE** | `d2aa1b3` | `not pushed` | Move crawler listing outcome event payload | 新增 `crawler_asset_listing_outcome_event_payload()`，由 `frontends/tk/crawler_asset_ui_helpers.py` 集中建立 listing outcome 的 compact event context 與 sidebar preview payload；`crawler_asset_workflows.py` 的 `record_crawler_asset_listing_outcome()` 不再直接呼叫 backend listing event helper 或自行組 preview payload，workflow 只負責寫 event log 與更新 UI cache。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 通過，126 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_033933.log` 通過，924 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Tk listing event/preview payload 組裝邊界，不改 UI 操作、crawler、download/import、credential 或 user guide。 |
| 03:30 | Tk / Consolidation | **LOCAL SMOKE** | `39bd423` | `not pushed` | Move crawler plan outcome event payload | 新增 `crawler_asset_plan_outcome_event_payload()`，由 `frontends/tk/crawler_asset_ui_helpers.py` 集中建立 crawler asset plan outcome 的 event context 與 plan passport payload；`crawler_asset_workflows.py` 的 `record_crawler_asset_plan_outcome()` 不再直接呼叫 backend display helpers 組 event context，也不再自行計算 review queue count，workflow 只負責 persist passport 與寫 event log。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 通過，125 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_033209.log` 通過，923 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Tk event payload 組裝邊界，不改 UI 操作、crawler、download/import、credential 或 user guide。 |
| 02:33 | Workflow / Smoke | **LOCAL SMOKE** | `e118bf4` | `not pushed` | Record crawler plan artifact helper CI pass | 使用者回報 app 閃退並重啟後，先暫停新功能切片並補跑完整 pre-push smoke。已驗證：`.\scripts\pre_push_smoke_brief.cmd` 通過，完整 log `state\logs\pre_push_smoke_20260530_023314.log`；922 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。Docs drift check：這是重啟後驗證 checkpoint，不改產品行為、UI/CLI 操作、crawler、download/import、credential 或 user guide；GTD 不需更新。 |
| 02:20 | Tk / Consolidation | **CI PASS** | `b880bfa` | `26654793363` | Move crawler plan artifact writing | 新增 `write_crawler_asset_download_plan_artifacts()`，由 `frontends/tk/crawler_asset_ui_helpers.py` 集中寫出 crawler asset original/resolved plan JSON；`crawler_asset_workflows.py` 不再直接 import `json`、`safe_path_part` 或 `state_file`，也不再在 worker 內拼 `state/crawler_asset_plans/{asset}.original/resolved.json`。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 通過，124 tests OK；`frontends\tk` / tests mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_022203.log` 通過，922 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions run `26654793363` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Tk plan artifact path/write ownership，不改 UI 操作、crawler、download/import、credential 或 user guide。 |
| 02:08 | Tk / Consolidation | **CI PASS** | `9eac107` | `26654234638` | Move crawler plan cache updates | 新增 `cache_crawler_asset_plan_state()`，由 `frontends/tk/crawler_asset_ui_helpers.py` 集中更新 plan outcome、resolved plan、content review 與 plan passport 的 Tk lookup cache；`crawler_asset_workflows.py` 的 `_finish_crawler_asset_download_plan()` 不再手動初始化 / 更新四組 cache，blocked 與 success 路徑都只委派 helper。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 通過，122 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_021026.log` 通過，920 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions run `26654234638` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Tk display cache 更新邊界，不改 UI 操作、crawler、download/import、credential 或 user guide。 |
| 02:05 | Tk / Consolidation | **CI PASS** | `251682c` | `26653677336` | Move crawler bounds payload cache parsing | 新增 `crawler_asset_bound_payload_from_cache()`，由 `frontends/tk/crawler_asset_ui_helpers.py` 集中把 bounds dialog cache dict 還原成 `CrawlerAssetBoundPayload`；`crawler_asset_workflows.py` 的 `crawler_asset_bound_payload_for_asset()` 只委派 helper，不再在 workflow event handler 內重複解析 `facet_values` / `field_values` / `maps_to_values` / `warning_codes`。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 通過，120 tests OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_015821.log` 通過，918 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`；GitHub Actions run `26653677336` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：已同步 GTD / handoff / development log；本輪只移動 Tk cache parsing 邊界，不改 UI 操作、crawler、download/import、credential 或 user guide。 |
| 01:51 | Git / Workspace | **LOCAL CHECKPOINT** | `4f689dc` | `not needed` | Repair recovery branch upstream tracking | 修正 L 槽 clone 的本機 Git fetchspec：`remote.origin.fetch` 原本只包含 `main`，導致 recovery branch 雖已 `push -u`，但 `@{u}` 仍報 upstream remote-tracking branch 不存在，pre-push smoke 因而顯示 `no upstream branch found`。已在本機 `.git/config` 加入 `+refs/heads/rrkal-32e215c-recovery:refs/remotes/origin/rrkal-32e215c-recovery` 並重新 fetch；現在 `git rev-parse --abbrev-ref --symbolic-full-name '@{u}'` 回傳 `origin/rrkal-32e215c-recovery`，`git status -sb --ahead-behind` 會顯示 tracking 狀態。這是工作區 Git metadata 修復，不改產品程式。Docs drift check：只補工作區治理紀錄；user guide 不需更新。 |
| 01:48 | Tk / Consolidation | **CI PASS** | `d653ccb` | `26652940368` | Record bounds schema helper smoke | `d3c1d8c` / `d653ccb` 已推送到 `origin/rrkal-32e215c-recovery`，GitHub Actions manual run `26652940368` 通過 Ubuntu、Windows 與 real DB smoke。本輪 verified behavior：`crawler_asset_download_plan_bounds_schema()` 集中 Tk crawler asset build-download-plan bounds schema lookup；workflow 仍只負責 UI orchestration，不接管 backend bounds、download plan 或 import 規則。Docs drift check：只補遠端 CI 證據，不改使用者操作流程、crawler、download/import、credential 或 user guide；user guide 不需更新。 |
| 01:45 | Tk / Consolidation | **LOCAL SMOKE** | `d2f645e` | `not pushed` | Record bounds schema helper checkpoint | `d3c1d8c` / `d2f645e` 已補完整 smoke：`.\scripts\pre_push_smoke_brief.cmd` 通過，完整 log `state\logs\pre_push_smoke_20260530_014250.log`；916 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`。注意：pre-push smoke 腳本仍顯示 `no upstream branch found; skipped pending-push diff check`，後續推送時應用 `git push -u origin rrkal-32e215c-recovery` 明確重設 upstream。Docs drift check：只補 smoke 證據，不改 UI/CLI 操作、crawler、download/import、credential 或 user guide；user guide 不需更新。 |
| 01:39 | Tk / Consolidation | **LOCAL TEST** | `d3c1d8c` | `not yet` | Move crawler bounds schema lookup | 新增 `crawler_asset_download_plan_bounds_schema()`，由 `frontends/tk/crawler_asset_ui_helpers.py` 集中讀取 crawler asset 的 `BUILD_DOWNLOAD_PLAN.bounds_schema`；`crawler_asset_workflows.py` 的 seed schema probe 與送進下載器前 bounds dialog 不再重複掃 `asset.capabilities`。這是 read-model helper 收斂，不改 bounds schema、本機 payload、download plan、seed probe、crawler、download/import 或使用者操作流程。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 通過，118 tests OK；`frontends\tk` / docs mojibake scan OK；`git diff --check` OK。Docs drift check：已同步 GTD；user guide 不需更新。 |
| 01:36 | Discovery / Crawler | **LOCAL CHECKPOINT** | `ad3f40f` | `not needed` | Focused crawler registry verification | 未改產品程式，針對既有 crawler declarative registry 做 focused verification：`registry.py` 的 `CrawlerSpec`、四維 matrix、capability code/mask、duplicate guard、handler signature guard，以及 `dataset_sources.py` 的 `crawler_handler()` dispatch、diagnostics、handoff/heartbeat surfaces 均有測試覆蓋。已驗證：`py -3 -B -m unittest tests.test_dataset_discovery tests.test_developer_diagnostics tests.test_handoff tests.test_heartbeat -v` 通過，92 tests OK。Docs drift check：只補驗證紀錄，不改行為、UI/CLI 操作、crawler handler、download/import 或 user guide；GTD/relationship/workspace docs 已於上一個 checkpoint 對齊。 |
| 01:32 | Docs / Governance | **LOCAL CHECKPOINT** | `39bf8bb` | `not needed` | Update docs drift records | 修正文檔漂移：`CODE_RELATIONSHIP_MAP`、`WORKSPACE_LAYOUT`、`PROJECT_GTD` 已把 `frontends/tk/dialogs.py` 更新為 33 行相容 re-export facade，並把 crawler source registry 描述更新為 `api_launcher/crawlers/registry.py` 的 `CrawlerSpec` / 四維 matrix / capability code/mask / `crawler_handler()` 路徑。已驗證：docs mojibake scan OK；`git diff --check` OK。未改產品程式，不需跑 RRKAL 單元測試或 full smoke。Docs drift check：已修正 dialog ownership 與 crawler registry 相關漂移；user guide 不需更新。 |
| 01:26 | Tk / Consolidation | **CI PASS** | `4ba563c` | `26651915573` | Record provider candidate dialog smoke | 從 `frontends/tk/dialogs.py` 移出 `ProviderCandidateReviewDialog`，新增 `frontends/tk/provider_candidate_review_dialog.py` 作為 provider/source 候選審核 dialog owner。`dialogs.py` 已降成 33 行相容 re-export facade，只保留各 dialog class 的穩定匯入面；後續不要再把新 dialog 實作塞回 `dialogs.py`。新 owner 只負責 review-only 表格、detail pane、開 URL、寫入 ignored local provider seed / dataset source draft 與 event log；正式 catalog promotion 仍必須走 local discovery draft audit / crawler audit，不在 dialog 內直接納管 provider/source。已驗證：in-memory compile `frontends\tk\dialogs.py` / `frontends\tk\provider_candidate_review_dialog.py` / `tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 106 tests OK；`frontends\tk` / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_012128.log` 通過，914 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`；GitHub Actions run `26651915573` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Provider candidate review dialog ownership 與 `dialogs.py` facade，不改使用者操作流程、local draft audit、crawler、download/import、credential、event schema 或 user guide；user guide 不需更新。 |
| 01:15 | Tk / Consolidation | **CI PASS** | `5c52a6c` | `26651385342` | Record dataset candidate dialog smoke | 從 `frontends/tk/dialogs.py` 移出 `DatasetCandidateReviewDialog`，新增 `frontends/tk/dataset_candidate_review_dialog.py` 作為資料集候選審核 dialog owner。`dialogs.py` 仍 re-export `DatasetCandidateReviewDialog`，所以 `frontends.tk.dialogs` 舊匯入點與 tests 不需改；新 owner 只負責候選 table、detail pane、source URL 開啟、candidate status 更新與「加入下載計畫」的既有 UI 委派，實際 repository status update 與 plan mutation 仍走既有 backend / 主 UI helpers。`dialogs.py` 從約 467 行降到 254 行。已驗證：in-memory compile `frontends\tk\dialogs.py` / `frontends\tk\dataset_candidate_review_dialog.py` / `tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 106 tests OK；`frontends\tk` / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_011008.log` 通過，914 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`；GitHub Actions run `26651385342` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Dataset candidate review dialog ownership，不改使用者操作流程、crawler、download/import、credential、event schema 或 user guide；user guide 不需更新。 |
| 01:02 | Tk / Consolidation | **CI PASS** | `f653ef1` | `26650752400` | Record adapter review dialog smoke | 從 `frontends/tk/dialogs.py` 移出 `AdapterReviewDialog`，新增 `frontends/tk/adapter_review_dialog.py` 作為 Adapter 待辦視窗 owner。`dialogs.py` 仍 re-export `AdapterReviewDialog`，所以 `frontends.tk.dialogs` 舊匯入點、crawler asset adapter review 入口與 tests 不需改；新 owner 只負責 review-only 表格、detail pane、開啟 source / landing URL 與委派主 UI 既有 resolver 入口，真正 adapter plan resolution、download/import、content parser review 仍留在後端與既有 workflow。`dialogs.py` 從約 589 行降到 467 行。已驗證：`py -3 -B -m py_compile frontends\tk\dialogs.py frontends\tk\adapter_review_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 106 tests OK；`frontends\tk` / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_005628.log` 通過，914 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`；GitHub Actions run `26650752400` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Adapter review dialog ownership，不改使用者操作流程、crawler、download/import、credential、event schema 或 user guide；user guide 不需更新。 |
| 00:47 | Tk / Consolidation | **CI PASS** | `22d4ff6` | `26650062525` | Record data store dialog smoke | 從 `frontends/tk/dialogs.py` 移出 `DataStoreConnectionSettingsDialog`，新增 `frontends/tk/data_store_connection_settings_dialog.py` 作為資料儲存連線設定 dialog owner。`dialogs.py` 仍 re-export `DataStoreConnectionSettingsDialog`，所以 `frontends.tk.dialogs` 舊匯入點、整合入口與 tests 不需改；新 owner 只負責 data-store profile 表格、測試按鈕、env template 按鈕與 active profile 按鈕的 UI 編排，實際連線測試、env template 產生、active profile 寫入與 event log 仍走既有 backend helpers。`dialogs.py` 從約 739 行降到 589 行。已驗證：`py -3 -B -m py_compile frontends\tk\dialogs.py frontends\tk\data_store_connection_settings_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 106 tests OK；`frontends\tk` / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_004216.log` 通過，914 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`；GitHub Actions run `26650062525` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 data-store settings dialog ownership，不改使用者操作流程、credential/env storage、crawler、download/import、event schema 或 user guide；user guide 不需更新。 |
| 00:33 | Tk / Consolidation | **CI PASS** | `0db19d3` | `26649428252` | Record AI settings dialog smoke | 從 `frontends/tk/dialogs.py` 移出 `AiModelSettingsDialog` 與 `GoogleGeminiSettingsDialog`，新增 `frontends/tk/ai_settings_dialogs.py` 作為 AI profile / Google-Gemini connection dialogs 的 owner。`dialogs.py` 仍 re-export 兩個 class，所以 `frontends.tk.dialogs` 舊匯入點、整合選單與 tests 不需改；新 owner 只負責 AI/Gemini 設定視窗、說明文案、表格 row projection 與按鈕編排，credential 寫入、OAuth browser/device-code flow、API key storage 與 AI summary generation 仍委派既有 UI/backend 服務。`dialogs.py` 從約 998 行降到 739 行。已驗證：`py -3 -B -m py_compile frontends\tk\dialogs.py frontends\tk\ai_settings_dialogs.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 106 tests OK；`frontends\tk` / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_002856.log` 通過，914 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`；GitHub Actions run `26649428252` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 AI/Gemini settings dialog ownership，不改使用者操作流程、credential storage、OAuth/API key 行為、crawler、download/import、event schema 或 user guide；user guide 不需更新。 |
| 00:20 | Tk / Consolidation | **CI PASS** | `a99c869` | `26648746037` | Record provider database dialog smoke | 從 `frontends/tk/dialogs.py` 移出 `ProviderEditorDialog` 與 `DatabaseClientSettingsDialog`，新增 `frontends/tk/provider_editor_dialog.py` 與 `frontends/tk/database_client_settings_dialog.py` 作為各自 owner。`dialogs.py` 仍 re-export 兩個 class，所以 `frontends.tk.dialogs` 舊匯入點、provider 編輯 workflow、database client settings 入口與 tests 不需改；provider dialog 只建立 `core.Provider` result，真正寫入仍由主 UI/repository 決定；database client dialog 只改本機 integration config 與開啟本機 DB 工具，不改資料庫內容。`dialogs.py` 從約 1317 行降到 998 行。已驗證：`py -3 -B -m py_compile frontends\tk\dialogs.py frontends\tk\provider_editor_dialog.py frontends\tk\database_client_settings_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 106 tests OK；`frontends\tk` / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260530_001456.log` 通過，914 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`；GitHub Actions run `26648746037` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 provider/database client settings dialog ownership，不改使用者操作流程、crawler、download/import、credential、event schema 或 user guide；user guide 不需更新。 |
| 00:04 | Tk / Consolidation | **CI PASS** | `d7ba3c1` | `26647816256` | Record language startup dialogs smoke | `frontends/tk/language_settings_dialog.py` 與 `frontends/tk/startup_environment_checks_dialog.py` 已從 `frontends/tk/dialogs.py` 拆出並完成遠端 CI 驗證。`dialogs.py` 仍 re-export `UiLanguageSettingsDialog` / `StartupEnvironmentChecksDialog`，所以 `frontends.tk.dialogs` 舊匯入點與 provider settings workflow 不需改；語言 dialog 只寫本機 integration config 並通知主 UI 重建 menu，啟動檢查 dialog 只讀 `core.run_startup_checks(DB_PATH)`。本地完整 smoke `state\logs\pre_push_smoke_20260529_235707.log` 通過，914 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`；GitHub Actions run `26647816256` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 settings/diagnostics dialog ownership，不改使用者操作流程、crawler、download/import、credential、event schema 或 user guide；user guide 不需更新。 |

### 2026-05-29

| 時間 | 階段 | 狀態 | SHA | Run | Commit | 變更與驗證 |
| --- | --- | --- | --- | --- | --- | --- |
| 23:57 | Tk / Consolidation | **LOCAL SMOKE** | `4219e7c` | `not pushed` | Move language and startup dialogs | 從 `frontends/tk/dialogs.py` 移出 `UiLanguageSettingsDialog` 與 `StartupEnvironmentChecksDialog`，新增 `frontends/tk/language_settings_dialog.py` 與 `frontends/tk/startup_environment_checks_dialog.py` 作為各自 owner。`dialogs.py` 仍 re-export 兩個 class，所以 `frontends.tk.dialogs` 舊匯入點與 provider settings workflow 不需改；語言 dialog 只寫本機 integration config 並通知主 UI 重建 menu，啟動檢查 dialog 只讀 `core.run_startup_checks(DB_PATH)`。`dialogs.py` 從 1416 行降到 1317 行，這是小型 settings/diagnostics dialog ownership cleanup，不改語言設定值、startup check 內容、UI 操作流程或修復行為。已驗證：`py -3 -B -m py_compile frontends\tk\dialogs.py frontends\tk\language_settings_dialog.py frontends\tk\startup_environment_checks_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 106 tests OK；`frontends\tk` / docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260529_235707.log` 通過，914 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`。Docs drift check：只改 settings/diagnostics dialog ownership，不改使用者操作流程、crawler、download/import、credential、event schema 或 user guide；user guide 不需更新。 |
| 23:50 | Tk / Consolidation | **CI PASS** | `f2e3815` | `26647086608` | Record import policy dialog smoke | 從 `frontends/tk/dialogs.py` 移出 `ImportExistingTablePolicyDialog`，新增 `frontends/tk/import_policy_dialog.py` 作為既有資料表處理策略 modal owner。`dialogs.py` 仍 re-export `ImportExistingTablePolicyDialog`，所以 `frontends.tk.dialogs` 舊匯入點與 import workflow 不需改；新 owner 只處理 `rename/skip/replace` 的使用者選擇與 replace 確認提示，真正 import/replace/skip guard 仍在 importer/pipeline 層。`dialogs.py` 從 1504 行降到 1416 行，這是小型 dialog ownership cleanup，不改匯入策略值、同名表處理行為或 UI 操作流程。已驗證：`py -3 -B -m py_compile frontends\tk\dialogs.py frontends\tk\import_policy_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 106 tests OK；`frontends\tk` mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260529_234256.log` 通過，914 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`；GitHub Actions run `26647086608` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Import Existing Table policy dialog ownership，不改使用者操作流程、import/replace 行為、crawler、download/import、credential、event schema 或 user guide；user guide 不需更新。 |
| 23:37 | Tk / Consolidation | **CI PASS** | `aa5f443` | `26646483112` | Record developer CLI dialog smoke | 從 `frontends/tk/dialogs.py` 移出 `DeveloperCliDialog`，新增 `frontends/tk/developer_cli_dialog.py` 作為開發者 CLI 視窗 owner。`dialogs.py` 仍 re-export `DeveloperCliDialog`，所以 `frontends.tk.dialogs` 舊匯入點與 `provider_settings_workflows.py` 不需改；新 owner 集中 subprocess、`shlex`、`PROJECT_ROOT` 與 single-flight job helper。`dialogs.py` 從 1625 行降到 1504 行，這是小型 dialog ownership cleanup，不改 CLI 命令解析、timeout、background job 或 UI 操作流程。已驗證：`py -3 -B -m py_compile frontends\tk\dialogs.py frontends\tk\developer_cli_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 106 tests OK；`frontends\tk` mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260529_233050.log` 通過，914 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`；GitHub Actions run `26646483112` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Developer CLI dialog ownership，不改使用者操作流程、crawler、download/import、credential、event schema 或 user guide；user guide 不需更新。 |
| 23:21 | Tk / Consolidation | **CI PASS** | `85faca7` | `26645636629` | Record recent event dialog smoke | 從 `frontends/tk/dialogs.py` 移出 `RecentEventLogsDialog`，新增 `frontends/tk/recent_event_logs_dialog.py` 作為事件紀錄視窗 owner。`dialogs.py` 仍 re-export `RecentEventLogsDialog`，所以既有 `frontends.tk.dialogs` import 與測試入口不斷；新 dialog 只讀 `latest_events()` / JSONL，不寫 event log、不改 crawler/download/import/state。`dialogs.py` 從 1715 行降到 1625 行，這是小型 dialog ownership cleanup，不是 Tk 對話框大搬家。已驗證：`py -3 -B -m py_compile frontends\tk\dialogs.py frontends\tk\recent_event_logs_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 106 tests OK；相關 Tk/docs mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260529_231341.log` 通過，914 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`；GitHub Actions run `26645636629` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Tk dialog ownership，不改使用者操作流程、事件 JSONL schema、crawler、download/import、credential 或 user guide；user guide 不需更新。 |
| 23:02 | Tk / Consolidation | **CI PASS** | `b38700c` | `26644859793` | Record Tk event state smoke | 新增 `frontends/tk/crawler_asset_event_state.py`，把 crawler asset plan/listing structured event 的狀態恢復邏輯從 `frontends/tk/crawler_asset_workflows.py` 移出。`crawler_asset_workflows.py` 現在只呼叫 `crawler_asset_plan_state_from_events()` / `crawler_asset_listing_outcomes_from_events()`，再把回傳 dict 放回 UI cache；helper 不跑 crawler、不重建 plan、不寫 profile，只恢復重開 Tk 後的可視狀態。舊行為保留：plan outcome label、content review label、plan passport、resolved plan JSON 與 listing seed enumeration preview 仍從 structured event / saved resolved plan 取得；遺失或壞掉的 resolved plan 只跳過，不阻斷 UI 啟動。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\crawler_asset_event_state.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_ui_helpers.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 116 tests OK；`frontends\tk` / `api_launcher` / `tests` / `docs` mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260529_225850.log` 通過，914 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`；GitHub Actions run `26644859793` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Tk event-state helper ownership，不改使用者操作流程、按鈕、crawler、download/import、credential、event schema 或 user guide；user guide 不需更新。 |
| 22:46 | Adapter Review / Consolidation | **CI PASS** | `8db60fa` | `26643848797` | Record resolver pipeline checkpoint | 在 `api_launcher/adapter_plan_resolver.py` 做小型準宣告式收斂：`direct_resource_entries_for_plan_entry()` 內的 bounded resolver、NCEI fallback resolver 與 metadata lookup resolver 改成顯式 resolver pipeline 迴圈。行為順序維持不變：STAC/CMR bounded resolver 先跑，generic resources 之後，Socrata bounded sample、NCEI data-file lookup、NCEI bounded search/access-data fallback，再進 CMR granule asset / CKAN / DataCite / Dataverse metadata lookup，ERDDAP sample 仍可並存。這不是全面拆 `adapter_plan_resolver.py`，只是先把可重複的 resolver 分流收斂成 table-like pipeline。已驗證：`py -3 -B -m py_compile api_launcher\adapter_plan_resolver.py tests\test_adapter_plan_resolver.py tests\test_dataset_download_plan.py` OK；`py -3 -B -m unittest tests.test_adapter_plan_resolver tests.test_dataset_download_plan -v` 72 tests OK；`api_launcher` mojibake scan OK；`git diff --check` OK；完整 smoke `state\logs\pre_push_smoke_20260529_224026.log` 通過，912 tests / 4 skipped，MVP `download_import_completed` / `row_count=3`；已推送 `origin/rrkal-32e215c-recovery`，GitHub Actions run `26643848797` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 resolver 內部分派寫法，不改 adapter review output shape、download/import 行為、UI/CLI 操作或 user guide；user guide 不需更新。 |
| 22:31 | Docs / Governance | **LOCAL CHECKPOINT** | `5937386` | `not pushed` | Add docs registry PoC | 新增 `docs/DOCS_REGISTRY.csv`，用 diff-friendly CSV 盤點文件路徑、角色、權威層級、last_verified、驗證證據與備註；`docs/DOCS_INDEX.zh-TW.md` 已把它登記為輔助索引，明確說明不取代 Markdown source of truth。`docs/PROJECT_GTD.md` 已把「diff-friendly docs registry PoC」從待辦改成第一版完成；SQLite/DB 型文件治理仍保留為後續 report/cache 方向。已驗證：docs mojibake scan OK；`git diff --check` OK；未改產品程式，不需跑 RRKAL 單元測試。Docs drift check：只新增文檔治理 registry 與索引，不改產品行為、UI/CLI 操作流程、crawler、download/import、credential 或 user guide。 |
| 22:27 | Recovery / CI | **CI PASS** | `0c0f8c8` | `26643001651` | Record Tk row detail checkpoint | 已將 `96bfc7e` / `0c0f8c8` 推到 `origin/rrkal-32e215c-recovery`，遠端 head 為 `0c0f8c8`。推送前本地完整 smoke 通過：912 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_222350.log`。手動 workflow dispatch 針對 recovery branch 跑完 GitHub Actions：run `26643001651` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：本段只補遠端驗證與 branch 狀態，不改產品行為、UI/CLI 操作流程或 user guide。 |
| 22:19 | Tk / Consolidation | **LOCAL CHECKPOINT** | `96bfc7e` | `not pushed` | Move crawler asset Tk row detail helpers | 把 crawler asset table row tuple 與右側 passport detail text 的組裝移到 `frontends/tk/crawler_asset_ui_helpers.py`。`frontends/tk/crawler_asset_workflows.py` 從約 1303 行降到約 1246 行；workflow class 仍保留 `crawler_asset_row_values()` wrapper 與 `on_crawler_asset_select()` orchestration，但不再直接組 table row、capability lines、credential summary、plan passport summary 或 bounds schema text。新 helper 只做 Tk read-model projection；它不載入 assets、不查 event log、不跑 crawler、不改 profile / credential / download plan。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_dialogs.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_tk_ui_helpers -v` 114 tests OK；`frontends\tk` mojibake scan OK；`git diff --check` OK。Docs drift check：只改 Tk helper ownership，不改使用者操作流程、按鈕、crawler、download/import、credential 或 user guide；user guide 不需更新。 |
| 22:13 | Recovery / GitHub | **BRANCH PUSHED** | `3cf23c9` | `not triggered` | Record recovery branch smoke | 已推送 `origin/rrkal-32e215c-recovery`，遠端 head 為 `3cf23c9`。推送前本地完整 smoke 已通過：912 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_221019.log`。`gh run list --branch rrkal-32e215c-recovery` 沒列出 run；檢查 `.github/workflows/ci.yml` 後確認 CI 只在 `main` push、PR to `main` 或 manual `workflow_dispatch` 觸發，所以 recovery branch 無 Actions run 是預期狀態，不是 CI failure。 |
| 22:06 | Recovery / Smoke | **LOCAL CHECKPOINT** | `c72c4f0` | `not pushed` | Record source pattern Tk message checkpoint | 針對 recovery branch 上 `40d9c0a` / `86b5974` 兩個 consolidation code checkpoint 與對應 docs checkpoint，補跑 `.\scripts\pre_push_smoke_brief.cmd` 通過。結果：912 tests / 4 skipped，MVP demo `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_220607.log`。`pre_push_smoke` 顯示此 recovery branch 尚無 upstream，pending-push diff check 被略過；若要上 GitHub，應推成 `rrkal-32e215c-recovery` 分支，不直接覆蓋 `origin/main`。Docs drift check：本段只補驗證紀錄，不改產品行為、UI/CLI 操作流程或 user guide。 |
| 22:04 | Tk / Consolidation | **LOCAL CHECKPOINT** | `86b5974` | `not pushed` | Move source pattern draft Tk messages | 新增 `frontends/tk/source_pattern_draft_ui_helpers.py`，把 source pattern draft 成功 / review-needed message formatting 從 `frontends/tk/crawler_asset_workflows.py` 移出。`crawler_asset_workflows.py` 從約 1382 行降到約 1303 行；workflow class 保留 `source_pattern_draft_message()` / `source_pattern_draft_review_message()` wrapper 作相容入口，但實際文案投影由新 helper 擁有。新 owner 只把 backend summary dict 轉成 Tk 可讀訊息，不決定 source draft 是否 promotion、不執行 discovery audit、不下載或匯入資料。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\source_pattern_draft_ui_helpers.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 106 tests OK；`frontends\tk` mojibake scan OK；`git diff --check` OK。Docs drift check：只改 Tk helper ownership，不改 source pattern draft dialog 操作流程、crawler/source pattern 行為、下載/匯入、credential 或 user guide；user guide 不需更新。 |
| 21:58 | Display Contract / Consolidation | **LOCAL CHECKPOINT** | `40d9c0a` | `not pushed` | Move crawler asset flow display helpers | 新增 `api_launcher/crawler_asset_flow_display.py`，把 `CrawlerAssetFlowStep`、`crawler_asset_card_capabilities()` 與 `crawler_asset_flow_steps()` 從 `api_launcher/crawler_asset_display.py` 移出。`crawler_asset_display.py` 從 542 行降到 459 行；仍 re-export flow display helpers 作相容 surface，但 `frontends/web/preview_assets.py` 已改讀新 owner。新 owner 只負責 crawler asset card capability rows 與 `seed -> source_pattern -> bounds -> download_plan -> review_gate` 流程條的 UI-neutral read model，不執行 crawler、不建立下載計畫、不決定 credential / review policy。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_display.py api_launcher\crawler_asset_flow_display.py frontends\web\preview_assets.py tests\test_crawler_assets.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview -v` 98 tests OK；`api_launcher` mojibake scan OK；`git diff --check` OK。Docs drift check：只改 backend display helper ownership 與 Web import owner，不改 Web/Tk/CLI 操作流程、crawler、download/import、credential 或 user guide；user guide 不需更新。 |
| 21:50 | Docs / Workflow | **LOCAL CHECKPOINT** | `8c4b214` | `not pushed` | Align GitHub owner and workspace references | 完成 recovery lane 對齊：`L:\RRKAL_project` 成為本 session 主工作區與提交來源，舊 `K:\APIkeys_collection` 只作唯讀參考，`L:` 其他專案資料夾也視為唯讀。以 `32e215c` 作乾淨基底補跑完整 smoke，912 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_210703.log`。Git metadata 已遷回 `L:\RRKAL_project\.git`，本機臨時 clone 已刪除。GitHub owner 改為 `Kagamihara-Ruruka`，`origin`、heartbeat repo slug、manual import docs URL、repo skill、Git handoff、OpenSpec workflow、heartbeat automation 與 active CI 指令已改用 `Kagamihara-Ruruka/APIkeys_collection`；歷史 log 保留舊 owner 作脈絡。已驗證：`py_compile api_launcher\heartbeat.py api_launcher\manual_import.py` OK；`tests.test_heartbeat tests.test_manual_import` 19 tests OK；`git ls-remote origin HEAD` 可讀；`git diff --check` OK；變更 `.md` / repo skill mojibake scan OK。Docs drift check：本輪改變工作區與 GitHub owner 工作流，已同步 active contract docs。 |
| 19:21 | Display Contract / Consolidation | **CHECKPOINT** | `8191751` | `26634236072` | Move next action display labels | 新增 `api_launcher/crawler_next_action_display.py`，把 `NEXT_ACTION_DISPLAY_LABELS` 與 `next_action_display_label()` 從 `api_launcher/crawler_asset_display.py` 移出。`crawler_asset_display.py` 從約 715 行降到約 676 行；仍 re-export next-action display helpers 作相容 surface，但 `api_launcher/crawler_asset_download.py`、`api_launcher/crawler_asset_schema_probe.py`、`api_launcher/developer_diagnostics.py`、`frontends/web/preview_assets.py`、`frontends/web/preview_payloads.py`、`frontends/tk/crawler_asset_ui_helpers.py` 與 `frontends/tk/ui_helpers.py` 已改讀新 owner。新 owner 是純 machine `next_action` id 到人類 label 的穩定表格，不 import Web/Tk、crawler service 或 download/import。已驗證：in-memory compile 9 檔 OK；`tests.test_crawler_assets` 45 tests OK；`tests.test_web_preview` 53 tests OK；`tests.test_tk_dialogs tests.test_tk_ui_helpers tests.test_crawler_asset_download` 118 tests OK；`api_launcher` mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，912 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_191322.log`；GitHub Actions run `26634236072` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 backend display helper ownership 與 import owner，不改 Web/Tk/CLI 操作流程、crawler、download/import、credential 或 user guide。 |
| 19:05 | Display Contract / Consolidation | **CHECKPOINT** | `8b2fc8e` | `26633608060` | Move seed enumeration display helpers | 新增 `api_launcher/crawler_seed_display.py`，把 seed enumeration 的 `SeedEnumerationDisplayProfile`、status display table 與 `seed_enumeration_display_payload()` 從 `api_launcher/crawler_asset_display.py` 移出。`crawler_asset_display.py` 從約 835 行降到約 715 行；仍 re-export seed enumeration display helpers 作相容 surface，但 `api_launcher/crawler_asset_listing_payloads.py` 與 `tests/test_crawler_assets.py` 已改讀新 owner。新 owner 是純 seed enumeration 狀態文案 / tone / next-action contract，不 import Web/Tk、crawler service 或 download/import。已驗證：in-memory compile 6 檔 OK；`tests.test_crawler_assets` 45 tests OK；`tests.test_web_preview` 53 tests OK；`tests.test_tk_dialogs` 106 tests OK；`api_launcher` mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，912 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_185833.log`；GitHub Actions run `26633608060` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 backend display helper ownership 與 import owner，不改 Web/Tk/CLI 操作流程、crawler、download/import、credential 或 user guide。 |
| 18:50 | Display Contract / Consolidation | **CHECKPOINT** | `99d634e` | `26632957820` | Move crawler asset bounds display helpers | 新增 `api_launcher/crawler_asset_bound_display.py`，把 crawler asset capability label、bounds field label/help、bounds group display text 與 `crawler_asset_bound_form_payload()` 從 `api_launcher/crawler_asset_display.py` 移出。`crawler_asset_display.py` 從約 893 行降到約 809 行；仍保留 plan outcome、download/import、flow steps 與其他 UI-neutral display contract，並 re-export bounds display helpers，避免 Tk/Web/tests 舊 import 斷裂。新 owner 只依賴 bounds form dataclass 與 capability dataclass，不回頭 import Web/Tk。已驗證：in-memory compile 5 檔 OK；`tests.test_crawler_assets` 45 tests OK；`tests.test_web_preview` 53 tests OK；`tests.test_tk_dialogs` 106 tests OK；`api_launcher` mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，912 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_184346.log`；GitHub Actions run `26632957820` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 backend display helper ownership，不改 Web/Tk/CLI 操作流程、crawler、download/import、credential 或 user guide。 |
| 18:34 | Display Contract / Consolidation | **CHECKPOINT** | `a67f4e7` | `26632346463` | Move adapter review display helpers | 新增 `api_launcher/crawler_asset_review_display.py`，把 adapter-review summary、content parser/import status、content review bucket label/tone、pipeline lane label/tone 等純 display helper 從 `api_launcher/crawler_asset_display.py` 移出。`crawler_asset_display.py` 從約 1124 行降到約 893 行；仍保留 crawler asset / plan outcome / download-import display contract，並 re-export adapter-review display helpers，避免 Tk/Web/tests 舊 import 斷裂。新 owner 只依賴 `adapter_review_agent_payload()` 與自己的 display table，不回頭 import Web/Tk。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_display.py api_launcher\crawler_asset_review_display.py frontends\web\preview_api.py frontends\tk\crawler_asset_workflows.py frontends\tk\dialogs.py frontends\tk\import_workflows.py tests\test_web_preview.py tests\test_tk_dialogs.py` OK；`tests.test_web_preview` 53 tests OK；`tests.test_tk_dialogs` 106 tests OK；`tests.test_crawler_asset_download tests.test_crawler_assets` 49 tests OK；`api_launcher` mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，912 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_182918.log`；GitHub Actions run `26632346463` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 backend display helper ownership，不改 Web/Tk/CLI 操作流程、crawler、download/import、credential 或 user guide。 |
| 18:19 | Crawler Asset / Consolidation | **CHECKPOINT** | `6806a58` | `26631709497` | Move crawler asset listing payload helpers | 新增 `api_launcher/crawler_asset_listing_payloads.py`，把 listing result 的 `seed_enumeration`、remote pagination 與 compact listing event context helper 從 `api_launcher/crawler_asset_service.py` 移出。`crawler_asset_service.py` 從約 753 行降到約 630 行；service 保留 source lookup、crawler execution、candidate upsert、download-plan orchestration，不再持有 listing result UI/event projection 規則。`frontends/web/preview_api.py`、`frontends/tk/crawler_asset_workflows.py`、`api_launcher/cli_crawler_assets.py` 與 `tests/test_crawler_assets.py` 已改讀新 owner；`crawler_asset_service.py` 仍 re-export helper 名稱作相容 surface。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_service.py api_launcher\crawler_asset_listing_payloads.py api_launcher\cli_crawler_assets.py frontends\tk\crawler_asset_workflows.py frontends\web\preview_api.py tests\test_crawler_assets.py` OK；`tests.test_crawler_assets` 45 tests OK；`tests.test_web_preview` 53 tests OK；`tests.test_tk_dialogs` 106 tests OK；`api_launcher` mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，912 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_181330.log`；GitHub Actions run `26631709497` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 helper ownership 與 import owner，不改 Web/Tk/CLI 操作流程、crawler、download/import、credential 或 user guide。 |
| 18:05 | Crawler Asset / Consolidation | **CHECKPOINT** | `9f44aa5` | `26631096034` | Move crawler asset payload adapters | 新增 `api_launcher/crawler_asset_payloads.py`，把 crawler asset bounds payload 轉成 `SourceDownloadOptions` / `SourceDownloadBounds` 的 adapter helper 從 `api_launcher/crawler_asset_service.py` 移出。`crawler_asset_service.py` 從約 968 行降到約 828 行，仍保留 listing、seed plan、candidate upsert 與 orchestration；bounds payload parsing、version selector、bbox / tuple / int / bool coercion 改由新 owner 擁有。`api_launcher.crawler_asset_service` 仍保留同名 import surface，舊呼叫不斷；`tests/test_crawler_assets.py` 已改讀新 owner。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_service.py api_launcher\crawler_asset_payloads.py tests\test_crawler_assets.py` OK；`py -3 -B -m unittest tests.test_crawler_assets -v` 45 tests OK；compat import smoke OK；`api_launcher` mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，912 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_175921.log`；GitHub Actions run `26631096034` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 helper ownership 與測試 import，不改 Web/Tk/CLI 操作流程、crawler、download/import、credential 或 user guide。 |
| 17:50 | Tk / UIUX | **CHECKPOINT** | `98b2805` | `26630439903` | Show credential next action in Tk dialog | `frontends/tk/crawler_asset_credential_dialog.py` 新增 `crawler_asset_credential_next_action_text()`，Tk credential dialog 會優先顯示後端 `next_action_label_zh_TW` / display profile label，不把 raw `next_action` id 顯示給一般使用者。這不改 credential 欄位、storage policy、下載/匯入或 Web route；dialog 仍只收集 payload 並交給後端 `update_crawler_asset_credentials()`。新增 regression `test_credential_dialog_next_action_uses_display_label_not_raw_id`。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_credential_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 106 tests OK；`frontends\tk` mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，912 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_174501.log`；GitHub Actions run `26630439903` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Tk credential dialog 顯示文案與測試，不改使用者操作流程文件。 |
| 17:39 | Web / Consolidation | **CHECKPOINT** | `8018b38` | `26629906445` | Move Web preview diagnostics helpers | 新增 `frontends/web/preview_diagnostics.py`，把 Web Preview 的 status、project maturity、crawler handler smoke diagnostics、real-download demo 與 developer demo helper 從 `frontends/web/preview_api.py` 移出。`preview_api.py` 從約 497 行降到約 428 行；仍保留 crawler asset listing、credential update、plan preview、asset download/import 與 seed download/import endpoint orchestration。`frontends/web/server.py` 與 `tests/test_web_preview.py` 已改由 `frontends.web.preview_diagnostics` 匯入新 owner。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py frontends\web\preview_assets.py frontends\web\preview_diagnostics.py frontends\web\server.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 53 tests OK；`frontends\web` mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，911 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_173302.log`；GitHub Actions run `26629906445` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Web diagnostics/status helper ownership 與 import/mock 路徑，不改 Web API route、JS 操作、後端 crawler/download/import/credential 行為或 user guide。 |
| 17:24 | Web / Consolidation | **CHECKPOINT** | `173f057` | `26629273947` | Move Web preview asset helpers | 新增 `frontends/web/preview_assets.py`，把 Web Preview 的 asset cards/detail、seed page、seed favorite 與 credential detail read-model helper 從 `frontends/web/preview_api.py` 移出。`preview_api.py` 從約 703 行降到約 497 行；仍保留 status、project maturity、developer diagnostics、listing、credential update、plan preview、download/import endpoint orchestration。`frontends/web/server.py` 與 `tests/test_web_preview.py` 已改由 `frontends.web.preview_assets` 匯入新 owner；`preview_api.py` 暫時保留同名 import 作相容 surface。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py frontends\web\preview_assets.py frontends\web\server.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 53 tests OK；`frontends\web` mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，911 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_171844.log`；GitHub Actions run `26629273947` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Web helper ownership 與 import/test 路徑，不改 Web API route、JS 操作、後端 crawler/download/import/credential 行為或 user guide。 |
| 17:00 | Web / Consolidation | **CHECKPOINT** | `0dffffc` | `26628565727` | Move Web preview event helpers | 新增 `frontends/web/preview_events.py`，把 `recent_crawler_asset_listing_outcomes()`、`compact_listing_outcome()`、`recent_crawler_asset_plan_outcomes()`、`recent_crawler_asset_plan_passports()`、`web_preview_recent_events()` 與 event payload summary 從 `frontends/web/preview_api.py` 移出。`preview_api.py` 從約 798 行降到約 703 行；仍保留 asset cards/detail、credential update、listing、plan preview、download/import 等 endpoint orchestration。`frontends/web/server.py` 改由 `frontends.web.preview_events` 匯入 `web_preview_recent_events()`；`tests/test_web_preview.py` 的 event helper 與 `latest_events` patch target 也對齊新 owner。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py frontends\web\preview_context.py frontends\web\preview_payloads.py frontends\web\preview_events.py frontends\web\server.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 53 tests OK；`frontends\web` / `docs` mojibake scan OK；`git diff --check` OK（僅 `PROJECT_GTD.md` CRLF/LF 提醒）；`.\scripts\pre_push_smoke_brief.cmd` 通過，911 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_170131.log`；GitHub Actions run `26628565727` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Web event summary helper ownership 與 import/mock 路徑，不改 Web API route、JS 操作、後端 crawler/download/import/credential 行為或 user guide。 |
| 16:41 | Web / Consolidation | **CHECKPOINT** | `61018b6` | `26627862336` | Move Web preview context helpers | 新增 `frontends/web/preview_context.py`，把 `WebPreviewRepositorySession`、`WebCrawlerAssetActionContext`、`web_preview_repository_context()`、`web_crawler_asset_action_context()`、`crawler_asset_bound_form()`、`crawler_asset_payload_from_web_values()` 與 crawler asset lookup 從 `frontends/web/preview_api.py` 移出。`preview_api.py` 從約 889 行降到約 798 行；endpoint 仍保留 route-specific credential blocking、plan build、download/import、event logging、passport update 與 commit 決策。`frontends/web/server.py` 改由 `frontends.web.preview_context` 匯入 `crawler_asset_payload_from_web_values()`；`tests/test_web_preview.py` 的 action-context helper import 也對齊新 owner。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py frontends\web\preview_context.py frontends\web\preview_payloads.py frontends\web\server.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 53 tests OK；`frontends\web` / `docs` mojibake scan OK；`git diff --check` OK（僅 `PROJECT_GTD.md` CRLF/LF 提醒）；`.\scripts\pre_push_smoke_brief.cmd` 通過，911 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_164536.log`；GitHub Actions run `26627862336` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Web context/setup helper ownership 與 import 路徑，不改 Web API route、JS 操作、後端 crawler/download/import/credential 行為或 user guide。 |
| 15:25 | Web / Consolidation | **CHECKPOINT** | `14a9d61` | `26627022320` | Move Web preview payload helpers | 新增 `frontends/web/preview_payloads.py`，把 `web_next_action_payload()`、`apply_web_next_action()`、`web_crawler_asset_listing_payload()`、`crawler_asset_listing_options()`、`web_download_import_target_paths()`、`web_download_import_event_context()`、`web_download_import_credential_blocked_response()` 與相關 path dataclass 從 `frontends/web/preview_api.py` 移出。`preview_api.py` 從約 1053 行降到約 889 行，保留 route/API-facing orchestration、repository session、asset lookup、credential guard、plan/download/import service 呼叫；純 Web response/path helper 由 `preview_payloads.py` 擁有。`tests/test_web_preview.py` 改由新 owner 匯入 helper，並修正 `default_local_downloads_root` patch target。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py frontends\web\preview_payloads.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 53 tests OK；`frontends\web` / `docs` mojibake scan OK；`git diff --check` OK（僅 `PROJECT_GTD.md` CRLF/LF 提醒）；`.\scripts\pre_push_smoke_brief.cmd` 通過，911 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_162409.log`；GitHub Actions run `26627022320` 通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 Web helper ownership 與測試隔離，不改 Web API route、JS 操作、後端 crawler/download/import/credential 行為或 user guide。 |
| 15:10 | Tk / Consolidation | **PUSHED / CI PASS** | `c4a108b` | `26623612788` | Move crawler asset helpers to dedicated module | 收斂 Tk helper ownership：把 crawler asset / seed download-import 專屬 dataclass、target path、download/import completion message、plan summary、listing blocked status、plan passport summary、credential guard prompt 從 generic `frontends/tk/ui_helpers.py` 移到 `frontends/tk/crawler_asset_ui_helpers.py`。`frontends/tk/crawler_asset_workflows.py` 現在只從專屬 helper module 匯入 crawler asset UI projection；`tests/test_tk_dialogs.py` / `tests/test_tk_ui_helpers.py` patch target 已對齊新 owner。`ui_helpers.py` 回到 yfinance / data-store / MVP smoke 等通用 helper ownership。已驗證：`py -3 -B -m py_compile frontends\tk\ui_helpers.py frontends\tk\crawler_asset_ui_helpers.py frontends\tk\crawler_asset_workflows.py tests\test_tk_ui_helpers.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 113 tests OK；`git diff --check` OK；`frontends\tk` / `tests` / `docs` mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，911 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_150355.log`；GitHub Actions run `26623612788` 已通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：只改 helper ownership 與測試隔離，不改使用者操作流程、按鈕、後端 crawler/download/import 行為或 user guide。 |
| 14:39 | Tk / Consolidation | **PUSHED / CI PASS** | `6822095` | `26622638928` | Extract Tk crawler asset display helpers | 新增 `frontends/tk/crawler_asset_ui_helpers.py`，把 seed page status / preview、seed enumeration note、credential badge / summary、credential event context、listing event preview、state label、review count 等純 Tk 投影 helper 從 `frontends/tk/crawler_asset_workflows.py` 移出。`crawler_asset_workflows.py` 從約 1601 行降到約 1384 行，保留 workflow、background job handoff、service 呼叫與 dialog routing；後端 crawler、download/import、credential、seed page、listing event 行為不變。`tests/test_tk_dialogs.py` 改由新 helper module 匯入這批函式。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\crawler_asset_ui_helpers.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 105 tests OK；`frontends\tk` / `tests` mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，911 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_144106.log`；GitHub Actions run `26622638928` 已通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：本輪只改 Tk helper ownership，不改使用者操作流程、按鈕、後端 service 或 UI 文案 contract，user guide 不需更新。 |
| 14:19 | Tk / Consolidation | **PUSHED / CI PASS** | `7f13bcf` | `26621919504` | Move Tk crawler asset UI helpers | 把 Tk crawler asset 的下載計畫摘要、listing blocked status、plan outcome 短標籤、plan passport 摘要與 credential guard prompt helper 從 `frontends/tk/crawler_asset_workflows.py` 移到 `frontends/tk/ui_helpers.py`。`crawler_asset_workflows.py` 現在只消費這些 helper，不再同時持有 workflow 與 message/summary helper ownership；後端 crawler、download/import、credential guard、plan outcome、plan passport 行為不變。`tests/test_tk_dialogs.py` 改由 `frontends.tk.ui_helpers` 匯入 helper。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\ui_helpers.py tests\test_tk_dialogs.py tests\test_tk_ui_helpers.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers -v` 8 tests OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 105 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，911 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_142011.log`；GitHub Actions run `26621919504` 已通過 Ubuntu、Windows 與 real DB smoke。Docs drift check：本輪只改 Tk helper ownership，不改使用者操作流程或 UI 文案 contract，user guide 不需更新。 |
| 14:00 | Docs / Workflow | **PUSHED / CI PASS** | `beb42d8` | `26621082393` | Document adjacent project context | 依使用者要求 read-only 盤點其他 GitHub repo，新增 `docs/EXTERNAL_PROJECT_CONTEXT.zh-TW.md` 並更新 `DOCS_INDEX.zh-TW.md`：`RRKAL_displaytools` 今天連續通過 smoke，可借鑑 display contract / renderer input acknowledgement；`CODE_KM` 維持 governed ingestion / next-action checkpoint；`rrkal-visual-compressor` 有 one-command MVP pipeline；`video-downloader` 的 output provenance 概念可對應 RRKAL manifest / source_url display。這份文件明確規定其他 repo 只作概念參考，不形成 RRKAL runtime dependency，也不允許 RRKAL 任務寫入其他工作區。GitHub Actions run `26621082393` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 07:46 | Web / Display Contract | **PUSHED / CI PASS** | `c3f53d6` | `26620918050` | Move recent plan event display helpers | `api_launcher/crawler_asset_display.py` 新增 `crawler_asset_recent_plan_outcomes_from_events()` 與 `crawler_asset_recent_plan_passports_from_events()`，把 structured event log 中最近 plan outcome / compact plan passport 的篩選與壓縮規則從 Web Preview 收回 backend display contract。`frontends/web/preview_api.py` 的 `recent_crawler_asset_plan_outcomes()` / `recent_crawler_asset_plan_passports()` 現在只讀 `latest_events()` 並呼叫後端 helper，移除 Web-only `compact_web_plan_passport_payload()` wrapper 與 direct compact import。新增 `tests.test_web_preview.WebPreviewApiTest.test_recent_plan_event_helpers_keep_ui_payloads_compact`，鎖住 helper 會忽略非 plan event，且不讓 `providers` / `resolved_plan` 進 UI status surface。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_display.py frontends\web\preview_api.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 53 tests OK；docs/source mojibake scan OK；`git diff --check` OK（僅 `PROJECT_GTD.md` CRLF/LF 提醒）；`.\scripts\pre_push_smoke_brief.cmd` 通過，911 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_074816.log`；GitHub Actions run `26620918050` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 07:29 | Tk / Display Contract | **PUSHED / CI PASS** | `49c3040` | `26608663640` | Reuse plan event context in Tk | Tk 的 `record_crawler_asset_plan_outcome()` 也改接 backend `crawler_asset_plan_event_context()`：Tk 不再手寫 asset id、outcome counts、content review、run record 與 plan passport event keys。Tk 仍保留本地 resolved plan path 作為事件中的 `resolved_plan`，並保留 `review_queue_count` 使用 resolved plan adapter review count；這兩個是 Tk 寫檔 / 顯示 queue 的本地 artifact，不移回 Web。移除 `frontends/tk/crawler_asset_workflows.py` 對 `crawler_run_record_from_result` 的直接 import。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_display.py frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 105 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，910 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_073145.log`；GitHub Actions run `26608663640` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 07:16 | Web / Display Contract | **PUSHED / CI PASS** | `66fd82b` | `26608126362` | Move plan event context display payload | `crawler_asset_plan_event_context()` 從 `frontends/web/preview_api.py` 移到 `api_launcher/crawler_asset_display.py`，與 plan badge / plan outcome display helpers 放在同一個 UI-neutral display contract module。Web Preview 仍負責在 plan preview 與 download/import completion 時寫 structured event，但 compact event shape、run record 與 plan passport 壓縮規則改由後端 helper 擁有，避免未來 Tk/Web/Qt 各自發明 event keys。既有 regression `tests.test_web_preview.WebPreviewApiTest.test_web_plan_event_context_keeps_badge_payload_compact` 改為直接測 backend helper；Web endpoint 行為不變。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_display.py frontends\web\preview_api.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 52 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，910 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_071718.log`；GitHub Actions run `26608126362` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 06:56 | Web / Display Contract | **PUSHED / CI PASS** | `deeb96b` | `26607436050` | Move credential-blocked plan display payloads | `api_launcher/crawler_asset_display.py` 新增 `credential_blocked_plan_outcome_payload()` 與 `credential_blocked_plan_passport_payload()`，把缺登入 / API Key 時的 plan outcome 與 plan passport 從 Web route 移回 backend display contract。`frontends/web/preview_api.py` 的 plan preview、asset download/import、seed download/import credential-blocked 分支改為消費這兩個 helper；Web 仍負責 endpoint-level next action 與 credential guard，不再自己組 `credential_setup_required` 形狀。新增 `tests.test_web_preview.WebPreviewApiTest.test_credential_blocked_plan_payloads_are_backend_display_contract`。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_display.py frontends\web\preview_api.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 52 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，910 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_065819.log`；GitHub Actions run `26607436050` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 06:38 | Web / Display Contract | **PUSHED / CI PASS** | `a54a931` | `26606736921` | Normalize web listing payloads | `frontends/web/preview_api.py` 新增 `web_crawler_asset_listing_payload()`，讓 listing blocked 分支與 live listing 成功分支都使用 `CrawlerAssetListingResult.to_dict()` 的 service-owned 結構，再由 Web 附加 `next_action_label`。`crawler_asset_listing()` 的 credential-blocked 分支不再手寫 candidate counts、seed enumeration、search scope 或 run-record 形狀，並移除 Web endpoint 對 `crawler_seed_enumeration_payload` 的直接 import。新增 `tests.test_web_preview.WebPreviewApiTest.test_web_crawler_asset_listing_payload_adds_label_without_rebuilding_shape`，並補強 nested `listing_result.next_action_label` 驗證。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 51 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，909 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_064048.log`；GitHub Actions run `26606736921` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 06:23 | Web / Display Contract | **PUSHED / CI PASS** | `87d7f7b` | `26606124857` | Extract web crawler action context | `frontends/web/preview_api.py` 新增 `WebCrawlerAssetActionContext` 與 `web_crawler_asset_action_context()`，把 plan preview、asset download/import、seed download/import 三條 endpoint 共用的 asset lookup、credential guard 與 bounds payload 解析集中起來。endpoint 仍明確保留 credential blocking、plan build、download/import、passport update 與 event logging；helper 只收斂重複 setup，不隱藏交易或業務決策。新增 `tests.test_web_preview.WebPreviewApiTest.test_web_crawler_asset_action_context_resolves_asset_credentials_and_bounds`。驗證時先發現測試假設錯誤：credential demo asset 的 limit facet 是 `granule_limit`，不是通用 `limit`；已改測試檢查後端 facet key。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 50 tests OK；`frontends\web` 與 docs mojibake scan OK；`git diff --check` OK（僅 `PROJECT_GTD.md` CRLF/LF 提醒）；`.\scripts\pre_push_smoke_brief.cmd` 通過，908 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_062631.log`；GitHub Actions run `26606124857` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 06:07 | Web / Display Contract | **PUSHED / CI PASS** | `e8e50d4` | `26605399831` | Consolidate web next-action payloads | `frontends/web/preview_api.py` 新增 `web_next_action_payload()` 與 `apply_web_next_action()`，讓 Web endpoint 以同一個後端 helper 組出 `next_action` + `next_action_label`。`crawler_asset_listing()`、`crawler_asset_plan_preview()`、asset-level download/import、seed-level download/import 與 credential-blocked download/import payload 都改用這個薄 contract，避免 route 各自翻譯 workflow 狀態。這不改 crawler、download/import、credential guard、plan outcome、plan passport、Web route 或 JS 操作流程。新增 `tests.test_web_preview.WebPreviewApiTest.test_web_next_action_payload_pairs_backend_id_with_display_label`。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 49 tests OK；`frontends\web` 與 docs mojibake scan OK；`git diff --check` OK（僅 `PROJECT_GTD.md` CRLF/LF 提醒）；`.\scripts\pre_push_smoke_brief.cmd` 通過，907 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_060852.log`；GitHub Actions run `26605399831` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 05:51 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `c5965d5` | `26604680441` | Cap Tk metadata and AI summary jobs | `frontends/tk/source_action_workflows.py` 的 metadata crawl 與 `frontends/tk/ai_summary_workflows.py` 的 AI summary 入口現在都有同一 UI 最多 2 個背景 worker 的 capacity guard；仍保留各自 provider scope / provider-profile single-flight key，queue 滿時只更新 status，不開新 worker。這不改 metadata crawler、provider row action、AI profile、credential 檢查、summary generation 或 repository upsert 行為，只降低外部請求、雲端/本機 AI 呼叫與 repository notes 回寫的併發壓力。新增兩個 capacity regression。已驗證：`py -3 -B -m py_compile frontends\tk\source_action_workflows.py frontends\tk\ai_summary_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs -v` 109 tests OK；`frontends\tk` 與 docs mojibake scan OK；`git diff --check` OK（僅 `PROJECT_GTD.md` / `frontends/tk/ai_summary_workflows.py` CRLF/LF 提醒）；`.\scripts\pre_push_smoke_brief.cmd` 通過，906 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_055235.log`；GitHub Actions run `26604680441` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 05:39 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `a8580b2` | `26604123009` | Cap Tk discovery background jobs | `frontends/tk/discovery_workflows.py` 新增 `MAX_TK_DISCOVERY_BACKGROUND_JOBS = 2` 與 queue-full status helper；provider candidate discovery、dataset candidate discovery、local discovery dry-run audit 仍保留各自 single-flight key，但同一 UI 已有 2 個 discovery/audit worker 時，第三個入口不再開 daemon thread，只提示「Discovery 背景工作已達上限」。這不改 provider discovery、dataset crawler audit、local promotion dry-run、repository upsert 或 review dialog；只降低連點與低算力設備下的 discovery worker 併發壓力。新增三個 capacity regression。已驗證：`py -3 -B -m py_compile frontends\tk\discovery_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs -v` 107 tests OK；`frontends\tk` 與 docs mojibake scan OK；`git diff --check` OK（僅 `PROJECT_GTD.md` / `frontends/tk/discovery_workflows.py` CRLF/LF 提醒）；`.\scripts\pre_push_smoke_brief.cmd` 通過，904 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_054019.log`；GitHub Actions run `26604123009` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 05:25 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `ac59e6f` | `26603488680` | Cap Tk SQLite import jobs | `frontends/tk/import_workflows.py` 的下載結果匯入與本機檔案匯入入口現在會在開 policy dialog / file picker 前檢查同一 UI 的 SQLite import queue 是否已滿；`MAX_TK_SQLITE_IMPORT_JOBS = 1`，已有其他 SQLite import worker 時只更新 status，不開新 thread，也不打開下一步使用者流程。`start_single_flight_thread()` 呼叫同步帶 `max_active_jobs=1` / `on_capacity` 作為保險。這不改 manifest、existing-table policy、local file provenance review、importer 或 ingestion pipeline 規則。新增 `tests.test_tk_dialogs` 兩個 capacity regression。已驗證：`py -3 -B -m py_compile frontends\tk\import_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs -v` 104 tests OK；`frontends\tk` 與 docs mojibake scan OK；`git diff --check` OK（僅 `PROJECT_GTD.md` CRLF/LF 提醒）；`.\scripts\pre_push_smoke_brief.cmd` 通過，901 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_052631.log`；GitHub Actions run `26603488680` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 05:10 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `2a657bc` | `26602836368` | Add crawler asset background capacity guard | `frontends/tk/background_jobs.py` 的 `start_single_flight_thread()` 新增 `max_active_jobs` / `on_capacity`，讓同一 owner 不只擋同 key duplicate，也可在不同 key 背景工作過多時拒絕開新 daemon thread。`CrawlerAssetWorkflowMixin._start_crawler_asset_background_job()` 先設定 crawler asset 分頁最多 4 個並行背景 worker，超過時只更新 status，不開新 worker。這不改 crawler/listing/schema probe/seed download/import service，不導入 asyncio，只先降低連點或多 seed 操作造成的無限制 thread / SQLite path 競爭風險。新增 `tests/test_tk_background_jobs.py::test_start_single_flight_thread_rejects_capacity_before_spawning_thread`。已驗證：in-memory compile `frontends\tk\background_jobs.py` / `frontends\tk\crawler_asset_workflows.py` / `tests\test_tk_background_jobs.py` OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs -v` 102 tests OK；`frontends\tk` 與 docs mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，899 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_051203.log`；GitHub Actions run `26602836368` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 04:57 | Web / Tk Display Contract | **PUSHED / CI PASS** | `9dbe3d2` | `26602160525` | Show callback diagnostics in Web and Tk | Web download/import result row 現在會顯示 backend `callback_diagnostics`：包含 warning chip、下一步與前兩筆 callback error preview；asset-level / seed-level download/import 完成後也會在 mission list 補一筆 callback diagnostics mission。Tk seed download/import completion message 會把同一份 callback warning 與「檢查事件紀錄或 UI 進度回報」寫進 message body。前端仍不重新判斷 callback error 是否代表 failed，成功 download/import 維持成功。本輪補 `tests/test_tk_ui_helpers.py::test_crawler_seed_download_import_ui_message_surfaces_callback_diagnostics`，並在 Web static contract 測試鎖住 helper 與使用者可讀文案。已驗證：`node --check frontends\web\static\app.js` OK；in-memory compile `frontends\tk\ui_helpers.py` / tests OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_web_preview tests.test_tk_dialogs -v` 154 tests OK；Web/Tk touched files mojibake scan OK；`git diff --check` OK（僅 CRLF 提醒）；`.\scripts\pre_push_smoke_brief.cmd` 通過，898 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_045827.log`；GitHub Actions run `26602160525` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 04:42 | Download / UI Display Contract | **PUSHED / CI PASS** | `d8b7f54` | `26601404592` | Surface callback diagnostics in display payload | `api_launcher/crawler_asset_display.py` 的 `crawler_asset_download_import_display_payload()` 現在輸出 top-level `callback_diagnostics`，並在 nested `download_import` 內提供 `callback_error_count`、`callback_errors` 與 `callback_diagnostics`；callback warning 會顯示「進度回報有警告」與「檢查事件紀錄或 UI 進度回報」，但仍不把成功 download/import 重新分類為 failed。`tests/test_crawler_asset_download.py` 已鎖住 display payload 對 callback warning 的呈現。已驗證：in-memory compile `api_launcher\crawler_asset_display.py` / `tests\test_crawler_asset_download.py` OK（K 槽 `__pycache__` lock 曾讓 `py_compile` 寫 pyc 失敗，非語法錯誤）；`py -3 -B -m unittest tests.test_crawler_asset_download -v` 4 tests OK；`py -3 -B -m unittest tests.test_web_preview -v` 48 tests OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 98 tests OK；`api_launcher\crawler_asset_display.py` 與 docs mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，897 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_044415.log`；GitHub Actions run `26601404592` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 04:26 | Download / Scheduler Guard | **PUSHED / CI PASS** | `adcec1c` | `26600577586` | Surface download callback diagnostics in CLI event | `api_launcher/cli_download_plan.py` 的 `download_plan_executed` structured event 現在記錄 `callback_error_count` 與 bounded `callback_errors` 預覽；`api_launcher/ingestion_pipeline.py` 的 CLI 人類摘要在 callback diagnostics 存在時會列出 `[download-plan] callback_errors=N` 與逐筆 callback diagnostic。這讓 UI/progress observer 錯誤可被 agent / handoff 看見，但仍不把成功下載改成 failed。新增 `tests/test_download_plan_runner.py::test_cli_render_lines_include_callback_diagnostics`，並在 CLI JSON summary 測試鎖住 event context 欄位。已驗證：`PYTHONDONTWRITEBYTECODE=1 py -3 -B -m py_compile api_launcher\cli_download_plan.py api_launcher\ingestion_pipeline.py tests\test_download_plan_runner.py` OK；`py -3 -B -m unittest tests.test_download_plan_runner -v` 13 tests OK；`py -3 -B -m unittest tests.test_download_jobs tests.test_download_plan_runner -v` 16 tests OK；`py -3 -B -m unittest tests.test_handoff -v` 21 tests OK；`git diff --check` OK；相關 source/docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，897 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_042759.log`；GitHub Actions run `26600577586` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 04:12 | Download / Scheduler Guard | **PUSHED / CI PASS** | `aaeb8a2` | `26599799798` | Surface download callback diagnostics in plan results | `api_launcher/downloads/plan_runner.py` 的 `DownloadPlanRunResult` 新增 `callback_errors`，`to_dict()` 也會輸出同欄位；`run_download_plan_payload()` 會在 queue shutdown 前讀取 `queue.callback_error_snapshot()`。progress callback 失敗會進入 diagnostics，但不增加 `failed`，也不會把已完成下載改成失敗。新增 `tests/test_download_plan_runner.py::test_runner_reports_progress_callback_errors_without_failing_download`。已驗證：`PYTHONDONTWRITEBYTECODE=1 py -3 -B -m py_compile api_launcher\downloads\plan_runner.py tests\test_download_plan_runner.py` OK；`py -3 -B -m unittest tests.test_download_plan_runner tests.test_download_jobs -v` 15 tests OK；`.\\scripts\\pre_push_smoke_brief.cmd` 通過，896 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_041321.log`；GitHub Actions run `26599799798` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 04:00 | Discovery / Crawler Registry | **PUSHED / CI PASS** | `fb03440` | `26599208635` | Guard crawler registry handler signatures | `api_launcher/crawlers/registry.py` 的 `@crawler(...)` 註冊現在會用共用六參數 signature `(source, timeout, limit, search_terms, full_crawl, max_pages)` 檢查 handler，補齊宣告式 registry 任務單中「handler 簽章不符要在 import/registration 階段報錯」的 contract。新增 `tests/test_dataset_discovery.py::test_crawler_registry_rejects_handler_signature_mismatch`，確認錯誤 handler 會被 registry 拒絕；既有 14 個 handler 行為不變。已驗證：`PYTHONDONTWRITEBYTECODE=1 py -3 -B -m py_compile api_launcher\crawlers\registry.py tests\test_dataset_discovery.py` OK；`py -3 -B -m unittest tests.test_dataset_discovery -v` 57 tests OK；`.\\scripts\\pre_push_smoke_brief.cmd` 通過，895 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_040135.log`；`api_launcher/crawlers` mojibake scan OK；GitHub Actions run `26599208635` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 03:45 | Download / Scheduler Guard | **PUSHED / CI PASS** | `fb8e0d6` | `26598592970` | Isolate download progress callback failures | `api_launcher/downloads/jobs.py` 現在會把 progress callback 例外記錄成 `DownloadCallbackError`，但不讓 UI/observer callback 失敗把實際下載 job 標成 failed；`add_callback()` 改在 lock 內註冊，`_publish()` 先 snapshot callback list 再逐一呼叫，避免 publish 期間 callback list 被修改。新增 `callback_error_snapshot()` 與 `tests/test_download_jobs.py::test_callback_failure_does_not_fail_download_job`。已驗證：`PYTHONDONTWRITEBYTECODE=1 py -3 -B -m py_compile api_launcher\downloads\jobs.py tests\test_download_jobs.py` OK；`py -3 -B -m unittest tests.test_download_jobs -v` 3 tests OK；`py -3 -B -m unittest tests.test_http_downloader tests.test_download_plan_runner tests.test_download_jobs -v` 22 tests OK；`.\\scripts\\pre_push_smoke_brief.cmd` 通過，894 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_034659.log`；`git diff --check` OK；`api_launcher/downloads` mojibake scan OK；GitHub Actions run `26598592970` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 03:26 | Discovery / Crawler Registry | **PUSHED / CI PASS** | `54a8538` | `26597565703` | Move crawler registry metadata to handler modules | 14 個 crawler 的 `@crawler(...)` 註冊移到各自 handler 模組附近；`dataset_sources.py` 不再持有中心宣告式 mapping，只保留匯入觸發、相容常數與 registry/matrix query facade。ERDDAP 與 HTML file index 因原 handler signature 與共用六參數 crawler signature 不同，已在各自模組新增薄 wrapper：`erddap_source_crawler()` 與 `html_file_index_source_crawler()`。這不改 source type、capability bits、crawler output、download/import 或 UI payload，只把 source_type metadata ownership 從中心檔移到 handler 檔。已驗證：crawler 模組與 `tests/test_dataset_discovery.py` py_compile OK；`py -3 -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets tests.test_web_preview -v` 149 tests OK；`py -3 -B -m unittest tests.test_crawler_audit_smoke tests.test_developer_diagnostics tests.test_source_patterns tests.test_discovery_drafts -v` 51 tests OK；`api_launcher/crawlers` mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，893 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_032740.log`；GitHub Actions run `26597565703` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 03:05 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `5adecdc` | `26596461917` | Clean crawler asset thread mock boundary | `frontends/tk/crawler_asset_workflows.py` 移除已不再使用的 `threading` import；`tests/test_tk_dialogs.py` 中 crawler asset workflow 相關 thread mock 改 patch `frontends.tk.background_jobs.threading.Thread`，讓測試對準真正的共用 single-flight helper，而不是舊 UI workflow 模組。這不改 crawler asset listing、source pattern draft、seed schema probe、seed download/import、download plan build 或 credential guard 行為，只是修正測試/程式碼邊界。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs -v` 101 tests OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，893 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_030649.log`；GitHub Actions run `26596461917` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 02:52 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `dbdb0d4` | `26595758705` | Guard Tk Developer CLI background job | `DeveloperCliDialog.run_command()` 不再直接建立裸 `threading.Thread`，改走 `frontends.tk.background_jobs.start_single_flight_thread()`；Developer CLI 使用 `("developer_cli", "command", "")` job key，同一 dialog 仍有 CLI command 執行中時不再清空輸出或開第二個 subprocess worker。這不改 command parsing、working directory、subprocess timeout、stdout/stderr capture 或 status/output 顯示語意。已驗證：`py -3 -B -m py_compile frontends\tk\dialogs.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs -v` 101 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，893 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_025254.log`；GitHub Actions run `26595758705` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 02:37 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `e586cf3` | `26594978569` | Guard Tk sidebar favicon background job | `SidebarWorkflowMixin.fetch_provider_icon_async()` 不再直接建立裸 `threading.Thread`，改走 `frontends.tk.background_jobs.start_single_flight_thread()`；provider favicon fetch 使用 `("provider_favicon", owner, favicon_url)` job key，同一 provider/favicon 下載已在執行時不再開第二個 worker。這不改 favicon URL 推導、cache path、PNG 下載、`PhotoImage` 建立或 sidebar provider filter 顯示語意。已驗證：`py -3 -B -m py_compile frontends\tk\sidebar_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs -v` 99 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，891 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_023718.log`；GitHub Actions run `26594978569` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 02:26 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `0278a93` | `26594347795` | Guard Tk OAuth login background jobs | `OAuthWorkflowMixin` 新增 `_start_oauth_background_job()`，把 OAuth/login 對話中的背景 worker 收斂到 `frontends.tk.background_jobs.start_single_flight_thread()`；Google browser login 使用 `("oauth_browser_login", profile_id, "")` job key，同一 profile 登入流程已在執行時不再開第二個 callback server / browser login worker；device-code polling 使用 `("oauth_device_poll", profile_id, device_code)` job key，避免重複 token polling request。這不改 OAuth authorization URL、PKCE、callback server、device-code polling、token exchange、token 保存或本機 credential storage。已驗證：`py -3 -B -m py_compile frontends\tk\oauth_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_oauth_device tests.test_tk_dialogs -v` 107 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，889 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_022611.log`；GitHub Actions run `26594347795` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 02:11 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `d972c7c` | `26593575008` | Guard Tk showcase download job | `ShowcaseWorkflowMixin.run_showcase_download_from_ui()` 保留既有 `showcase_download_running` 使用者提示，同時把 bounded showcase download worker 改走 `frontends.tk.background_jobs.start_single_flight_thread()`；showcase download 使用 `("showcase_download", "bounded_public", "")` job key，同一展示下載工作已在執行時不再開第二個 worker。這不改 showcase download 的有界 demo source、sample limit、progress dialog、download/import service 或 resumable showcase download queue。已驗證：`py -3 -B -m py_compile frontends\tk\showcase_workflows.py tests\test_tk_dialogs.py` OK；targeted 2 tests OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs tests.test_showcase_workflows -v` 99 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，887 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_021213.log`；GitHub Actions run `26593575008` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 01:58 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `24f1a1a` | `26592898718` | Guard Tk import SQLite jobs | 將 `ImportWorkflowMixin.import_supported_plan_results_from_ui()` 與 `import_local_file_from_ui()` 從裸 `threading.Thread` 改為 `frontends.tk.background_jobs.start_single_flight_thread()`；下載結果匯入與本機檔案匯入共用 `("sqlite_import", sqlite_path, "")` job key，同一 curated SQLite 正在匯入時不再開第二個 worker，且本機檔案匯入會在重複 worker 被擋住時不彈出檔案選擇器。這不改 importer、manifest、existing-table policy、local file provenance review 或 ingestion pipeline 規則。已驗證：`py -3 -B -m py_compile frontends\tk\import_workflows.py tests\test_tk_dialogs.py` OK；targeted 4 tests OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs tests.test_launcher_ui -v` 125 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，885 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_015909.log`；GitHub Actions run `26592898718` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 01:44 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `32bf476` | `26592201477` | Guard AI summary background job | 將 `AiSummaryWorkflowMixin.generate_active_summary()` 從裸 `threading.Thread` 改為 `frontends.tk.background_jobs.start_single_flight_thread()`；AI 摘要 job key 以 provider id 與 AI profile id 建立，同一 provider/profile 的摘要工作同時只會有一個 worker。這不改 AI 摘要 profile 選擇、credential 檢查、summary 生成、repository upsert 或 detail panel 顯示語意。已驗證：`py -3 -B -m py_compile frontends\tk\ai_summary_workflows.py tests\test_tk_dialogs.py` OK；targeted 2 tests OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs -v` 89 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，881 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_014547.log`；GitHub Actions run `26592201477` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 01:33 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `1dac898` | `26591378222` | Guard source action metadata crawl | 將 `SourceActionWorkflowMixin.crawl_provider_ids()` 從裸 `threading.Thread` 改為 `frontends.tk.background_jobs.start_single_flight_thread()`；metadata crawl job key 以排序後 provider scope 建立，同一批 provider metadata crawl 同時只會有一個 worker。這不改 provider metadata crawler、repair/self-check、row action、repository update 或 UI menu 語意。已驗證：`py -3 -B -m py_compile frontends\tk\source_action_workflows.py tests\test_tk_dialogs.py` OK；targeted 2 tests OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs -v` 87 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，879 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_012947.log`；GitHub Actions run `26591378222` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 01:20 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `e183ae0` | `26590713282` | Guard discovery workflow background jobs | 將 `DiscoveryWorkflowMixin` 的 provider candidate discovery、dataset candidate discovery 與 local discovery dry-run audit 從裸 `threading.Thread` 改為 `frontends.tk.background_jobs.start_single_flight_thread()`；同一 provider discovery、同一 provider scope 的 dataset candidate discovery、同一 local discovery audit 同時只會有一個 worker。這不改 provider discovery、dataset crawler audit、local promotion dry-run、repository upsert 或 dialog 顯示語意。已驗證：`py -3 -B -m py_compile frontends\tk\discovery_workflows.py tests\test_tk_dialogs.py` OK；targeted 3 tests OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs -v` 85 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，877 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_011717.log`；GitHub Actions run `26590713282` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 01:00 | Web / Test Hardening | **PUSHED / CI PASS** | `8883f1f` | `26589864084` | Web Preview POST body drain hardening | 回應 docs-only checkpoint `505ecbf` 的 GitHub Actions Windows failure：developer diagnostic POST 因 Windows localhost short-response abort 被測試 retry helper 重送，導致 `developer_real_download_demo()` mock 呼叫兩次。`frontends/web/server.py` 新增 `discard_request_body()`，developer diagnostic 與 unknown POST route 會先 drain unused request body 再回短 JSON；`tests/test_web_preview.py` 的 `post_json_to_preview_server()` 改成預設不重試 POST，只有 legacy 404 route 顯式 opt-in retry，避免重複執行有副作用 endpoint。已驗證：`py -3 -B -m py_compile frontends\web\server.py tests\test_web_preview.py` OK；targeted route tests OK；`py -3 -B -m unittest tests.test_web_preview -v` 48 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，874 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_010020.log`；GitHub Actions run `26589864084` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 00:44 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `320abad` | `26588777239` | Guard MVP demo smoke background job | 將 `MvpDemoWorkflowMixin.run_mvp_demo_smoke_from_ui()` 從裸 `threading.Thread` 改為 `frontends.tk.background_jobs.start_single_flight_thread()`；保留既有 `mvp_demo_smoke_running` 使用者提示，同時讓 canonical MVP demo smoke 使用統一 single-flight active job set / lock / release 機制，避免展示或驗收連點造成重複 demo DB、flow artifacts 或 event log 寫入。這不改 MVP demo smoke 的下載、匯入或 closure 判斷。已驗證：`py -3 -B -m py_compile frontends\tk\mvp_demo_workflows.py tests\test_launcher_ui.py` OK；`py -3 -B -m unittest tests.test_launcher_ui -v` 32 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，874 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_004114.log`；GitHub Actions run `26588777239` 已通過 Ubuntu、Windows 與 real DB smoke；docs-only checkpoint `a6f776e` 的 run `26588937925` 也已通過。 |
| 00:33 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `8cfa0ae` | `26588169356` | Guard plan bounds probe background job | 將 `PlanWorkflowMixin.configure_selected_plan_bounds_from_ui()` 的下載計畫界域欄位探測從裸 `threading.Thread` 改為 `frontends.tk.background_jobs.start_single_flight_thread()`；同一 plan item 的 schema/bounds probe 同時只能跑一個，避免連點造成重複欄位探測、重複界域 dialog 或 `download_plan_entries_by_provider` 競爭。這不改欄位探測、bounds 套用、下載或匯入行為。已驗證：`py -3 -B -m py_compile frontends\tk\plan_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 79 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，872 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_002957.log`；GitHub Actions run `26588169356` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 00:16 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `a8ba8cb` | `26587355365` | Guard source draft background job | 將 `open_source_pattern_draft_dialog()` 從裸 `threading.Thread` 改為既有 `_start_crawler_asset_background_job()` / `frontends.tk.background_jobs` single-flight helper；同一 source URL 的 detector / local source draft worker 同時只能跑一個，避免連點造成重複寫入 local discovery draft 或 UI/event 競爭。這不改 source-pattern detector、local draft service、crawler、download 或 import 行為。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 77 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，870 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260529_001201.log`；GitHub Actions run `26587355365` 已通過 Ubuntu、Windows 與 real DB smoke。 |

### 2026-05-28

| 時間 | 階段 | 狀態 | SHA | Run | Commit | 變更與驗證 |
| --- | --- | --- | --- | --- | --- | --- |
| 23:37 | Tk / UI Helper Consolidation | **PUSHED / CI PASS** | `54c2dbd` | `26585404548` | Extract Tk seed download import target path helper | 新增 `frontends.tk.ui_helpers.CrawlerSeedDownloadImportTargetPaths` 與 `crawler_seed_download_import_target_paths()`，把 seed download/import worker 的 downloads root、curated SQLite 與 resolved plan path 組裝從 `crawler_asset_workflows.py` 抽出。這不改 Tk 操作流程、正式 seed download/import service 或事件 payload；Tk workflow 仍只負責 thread wrapper、repository handoff、status 與 dialog，路徑 sanitize / target layout 改由 helper 測試鎖住。已驗證：`py -3 -B -m py_compile frontends\tk\ui_helpers.py frontends\tk\crawler_asset_workflows.py tests\test_tk_ui_helpers.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 82 tests OK；docs mojibake scan OK；時間佔位掃描無結果；`git diff --check` OK（僅既有 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，868 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_233856.log`；GitHub Actions run `26585404548` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 23:17 | Web / Repository Bootstrap Consolidation | **PUSHED / CI PASS** | `71d498c` | `26584181347` | Extract Web repository bootstrap helper | 新增 `frontends.web.preview_api.WebPreviewRepositorySession` 與 `web_preview_repository_context()`，集中 Web endpoint 重複的 SQLite connection、`ApiCatalogRepository`、schema init 與 optional builtin provider bootstrap。這不改 Web API response、crawler/listing/plan/download/import service 或交易邊界；endpoint 仍明確決定何時 `commit()`，helper 只負責開 session 與初始化 repository。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 48 tests OK；docs mojibake scan OK；時間佔位掃描無結果；`git diff --check` OK（僅既有 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，867 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_231815.log`；GitHub Actions run `26584181347` 已通過 Ubuntu、Windows 與 real DB smoke；docs-only checkpoint `851ea44` 的 run `26584356568` 也已通過。 |
| 23:02 | Web / Download Import Consolidation | **PUSHED / CI PASS** | `266dbcd` | `26583382254` | Extract Web download import target path helper | 新增 `frontends.web.preview_api.WebDownloadImportTargetPaths` 與 `web_download_import_target_paths()`，asset-level 與 seed-level download/import endpoint 共用 DB、downloads root、curated SQLite 與 resolved plan path 組裝。這不改 Web API response、正式下載/匯入 service、目錄語意或使用者操作流程；seed-level run 仍只在預設 Web downloads root 下自動加 seed 子目錄，測試/呼叫端傳入 `downloads_root` 時保留精確路徑控制。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 48 tests OK；docs mojibake scan OK；時間佔位掃描無結果；`git diff --check` OK（僅既有 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，867 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_230350.log`；GitHub Actions run `26583382254` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 22:47 | Web / Event Context Consolidation | **PUSHED / CI PASS** | `b8e32ed` | `26582485477` | Extract Web download import event context helper | 新增 `frontends.web.preview_api.web_download_import_event_context()`，asset-level 與 seed-level download/import 完成事件共用同一份 event context builder。這不改 Web API response、正式下載/匯入 service 或 event 欄位；只移除兩段重複的 stage、success、download_import、artifacts event context 組裝。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 46 tests OK；docs/Web mojibake scan OK；時間佔位掃描無結果；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，865 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_224847.log`；GitHub Actions run `26582485477` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 22:34 | Web / Download Import Consolidation | **PUSHED / CI PASS** | `ab9cf83` | `26581728210` | Extract Web download import credential block helper | 新增 `frontends.web.preview_api.web_download_import_credential_blocked_response()`，asset-level 與 seed-level download/import endpoint 遇到缺憑證時共用同一份 `blocked_before_download` / `edit_local_credentials_before_live_download` payload。這不改 credential guard、正式下載/匯入 service 或 response shape；只移除 Web endpoint 內部重複 blocked payload 組裝。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 46 tests OK，新增 asset/seed download-import 缺憑證時不呼叫正式 service 的 regression；docs/Web mojibake scan OK；時間佔位掃描無結果；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，865 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_223514.log`；GitHub Actions run `26581728210` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 22:19 | Tk / UI Helper Consolidation | **PUSHED / CI PASS** | `8876439` | `26580910210` | Extract Tk seed download import message helper | 新增 `frontends.tk.ui_helpers.crawler_seed_download_import_ui_message()` 與 `CrawlerSeedDownloadImportUiMessage`，把 Tk seed download/import completion message 的 stage、success、artifact 與 next-action label 組裝從 `crawler_asset_workflows.py` 抽出。`_finish_crawler_asset_seed_download_import()` 現在只呼叫 helper、設定 status，然後依 `succeeded` 選擇 info/warning dialog；不改 seed 下載、匯入或使用者操作流程。已驗證：`py -3 -B -m py_compile frontends\tk\ui_helpers.py frontends\tk\crawler_asset_workflows.py tests\test_tk_ui_helpers.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 81 tests OK；`py -3 -B -m unittest tests.test_launcher_ui tests.test_tk_ui_helpers tests.test_tk_dialogs -v` 111 tests OK；docs/Tk mojibake scan OK；時間佔位掃描無結果；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，863 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_222110.log`；GitHub Actions run `26580910210` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 22:02 | Tk / Display Contract | **PUSHED / CI PASS** | `ea3186a` | `26579978458` | Align Tk download import display payload | 將 Tk seed download/import 完成訊息接到共用 `crawler_asset_download_import_display_payload()`。Tk 不再直接從 raw `result.to_dict()` 自行重組 stage、success 與 next-action label，而是讀同一份 backend display payload；helper 也補上無 `pipeline` 物件時的 fallback，會從 legacy / test `download_result` 補回 `download_import.stage`、`succeeded` 與 `next_action`。這不改 seed 下載、匯入或使用者操作流程，只收斂顯示 payload 邊界。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_display.py frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py tests\test_crawler_asset_download.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_crawler_asset_download tests.test_web_preview -v` 123 tests OK；docs mojibake scan OK；時間佔位掃描無結果；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，862 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_220333.log`；GitHub Actions run `26579978458` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 21:48 | Web / Display Contract | **PUSHED / CI PASS** | `3e3d7af` | `26579114791` | Consolidate download import display payload | 做小型 consolidation：新增 `api_launcher.crawler_asset_display.crawler_asset_download_import_display_payload()`，集中包裝 formal asset-level / seed-level download-import run 的 `download_result`、`plan_result`、`plan_outcome`、`plan_passport`、`adapter_review`、`download_import` 與 `next_action_label`。`frontends/web/preview_api.py` 的 asset / seed 下載匯入 endpoint 改用這個後端 display helper，Web 不再各自重組 plan outcome、plan passport、adapter review 與 next-action label；endpoint 仍只處理輸入、destination、profile passport update 與 event logging。這不改 crawler、plan、download、import 或 response shape。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_display.py frontends\web\preview_api.py tests\test_crawler_asset_download.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_crawler_asset_download tests.test_web_preview -v` 48 tests OK；docs mojibake scan OK；時間佔位掃描無結果；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，862 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_214932.log`；GitHub Actions run `26579114791` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 21:29 | Tk / Project Maturity | **PUSHED / CI PASS** | `e19d8e3` | `26578190695` | Add Tk project maturity matrix action | 將後端成熟度矩陣接回 Tk 控制台：新增 `frontends/tk/project_maturity_workflows.py`，工具選單新增「專案成熟度矩陣」。Tk 只開 DB / repository 並呈現 `build_project_maturity_payload()`，顯示 canonical delivery scope、各成熟度 row、`🚧` 施工中 / 規劃中圖示、限制數與「不要用單一百分比回答」口徑，不在 Tk 內重新推理 maturity 或重算完成率。已驗證：`py -3 -B -m py_compile frontends\tk\project_maturity_workflows.py frontends\tk\launcher_ui.py frontends\tk\window_layout_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_launcher_ui tests.test_project_maturity -v` 109 tests OK；temp DB payload smoke 確認 renderer / Qt rows 帶 `status_icon=🚧` 且 Tk message 含施工中圖示；docs mojibake scan OK；時間佔位掃描無結果；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，861 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_213112.log`；GitHub Actions run `26578190695` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 20:47 | Web / Project Maturity | **PUSHED / CI PASS** | `6e51329` | `26575702230` | Add Web project maturity workspace | 將後端成熟度矩陣接到 Web Preview：新增 `frontends/web/preview_api.py::web_project_maturity()` 與 `GET /api/project-maturity`，瀏覽器新增「成熟度」工作區，顯示 canonical delivery scope、各成熟度 row、`🚧` 施工中圖示、display tone、限制與下一步。Web 只呈現 `api_launcher.project_maturity` payload，不重新推理專案完成率，也不改 crawler/download/import 或 maturity 判斷。已驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview tests.test_project_maturity -v` 48 tests OK；docs mojibake scan OK；時間佔位掃描無結果；`.\scripts\pre_push_smoke_brief.cmd` 通過，859 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_204151.log`；GitHub Actions run `26575702230` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 19:42 | Project Maturity / UI Contract | **PUSHED / CI PASS** | `ce6bac3` | `26572843583` | Add maturity construction display profile | 成熟度矩陣補上 UI-neutral display contract：`api_launcher/project_maturity.py` 的 row payload 現在輸出 `status_icon`、`display_tone`、`display_label` 與 `display_profile`；`contract_only` / `planned_not_started` 會輸出 `🚧`，Markdown renderer 也會用 `maturity_level` fallback 補 icon，讓 Tk/Web/未來 Qt 可直接顯示施工中狀態，不必各自推理未完成能力。這不改 maturity 判斷、不改 crawler/download/import。已驗證：`py -3 -B -m py_compile api_launcher\project_maturity.py tests\test_project_maturity.py` OK；`py -3 -B -m unittest tests.test_project_maturity -v` 4 tests OK；`py -3 -B -m unittest tests.test_project_maturity tests.test_handoff -v` 25 tests OK；CLI write/read smoke 確認 renderer/simulation row 輸出 `status_icon=\U0001f6a7`、`display_tone=review`；`.\scripts\pre_push_smoke_brief.cmd` 通過，857 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_194439.log`；GitHub Actions run `26572843583` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 19:31 | Web / Bounds Service | **PUSHED / CI PASS** | `7d63f1c` | `26572118977` | Delegate Web base bounds form to backend service | 延續 schema probe service consolidation：`frontends/web/preview_api.py::crawler_asset_bound_form()` 現在直接委託 `api_launcher.crawler_asset_schema_probe.crawler_asset_bound_form_spec()`，Web 不再自己讀 `BUILD_DOWNLOAD_PLAN` capability、載入 source、組 base form spec。這不改 Web route、payload shape、bounds form 欄位或 schema probe 行為；只是把 base bounds form 組裝邏輯一併收回 backend service module，讓 Web Preview 繼續保持薄 adapter。已驗證：`py -3 -B -m py_compile frontends\web\preview_api.py api_launcher\crawler_asset_schema_probe.py` OK；focused Web bounds/schema tests OK；`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets -v` 87 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，856 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_192824.log`；GitHub Actions run `26572118977` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 19:18 | Web/Tk / Bounds Service | **PUSHED / CI PASS** | `fd43435` | `26571568872` | Extract crawler asset schema probe service | 做 bounded consolidation：新增 `api_launcher/crawler_asset_schema_probe.py`，把 seed/resource URL 正規化、schema probe、bounds form spec 建立、probe enrichment 與 `next_action_label` payload 收成 UI-neutral backend service。Web Preview 仍重新匯出 `crawler_asset_bound_form_schema_probe()` 保持 route/import 相容，但不再保存 probe runner、row-limit/timeout bounded parsing 與 form enrichment；Tk seed schema probe worker 改吃 `crawler_asset_bound_form_schema_probe_result()`，只負責開 dialog 與保存 bounds payload。已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_schema_probe.py frontends\web\preview_api.py frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py tests\test_web_preview.py` OK；focused schema probe tests OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview tests.test_crawler_assets -v` 160 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，856 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_191340.log`；GitHub Actions run `26571568872` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 18:54 | Docs / Log Governance | **PUSHED / CI PASS** | `55f357e` | `26570514520` | Fix exact checkpoint times in docs | 修正文檔治理漂移：近期 checkpoint 曾以「小時精確、分鐘佔位」格式作為暫存時間，後續 commit / CI 完成後未回填實際分鐘。已將相關 rows / handoff headings 改為實際本地時間，並補充 guardrail：正式 log 不應提交分鐘佔位，pending checkpoint 應在 commit 前回填精確 `HH:mm` 或明確標示 pending。已驗證：時間佔位掃描無結果；`git diff --check` OK；docs mojibake scan OK；GitHub Actions run `26570514520` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 18:42 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `f8c0aed` | `26569870367` | Extract Tk background job helper | 做小型 consolidation：新增 `frontends/tk/background_jobs.py`，把 single-flight active job set、lock、duplicate guard、release 的共通邏輯從 `crawler_asset_workflows.py` 抽出。`CrawlerAssetWorkflowMixin` 仍保留 crawler asset 專用薄包裝與 UI 狀態文案，但不再直接維護 active job set 細節。這不改 crawler/listing/plan/download/import 行為，也不把 Tk 全面改成 async；只是先建立可測 helper，讓後續其他 Tk workflow 可共用 bounded scheduler / single-flight guard 模式。已驗證：`py -3 -B -m py_compile frontends\tk\background_jobs.py frontends\tk\crawler_asset_workflows.py tests\test_tk_background_jobs.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_background_jobs -v` 3 tests OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 73 tests OK；`py -3 -B -m unittest tests.test_tk_background_jobs tests.test_tk_dialogs tests.test_web_preview -v` 118 tests OK；`git diff --check` OK（僅 CRLF/LF warning）；docs/frontends/tk/tests mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，856 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_183726.log`; GitHub Actions run `26569870367` passed Ubuntu, Windows, and real DB smoke. |
| 18:29 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `c075581` | `26569271791` | Harden crawler asset job guards | 延伸上一個 single-flight guard：`run_selected_crawler_asset_listing()` 與 `prepare_selected_crawler_asset_download()` 也改用 `_start_crawler_asset_background_job()`。同一 crawler asset 的清單擷取或下載計畫建立正在執行時，Tk 不再開第二個 worker；下載計畫會在打開 bounds dialog 前先檢查 active job，避免連點時重複彈表單。這仍不改後端 crawler/listing/plan/download/import 語意，也不引入全面 async，只是 Tk control panel 的 bounded job guard。第一次 pre-push smoke 暴露兩個本地環境韌性缺口，已一併修補：`event_log.latest_events()` 在雲端碟 binary seek/read 被拒時退回 bounded streaming tail；Web Preview port scan 遇到 Windows 保留或安全阻擋整段候選 port 時退回 OS-assigned local port。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；targeted 4 tests OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 73 tests OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview tests.test_crawler_assets -v` 159 tests OK；robustness targeted tests OK；`py -3 -B -m unittest tests.test_web_preview tests.test_event_log tests.test_tk_dialogs -v` 119 tests OK；`git diff --check` OK（僅 CRLF/LF warning）；docs/api_launcher/frontends/tests mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，853 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_182328.log`；GitHub Actions run `26569271791` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 18:07 | Tk / Scheduler Guard | **PUSHED / CI PASS** | `ee702cd` | `26568299155` | Add Tk seed job single-flight guard | 做 bounded scheduler/consolidation 小切片：`frontends/tk/crawler_asset_workflows.py` 新增 `_start_crawler_asset_background_job()` / `_release_crawler_asset_background_job()`，用 `(job_type, asset_id, dataset_uid)` 登記 active Tk 背景任務。同一 seed 的欄位探測或 seed download/import 已在執行時，Tk 不再重複開 `threading.Thread`，而是回報 already-running 狀態。這不是全面改 async/await，也不改後端下載、匯入、schema probe 或 crawler 行為；只是先在 Tk 薄殼加 single-flight guard，降低連點造成的重複 worker、SQLite path / download path 競爭與對話框狀態混亂風險。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；targeted 3 tests OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 71 tests OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview tests.test_crawler_assets -v` 157 tests OK；docs/Tk mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，849 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_180047.log`；GitHub Actions run `26568299155` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 17:52 | Web / CI Hardening | **PUSHED / CI PASS** | `1e5e7d0` | `26567621356` | Harden Web preview JSON connection close | 修補 `a862a7c` 後 Windows CI 在 `POST /api/demo/real-download` legacy 404 route 讀取短 JSON body 時的 flaky `ConnectionAbortedError: [WinError 10053]`。`frontends/web/server.py` 的 `write_json()` 現在明確送出 `Connection: close`、flush body 並設定 `close_connection = True`；`tests/test_web_preview.py` 的 preview POST helper 也送出 `Connection: close` 並使用 bounded retry。這不改 crawler、seed enumeration、download/import 或正式 Web endpoint 語意，只讓 local preview HTTP close contract 更明確。已驗證：`py -3 -B -m py_compile frontends\web\server.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview.WebPreviewApiTest.test_real_download_demo_route_is_developer_diagnostic_only -v` OK；`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets -v` 86 tests OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_developer_diagnostics -v` 73 tests OK；docs/frontends-web mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，847 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_174707.log`；GitHub Actions run `26567621356` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 17:40 | Crawler Asset / UI Contract | **CI 失敗** | `a862a7c` | `26567064570` | Move seed enumeration display text into display profile | 做 bounded consolidation：`crawler_seed_enumeration_payload()` 仍在 service 層決定 blocked/error/empty/local-limit/warning/within-limits/sample 狀態，但顯示文案、tone、default next_action 與 completion confidence 的預設值已抽到 `api_launcher/crawler_asset_display.py` 的 `SeedEnumerationDisplayProfile` / `seed_enumeration_display_payload()`。Web/Tk/未來 Qt 繼續消費同一份 `seed_enumeration` payload，同時避免 `crawler_asset_service.py` 繼續吸收 UI 顯示字串。沒有改 crawler、download/import、seed page window 或 remote pagination 行為。本地已驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_display.py api_launcher\crawler_asset_service.py tests\test_crawler_assets.py` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview tests.test_tk_dialogs -v` 155 tests OK；docs/api_launcher mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，847 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_173534.log`。GitHub Actions run `26567064570` 的 Ubuntu 與 real DB smoke 通過，但 Windows Python job 在 Web Preview legacy 404 route 測試讀短 JSON response 時遇到 `ConnectionAbortedError: [WinError 10053]`，後續以 Web HTTP close contract hardening 修補。 |
| 17:25 | Discovery / Diagnostics | **PUSHED / CI PASS** | `f002ff1` | `26566344259` | Add crawler registry report CLI JSON | 把 `api_launcher/crawler_registry_report.py` 接成正式 CLI JSON 入口：`py -3 -B APIkeys_collection.py --crawler-registry-report-json` 會輸出 source type count、dimension counters、matrix cells、capability groups 與 spec payload，供 agent / CI / diagnostics 直接讀取，不必解析 Web/Tk 或人類文字。`api_launcher/cli_flags.py` 維持 lazy import，並將原本已壞掉的註解重寫成乾淨 ASCII 維護註解；`core.py` 只新增薄入口 `show_crawler_registry_report()`，不在 core 內重建 registry 邏輯。已驗證：`py -3 -B -m py_compile api_launcher\cli_flags.py api_launcher\core.py api_launcher\crawler_registry_report.py tests\test_developer_diagnostics.py` OK；`py -3 -B -m unittest tests.test_developer_diagnostics tests.test_cli_flags tests.test_project_maturity -v` 8 tests OK；實跑 `py -3 -B APIkeys_collection.py --db state\tmp\crawler_registry_report.sqlite --init-db --seed --crawler-registry-report-json` 只輸出 JSON，無 `[db]` / `[seed]` 人類訊息；docs/api_launcher mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，846 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_171845.log`；GitHub Actions run `26566344259` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 17:06 | Discovery / Diagnostics | **PUSHED / CI PASS** | `628b6bf` | `26565469450` | Add crawler registry diagnostics report | 新增 `api_launcher/crawler_registry_report.py`，把 `CrawlerSpec` registry 轉成 UI-neutral report / summary：包含 source type count、dimension counters、matrix cells、capability groups 與 spec payload。`developer_diagnostics.py` 的 `crawler_handler_smoke_diagnostics_payload()` 現在會帶 `registry_summary`，讓 Tk/Web/未來 Qt developer-only diagnostics 可讀同一份 registry 維度摘要，不需要各自掃 globals 或重建 source_type 分組。這不打 live source，也不改 crawler handler、download/import 或正式 UI flow。已驗證：`py -3 -B -m py_compile api_launcher\crawler_registry_report.py api_launcher\developer_diagnostics.py tests\test_developer_diagnostics.py` OK；`py -3 -B -m unittest tests.test_developer_diagnostics tests.test_tk_dialogs tests.test_web_preview -v` 113 tests OK；docs/api_launcher mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，845 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_165956.log`；GitHub Actions run `26565469450` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 16:51 | Discovery / Crawler Registry | **PUSHED / CI PASS** | `b1091b2` | `26564776648` | Route crawler dispatch through registry | Crawler dispatch 往宣告式 registry 再收一刀：`discover_dataset_candidate_output_for_source()` 與 `discover_dataset_candidates_for_source()` 現在直接透過 `crawler_handler(source.source_type)` 讀 `CrawlerSpec` registry；`SOURCE_CRAWLER_HANDLERS` 保留為相容/診斷 surface，但不再承載正式分派責任。新增 `iter_crawler_specs_by_dims()` / `crawler_specs_by_dims()` 與 `list_registered_crawlers()` / `list_crawlers_by_dims()`，讓 CLI/UI/debug 可按 `source_family`、`transport`、`auth_profile`、`result_shape` 做 partial matrix query，不必回到散落的 `source_type` 分支。這不改 14 個 crawler handler、pagination、download/import 或 UI 行為。已驗證：`py -3 -B -m py_compile api_launcher\crawlers\registry.py api_launcher\crawlers\dataset_sources.py tests\test_dataset_discovery.py` OK；`py -3 -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets tests.test_web_preview -v` 141 tests OK；docs/crawlers mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，844 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_164315.log`；GitHub Actions run `26564776648` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 16:09 | Tk / Bounds UX | **PUSHED / CI PASS** | `430d278` | `26562872018` | Add Tk seed schema probe action | Tk Seed 清單 dialog 已接上 seed-level schema/head probe：選取 seed 後可按「探測欄位」，dialog 只把 `api_url` / `download_url` / `landing_url` 包成 entry；workflow 背景呼叫既有 `probe_plan_entry_schema()`，再用 `apply_schema_probe_to_crawler_asset_bound_form_spec()` 回填同一份 bounds form，套用後的 payload 暫存在 `crawler_asset_bound_payloads[asset_id]` 給後續 seed download/import。這是 Tk/Web 對齊與防盲填切片，不改 crawler、download、import。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_seed_dialog.py frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 69 tests OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview tests.test_crawler_assets -v` 154 tests OK；docs/Tk/tests mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，843 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_160421.log`；GitHub Actions run `26562872018` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 15:43 | Web / Bounds UX | **PUSHED / CI PASS** | `6f3e2de` | `26561993780` | Add Web seed schema probe action | Web Preview 已把 seed-level schema/head probe 接到 UI：推薦 seed 面板與每筆 seed row 都有「探測欄位」按鈕，會把可見 seed 的 `api_url` / `download_url` / `landing_url` 交給既有 `/bounds-form/schema-probe` endpoint，再用後端回傳的 bounds form 重繪欄位。這是防盲填接線，不改 schema probe 後端、crawler、download 或 import。驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview -v` 41 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，840 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_154516.log`；GitHub Actions run `26561993780` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 15:31 | Web / Capability UI | **PUSHED / CI PASS** | `6d2cf77` | `26561194976` | Show crawler capability address in Web Preview | Web Preview 現在消費後端 `capability_profile`，在 crawler asset card 顯示「能力 0000」徽章，並在 Passport 顯示「能力位址」與「能力膠囊」摘要；Web 不自行用 `source_type` 推算能力分組。已補 `tests.test_web_preview` regression，鎖住 cards API、靜態 UI 文字與 `.capability-address-badge` 樣式。本地驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m py_compile frontends\web\preview_api.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview -v` 41 tests OK；`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets tests.test_dataset_discovery -v` 140 tests OK；臨時 Web server `127.0.0.1:8795` 驗證 `/api/crawler-assets`、`/app.js`、`/styles.css` 皆包含 capability profile / badge 內容；`.\scripts\pre_push_smoke_brief.cmd` 通過，840 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_152601.log`；GitHub Actions run `26561194976` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 15:08 | Discovery / Capability Profile | **PUSHED / CI PASS** | `7d319e2` | `26560284599` | Surface crawler capability address in profiles | 將 4-bit crawler capability address 從 registry 推進到 `CrawlerCapabilityProfile`：asset payload 現在會輸出 `capability_code`、`capability_bits`、`capability_binary`，讓 Tk/Web/未來 Qt 可讀同一份能力膠囊地址，不必各自從 source_type 猜分組。未知或未註冊 source type 仍回空地址，避免 backlog handler 被誤標成已完成能力。已驗證：`py -3 -B -m unittest tests.test_crawler_assets -v` 44 tests OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_dataset_discovery tests.test_web_preview -v` 140 tests OK；`py -3 -B -m py_compile api_launcher\crawler_capability_profiles.py tests\test_crawler_assets.py` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，840 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_150514.log`；GitHub Actions run `26560284599` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 14:40 | Discovery / Crawler Registry | **PUSHED / CI PASS** | `32d2325` | `26559567014` | Add crawler capability address mask index | 將使用者提出的 IPv4/CIDR 類比先落成小型 registry PoC：`api_launcher/crawlers/registry.py` 新增 4-bit `CrawlerCapabilityCode` 與 `CrawlerCapabilityMask`，固定維度為來源表面、transport、auth、輸出形狀；`dataset_sources.py` 匯出 `CRAWLER_CAPABILITY_INDEX`。這不是重寫 14 個 crawler，也不取代 `CrawlerSpec`，只提供可遮罩查詢的能力膠囊索引，讓後續 UI/debug/API 不必重寫 source_type 分支。已驗證：`py -3 -B -m unittest tests.test_dataset_discovery -v` 55 tests OK；`py -3 -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets tests.test_web_preview -v` 140 tests OK；docs / crawler mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，840 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_144419.log`；GitHub Actions run `26559567014` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 14:09 | Architecture / Guardrail | **PUSHED / CI PASS** | `c4970e2` | `26558075686` | Document branch threshold profile storage guardrails | 補充準宣告式架構邊界：`if/else` 適合 2 到 3 條人類可一眼讀懂的簡單分支；4 條路已接近 `2 x 2` matrix，應考慮 table / registry / decorator dispatch。decorator 不只是語義標籤，也可作為大量條件分支下避免中心 `if/elif` 膨脹的註冊/分派工具，但不得吞掉 handler 回傳值；payload 包裝與狀態正規化仍由 gateway/normalizer 出口處理。YAML/JSON/TOML/`.env` 適合放有人類語意、需要使用者或維護者填寫的 profile；純邏輯高維分派優先用 typed Python table / dataclass / tuple index / dict registry。這是文檔切片，不改產品程式碼。已驗證：`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；GitHub Actions run `26558075686` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 13:46 | Architecture / Guardrail | **PUSHED / CI PASS** | `5c8dece` | `26557432640` | Document loop range slice guardrails | 補充 loop / ordered collection / decorator 設計規則：range、slice、array/list/page window 是避免硬編碼的重要工具；迴圈停止條件優先來自 protocol response、source profile、使用者 bounds、job budget、schema size、remote pagination metadata 或 runtime policy。硬寫哨兵值或 magic page size 若不可避免，必須命名、可覆寫、可測，並在 payload 回報 `limit_reached` / `sentinel_stop` 類 structured warning。裝飾器可用於註冊 crawler metadata 與保留 handler 回傳值，不應吞掉 candidates、warnings 或 pagination metadata。宣告式方向採混合式準宣告式，不做上帝 YAML；主管道中的分流膠囊只讓條件分支選路，payload 包裝與狀態正規化由 gateway/normalizer 出口統一處理。這是文檔切片，不改產品程式碼。已驗證：`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；GitHub Actions run `26557432640` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 13:45 | Architecture / Guardrail | **PUSHED / CI PASS** | `f1b38b8` | `26557008628` | Document recursion and pipeline guardrails | 補充架構決策：宣告式與管道式不是互斥，RRKAL 應以 profile 宣告能力/政策/budget，以 middleware pipeline 執行 credential guard、bounded fetch、pagination、rate-limit、dedupe、audit warning 等安全步驟。新增遞迴 guardrail：遠端 crawler、網站探索、HTML index traversal、pagination、下載/匯入主路徑預設不用 recursive call stack，改用 queue/stack/deque 與 `seen`、`max_depth`、`max_pages`、`max_nodes`、timeout、rate-limit；互動式遠端探索以 Raspberry Pi-class 裝置為基準，預設 `max_depth=2`，無明確 profile/測試/確認時不超過 `max_depth=4`。這是文檔切片，不改產品程式碼。已驗證：`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；GitHub Actions run `26557008628` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 13:29 | Docs / Workflow Governance | **PUSHED / CI PASS** | `835d673` | `26556736959` | Record governance intake before implementation | 開工前文檔治理 checkpoint：吸收使用者新增的後端解耦、文檔治理、註釋新鮮度、施工中 UI 標示與資料夾分艙規則。更新 GTD / handoff / workspace layout / code relationship map / docs index，明確規定大檔解耦要排進固定 consolidation slice、邏輯/service 邊界先於物理搬檔、文檔可作為資料資產用 CSV/JSON/SQLite registry 治理、註釋必須維持新鮮，以及未完成能力要在 UI 顯示 `🚧` / construction / `contract_only` / `planned`，不能冒充已交付。這是文檔切片，不改產品程式碼。已驗證：`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；GitHub Actions run `26556736959` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 12:45 | Discovery / Capability Profile | **PUSHED / CI PASS** | `c0cbc6f` | `26555497364` | Surface crawler spec metadata in capability profile | 將上一個 `CrawlerSpec` registry metadata 接進既有 `CrawlerCapabilityProfile`，asset payload 現在會帶 `source_family`、`transport`、`result_shape` 與 `supports_full_crawl`，讓 Web/Tk/agent 能讀同一份 source capability contract，不必從 source type 或 handler 名稱猜來源能力。這仍是 read/describe contract，不改 crawler、download、import 或 UI 行為；未登記 source type 會保守回 `unknown` / `False`。已驗證：`py -3 -B -m unittest tests.test_crawler_assets tests.test_dataset_discovery tests.test_web_preview -v` 136 tests OK；`crawler_capability_profiles.py` / `test_crawler_assets.py` in-memory compile OK；docs / source mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，836 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_124725.log`；GitHub Actions run `26555497364` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 12:31 | Discovery / Crawler Registry | **PUSHED / CI PASS** | `bc80e96` | `26555005114` | Add crawler spec registry compatibility layer | 採納「CrawlerSpec + Registry + decorator」方向，但先做相容層而非搬動 14 個 handler。新增 `api_launcher/crawlers/registry.py`，提供 `CrawlerSpec`、`@crawler(...)`、`crawler_specs_by_source_type()` 與四維 `crawler_matrix()`；`dataset_sources.py` 的 `SOURCE_CRAWLER_HANDLERS` 仍保留給既有呼叫與測試，但已改由 registry specs 生成。這讓 source_type 分派開始有宣告式屬性：`source_family`、`transport`、`auth_profile`、`result_shape`、`supports_full_crawl`，下一步才逐步把 dispatch/gateway 往 profile 化推。已驗證：`py -3 -B -m unittest tests.test_dataset_discovery tests.test_crawler_audit_smoke tests.test_crawler_assets tests.test_source_patterns tests.test_discovery_drafts -v` 142 tests OK；`registry.py` / `dataset_sources.py` / `test_dataset_discovery.py` in-memory compile OK；docs / crawler mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，836 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_123315.log`；GitHub Actions run `26555005114` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 12:13 | Web / Bounds UX | **PUSHED / CI PASS** | `1f1cd71` | `26554444984` | Add schema probe bounds form enrichment | 將 schema/head probe 與 crawler asset 界域表單接成後端契約：`apply_schema_probe_to_crawler_asset_bound_form_spec()` 會把 probe 欄位套入 `time_field` / `columns` selector，並在時間欄位已解析時清掉起訖日期的 schema-probe warning；probe 失敗保留 warning，不假裝可用。Web Preview 新增 `POST /api/crawler-assets/{asset_id}/bounds-form/schema-probe`，薄薄呼叫既有 `probe_plan_entry_schema()` 後回傳 enriched `bound_form`，讓未來 Web/Tk 都可用同一份後端欄位 selector contract。本輪也補上 nested `entry.url` 正規化為 `download_url`，降低前端接線時 payload 形狀不一致的失敗率。已驗證：`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview tests.test_tk_dialogs -v` 151 tests OK；docs mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，835 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_121359.log`；GitHub Actions run `26554444984` 已通過 Ubuntu、Windows 與 real DB smoke。下一步仍需把此 route 接到 seed 選取 / UI 按鈕。 |
| 11:48 | Tk / Bounds UX | **PUSHED / CI PASS** | `3af4a91` | `26553622156` | Add Tk bounds quick values | 先複核另一 agent 的 `api_launcher/cli_flags.py` lazy import 變更：HEAD `055a9fc`，`cli_flags.py` 無未提交 diff，`py -3 -B -m unittest tests.test_cli_flags tests.test_handoff -v` 22 tests OK，故不需回退。接著把 Tk 界域 dialog 接上後端既有 `recommended_values` / `presets`，新增「快速界域」、「套用推薦值」與區域預設按鈕；UI 只複製後端明確值，不自行猜 collection、time field、bbox 或版本。已驗證：`py -3 -B -m unittest tests.test_tk_dialogs -v` 66 tests OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_tk_dialogs -v` 108 tests OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_crawler_assets tests.test_cli_flags tests.test_handoff -v` 130 tests OK；docs mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，830 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_114745.log`；GitHub Actions run `26553622156` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 11:27 | Backend/Core Hardening | **PUSHED / CI PASS** | `7a53eca` | `26553034392` | Defer CLI command imports | 將 `api_launcher/cli_flags.py` 的子命令活性判斷模組改成 `command_requested()` 內的延遲導入，避免普通啟動或單純 import `cli_flags` 時預先載入所有 CLI workflow 依賴；新增 `tests/test_cli_flags.py` 鎖住 import `api_launcher.cli_flags` 時不得連帶載入其他 `api_launcher.cli_*` command module。已驗證：不寫 pyc 的 compile OK；`py -3 -B -m unittest tests.test_cli_flags tests.test_handoff -v` 22 tests OK；docs mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，828 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_112847.log`；GitHub Actions run `26553034392` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 11:18 | Tk / Seed UX | **PUSHED / CI PASS** | `22ccefa` | `26552672553` | Add recommended seed action to Tk dialog | 將後端 `crawler_seed_page()` 提供的 `recommended_seed_uid` 接到 Tk Seed 清單 dialog。Dialog 現在會顯示「推薦 seed」摘要、自動選中推薦列，並提供「下載推薦 Seed」按鈕；推薦邏輯仍只來自後端 payload，Tk 不自行挑 seed 或推斷 import policy。已驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_seed_dialog.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 64 tests OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_crawler_seed_registry tests.test_handoff -v` 107 tests OK；docs / modified Tk/test mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，827 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_111749.log`；GitHub Actions run `26552672553` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 10:41 | Web / Seed UX | **PUSHED / CI PASS** | `46601f2` | `26552151790` | Surface recommended seed in Web Preview | 將後端 `crawler_seed_page()` 已提供的 `recommended_seed_uid` 接到 Web Preview seed 清單。畫面現在會在 seed 列表上方顯示「推薦 seed」與「下載推薦 seed」，直接呼叫正式 `POST /api/crawler-assets/{asset_id}/seed-download-import`，不在 JS 內自行挑 seed 或判斷 import policy。測試也補上 localhost POST retry helper，避免 Windows `WinError 10053` 讓舊 `/api/demo/real-download` 已退場的 404 regression 偶發失真。已驗證：Web Preview 瀏覽器選到 NOAA NCEI seed list 時可見「下載推薦 seed」，且未露出 raw `download_recommended_seed`；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_seed_registry tests.test_handoff -v` 81 tests OK；modified Web/test mojibake scan OK；docs mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 通過，826 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_105848.log`；GitHub Actions run `26552151790` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 09:45 | Planning / Scope Control | **PUSHED / CI PASS** | `806b6cd` | `26549667153` | Narrow active target to crawler closure | 依使用者更新目標，近期主線收斂到 crawler / data asset 小閉環：入口 -> 枚舉 seed -> 推薦 seed -> 有界下載 / 匯入 -> 可驗收 JSON/UI。暫時略過資料渲染、Unreal 5 對接、simulation bridge 與 renderer ecosystem，不再把這些 contract-only / planned work 當作當前交付阻塞。已驗證：`py -3 -B -m unittest tests.test_handoff -v` 21 tests OK；docs mojibake scan 0 hits；`git diff --check` OK（僅 CRLF/LF warning）；GitHub Actions run `26549667153` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 09:37 | Crawler Seed / UX Defaults | **PUSHED / CI PASS** | `8083578` | `26549445961` | Add recommended crawler seed payload | Seed page payload 補上 `recommended_seed`、`recommended_seed_uid` 與 `recommended_seed_next_action=download_recommended_seed`，由後端從目前可見頁面挑第一筆可進 `sqlite_curated_import` 且不需 review 的 seed，讓 Web/Tk 未來可做「一鍵推薦下載」而不是讓前端自行猜預設值。已驗證：in-memory syntax compile OK；`py -3 -B -m unittest tests.test_crawler_seed_registry tests.test_handoff -v` 43 tests OK；docs / source / tests mojibake scan 0 hits；`git diff --check` OK（僅 CRLF/LF warning）；用 Python `subprocess.run(..., encoding='utf-8')` 捕捉 live Socrata seed page JSON，確認 `nyc_open_data_socrata_catalog` 推薦 `ds_5dce200fd403246bca0031d5` / `Civil Service List (Active)`，且中文 label `可有界匯入 SQLite` 正常；`.\scripts\pre_push_smoke_brief.cmd` 通過，826 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_093628.log`；GitHub Actions run `26549445961` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 09:19 | Crawler Seed / UI Contract | **PUSHED / CI PASS** | `4dc573b` | `26548948298` | Mark Socrata seeds as bounded importable | 修正 seed 清單對 Socrata resource 的內容能力描述：`api_launcher/content_registry.py` 現在把 `socrata_resource` 視為 resolver-backed API resource，由 `socrata_bounded_sample_query_resolver` 產生有界 JSON sample 後可走 JSON -> SQLite 匯入；seed row 不再顯示「未知內容格式」。已驗證：in-memory syntax compile OK；`py -3 -B -m unittest tests.test_content_registry tests.test_crawler_seed_registry tests.test_handoff -v` 55 tests OK；docs / source / tests mojibake scan 0 hits；`git diff --check` OK（僅 CRLF/LF warning）；用本地 temp DB 查 `nyc_open_data_socrata_catalog` seed page，第一筆 live Socrata seed 顯示 `content_display_label=可有界匯入 SQLite`、`review_required=false`、`parser_id=socrata_bounded_sample_query_resolver`；`.\scripts\pre_push_smoke_brief.cmd` 通過，825 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_092121.log`；GitHub Actions run `26548948298` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 08:54 | Crawler Seed / CLI Closure | **PUSHED / CI PASS** | `d667f65` | `26548277425` | Add crawler seed download import CLI | 補上 seed-level formal download/import 的 CLI JSON 入口：`--run-crawler-seed-download-import ASSET_ID DATASET_UID --crawler-seed-download-import-json`。CLI 只做薄調度，重用 `run_crawler_seed_download_import()`，從本機 catalog seed 建立 resolved seed plan、走既有 download/import pipeline，並把結果放進 `seed_download_import` JSON payload；這讓 Web/Tk 已有的「下載此 seed」正式路徑也可被 agent-readable CLI 驗收。已驗證：不寫 pyc 的 in-memory syntax compile OK；CLI help 可看到新參數；docs / source mojibake scan 0 hits；`git diff --check` OK（僅 CRLF/LF warning）；`py -3 -B -m unittest tests.test_crawler_seed_registry tests.test_handoff tests.test_project_maturity -v` 44 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，823 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_085742.log`。GitHub Actions run `26548277425` 已通過 Ubuntu、Windows 與 real DB smoke。初次 `py_compile` 在 K/RaiDrive `__pycache__` 遇到 `WinError 5`，改用不寫 pyc compile 檢查。 |
| 07:55 | Project Maturity / Delivery Scope | **PUSHED / CI PASS** | `aff46b9` | `26546301932` | Add project maturity matrix | 將「整體進度不能再用單一百分比」落成後端與文件契約：新增 `api_launcher/project_maturity.py`，CLI 新增 `--project-maturity-json`、`--write-project-maturity-json`、`--project-maturity-markdown`，輸出 `canonical_delivery_scope`、成熟度等級、矩陣 rows 與 answer template。新增 `docs/PROJECT_MATURITY_MATRIX.zh-TW.md`，定義 `deliverable_100`、`implemented_bounded`、`partial_bounded`、`contract_only`、`planned_not_started`、`hardening_needed`，並更新 GTD / DOCS_INDEX / DOCS_DRIFT_AUDIT，固定之後回答整體進度時使用成熟度矩陣。驗證：`py -3 -B -m py_compile api_launcher\project_maturity.py api_launcher\core.py api_launcher\cli_flags.py tests\test_project_maturity.py` OK；`py -3 -B -m unittest tests.test_project_maturity tests.test_handoff -v` 24 tests OK；實跑 `--project-maturity-json` 寫出 `state\reports\project_maturity_smoke.json`，確認 `canonical_delivery_scope.closure_percent=100` 且 rows 包含 `contract_only` / `partial_bounded`；docs mojibake scan OK；`git diff --check` 僅 CRLF/LF warning；`.\scripts\pre_push_smoke_brief.cmd` 通過，818 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_080033.log`；GitHub Actions run `26546301932` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 06:25 | MVP Demo Closure / Observability Hardening | **PUSHED / CI PASS** | `bae2f19` | `26544580172` | Add MVP readiness closure report | 將 canonical MVP demo 小閉環從 handoff 子欄位提升成獨立 CLI artifact：新增 `api_launcher/mvp_readiness.py`、`--mvp-readiness-json` 與 `--write-mvp-readiness-json`，輸出 `closure_id=canonical_mvp_demo_closure`、`closure_percent=100`、verified steps、rerun commands，並明確標示這不是全產品成熟度。實跑 smoke：`download_import_completed`、`row_count=3`；實跑 readiness：`status=ready_for_mvp_demo`、`blockers=[]`、`warnings=[]`。同輪修補 `event_log.latest_events()` 大型 JSONL 整份讀取、`log_event()` / renderer profile / startup checks / integration profile parsing 的 `platform.*` probe 卡住風險，改為尾端 bounded 讀取與非阻塞平台推斷；`scripts/pre_push_smoke.ps1` 也補 K/RaiDrive upstream 探測 guard。驗證：`py -3 -B -m unittest tests.test_environment tests.test_cli_renderer_contracts tests.test_event_log tests.test_handoff tests.test_mvp_demo_flow -v` 35 tests OK；py_compile modified entrypoints/modules OK；docs mojibake scan OK；`git diff --check` 僅 CRLF/LF warning；`.\scripts\pre_push_smoke_brief.cmd` 通過，815 tests / 4 skipped，MVP smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_071419.log`；GitHub Actions run `26544580172` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 05:52 | Docs / Source-code Maturity Audit | **PUSHED / CI PASS** | `f753e94` | `26541108253` | Clarify source capability maturity | 針對 `dataset_adapters.py`、`api_launcher/adapters/`、`simulation_bridge.py`、`unreal_bridge.py` 做源碼成熟度補洞審計：確認 simulation bridge 是 `contract_only`、Unreal bridge 是 `planned` target mapping、provider-specific dataset adapter 目前為 GEBCO / HYG / yfinance 三條；同時明確區分 source crawler handler、adapter resolver、content parser/importer 與 renderer bridge，不再把「14 個 supported source type」寫成「14 個來源都已完成 deep adapter / curated import / renderer bridge」。已更新 `DOCS_DRIFT_AUDIT`、`DATASET_DISCOVERY_NOTES`、`PROJECT_GTD` 與 handoff；docs mojibake scan OK，`git diff --check` OK（僅 CRLF/LF warning）；GitHub Actions run `26541108253` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 05:43 | Developer Diagnostics / UX | **PUSHED / CI PASS** | `eaae796` | `26540644756` | Add diagnostics next action label | 將 developer-only crawler handler smoke diagnostics payload 補上 `next_action_label`：`run_dataset_discovery_handler_smoke_json_if_summary_fails` 會顯示為「摘要失敗時，執行 handler smoke JSON 診斷」，Tk diagnostics message 優先使用 label，不再把 raw action id 當主文案。這輪不改 handler smoke、crawler audit 或 diagnostics 執行邏輯。已完成本地驗證：in-memory Python syntax check OK；`py -3 -B -m unittest tests.test_developer_diagnostics tests.test_tk_dialogs tests.test_web_preview -v` 103 tests OK；docs mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，812 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_054131.log`；GitHub Actions run `26540644756` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 05:34 | Tk / MVP Smoke UX | **PUSHED / CI PASS** | `c044d3e` | `26540081387` | Use labels for Tk MVP smoke next action | 將 Tk MVP Demo Smoke 失敗摘要接到共用 `next_action_display_label()`：`inspect_manifest` 會顯示為「檢查 manifest 與最近事件紀錄」，不再把 raw machine action 顯示給使用者；CLI JSON / agent payload 仍保留穩定 `next_action`。已完成本地驗證：in-memory Python syntax check OK；`py -3 -B -m unittest tests.test_launcher_ui -v` 30 tests OK；`py -3 -B -m unittest tests.test_tk_ui_helpers tests.test_launcher_ui -v` 35 tests OK；docs mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，812 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_052930.log`；GitHub Actions run `26540081387` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 05:16 | Web / Crawler Asset UX | **PUSHED / CI PASS** | `4ffc618` | `26539515284` | Add labels to Web plan next actions | 將 Web Preview 非缺憑證 listing 與 plan-preview response 也補上 top-level `next_action_label`：seed 枚舉 mission、dry-run 界域 payload、execute 建計畫結果與 plan form state 會優先顯示人類可讀下一步，不再退回 raw `next_action`。這輪不改 crawler、download、import 或 adapter review 行為。已完成本地驗證：in-memory Python syntax check OK；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview -v` 38 tests OK；docs mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，812 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_051719.log`；GitHub Actions run `26539515284` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 05:04 | Tk / Crawler Asset UX | **PUSHED / CI PASS** | `27a2072` | `26538937815` | Use labels for blocked Tk listing action | 將 Tk 爬蟲資產 listing blocked status bar 訊息接到共用 `next_action_display_label()`：新增 `crawler_asset_listing_blocked_status_text()`，讓 `enable_before_crawl` 顯示為「先啟用爬蟲資產，再枚舉 seed」，不再把 machine action id 丟給使用者。這輪不改 crawler、repository、download 或 import 行為。已完成本地驗證：in-memory Python syntax check OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 63 tests OK；docs mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，812 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_050459.log`；GitHub Actions run `26538937815` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 04:50 | Web / Crawler Asset UX | **PUSHED / CI PASS** | `b0a182f` | `26538299583` | Add labels to crawler asset next actions | 將 Web crawler asset card、Crawler Passport、下載器清單與缺憑證 blocked payload 接到共用 `next_action_label` contract：`api_launcher.crawler_asset_display.next_action_display_label()` 集中將 machine `next_action` 轉成人類可讀 label；Web API 在 asset card、credential-blocked listing、plan preview、asset/seed download-import 與 blocked plan passport 回傳 label；`frontends\web\static\app.js` 的 hero、passport、下載器 row 與搜尋優先使用 label。已完成本地驗證：in-memory Python syntax check OK；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview -v` 38 tests OK；docs mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，811 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_045134.log`。第一次完整 Web test 遇到一次 Windows ephemeral port `WinError 10013`，重跑單條與完整 suite 通過，判定為環境性 port flake。GitHub Actions run `26538299583` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 04:30 | Web/Tk / Download Import UX | **PUSHED / CI PASS** | `b504544` | `26537337144` | Add labels to download import next actions | 將 formal crawler asset / seed 下載匯入結果接到 UI-neutral `next_action_label` contract：`CrawlerAssetDownloadImportResult.to_dict()` 會把 machine `next_action` 轉成人類可讀 label，Tk seed 完成/未完成提示、Web formal download/import endpoint、Web 下載器 mission 與結果列都優先顯示 label，不再把 `run_adapter_review_or_resolve_adapter_plan_before_downloading` 顯示給使用者。已完成本地驗證：in-memory Python syntax check OK；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_crawler_asset_download tests.test_tk_dialogs tests.test_web_preview -v` 103 tests OK；docs mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，811 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_043200.log`；GitHub Actions run `26537337144` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 04:09 | Tk / Crawler Asset UX | **PUSHED / CI PASS** | `ff0f283` | `26536339890` | Use labels for blocked Tk plan next action | 將 Tk「爬蟲資產無法建立下載計畫」警告接到 `crawler_asset_plan_outcome_payload().next_action_label`，blocked path 不再把 `enable_before_building_download_plan` 這類 machine `next_action` 當使用者文案。新增 regression：blocked summary 要顯示「先啟用爬蟲資產」，且 raw machine action 不得出現。已驗證：in-memory Python syntax check OK；`py -3 -B -m unittest tests.test_tk_dialogs -v` 61 tests OK；docs mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，809 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_041048.log`；GitHub Actions run `26536339890` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 03:53 | Tk / Source Draft UX | **PUSHED / CI PASS** | `032dc53` | `26535420927` | Add source draft next-action labels | 將 source pattern draft 的 next-action 也補成 UI-neutral display label：`api_launcher/source_pattern_drafts.py` 在成功與 blocked review payload 中輸出 `next_action_label`、`next_action_label_zh_TW`、`next_action_label_en`；Tk source draft 成功與保留審核訊息優先顯示人類下一步，不再把 `run_local_discovery_audit_before_catalog_promotion` 或 `review_source_profile_or_add_detector` 當主文案。成功路徑仍保留 audit command，但改列為「可重跑命令 / Command」。已驗證：in-memory Python syntax check OK；`py -3 -B -m unittest tests.test_source_pattern_drafts tests.test_tk_dialogs -v` 74 tests OK；`git diff --check` OK；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，808 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_035402.log`；GitHub Actions run `26535420927` 已通過 Ubuntu、Windows 與 real DB smoke。 |
| 03:36 | Tk / Credential UX | **PUSHED / CI PASS** | `6403836` | `26534494911` | Use credential next-action labels in Tk | 將 credential next-action label 的 UI-neutral contract 回補到 Tk：`crawler_asset_credential_summary_text()` 與 `crawler_asset_credential_guard_message()` 現在會優先讀 `display_profile.next_action_label_zh_TW/en` 或 top-level `next_action_label_zh_TW/en`，讓 Tk 缺憑證提示顯示「先完成登入設定，再下載資料」，不再把 `edit_local_credentials_before_live_download` 當作主要使用者文案。新增 regression 鎖住 summary fallback 與 guard message 都不把 raw machine next_action 顯示給使用者。已驗證：in-memory Python syntax check OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_local_credentials -v` 61 tests OK；`git diff --check` OK；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，807 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_033736.log`；GitHub Actions run `26534494911` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 03:29 | Web / Credential UX | **PUSHED / CI PASS** | `26f7d84` | `26533611438` | Add credential next-action labels | 延續 credential display profile，將 credential `next_action` 機器碼補成人類可讀 label：`CredentialDisplayProfile` 現在輸出 `next_action_label`、`next_action_label_zh_TW`、`next_action_label_en`，summary 改用「先完成登入設定，再下載資料」這類文案；raw `next_action` 仍保留給 agent / JSON。Web Preview 的 credential badge、credential panel 與 blocked guard banner 已改成優先讀 `display_profile` / `display_badge_label` / `display_summary_zh_TW`，不再把 `edit_local_credentials_before_live_download` 直接露給使用者。已驗證：不寫 pyc 的 in-memory Python syntax check OK；`node --check frontends\web\static\app.js` OK；`py -B -m unittest tests.test_local_credentials tests.test_web_preview tests.test_tk_dialogs -v` 98 tests OK；`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，806 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_031946.log`；GitHub Actions run `26533611438` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 03:11 | Architecture / Credential Display Profile | **PUSHED / CI PASS** | `71ee4c8` | `26532840227` | Add credential display profile | 將 credential 顯示文案從 Tk helper 收斂到後端 UI-neutral contract：`api_launcher.local_credentials.CredentialDisplayProfile` / `credential_display_profile()` 現在會輸出 `label`、`tone`、`badge_label`、繁中/英文 summary 與 `next_action`；`crawler_asset_credential_status()` 同步帶出 `display_profile`、`display_badge_label`、`display_summary_zh_TW`、`display_summary_en`。Tk helper 只優先呈現後端 payload，舊欄位保留相容 fallback；Web detail payload 測試也鎖住 `display_profile`。已驗證：`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；targeted syntax check 使用不寫 pyc 的 in-memory `compile()` OK（K 槽雲端 `__pycache__` 曾出現 WinError 5）；`py -B -m unittest tests.test_local_credentials tests.test_tk_dialogs tests.test_web_preview -v` 98 tests OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，806 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_030413.log`；GitHub Actions run `26532840227` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 02:41 | Tk / Credential Status | **PUSHED / CI PASS** | `79a4d8d` | `26531730322` | Surface Tk credential status badge | Tk crawler asset 表格新增「登入」欄，右側 Crawler Passport 也會顯示後端 `crawler_asset_credential_status()` 的登入 label、設定數、缺少欄位與 next action；儲存登入設定後只刷新目前 asset row / Passport，不重載整張表。新增 `crawler_asset_credential_badge_label()` 與 `crawler_asset_credential_summary_text()`，Tk 只轉譯後端 UI-safe payload，不複製 credential blocking policy，也不顯示或記錄明文 secret。已驗證：`py -B -m py_compile frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；`py -B -m unittest tests.test_tk_dialogs -v` 58 tests OK；`py -B -m unittest tests.test_local_credentials tests.test_tk_dialogs tests.test_launcher_ui -v` 89 tests OK；`git diff --check` OK；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，805 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_024335.log`；GitHub Actions run `26531730322` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 02:22 | Tk / Credential Editor | **PUSHED / CI PASS** | `b1e4380` | `26530735262` | Add Tk crawler credential editor | 新增 Tk「登入設定 / 記住我的帳號」dialog：`frontends/tk/crawler_asset_credential_dialog.py` 只讀後端 `crawler_asset_credential_status()` 的 UI-safe 欄位，提供官方登入 / 申請 API Key 入口、貼上 API Key/Token、清除已保存值與「記住我的帳號」勾選；不預填或回傳已保存明文 secret。`frontends/tk/crawler_asset_workflows.py` 右側 Crawler Passport 新增登入設定入口；缺憑證時 seed 下載 guard 會先開本機 credential editor，保存仍交給 `api_launcher.local_credentials.update_crawler_asset_credentials()`。structured event 只記 status、counts、field names 與 next action，不記明文 token 或 masked preview。已驗證：`py -B -m py_compile frontends\tk\crawler_asset_credential_dialog.py frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；`py -B -m unittest tests.test_tk_dialogs -v` 57 tests OK；`py -B -m unittest tests.test_local_credentials tests.test_tk_dialogs tests.test_web_preview -v` 96 tests OK；`py -B -m unittest tests.test_launcher_ui tests.test_tk_dialogs -v` 87 tests OK；`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，804 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_022421.log`；GitHub Actions run `26530735262` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 02:03 | Tk / Credential Guard | **PUSHED / CI PASS** | `2ef082d` | `26529714810` | Add Tk seed credential guard | 將 Web Preview 已有的 credential-blocking 判斷抽回 `api_launcher.local_credentials.credential_status_blocks_download()`，讓 Web/Tk/未來 Qt 共用 missing/partial/profile-required guard；Tk seed 下載路徑現在會在啟動背景 worker 前讀 `crawler_asset_credential_status()`，缺登入 / API Key 時先顯示「需要登入 / API Key」、缺少欄位、next action 與官方入口，不送出必然失敗的 live request。已驗證：`py -B -m py_compile api_launcher\local_credentials.py frontends\web\preview_api.py frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py tests\test_web_preview.py` OK；`py -B -m unittest tests.test_tk_dialogs -v` 54 tests OK；`py -B -m unittest tests.test_web_preview -v` 38 tests OK；`py -B -m unittest tests.test_local_credentials tests.test_tk_dialogs tests.test_web_preview -v` 93 tests OK；`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，801 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_020439.log`；GitHub Actions run `26529714810` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 01:44 | Tk / Seed Import Path | **PUSHED / CI PASS** | `df10e7d` | `26528721624` | Surface seed import path in Tk | 將 Web 已有的 seed import profile 顯示同步到 Tk：`CrawlerAssetSeedDialog` 表格新增「匯入路徑」欄，讀取後端 seed row 的 `content_display_label` / `content_import_profile.display_label` / `content_pipeline_lane`；右側 Crawler Passport 的 seed preview 也顯示同一個 import label。這是顯示層 contract 對齊，不改 crawler、resolver、downloader 或 importer 執行行為。已驗證：`py -B -m py_compile frontends\tk\crawler_asset_seed_dialog.py frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；`py -B -m unittest tests.test_tk_dialogs -v` 52 tests OK；`py -B -m unittest tests.test_crawler_seed_registry tests.test_web_preview tests.test_tk_dialogs -v` 105 tests OK；`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，799 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_014549.log`；GitHub Actions run `26528721624` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 01:27 | Web / Seed Import Badge | **PUSHED / CI PASS** | `e9b39ab` | `26527820441` | Surface seed import profile badges | 將 `ContentImportProfile` 再前移到 seed 分頁 payload：`api_launcher.crawler_seed_registry.crawler_seed_row()` 現在會輸出 `content_import_profile`、`content_importability`、`content_pipeline_lane`、`content_next_action`、display label/tone 與 `content_review_required`；若 candidate metadata 已有 profile 會優先使用，否則依 `native_format`，再退到 URL suffix 弱偵測。Web seed row 會顯示後端給的匯入路徑 badge，讓使用者在按「下載此 seed」前先看到可匯入 SQLite、需解壓轉換、內容 Parser 待辦或 Adapter review。已驗證：`py -B -m py_compile api_launcher\crawler_seed_registry.py tests\test_crawler_seed_registry.py tests\test_web_preview.py` OK；`node --check frontends\web\static\app.js` OK；`py -B -m unittest tests.test_crawler_seed_registry tests.test_web_preview -v` 53 tests OK；`py -B -m unittest tests.test_crawler_asset_download tests.test_dataset_download_plan tests.test_adapter_plan_resolver tests.test_content_registry -v` 86 tests OK；`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，799 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_012837.log`；GitHub Actions run `26527820441` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 01:08 | Web/Tk / Content Import Display | **PUSHED / CI PASS** | `53307c2` | `26526948071` | Surface content import profile in UI | 延續 `ContentImportProfile` 切片，將 `content_import_profile` 接進 Adapter review 與 Web/Tk 顯示層：`adapter_review_agent_payload()` 現在會帶出 item-level `content_import_profile`、`content_pipeline_lane`、`content_next_action` 與 summary 的 `by_content_pipeline_lane` / `by_content_importability`；`adapter_review_display_payload()` 會輸出 `content_pipeline_lanes`，Web Review Workspace 顯示 Import Lane，Tk Adapter detail 也顯示 pipeline lane / next action。`plan_entry_content_status_payload()` 會優先讀 `content_import_profile` 的 label/tone/next_action，避免前端各自從 raw `manual_review_required` 推斷。此切片不改 downloader/importer 執行行為。已驗證：`py -B -m py_compile api_launcher\adapter_review.py api_launcher\crawler_asset_display.py frontends\tk\dialogs.py tests\test_web_preview.py tests\test_tk_dialogs.py` OK；`node --check frontends\web\static\app.js` OK；`py -B -m unittest tests.test_web_preview -v` 38 tests OK；`py -B -m unittest tests.test_tk_dialogs tests.test_dataset_download_plan tests.test_adapter_plan_resolver tests.test_content_registry -v` 136 tests OK；`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，797 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_011007.log`；GitHub Actions run `26526948071` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 00:36 | Architecture / Content Import Profile | **PUSHED / CI PASS** | `dd360dc` | `26525140539` | Add content import profile contract | 依宣告式架構第二階段收斂方向，新增 `api_launcher.content_registry.ContentImportProfile` 與 `content_import_profile()`，將下載物格式的 import 能力集中成 UI-neutral profile：CSV/JSON/GeoJSON 走 `sqlite_curated_import` 並標示可匯入 SQLite；ZIP/壓縮檔走 `downloaded_payload_transform`；NetCDF/HDF/Zarr/GRIB、GeoTIFF/COG/GeoPackage/tiles、Parquet/Arrow、SQLite snapshot、PDF/XML/HTML/txt 與 unknown 走 content parser / adapter review。`ContentParserCapability.to_dict()`、`ContentDetection.to_dict()` 與 dataset `import_plan` 現在會輸出 `import_profile` / `content_import_profile`，讓 Tk/Web/未來 Qt 不必各自猜內容格式是否可匯入。這輪不改 downloader/importer 執行行為，只增加可序列化 contract。已驗證：`py -B -m py_compile api_launcher\content_registry.py api_launcher\plans.py tests\test_content_registry.py` OK；`py -B -m unittest tests.test_content_registry -v` 12 tests OK；`py -B -m unittest tests.test_adapter_plan_resolver -v` 54 tests OK；`py -B -m unittest tests.test_web_preview tests.test_crawler_asset_download tests.test_crawler_assets -v` 81 tests OK；`git diff --check` OK（僅 docs CRLF/LF warning）；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，796 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_003747.log`；GitHub Actions run `26525140539` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 00:17 | Maintainability / Flow Comments | **PUSHED / CI PASS** | `1a85a3f` | `26524137769` | Add crawler asset flow comments | 依使用者要求繼續提高現有檔案註釋密度，做第二批不改行為的可讀性切片：在 `api_launcher/crawler_asset_service.py`、`frontends/tk/crawler_asset_workflows.py` 與 `frontends/web/static/app.js` 補上模組 docstring、流程邊界註解與 service/UI/Web adapter 的責任說明。這輪只說明 crawler asset listing、seed download/import、source download options/bounds、Tk workflow adapter 與 Web Preview state/render/action flow，不改函式簽名、不改 UI 行為、不改 crawler/download/import 邏輯。已驗證：`py -B -m py_compile api_launcher\crawler_asset_service.py frontends\tk\crawler_asset_workflows.py` OK；`node --check frontends\web\static\app.js` OK；`py -B -m unittest tests.test_crawler_assets` 42 tests OK；`py -B -m unittest tests.test_crawler_asset_download` 2 tests OK；`py -B -m unittest tests.test_crawler_seed_registry` 13 tests OK；`py -B -m unittest tests.test_tk_dialogs tests.test_launcher_ui` 82 tests OK；`py -B -m unittest tests.test_web_preview` 37 tests OK；`git diff --check` OK（僅 `frontends/web/static/app.js` CRLF/LF warning）；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，795 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260528_001723.log`；GitHub Actions run `26524137769` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |

### 2026-05-27


| 時間 | 階段 | 狀態 | SHA | Run | Commit | 變更與驗證 |
| --- | --- | --- | --- | --- | --- | --- |
| 23:41 | Maintainability / Architecture Comments | **PUSHED / CI PASS** | `5155323` | `26522281618` | Add crawler asset architecture comments | 依使用者要求提高可讀性，先做不改行為的註釋 pass：在 `crawler_asset_bound_forms.py`、`crawler_capability_profiles.py`、`crawler_asset_display.py`、`crawler_asset_download.py`、`crawler_seed_registry.py` 與 `frontends/web/preview_api.py` 補上模組 docstring、架構邊界說明、profile / display / download / seed paging 的責任註解。這輪只說明現有資料流與邊界，不改函式簽名、不改 UI 行為、不改 crawler/download/import 邏輯。已驗證：`py -B -m py_compile` 目標檔案 OK；`py -B -m unittest tests.test_crawler_assets` 42 tests OK；`py -B -m unittest tests.test_web_preview` 37 tests OK；`py -B -m unittest tests.test_tk_dialogs` 52 tests OK；`git diff --check` OK；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，795 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260527_234318.log`；GitHub Actions run `26522281618` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 22:55 | Architecture / Bounds Form Profile | **PUSHED / CI PASS** | `8933534` | `26519665320` | Add crawler bounds form profile | 依宣告式架構方向，將 crawler asset bounds form 的表單狀態收斂成 typed `CrawlerAssetBoundFormProfile` 與 `crawler_asset_bound_form_profile()`。`CrawlerAssetBoundFormSpec.to_dict()` 現在會輸出 `form_profile`，包含欄位數、facet、groups、controls、schema probe 欄位、preset ids、recommended value keys、warning codes 與 next action；既有 `fields` / `presets` / `recommended_values` payload 保持相容。已驗證：`py -B -m py_compile api_launcher\crawler_asset_bound_forms.py api_launcher\crawler_asset_display.py tests\test_crawler_assets.py tests\test_web_preview.py` OK；`py -B -m unittest tests.test_crawler_assets -v` 42 tests OK；`py -B -m unittest tests.test_web_preview -v` 37 tests OK；`py -B -m unittest tests.test_crawler_assets tests.test_web_preview tests.test_tk_dialogs -v` 131 tests OK；`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，795 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260527_225749.log`；GitHub Actions run `26519665320` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 22:44 | Docs / Drift Guard | **PUSHED / CI PASS** | `f251d8c` | `26518617851` | Align web demo cleanup docs | 依 Documentation Drift Guard 複查 Web demo route cleanup 後，修正 `PROJECT_GTD.md` 與 `DOCS_DRIFT_AUDIT.zh-TW.md` 中仍把舊 `執行真下載示範` 寫成一般使用者主流程的敘述。現況對齊為：Web 下載器主 CTA 是正式 `下載 / 匯入目前資產`，舊 public CSV helper 只保留在 developer diagnostics `POST /api/diagnostics/real-download-demo`，舊 `/api/demo/real-download` 不再是一般 API。已驗證：`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；GitHub Actions run `26518617851` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 22:32 | Architecture / UI Display Profile | **PUSHED / CI PASS** | `246d333` | `26517816388` | Add plan outcome display profile | 依宣告式架構方向，將 plan outcome 的 UI 顯示狀態收斂成 typed `DisplayProfile` 與 `plan_outcome_display_profile()`。`crawler_asset_plan_outcome_payload()` 現在會輸出 `display_profile`，同時保留既有 `display_label` / `display_tone` / `short_label` / `summary` 欄位，讓 Web/Tk/未來 Qt 可逐步改讀同一份後端 display contract，不必各自用 `outcome_bucket` 推文案。這是 UI 狀態 contract 切片，不改 crawler、download 或 import 行為。已驗證：`py -B -m py_compile api_launcher\crawler_asset_display.py tests\test_web_preview.py` OK；`py -B -m unittest tests.test_web_preview -v` 37 tests OK；`py -B -m unittest tests.test_tk_dialogs -v` 52 tests OK；`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；GitHub Actions run `26517816388` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 22:00 | Architecture / Crawler Capability Profile | **PUSHED / CI PASS** | `d125a3d` | `26517164906` | Add crawler capability profile contract | 依使用者要求往宣告式架構推進，但不重寫 crawler handler。新增 `api_launcher/crawler_capability_profiles.py`，以 `CrawlerCapabilityProfile` 將 source type、auth mode、terms risk、pagination mode、content format hints、bounds facets、middleware ids、failure policy 與 effective `SourceRequestPolicy` 包成可序列化 profile，並掛到 `CrawlerAsset.capability_profile` / `asset.to_dict()["capability_profile"]`。這是 `Matrix Cell -> Validated Profile -> Capability Gateway -> Middleware Pipeline` 的資料契約切片，不是 universal interpreter。本輪也修補 `scripts/pre_push_smoke.ps1`，讓 `git diff --check`、`py_compile`、`unittest discover`、CLI summary 後都檢查 `$LASTEXITCODE`，避免 native command 失敗後腳本繼續跑造成假綠燈。已驗證：`py -B -m py_compile api_launcher\crawler_capability_profiles.py api_launcher\crawler_assets.py api_launcher\crawlers\request_policy.py tests\test_crawler_assets.py` OK；`py -B -m unittest tests.test_crawler_assets -v` 40 tests OK；`py -B -m unittest tests.test_dataset_discovery -v` 50 tests OK；`py -B -m unittest tests.test_crawler_assets tests.test_dataset_discovery tests.test_web_preview -v` 126 tests OK；`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，792 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260527_221311.log`；GitHub Actions run `26517164906` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 21:38 | Tk / Seed Row Actions | **PUSHED / CI PASS** | `d0b42f8` | `26515224421` | Add Tk crawler seed row actions | Tk「爬蟲資產」分頁右側 Crawler Passport 新增「開 Seed 表格 / 下載」，開啟可操作 seed row dialog；dialog 只回傳 action payload，收藏交給 `save_crawler_seed_favorite()`，下載 / 匯入交給正式 `run_crawler_seed_download_import()` service。Tk seed 下載輸出預設落在 OS Downloads 的 `RuRuKa Asset Launcher/downloads/crawler_assets/<asset>/<seed>`，避免 K 槽 live SQLite import lock。已驗證：read/compile（不寫 pyc）OK；`py -B -m unittest tests.test_tk_dialogs -v` 52 tests OK；`py -B -m unittest tests.test_tk_dialogs tests.test_crawler_seed_registry tests.test_crawler_asset_download tests.test_web_preview -v` 103 tests OK；`git diff --check` OK（僅 CRLF/LF warning）；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，792 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260527_214126.log`；GitHub Actions run `26515224421` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 21:05 | Tk/Web / Seed Enumeration UX | **PUSHED / CI PASS** | `441517f` | `26513855548` | Surface seed enumeration confidence in UI | Tk「爬蟲資產」分頁現在會從 `crawler_asset_listing_recorded` structured event 恢復最近一次 `seed_enumeration` / `remote_pagination`，並在右側 Seed 清單摘要顯示「遠端仍有下一頁線索 / 遠端已列完 / handler 尚未回報遠端完整度」等提示，不暴露 raw pagination token。Web Preview 的 recent listing compact payload 也保留 `seed_enumeration` 與 `remote_pagination`，避免頁面重載後 seed bar 退回純 counts。已驗證：read/compile（不寫 pyc）OK；`py -B -m unittest tests.test_tk_dialogs -v` 48 tests OK；`py -B -m unittest tests.test_tk_dialogs tests.test_web_preview tests.test_crawler_assets tests.test_crawler_seed_registry -v` 137 tests OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 通過，788 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260527_211759.log`；GitHub Actions run `26513855548` 的 Ubuntu、Windows 與 real DB smoke 全部 success。`py_compile` 因 K 槽 `frontends\tk\__pycache__` 已知雲端碟 cache lock 回 `WinError 5`，本輪以 read/compile 取代。 |
| 20:54 | Tk / Seed Registry UX | **PUSHED / CI PASS** | `d582c63` | `26512757129` | Add Tk crawler seed page preview | Tk「爬蟲資產」分頁右側 Crawler Passport 新增「Seed 清單」區塊與「查看 Seed 清單 / 顯示更多 Seed」動作；Tk 重用 `crawler_seed_page()` 讀本機 catalog 已枚舉 seed，不重新 live crawl，也不自行重算 page summary。側欄會顯示第幾筆到第幾筆、總數、剩餘筆數、收藏星號與下一頁提示。已驗證：`py -B -m py_compile frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；`py -B -m unittest tests.test_tk_dialogs -v` 45 tests OK；`py -B -m unittest tests.test_crawler_seed_registry tests.test_launcher_ui -v` 43 tests OK；`py -B -m unittest tests.test_tk_dialogs tests.test_crawler_seed_registry tests.test_launcher_ui tests.test_crawler_assets -v` 128 tests OK。.\scripts\pre_push_smoke_brief.cmd 通過，784 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`，log：`state\logs\pre_push_smoke_20260527_205728.log`；GitHub Actions run `26512757129` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 20:28 | Discovery / Seed Registry CLI | **PUSHED / CI PASS** | `407525e` | `26511610643` | Add crawler asset seed page CLI | 將 Web 已有的 seed 分頁 contract 暴露成 CLI JSON：新增 `--crawler-asset-seeds ASSET_ID --crawler-asset-seeds-json`，並支援 `--crawler-asset-seed-page`、`--crawler-asset-seed-page-size`、`--crawler-asset-seeds-provider-id` 與 profile path。CLI 重用 `crawler_seed_page()`，只讀本機 catalog 已枚舉 seed，不重新 live crawl。已驗證：py_compile OK；`tests.test_crawler_seed_registry` 13 tests OK；`tests.test_crawler_seed_registry tests.test_crawler_assets tests.test_web_preview` 88 tests OK；實際 CLI smoke 對 `noaa_ncei_dataset_search` 回傳 JSON 並解析 provider id 為 `noaa_ncei_access_data`。`.\scripts\pre_push_smoke_brief.cmd` 通過，781 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26511610643` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 20:02 | Discovery / Remote Pagination Metadata | **PUSHED / CI PASS** | `e070beb` | `26510356684` | Expand remaining crawler pagination metadata | 將 `DatasetCrawlerOutput` 遠端 pagination metadata 繼續延伸到 NCEI、GBIF、Dataverse、OGC API Records、STAC full-crawl handler。NCEI / GBIF / Dataverse 在本機 page cap 先截斷時回報下一個 offset/start token；OGC / STAC 在 next link 尚存在但本機 page cap 停止時回報 token-present，讓 seed 枚舉 UI 不再只靠本機 `max_results` 猜完整度。已驗證：讀檔 compile（不寫 pyc）OK；`py -B -m unittest tests.test_dataset_discovery` 50 tests OK；`py -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets tests.test_crawler_seed_registry tests.test_web_preview -v` 134 tests OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 777 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26510356684` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 19:36 | Discovery / Remote Pagination Metadata | **PUSHED / CI PASS** | `193f244` | `26509127789` | Expand crawler remote pagination metadata | 將 `DatasetCrawlerOutput` 遠端 pagination metadata 從 Socrata / CKAN 延伸到 OpenAlex、DataCite、Zenodo、CMR full-crawl handler。OpenAlex 讀 `meta.next_cursor`，DataCite / Zenodo 讀 `links.next`，CMR 在頁面滿版且本機 page cap 先截斷時回報下一頁 page number；UI 後續可用 `remote_pagination_status=has_more` 判斷「仍有更多 seed」，不用只靠本機上限猜測完整度。已驗證：`py -B -m unittest tests.test_dataset_discovery` 45 tests OK；`py -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets tests.test_crawler_seed_registry tests.test_web_preview` 129 tests OK；相關 crawler 檔案 py_compile OK；docs mojibake scan OK；`git diff --check` OK；`.\scripts\pre_push_smoke_brief.cmd` 772 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26509127789` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 18:52 | Web Preview / Demo Route Cleanup | **PUSHED / CI PASS** | `4796f4b` | `26508314088` | Move web real download demo to diagnostics | 舊 Web real-download demo 從一般路由 `/api/demo/real-download` 移到 `POST /api/diagnostics/real-download-demo`；舊路由現在回 404，新 payload 明確帶 `developer_only=true`、`scope=developer_diagnostic_public_csv_not_main_download_flow`，並指向正式主流程 `POST /api/crawler-assets/{asset_id}/download-import` 與 seed 流程 `POST /api/crawler-assets/{asset_id}/seed-download-import`。這完成 GTD 中「真下載示範退到 developer/demo-only」的 cleanup。已驗證：`py -B -m unittest tests.test_web_preview tests.test_crawler_asset_download tests.test_crawler_assets tests.test_crawler_seed_registry` 86 tests OK；`py -B -m py_compile frontends\web\preview_api.py frontends\web\server.py tests\test_web_preview.py api_launcher\crawler_asset_download.py` OK；`node --check frontends\web\static\app.js` OK；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd` 768 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26508314088` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 17:44 | Web Preview / Seed Download Import | **PUSHED / CI PASS** | `7dd4570` | `26504382256` | Add seed-level download import path | Web seed 清單補上正式「下載此 seed」動作，送到新的 `POST /api/crawler-assets/{asset_id}/seed-download-import`；後端新增 `build_crawler_seed_download_plan()` 與 `run_crawler_seed_download_import()`，會先驗證 seed 屬於該 crawler asset，再從 catalog seed 建立正式 resolved plan 並走既有 download/import pipeline，不重新 live crawl 來源。這補齊「選入口 -> 枚舉 seed -> 針對可見 seed 送進下載 / 匯入」的 UX 閉環。已驗證：`py -B -m unittest tests.test_crawler_asset_download tests.test_web_preview` 35 tests OK；`py -B -m unittest tests.test_crawler_asset_download tests.test_web_preview tests.test_crawler_assets tests.test_crawler_seed_registry` 84 tests OK；`py -B -m py_compile api_launcher\crawler_asset_service.py api_launcher\crawler_asset_download.py frontends\web\preview_api.py frontends\web\server.py` OK；`node --check frontends\web\static\app.js` OK；docs mojibake scan OK；`git diff --check` 無 whitespace error（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 766 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26504382256` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 16:47 | Web Preview / Demo CTA Guard | **WORKING / 未推送** | `local` | `local` | Guard web download CTA demotion | 補上靜態 UI regression：Web 主 UI 必須包含 `crawlerAssetDownloadButton`、`下載 / 匯入目前資產` 與 `/download-import`，且不得再包含 `realDownloadDemoButton` 或 `執行真下載示範`。這是防止舊 demo CTA 回到主要使用者流程的薄 guard；不移除 `/api/demo/real-download` regression endpoint。已驗證：`py -B -m unittest tests.test_web_preview.WebPreviewApiTest.test_static_ui_uses_rrkal_product_vocabulary -v` OK；`git diff --check` OK。GitHub Actions run `26511610643` 的 Ubuntu、Windows 與 real DB smoke 全部 success。 |
| 16:25 | Web Preview / Formal Crawler Download Import | **CHECKPOINT** | `87cfa21` | `26500493354` | Add crawler asset web download import path | 新增 `api_launcher/crawler_asset_download.py`，把 `crawler asset + bounds -> resolved plan -> direct downloads -> import` 收成正式 service；Web Preview 新增 `POST /api/crawler-assets/{asset_id}/download-import`，下載器主 CTA 從「執行真下載示範」改成「下載 / 匯入目前資產」，並顯示 Submitted / Completed / Imported 與 Downloads / Plan / SQLite artifacts。舊 `/api/demo/real-download` 暫時保留 regression / developer demo，不再是主按鈕。Live smoke 發現 K/RaiDrive 匯入 SQLite 可能遇到 `database is locked`；改用本地 temp/downloads 後成功 `download_import_completed`、`submitted=1`、`completed=1`、`imported=1`。已驗證：formal endpoint live smoke 成功；`py -B -m unittest tests.test_crawler_asset_download tests.test_web_preview -v` 33 tests OK；`py -B -m unittest tests.test_crawler_asset_download tests.test_web_preview tests.test_crawler_assets tests.test_crawler_seed_registry -v` 82 tests OK；`py -B -m unittest tests.test_dataset_discovery tests.test_source_download tests.test_ingestion_pipeline -v` 54 tests OK；docs mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd` 764 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26500493354` Ubuntu / `windows-2025-vs2026` / Real DB smoke 全部 success。 |
| 15:55 | Architecture / Declarative Profile Gateway | **WORKING / 未推送** | `local` | `local` | Document matrix profile gateway concept | 將使用者補充的中期架構概念寫入 `docs/DECLARATIVE_ARCHITECTURE_DECISION.zh-TW.md`：正式方向是 `Matrix Cell -> Validated Profile -> Capability Gateway -> Middleware Pipeline`，不是萬能 YAML、元編程黑魔法或一次性重寫所有 crawler。文件也明確列出 profile 家族、PoC 順序與成功標準。這是文檔決策切片，不改產品碼。待 docs scan、diff check 與 CI。 |
| 15:48 | Hardening / Source Request Policy | **CHECKPOINT** | `0863357` | `26498010323` | Extract source request policy helper | 將 source profile 的 timeout / max pages / page size / rate-limit / credential mode / terms risk 收斂到 `api_launcher.crawlers.request_policy.SourceRequestPolicy` 與 `source_request_policy()`；`dataset_sources.py` 先取得 effective policy，再呼叫既有 crawler handler。這是為使用者提出的「數據驅動裝飾器爬蟲」保留 typed middleware 接點，不是第一階段重寫 crawler。驗證：`py -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets -v`，81 tests OK；`py -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets tests.test_web_preview -v`，112 tests OK；`py -B -m py_compile api_launcher\crawlers\request_policy.py api_launcher\crawlers\dataset_sources.py tests\test_dataset_discovery.py` OK；docs mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd`，762 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26498010323` Ubuntu / `windows-2025-vs2026` / Real DB smoke 全部 success。 |
| 15:27 | Hardening / Source Profile Access Policy | **CHECKPOINT** | `217231a` | `26497125509` | Normalize source access policy values | Source profile access policy 補上白名單 normalization：`credential_mode` 只接受 `public_or_review` / `user_credential_required`，`terms_risk` 只接受 `public_or_review` / `terms_review_required`；未知字串不會寫回 source JSON，也不會漏進 Web/Tk/Qt capability contract，而是退回既有 public/review fallback heuristic。這是登入/API key 與條款風險治理欄位的防呆補強，不改 UI 操作流程。驗證：`py -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets -v`，80 tests OK；`py -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets tests.test_web_preview -v`，111 tests OK；docs mojibake scan OK；`git diff --check` OK（僅 CRLF/LF warning）；`.\scripts\pre_push_smoke_brief.cmd`，761 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26497125509` Ubuntu / `windows-2025-vs2026` / Real DB smoke 全部 success。 |
| 15:04 | Hardening / Source Profile Access Policy | **CHECKPOINT** | `e98213f` | `26496327588` | Add source profile access policy overrides | 延續 source profile 收斂，新增 `credential_mode` 與 `terms_risk` 到 `DatasetDiscoverySource` / source JSON；crawler asset capability 會先讀明示 profile 欄位，再退回既有文字 heuristic。這讓登入/API key 與條款風險屬於 crawler/source profile，而不是資料集本身或 UI 猜測。`crawler_asset_source_signature()` 也納入 access policy 欄位，讓保存過的 plan passport 在來源治理設定改變後被標成 stale。驗證：`py -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets -v`，78 tests OK；`py -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets tests.test_web_preview -v`，109 tests OK；`git diff --check` OK；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd`，759 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26496327588` Ubuntu / `windows-2025-vs2026` / Real DB smoke 全部 success。 |
| 14:54 | Hardening / Source Profile Politeness | **CHECKPOINT** | `8962dd9` | `26495740693` | Add source profile rate-limit guard | 接續 source profile politeness，新增 `crawl_rate_limit_seconds`，讓 `DatasetDiscoverySource` / source JSON 可宣告 paginated discovery request 間隔；各 paginated crawler handler 透過共用 `polite_crawl_delay()` 套用 source-level delay，不把 `time.sleep()` 分散塞進 parser。`crawler_asset_source_signature()` 也納入 timeout / max_pages / page_size / rate-limit 欄位，讓保存過的 plan passport 在來源 politeness profile 改變後會被標成 stale。驗證：`py -B -m unittest tests.test_dataset_discovery -v`，39 tests OK；`py -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets tests.test_crawler_audit_smoke tests.test_web_preview -v`，113 tests OK；`git diff --check` OK；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd`，758 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26495740693` Ubuntu / `windows-2025-vs2026` / Real DB smoke 全部 success。 |
| 14:31 | Hardening / Source Profile Politeness | **CHECKPOINT** | `8fec58f` | `26494877172` | Add source profile page-size guard | 延續 source profile politeness，新增 `crawl_page_size`，讓 `DatasetDiscoverySource` / source JSON 可限制單次請求 page size；當 Web/Tk/CLI 給出較大的 `max_results_override` 時，source profile 可以把 per-request page size 壓低，避免完整 seed 枚舉對特定入口送出過大頁面請求。驗證：`py -B -m unittest tests.test_dataset_discovery -v`，38 tests OK；`py -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets tests.test_crawler_audit_smoke -v`，81 tests OK；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd`，757 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26494877172` Ubuntu / `windows-2025-vs2026` / Real DB smoke 全部 success。 |
| 14:15 | Hardening / Source Profile Politeness | **CHECKPOINT** | `88698ad` | `26494263728` | Add source profile politeness defaults | 接續 code health audit 的 P2 hardening，讓 `DatasetDiscoverySource` / `dataset_discovery_sources*.json` 可宣告 `crawl_timeout_seconds` 與 `crawl_max_pages`。Crawler 執行時會套用 source-level timeout，並把 `crawl_max_pages` 當成來源安全上限；若執行期 `max_pages` 更低，會採更低值。這是 source profile 收斂，不是重寫 crawler。驗證：`py -B -m unittest tests.test_dataset_discovery -v`，38 tests OK；`py -B -m unittest tests.test_dataset_discovery tests.test_crawler_assets tests.test_crawler_audit_smoke -v`，81 tests OK；docs mojibake scan OK；`.\scripts\pre_push_smoke_brief.cmd`，757 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26494263728` Ubuntu / `windows-2025-vs2026` / Real DB smoke 全部 success。 |
| 13:45 | Hardening / HTML File Index Crawler | **CHECKPOINT** | `0294621` | `26493410406` | Keep HTML index candidates on partial crawl warnings | HTML file index full crawl 現在把 linked index page fetch failure 轉成 `index_page_fetch_failed` warning，並保留已找到的 file-shard candidates；`DatasetCrawlerOutput` 新增 `warnings` 欄位，orchestrator 會把 handler-level warning 併入 source audit，讓 UI/agent 可讀到 partial failure，而不是讓單頁壞掉造成整個入口 seed 枚舉失敗。驗證：targeted 79 tests OK；`scripts\pre_push_smoke_brief.cmd` 755 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26493410406` Ubuntu / `windows-2025-vs2026` / Real DB smoke 全部 success。 |
| 13:22 | Hardening / Code Health Audit | **CHECKPOINT** | `9e63f6c` | `26492936566` | Harden import fetch and credential writes | 進行程式健康審計並修補三個 P1 級風險：CSV/JSON replace import 改成先寫 SQLite 暫存表、成功後 swap target，避免失敗時刪掉既有 curated table；crawler metadata fetch 加入 8MB 預設上限，避免 metadata probe 誤讀大型 payload；本機 credential `.env` 寫入改成 UTF-8 暫存檔 + `os.replace()`，替換失敗時保留舊檔。新增 `docs/CODE_HEALTH_AUDIT.zh-TW.md`，並把使用者提出的「數據驅動裝飾器爬蟲架構」記為宣告式架構第二階段候選 PoC，不在本輪重寫現有 crawler。驗證：targeted 52 tests OK；`scripts\pre_push_smoke_brief.cmd` 754 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26492936566` Ubuntu / `windows-2025-vs2026` / Real DB smoke 全部 success。 |
| 13:01 | Docs / GUI Documentation Drift Audit | **CHECKPOINT** | `f1b5f7b` | `26492022161` | Close GUI documentation drift audit | 補完先前未完成的 GUI-level drift audit，並推送為 `f1b5f7b Close GUI documentation drift audit`；GitHub Actions run `26492022161` 的 Ubuntu、Windows 與 real DB smoke 全部 success。Web：HTTP/API smoke 與 in-app browser 實測四分頁、下載器 `執行真下載示範`、NASA CMR `需要登入 / API Key`、官方登入入口與 `記住我的帳號`；`/api/crawler-assets` 回 23 張資產卡，NCEI seed page 回 49 筆本機 seed 視窗。Tk：程式碼入口確認「爬蟲資產」第一分頁、「下載器」第二分頁、展示模式工具選單與 developer diagnostics；targeted headless tests 驗證下載器雙擊、開始/暫停主按鈕、爬蟲資產送進下載器與 dialog 文案。驗證：`node --check frontends\web\static\app.js` OK；`py -B -m unittest tests.test_web_preview tests.test_crawler_seed_registry tests.test_tk_dialogs tests.test_launcher_ui -v` 112 tests OK；`--handoff-report-json` 成功。 |
| 12:55 | Docs / Documentation Drift Audit | **CHECKPOINT** | `1e08e21` | `26491482241` | Complete documentation drift audit | 第二輪文檔漂移審計擴大到使用者文件、Web Preview 文件、MVP 稽核與架構文件，並已推送為 `1e08e21 Complete documentation drift audit`，GitHub Actions run `26491482241` success。Verified behavior：`--handoff-report-json` 成功且 canonical MVP demo `download_import_completed` / `row_count=3`；`--crawler-run-summary-json` 顯示 `missing_listing`，所以文件已補上「展示 seed 前需重新枚舉入口」；`--dataset-discovery-handler-smoke-json` 14 個 supported source type 離線 contract pass；Web Preview in-process HTTP smoke 確認 `/api/health`、`/api/crawler-assets`、`/api/diagnostics/crawler-handler-smoke`、`/api/events/recent` 可回應，crawler asset card 數 23。另補強全域 UTF-8/mojibake guard：PowerShell 預設輸出亂碼不等於檔案損壞，讀寫中文必須明確指定 UTF-8，並以 Python strict scanner 驗證 RRKAL docs 無 mojibake 風險。 |
| 12:22 | Docs / Documentation Drift Audit | **WORKING / 未推送** | `local` | `local` | Align docs with verified current state | 依使用者要求先暫停產品功能推進，進行文檔漂移審計。已驗證 `git status --short --branch` 為 `main...origin/main`、`git log -1` 為 `170b236 Log CKAN pagination output checkpoint`，且 `gh run list --limit 5` 顯示最新 GitHub Actions run `26489024004` success。發現 `AGENT_HANDOFF.zh-TW.md` 仍把 `3ca9a37` 寫成最新 HEAD，屬於明確漂移；本輪修補 `AGENT_START_HERE.zh-TW.md` 權威順序、handoff 最新 Git/CI 狀態、GTD、Docs Index，並新增 `DOCS_DRIFT_AUDIT.zh-TW.md`。這是本地文檔 checkpoint，尚未推送。 |
| 11:22 | Discovery / Crawler Pagination | **CHECKPOINT** | `3ca9a37` | `26488915209` | Add CKAN remote pagination output | CKAN full crawl 接上 `DatasetCrawlerOutput`：handler 會用 `result.count` 與 `start` 判斷遠端已列完或仍有下一頁；若因 `max_pages` 安全上限停止，會回報 `remote_pagination.status=has_more` 與後端保留的下一個 start token。這是 Socrata 之後第二個真 handler PoC。驗證：`py_compile` OK；`py -3 -B -m unittest tests.test_dataset_discovery -v` 35 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 750 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26488915209` Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 11:06 | Discovery / Crawler Pagination | **CHECKPOINT** | `cfa5367` | `26488561562` | Add crawler remote pagination output contract | 新增 `DatasetCrawlerOutput` richer handler return contract，讓支援遠端分頁的 crawler 可回報 `remote_pagination_status`、`remote_exhausted` 與後端保留的 next-page token；orchestrator 會保留 metadata，crawler asset listing 會輸出 UI-safe `remote_pagination` / `completion_confidence`。Socrata full crawl 已作第一個真 handler PoC：碰到 `max_pages` 安全上限時回報 `has_more` / `remote_has_more`，UI 只看 token-present，不取得 raw token。驗證：`py_compile` OK；`tests.test_dataset_discovery tests.test_crawler_assets` 72 tests OK；`tests.test_source_download tests.test_web_preview tests.test_crawler_audit_smoke tests.test_handoff` 64 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 749 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26488561562` Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 10:10 | Discovery / Seed Registry | **CHECKPOINT** | `d24998f` | `26486700381` | Add crawler seed page summaries | Seed 分頁 contract 新增 `page_summary`，由 `crawler_seed_page_summary()` 統一輸出 `shown_start`、`shown_end`、`row_count`、`remaining`、`page_count`、`has_more`、`next_page` 與 `next_action`；Web/Tk/Qt 後續做動態 seed bar / 顯示更多時不需要各自重算頁碼。同步把 `crawler_seed_registry.py` 內先前亂碼註解改成可讀維護註解。驗證：臨時 pycache `py_compile` OK；`py -3 -B -m unittest tests.test_crawler_seed_registry tests.test_web_preview -v` 40 tests OK；`py -3 -B -m unittest tests.test_crawler_seed_registry tests.test_web_preview tests.test_crawler_assets -v` 75 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 744 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26486700381` Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 09:53 | Discovery / Seed Registry | **CHECKPOINT** | `4fc1bd8` | `26486261145` | Share crawler seed favorite service | Seed 收藏寫入已從 Web Preview endpoint 抽到 `api_launcher/crawler_seed_registry.py::save_crawler_seed_favorite()`，集中檢查 `asset_id` / `dataset_uid`、呼叫 profile lane 寫入 `favorite_seed_uids`，並回傳 `favorite_seed_count` 與 `next_action=seed_favorite_saved`；Web endpoint 只保留 asset/profile adapter 與 event logging。已補 `tests/test_crawler_seed_registry.py` 覆蓋新增 / 移除收藏、空 dataset uid 擋板與 profile persistence。驗證：臨時 pycache `py_compile` OK；`py -3 -B -m unittest tests.test_crawler_seed_registry tests.test_web_preview tests.test_crawler_assets -v` 74 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 743 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26486261145` Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 09:33 | Discovery / Seed Registry | **CHECKPOINT** | `93a2b58` | `26485709468` | Extract crawler seed paging service | 把 Web Preview 的 seed 清單分頁邏輯抽到 `api_launcher/crawler_seed_registry.py`，集中處理 asset filter、page clamp、row payload 與 favorite flag；Web endpoint 只保留 asset / DB / profile adapter 職責，後續 Tk/Qt/CLI 可重用同一份 seed page contract。同步新增 `tests/test_crawler_seed_registry.py`，並更新 GTD、handoff、discovery notes。驗證：`py -3 -B -m unittest tests.test_crawler_seed_registry tests.test_web_preview tests.test_crawler_assets -v` 72 tests OK；`scripts\pre_push_smoke_brief.cmd` 741 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26485709468` Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 09:22 | Developer Diagnostics / Shared Payload | **CHECKPOINT** | `d33e32f` | `26485077082` | Share developer diagnostics payload | 新增 `api_launcher/developer_diagnostics.py` 作為 Tk / Web / 未來 Qt 的 developer diagnostics surface payload 共用入口；`crawler_handler_smoke_diagnostics_payload(surface)` 統一輸出 `purpose`、`diagnostic_id`、`developer_only`、`scope`、compact handler smoke summary 與 next action。Web Preview 與 Tk diagnostics 改成只傳自己的 surface，不再各自複製 diagnostics contract 字串。已同步 GTD、handoff 與 discovery notes。驗證：`py_compile` OK；`py -3 -B -m unittest tests.test_developer_diagnostics tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_audit_smoke -v` 80 tests OK；GitHub Actions run `26485077082` Ubuntu / `windows-2025-vs2026` / real DB smoke 全部 success。|
| 09:13 | Tk / Crawler Audit Diagnostics | **CHECKPOINT** | `7bc0040` | `26484772725` | Add Tk crawler smoke diagnostics | Tk 工具選單新增「開發者：Crawler handler diagnostics」，實作集中在 `frontends/tk/developer_diagnostics_workflows.py`；它讀同一份 `crawler_handler_audit_smoke_summary()` compact summary，顯示 supported source type count、zero-candidate warning、candidate pass、next action 與可重跑 command，並寫入 `tk_crawler_handler_smoke_diagnostics_opened` structured event。這是 developer-only offline contract smoke，不進入一般使用者下載心流，也不代表 live endpoint 可用。已同步 GTD、handoff、discovery notes 與 user guide。驗證：`py_compile` OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_launcher_ui tests.test_crawler_audit_smoke -v` 77 tests OK；GitHub Actions run `26484772725` Ubuntu / `windows-2025-vs2026` / real DB smoke 全部 success。|
| 08:57 | Web Preview / Crawler Audit Diagnostics | **CHECKPOINT** | `e909194` | `26484401061` | Expose crawler smoke diagnostics endpoint | 將 crawler handler audit compact summary 下沉到 `api_launcher/crawler_audit_smoke.py::crawler_handler_audit_smoke_summary()`，讓 handoff、heartbeat 與 Web Preview 共用同一份 counts / next_action 摘要；Web Preview 新增 developer-only `GET /api/diagnostics/crawler-handler-smoke` endpoint，只回傳 `developer_only=true`、`scope=offline_contract_smoke_no_live_network` 與 compact summary，不包含 per-source `source_results`，也不代表 live endpoint 可用。已同步 GTD、handoff、discovery notes 與 user guide。驗證：`py_compile` OK；`py -3 -B -m unittest tests.test_crawler_audit_smoke tests.test_handoff tests.test_heartbeat tests.test_web_preview -v` 65 tests OK；local HTTP smoke 回傳 `supported_source_type_count=14`、`candidate_case_status=pass`；GitHub Actions run `26484401061` Ubuntu / `windows-2025-vs2026` / real DB smoke 全部 success。|
| 08:41 | Heartbeat / Crawler Audit Contract | **CHECKPOINT** | `1f757b9` | `26483735673` | Surface crawler handler smoke in heartbeat | 將同一份 `crawler_handler_smoke_summary` 接進 heartbeat dry-run payload、Markdown report 與 agent prompt，讓長時間工作或外部 agent 接力時不用先跑完整 `--dataset-discovery-handler-smoke-json`，就能看見 14 個 supported source type 的零候選 warning contract 與正常候選 pass contract。同步更新 GTD、handoff、discovery notes 與 user guide。驗證：`py -3 -B -m py_compile api_launcher\heartbeat.py tests\test_heartbeat.py` OK；`py -3 -B -m unittest tests.test_heartbeat tests.test_handoff tests.test_crawler_audit_smoke -v` 33 tests OK；`--heartbeat-plan-json --heartbeat-skip-ci` 確認 `heartbeat_handler_count=14 empty=14 pass=14`。GitHub Actions run `26483735673` Ubuntu / `windows-2025-vs2026` / real DB smoke 全部 success。 |
| 08:30 | Agent Handoff / Crawler Audit Contract | **CHECKPOINT** | `b80d815` | `26483376979` | Expose crawler handler smoke in handoff | 將離線 crawler handler audit contract smoke 接進 `--handoff-report` / `--handoff-report-json`：`HandoffSnapshot` 現在含 `crawler_handler_smoke_summary`，只保留可重跑命令、supported source type 數、零候選 warning count、正常候選 pass count 與 next action，不把完整 per-source `source_results` 塞進 handoff。同步更新 GTD、handoff、discovery notes 與 user guide。驗證：`py -3 -B -m py_compile api_launcher\handoff.py tests\test_handoff.py` OK；`py -3 -B -m unittest tests.test_handoff tests.test_crawler_audit_smoke -v` 23 tests OK；`--handoff-report-json` 確認 `handler_count=14 empty=14 pass=14`。GitHub Actions run `26483376979` Ubuntu / `windows-2025-vs2026` / real DB smoke 全部 success。 |
| 08:19 | Discovery / Crawler Audit Contract | **CHECKPOINT** | `da9feff` | `26482972446` | Add crawler handler audit smoke | 新增 `api_launcher/crawler_audit_smoke.py` 與 CLI `--dataset-discovery-handler-smoke-json`，用 fixture source 覆蓋 14 個 supported source type，驗證零候選時統一輸出 `zero_candidates` / `repair_crawler_query_or_parser`，正常候選時 audit summary 為 pass。`crawl_dataset_sources()` 支援注入式 `source_crawler`，正式流程仍走預設 live handler，contract smoke 不碰外部網路。本地驗證：`py_compile` OK；`tests.test_crawler_audit_smoke` 4 tests OK；`tests.test_dataset_discovery tests.test_crawler_audit_smoke tests.test_crawler_assets` 71 tests OK；CLI JSON smoke 顯示 `source_types=14 empty_warning=14 candidate_pass=14`。GitHub Actions run `26482972446` Ubuntu / `windows-2025-vs2026` / real DB smoke 全部 success。 |
| 08:07 | Web Preview / Seed Enumeration UX | **CHECKPOINT** | `6be2061` | `26482464695` | Fix Tk crawler listing event context | 修復 `b8b45f9` 推送後 CI 抓到的 Tk import 語法錯誤：`frontends/tk/crawler_asset_workflows.py` 的 listing event logging call 被錯誤混成半個 dict 與 keyword argument，導致 `test_launcher_ui` / `test_tk_dialogs` 匯入失敗。修正後本機驗證：`py_compile frontends\tk\crawler_asset_workflows.py frontends\tk\launcher_ui.py tests\test_launcher_ui.py tests\test_tk_dialogs.py` OK、`tests.test_launcher_ui tests.test_tk_dialogs` 70 tests OK；GitHub Actions run `26482464695` Ubuntu / `windows-2025-vs2026` / real DB smoke 全部 success。 |
| 08:03 | Web Preview / Seed Enumeration UX | **CI 失敗** | `b8b45f9` | `26482328160` | Add crawler asset web seed UX | 推送 crawler asset / Web seed UX 大切片：source pattern handler coverage、Web 本機登入設定、真下載示範、seed 自動枚舉、seed 分頁、seed 收藏、`seed_enumeration` display-safe payload 與對應文件。推送前本機 targeted 驗證通過：`node --check`、`py_compile`、`tests.test_web_preview tests.test_crawler_assets` 65 tests OK；但 GitHub Actions run `26482328160` 在 Ubuntu / Windows full discovery 中失敗，原因是 `frontends/tk/crawler_asset_workflows.py` 第 585-587 行語法錯誤，Tk import path 未被 targeted Web 測試覆蓋。修復見 `6be2061`。 |
| 07:55 | Web Preview / Seed Enumeration UX | **WORKING / 未推送** | `local` | `local` | Add seed enumeration status payload | Crawler asset listing result 新增 `seed_enumeration` display-safe payload，統一輸出 blocked、error、empty、warning、within-current-limits、local-limit-reached、bounded-sample 等狀態，包含 `label`、`help`、`display_tone`、`limited_by_max_results`、`candidate_count` 與 `max_results`。Web seed 面板改吃這份後端 payload，候選數打滿本機上限時會顯示本機上限徽章，避免使用者誤以為入口只有這些 seed。同步更新 GTD、handoff、Web Preview UIUX 與 discovery notes。驗證：`node --check frontends\web\static\app.js` OK、`py_compile` OK、`tests.test_web_preview tests.test_crawler_assets` 65 tests OK；尚未 commit / push。 |
| 01:44 | Docs / Workflow | **WORKING / 未推送** | `local` | `local` | Document checkpoint-before-push rule | 依使用者校正，將「文檔更新觸發點是做到 checkpoint，不是 GitHub push 成功」寫入 `docs/AGENT_START_HERE.zh-TW.md`。後續若本地已完成可驗證切片，即使 GitHub push / CI 因帳號、網路或平台問題失敗，也要更新 `DEVELOPMENT_LOG`，並標成 `WORKING / 未推送`、`LOCAL PASS / CI 未完成` 或 `CI 失敗`，寫清本地驗證與剩餘風險。 |
| 01:42 | Web Preview / Seed Enumeration UX | **WORKING / 未推送** | `local` | `local` | Seed enumeration paging and seed favorites | Web Preview 選取爬蟲入口後，預設以 `complete_seed=true`、`full_crawl=true`、`max_results=1000` 觸發 seed 枚舉，並新增 `/api/crawler-assets/{asset_id}/seeds?page=&page_size=50` 從本機 catalog 分頁讀取已枚舉 seed；UI 顯示前 50 筆，按「顯示更多 seed」展開下一批。收藏語意已修正為「收藏 seed」，不是收藏 crawler asset / 入口；收藏 API 已往 profile-backed 方向收斂，Web 透過 `/seed-favorites` 寫入 crawler asset profile 的 `favorite_seed_uids`，並用同一個 `favorite_key` 對齊後端回傳與前端點選。同步更新 `PROJECT_GTD`、`AGENT_HANDOFF`、`WEB_PREVIEW_UIUX`、`DATASET_DISCOVERY_NOTES`。目前已驗證：`node --check frontends\web\static\app.js` OK、`py_compile` OK、`tests.test_web_preview tests.test_crawler_assets` 64 tests OK、`git diff --check` 無 whitespace error（僅既有 CRLF warning）。尚未 commit / push，避免把現有多檔 dirty working tree 混成未審核 checkpoint。 |

### 2026-05-26

| 時間 | 階段 | 狀態 | SHA | Run | Commit | 變更與驗證 |
| --- | --- | --- | --- | --- | --- | --- |
| 22:26 | Agent Handoff / Crawler Run Summary Window | **CHECKPOINT** | `b486db2` | `26454270149` | Widen crawler summary handoff window | 將 `DEFAULT_CRAWLER_RUN_EVENT_SCAN_LIMIT` 從 500 提高到 5000，避免 active session 中大量 smoke / CI / log-only structured events 把真實 crawler listing / plan-build event 擠出 handoff/CLI 摘要視窗；仍保持 bounded lookup，不重跑 crawler、不讀完整 resolved plan。同步更新 `PROJECT_GTD`、`AGENT_HANDOFF`、`DATASET_DISCOVERY_NOTES`。驗證：`py_compile` OK；`tests.test_crawler_run_records tests.test_handoff` 27 tests OK；`--crawler-run-summary-json` 確認 `event_limit=5000` 且目前狀態仍正確顯示 `missing_listing`（本機 log 內沒有 listing event，不是掃描窗不足）；`tests.test_crawler_run_records tests.test_handoff tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets` 118 tests OK；`git diff --check` OK（既有 docs CRLF warning）；`scripts\pre_push_smoke_brief.ps1` 707 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26454270149` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 22:12 | CI / Dev Workflow | **CHECKPOINT** | `8a3534f` | `26453488987` | Add pre-push smoke help guard | 補強 `scripts/pre_push_smoke_brief.ps1` / `.cmd` 與 `scripts/pre_push_smoke.ps1` 的 help guard：`-Help` / `--help` 只列出用法並立即退出，不再誤啟動完整 smoke、建立 log 或啟動 unittest；同步更新 `PROJECT_GTD` 與 `AGENT_HANDOFF`，提醒正式 checkpoint 仍需不加 help 執行完整 brief smoke。驗證：`scripts\pre_push_smoke_brief.cmd -Help` OK、`scripts\pre_push_smoke_brief.cmd --help` OK、`pre_push_smoke.ps1 -Help` OK；`git diff --check` OK（既有 docs CRLF warning）；`scripts\pre_push_smoke_brief.ps1` 707 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26453488987` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 21:59 | Agent Handoff / Crawler Run Summary Status | **CHECKPOINT** | `f36d915` | `26452651769` | Add crawler summary scope status | 將 `crawler_run_summary.summary_scope` 從純證據欄位補成可機讀狀態：新增 `status`、`missing_event_names` 與 `next_action`，讓 agent 能分辨 `complete`、`missing_listing`、`missing_download_plan_build`、`empty` 等狀態，不必自行組合 bool，也不會把缺 listing 的舊 plan summary 誤當完整 crawler run。同步更新 `PROJECT_GTD`、`AGENT_HANDOFF`、`DATASET_DISCOVERY_NOTES`。驗證：臨時 pycache `py_compile` OK；`tests.test_crawler_run_records tests.test_handoff` 27 tests OK；`APIkeys_collection.py --crawler-run-summary-json` 確認目前回報 `summary_scope.status=missing_listing` 與 `next_action=run_crawler_listing_to_refresh_candidate_counts`；`tests.test_crawler_run_records tests.test_handoff tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets` 118 tests OK；`git diff --check` OK（僅既有 docs CRLF warning）；`scripts\pre_push_smoke_brief.ps1` 707 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26452651769` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 21:47 | Agent Handoff / Crawler Run Summary Scope | **CHECKPOINT** | `3220038` | `26451971580` | Expose crawler run summary scope | 在 `crawler_run_summary` 補上 `summary_scope`，明確輸出本次掃描的 structured event 數、是否找到 latest listing / latest plan build，以及兩個 latest event timestamp；這讓 agent/CLI/handoff 能判斷摘要來自哪個事件視窗，但不把它誤當 freshness/stale policy。同步更新 `PROJECT_GTD`、`AGENT_HANDOFF`、`DATASET_DISCOVERY_NOTES`。驗證：臨時 pycache `py_compile` OK；`tests.test_crawler_run_records tests.test_handoff` 25 tests OK；`APIkeys_collection.py --crawler-run-summary-json` 確認輸出 `summary_scope.event_scan_count=500` 與 latest plan timestamp；`tests.test_crawler_run_records tests.test_handoff tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets` 116 tests OK；`git diff --check` OK（僅既有 docs CRLF warning）；`scripts\pre_push_smoke_brief.ps1` 705 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26451971580` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 21:31 | Agent Handoff / Crawler Run Event Window | **CHECKPOINT** | `6b04ffe` | `26451071887` | Widen crawler run summary event scan | 將 crawler run summary 的預設 event scan window 從 50 擴到 500，避免 CI、smoke、log-only event crowding 讓 `noaa_ncei_dataset_search` 這類真實 plan summary 被擠出最新事件視窗。`--crawler-run-summary-limit` 仍可覆寫；handoff 現在會以 500 筆事件掃描 crawler summary，但顯示用 recent logs 仍保留原本的 display limit。文件同步更新 `PROJECT_GTD`、`AGENT_HANDOFF`、`DATASET_DISCOVERY_NOTES`。驗證：臨時 pycache `py_compile` OK；`tests.test_crawler_run_records tests.test_handoff` 24 tests OK；`--crawler-run-summary-json` 與 `--handoff-report-json` 皆能看到 `latest_download_plan_build.asset_id=noaa_ncei_dataset_search`；`tests.test_crawler_run_records tests.test_handoff tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets` 115 tests OK；`git diff --check` OK（僅既有 docs CRLF warning）；`scripts\pre_push_smoke_brief.cmd` 704 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26451071887` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 21:20 | Agent Handoff / Crawler Run Summary CLI | **CHECKPOINT** | `237aa8e` | `26450395908` | Add crawler run summary CLI | 新增輕量 CLI `--crawler-run-summary-json` 與 `api_launcher/cli_crawler_run_records.py`，只讀 structured event log，輸出最新 `crawler_asset_listing_recorded` / `crawler_asset_plan_outcome_recorded` 的 bounded summary、event count 與 event scan limit；若 agent 只需要 crawler run counts / next_action，不必解析完整 handoff snapshot，也不會重跑 crawler 或讀回完整 resolved plan。`api_launcher/crawler_run_records.py` 新增 `crawler_run_summary_from_events()` / `latest_crawler_run_event()`，handoff 也改走同一個 helper。文件同步更新 `PROJECT_GTD`、`AGENT_HANDOFF`、`DATASET_DISCOVERY_NOTES`。驗證：臨時 pycache `py_compile` OK；`tests.test_crawler_run_records tests.test_handoff` 20 tests OK；`APIkeys_collection.py --crawler-run-summary-json` 輸出純 JSON；`tests.test_crawler_run_records tests.test_handoff tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets` 111 tests OK；`git diff --check` OK（僅既有 docs CRLF warning）；`scripts\pre_push_smoke_brief.cmd` 700 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26450395908` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 21:06 | Web Preview / Handoff Summary Consolidation | **CHECKPOINT** | `b517cd8` | `26449708071` | Share crawler run event summaries | 將 Web Preview 與 handoff 的 crawler event 壓縮規則集中到 `api_launcher/crawler_run_records.py`：新增 `crawler_run_context_summary()` / `crawler_run_event_summary()`，由同一份白名單輸出 bounded counts、compact `run_record`、`content_review` 與 `resolved_plan_available`，不複製完整 `resolved_plan` / `providers` body。`frontends/web/preview_api.py` 與 `api_launcher/handoff.py` 已改為共用 helper，避免 Qt/Tk/Web/agent 未來各自重寫摘要規則。文件同步更新 `PROJECT_GTD`、`AGENT_HANDOFF`、`WEB_PREVIEW_UIUX`。驗證：臨時 pycache `py_compile` OK；`tests.test_crawler_run_records tests.test_handoff tests.test_web_preview` 38 tests OK；`tests.test_crawler_run_records tests.test_handoff tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets` 109 tests OK；`git diff --check` OK（僅既有 docs CRLF warning）；`scripts\pre_push_smoke_brief.cmd` 698 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26449708071` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 20:48 | Agent Handoff / Crawler Run Summary | **CHECKPOINT** | `3d5e7ce` | `26448921673` | Expose crawler run summaries in handoff | `--handoff-report` / `--handoff-report-json` 現在輸出 `crawler_run_summary`，把最近一次 `crawler_asset_listing_recorded` 與 `crawler_asset_plan_outcome_recorded` 的 counts、compact `run_record`、`next_action` 與 `resolved_plan_available` 壓成白名單摘要，不攜帶完整 `resolved_plan` 或候選清單；接手 agent 可先讀 handoff JSON，再視需要查 Web `/api/events/recent` 或完整 JSONL。同步更新 GTD、handoff、discovery notes 與 Web Preview UIUX notes。驗證：本機 `PYTHONPYCACHEPREFIX` py_compile OK；`tests.test_handoff` 13 tests OK；`tests.test_handoff tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets` 104 tests OK；`--handoff-report-json` 確認含 `crawler_run_summary`；`git diff --check` OK（僅既有 docs CRLF warning）；`scripts\pre_push_smoke_brief.cmd` 696 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26448921673` Ubuntu / `windows-2025-vs2026` / Real DB smoke 全部 success。 |
| 20:32 | Web Preview / Crawler Run Handoff | **CI 部分成功 / Checkout 403** | `f0473ca` | `26448273507` | Surface crawler listing event counts in web preview | Web Preview `/api/events/recent` 現在會保留 `crawler_asset_listing_recorded` 的候選、upsert、skipped provider、duplicate、warning/error counts，並在 compact `run_record` summary 中保留 crawler listing / download-plan 的核心 counts；事件分頁 chip 也會優先顯示 `asset_id`、`run_record`、核心 counts 與 next action，避免 agent 或人類為了知道上一輪 crawler 清單結果而重跑遠端 crawler。文件已同步 `PROJECT_GTD`、handoff、Web Preview UIUX 與 discovery notes。驗證：`py -3 -B -m py_compile frontends\web\preview_api.py tests\test_web_preview.py` OK；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets -v` 91 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 695 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`。GitHub Actions run `26448273507`：Ubuntu Python 3.13 job success；Windows 與 Real DB smoke 在 `actions/checkout@v6` checkout 階段 HTTP 403 (`unable to access ... requested URL returned error: 403`) 失敗，未進入專案測試，屬 GitHub 存取/帳號狀態風險，不是本次 patch 測試失敗。|

| 時間 | 階段 | 狀態 | SHA | Run | Commit | 變更與驗證 |
| --- | --- | --- | --- | --- | --- | --- |
| 20:16 | Discovery / Crawler Run Handoff | **CI 失敗 / Checkout 403** | `935d18a` | `26447521838` | Record crawler listing run events | Tk 清單擷取成功時新增 `crawler_asset_listing_recorded` structured event，只保存 bounded counts、`next_action` 與 `run_record.stage=crawler_listing`，補齊 `source profile -> crawler run -> candidate -> plan` 前半段的 handoff trace；新增 headless Tk regression 鎖住 listing event context 的 `run_record`。驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_crawler_assets tests.test_web_preview -v` 90 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 694 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`。GitHub Actions run `26447521838` 與 rerun 都在 `actions/checkout@v6` 階段失敗，遠端回覆 `Your account is suspended` / HTTP 403，三個 job 均未進入專案測試；目前判定為 GitHub 帳號或存取狀態問題，不是本 patch 行為測試失敗。 |

| 時間 | 階段 | 狀態 | SHA | Run | Commit | 變更與驗證 |
| --- | --- | --- | --- | --- | --- | --- |
| 19:43 | Discovery / Crawler Run Handoff | **LOCAL PASS / CI 待查** | `5b83940` | `local pre-push` | Guard crawler run record extraction | `crawler_run_record_from_result()` 現在是降級式 helper：result 沒有 `to_dict()`、`to_dict()` 拋錯、或 payload 不含 dict 型 `run_record` 時會回傳 `{}`，避免交接 payload 壞掉時拖垮 Tk/Web event logging。新增 `tests/test_crawler_run_records.py` 鎖住 missing contract、broken `to_dict()` 與 copy behavior；同步更新 GTD 與 handoff。驗證：`py -3 -B -m py_compile api_launcher\crawler_run_records.py tests\test_crawler_run_records.py` OK；`py -3 -B -m unittest tests.test_crawler_run_records tests.test_web_preview tests.test_tk_dialogs -v` 61 tests OK；`scripts\pre_push_smoke_brief.cmd` 693 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`。Push 已到 `origin/main`，但 `gh run list` 尚未出現此 SHA 的 GitHub Actions run，延續 Actions enqueue / dispatch 異常，因此本列先保留 local pass，不標成 CHECKPOINT。 |
| 19:34 | Discovery / Crawler Run Handoff | **LOCAL PASS / CI 待查** | `62ccfb8` | `local pre-push` | Record crawler run payloads in UI events | Tk 與 Web Preview 建立爬蟲資產下載計畫時，`crawler_asset_plan_outcome_recorded` structured event context 現在會帶 compact `run_record`；Web `/api/events/recent` 只暴露 bounded summary，讓 agent 可從 event log 讀 `record_key`、stage、status、outcome bucket 與 next action，不必重新從分散欄位推理 crawler run 狀態。同步更新 GTD、handoff 與 discovery notes。驗證：`py -3 -B -m py_compile api_launcher\crawler_run_records.py frontends\web\preview_api.py frontends\tk\crawler_asset_workflows.py tests\test_web_preview.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets -v` 89 tests OK；`scripts\pre_push_smoke_brief.cmd` 690 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`。Push 已到 `origin/main`，但 `gh run list` 尚未出現此 SHA 的 GitHub Actions run，延續本日下午的 Actions enqueue / dispatch 異常，因此本列先保留 local pass，不標成 CHECKPOINT。 |
| 18:57 | Docs / Workflow | **LOCAL PASS / CI DISPATCH ERROR** | `62f5677` | `git diff --check`; dispatch HTTP 500 | Add manual CI dispatch fallback | GitHub Actions push-event queue 連續沒有替 `c001f5b`、`f5b10d4`、`62f5677` 建立 CI run，因此新增 `.github/workflows/ci.yml` 的 `workflow_dispatch`，並把手動補證據流程寫入 OpenSpec workflow 文件、handoff、Git handoff 與 GTD。`git diff --check` 通過，push 已抵達 `origin/main`；但 `gh workflow run CI --ref main` 與 REST dispatch API 目前回 HTTP 500 `Failed to run workflow dispatch`，且 GitHub 只建立 Vercel check suite、未建立 Actions run。此列保留為遠端 CI 排隊/dispatch 異常證據；後續 checkpoint 仍須補回 GitHub Actions 成功紀錄，不能把這次視為 CI 已通過。 |
| 18:45 | Crawler Asset / Status Gate Contract | **LOCAL PASS / CI 待查** | `c001f5b` | `local pre-push` | Add crawler asset status gates | Crawler asset health payload 新增 `status_gate`，把 archived/disabled/missing handler/needs bounds/review/healthy 收斂成 `restricted`、`staged`、`adapter_review`、`review`、`completed` 等 UI-neutral gate；Tk/Web/Qt/agent 後續可讀 `asset.health.status_gate` 做任務分流，不必各自從 `risk_tier`、`maturity` 或 capability status 重建規則。同步更新 GTD、handoff 與 discovery notes。驗證：`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview tests.test_tk_dialogs -v` 88 tests OK；`scripts\pre_push_smoke_brief.cmd` 689 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`。注意：此 push 已到 `origin/main`，但 GitHub Actions 在本次檢查期間未為 `c001f5b` 排入 CI run；此列先保留為 local pass，待後續 push/CI 證據補正。 |
| 18:32 | Tk Crawler Asset / Source Draft Review UX | **CHECKPOINT** | `32959e4` | `26447054568` | Show source draft review warnings in Tk | Tk 爬蟲資產分頁現在會把 `SourcePatternDraftError` 呈現成「來源草稿保留審核」warning，而不是 generic error：對 unknown、低信心、缺 source type 或 unsupported source type 的 URL 顯示 `review_reason`、pattern、confidence、evidence 與 `next_action=review_source_profile_or_add_detector`，並寫入 `source_pattern_source_draft_blocked` warning event；真正未預期例外才走 `showerror` / `log_exception`。同步更新 GTD、handoff 與 discovery notes。驗證：`py -3 -B -m py_compile frontends\tk\crawler_asset_workflows.py tests\test_tk_dialogs.py` OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_source_pattern_drafts tests.test_discovery_drafts -v` 69 tests OK；`scripts\pre_push_smoke_brief.cmd` 689 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26447054568` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 18:14 | Discovery / Source Draft Review Payload | **CHECKPOINT** | `f71ea87` | `26446252423` | Add structured source draft review payload | Source pattern source draft 擋板新增 `SourcePatternDraftError`，unknown、低信心、缺 source type hint 與 unsupported source type 會輸出 structured review payload，包含 `review_reason`、`minimum_confidence`、`source_pattern_detection`、`skipped` 與 `next_action=review_source_profile_or_add_detector`；CLI 指定 `--write-source-draft-json` 時會先寫 blocked summary JSON 再保留失敗狀態，讓 Tk/Web/Qt/agent 可讀 JSON 判斷補 source profile 或新增 detector/adapter，而不是只看 traceback；同步更新 GTD、handoff 與 discovery notes。驗證：`py -3 -B -m unittest tests.test_source_pattern_drafts tests.test_discovery_drafts tests.test_source_patterns tests.test_crawler_assets -v` 83 tests OK；`scripts\pre_push_smoke_brief.cmd` 687 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26446252423` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 17:56 | Web Preview / Display-safe Outcome Labels | **CHECKPOINT** | `015ab8b` | `26445428575` | Use safe outcome labels in web preview | Web Preview 的 plan outcome、Plan Passport 與 Adapter review chip 現在透過 `planOutcomeLabel()`、`planPassportLabel()`、`reviewOutcomeLabel()` 呈現 display-safe fallback；缺後端 label 時顯示中性「計畫結果 / 計畫護照 / 待辦分類」，raw `outcome_bucket` 保留給 JSON / detail / agent debug，不再作為主要 UI 文案。同步更新 GTD 與 handoff，並以靜態測試鎖住不得回退到 raw outcome bucket fallback。驗證：`node --check frontends\\web\\static\\app.js` OK、`tests.test_web_preview tests.test_crawler_assets` 49 tests OK、`scripts\\pre_push_smoke_brief.cmd` 685 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`、GitHub Actions run `26445428575` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 17:20 | Web Preview / Display-safe Stale Labels | **CHECKPOINT** | `169d09d` | `26443675501` | Use safe stale labels in web preview | Web Preview 下載器列與 Plan Passport 面板現在透過 `stalePassportLabel()` / `stalePassportNextAction()` 呈現 stale 狀態；缺少 `stale_label` 時只顯示中性「計畫需重建」提示，`stale_reason` 保留給 JSON / agent debug，不再串進主要 UI。同步更新 GTD 與 handoff，並以靜態測試鎖住不得回退到 raw reason fallback。驗證：`node --check frontends\\web\\static\\app.js` OK、`tests.test_web_preview tests.test_crawler_assets` 49 tests OK、`scripts\\pre_push_smoke_brief.cmd` 685 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`、GitHub Actions run `26443675501` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 17:08 | Tk Crawler Passport / Display-safe Stale Labels | **CHECKPOINT** | `1de3b10` | `26443070707` | Use stale passport labels in Tk summaries | Tk Crawler Passport 摘要現在優先使用後端 `stale_label` / `stale_next_action`，不再把 `asset_disabled`、`source_changed` 這類 raw stale reason 當作主要使用者提示；raw reason 仍保留給 agent/debug 對照。同步更新 GTD 與 handoff，讓未來 Web/Qt 也維持只呈現 display-safe payload 的邊界。驗證：temp pycache `py_compile` OK、`tests.test_tk_dialogs tests.test_crawler_assets` 67 tests OK、`scripts\pre_push_smoke_brief.cmd` 685 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`、GitHub Actions run `26443070707` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 16:57 | Crawler Asset Plan Passport / Regression Guard | **CHECKPOINT** | `8d38ef1` | `26442500275` | Guard unchanged crawler candidate snapshots | 補上同候選快照重建的 regression：同一批 candidate metadata / version / source URL 重新建立下載計畫時，`candidate_snapshot_changed` 必須維持 `false`，避免 Web/Tk/未來 Qt 把所有 rebuild 都誤導成「候選清單已變更」。同步更新 GTD 與 handoff，明確保留「相同 digest 不算變更」這條防線。驗證：temp pycache `py_compile` OK、`tests.test_crawler_assets` 30 tests OK、`scripts\pre_push_smoke_brief.cmd` 685 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`、GitHub Actions run `26442500275` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 16:44 | Web Preview / Crawler Asset Plan Passport | **CHECKPOINT** | `e269e9f` | `26441934002` | Show candidate snapshot changes in UI passports | Web Preview 下載器列與 Plan Passport 面板現在會顯示後端 compact `plan_passport` 的 `candidate_snapshot_changed` 徽章；Tk Crawler Passport 摘要也會顯示同一個候選快照變更狀態。這個切片只呈現後端 service payload，不在 JS/Tk 端自行比較 digest 或重跑 crawl，讓未來 Qt 也能直接沿用同一欄位。驗證：`node --check frontends\web\static\app.js` OK、temp pycache `py_compile` OK、`tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets` 85 tests OK、`scripts\pre_push_smoke_brief.cmd` 684 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`、GitHub Actions run `26441934002` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 16:30 | Web Preview / Crawler Asset Plan Passport | **CHECKPOINT** | `f7b8d74` | `26441306317` | Compare crawler candidate snapshots on rebuild | 完成 explicit fresh crawl / rebuild plan 的候選快照比對：`build_crawler_asset_download_plan()` 會讀取上一版 asset profile 護照中的 `candidate_snapshot_signature`，和本次 `SourceDownloadPlanBuild` 的 digest 比對，輸出 `previous_candidate_snapshot_signature` 與 `candidate_snapshot_changed`；compact `plan_passport` / asset profile 白名單同步保留這個旗標，Web/Tk/未來 Qt 可以顯示「本次重建後候選快照有變」，但仍不能在單純讀取 profile 時推測遠端 freshness。測試補上 profile compact persistence、service rebuild compare、Web plan preview persistence；驗證：temp pycache `py_compile` OK、`tests.test_crawler_assets tests.test_web_preview tests.test_source_download` 57 tests OK、`scripts\pre_push_smoke_brief.cmd` 684 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`、GitHub Actions run `26441306317` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 16:06 | Web Preview / Crawler Asset Plan Passport | **CHECKPOINT** | `6e9ecfb` | `26440247102` | Track crawler candidate plan snapshots | 補上候選清單 snapshot digest：`SourceDownloadPlanBuild` 會在每次 crawl/build plan 時計算 `candidate_snapshot_signature` / `candidate_snapshot_count`，`plan_passport` 與 asset profile compact 白名單會保留這份摘要，Web/Tk/未來 Qt 可追溯本次 plan 由哪一批 candidate metadata / version / source URL 形成；同時在 GTD、handoff、Web Preview UIUX 文件明確標註它是當次 crawl 快照，不是 live 遠端 freshness check，未來只有 explicit fresh crawl / rebuild plan 後才能比較新舊 digest 並顯示 candidate list changed。驗證：本機 temp pycache `py_compile` OK；`tests.test_source_download tests.test_crawler_assets tests.test_web_preview` 57 tests OK；`scripts\pre_push_smoke_brief.cmd` 684 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26440247102` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。剩餘風險：目前 profile/source/bounds schema 變更可立即標記 stale；候選清單本體變動仍需要下一輪 fresh crawl 比對流程，不應由 UI 自行推測。 |
| 15:44 | Web Preview / Crawler Asset Plan Passport | **CHECKPOINT** | `d8f21b2` | `26439199494` | Show readable stale passport guidance | 將 `latest_plan_passport` 的 stale 狀態補成 display-safe 指引：後端 `crawler_asset_plan_passport_for_profile()` 現在會輸出 `stale_label` 與 `stale_next_action`，例如「資產已停用，啟用後重新建立下載計畫」、「來源設定已改變，請重新建立下載計畫」、「界域表單已改變，請重新建立下載計畫」。Web Preview 下載器與 Plan Passport 面板改為呈現這兩個後端欄位，不再把 raw `source_changed` / `bounds_schema_changed` 直接交給使用者。同步更新 GTD、handoff 與 Web Preview UIUX 契約，鎖住 UI 只呈現 service payload 的邊界。驗證：`py_compile` OK、`node --check frontends\web\static\app.js` OK、`tests.test_web_preview tests.test_crawler_assets` 48 tests OK、Web Preview HTTP health 以 `--port-scan` 從 8765 fallback 到 `127.0.0.1:8766`，只停止本次啟動的 8766 PID、`scripts\pre_push_smoke_brief.cmd` 683 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`、GitHub Actions run `26439199494` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。剩餘風險：目前 stale 的內容變更仍停在 profile/source/bounds schema 層，尚未對候選版本或遠端清單 revision 做 digest。 |
| 15:24 | Web Preview / Crawler Asset Plan Passport | **CHECKPOINT** | `2f51845` | `26438422611` | Invalidate plan passports on source changes | 將 profile-backed `latest_plan_passport` stale guard 從 profile state 延伸到來源與界域表單簽名：`crawler_asset_source_signature()` 會保存 source id、provider、source type、endpoint、dataset、file regex、seed mode、max results 等高風險欄位，`crawler_asset_bounds_signature()` 會保存 bounds facets；`load_crawler_assets()` 讀回 profile passport 時若 current source/bounds signature 不同，會輸出 `stale=true` 與 `source_changed` / `bounds_schema_changed`。`CrawlerAssetDownloadPlanResult` 與 compact plan passport payload 也會帶這兩個簽名，Web/Tk/未來 Qt 只顯示「計畫需重建」，不在 UI 端重算規則。順手清理 `WEB_PREVIEW_UIUX` 中重複插入的 stale guard 段落，收斂成單一 freshness guard。驗證：`py_compile` OK、`node --check frontends\web\static\app.js` OK、crawler/web/Tk/source targeted 152 tests OK、`scripts\pre_push_smoke_brief.cmd` 683 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`、GitHub Actions run `26438422611` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。剩餘風險：尚未對遠端候選清單內容本身做 revision 指紋，下一輪可評估 candidate/version snapshot digest。 |
| 15:05 | Web Preview / Crawler Asset Plan Passport | **CHECKPOINT** | `971b38a` | `26437635180` | Guard stale crawler asset plan passports | 新增 profile-backed `latest_plan_passport` stale guard：保存 `saved_at`、`profile_state`、`stale`、`stale_reason`，並在 crawler asset 被停用、封存或 profile state 改變時由後端輸出 `stale=true` / `display_tone=warning`。Web Preview 下載器與 plan passport 面板顯示「計畫需重建」，Tk summary 顯示 stale reason；UI 只呈現 display-safe payload，不自行重做業務規則。驗證：`py_compile` OK、`node --check frontends\web\static\app.js` OK、targeted unittest 150 tests OK、`scripts\pre_push_smoke_brief.cmd` 681 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`、Browser 驗證 Web Preview 以 port-scan 從 8765 fallback 到 `127.0.0.1:8766`、console error/warn 為空、爬蟲資產選取與下載器分頁可互動、GitHub Actions run `26437635180` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。剩餘風險：下一輪可加入 source endpoint / bounds signature，讓來源與界域定義變更也能使既有 passport stale。 |
| 14:37 | Web Preview / Profile-backed Plan Passport | **CHECKPOINT** | `03ff457` | `26436526412` | Persist crawler asset plan passports in profiles | `plan_passport` 已從 event-only fallback 收斂成 crawler asset profile 的 compact `latest_plan_passport`：Tk 與 Web Preview 建立下載計畫後只保存 asset id、resolved-plan presence、candidate/direct/review/content-review counts、credential/missing-provider counts、簡化 bounds 與 next action，不保存完整 `providers` 或 resolved plan body；Web card/detail 會優先讀 profile passport，再退回近期 event。測試新增 profile compactness、Web profile-over-event precedence、execute plan 後 profile persistence；驗證：`py -3 -B -m py_compile ...` OK、`tests.test_crawler_assets tests.test_web_preview tests.test_tk_dialogs` 81 tests OK、crawler/source/web targeted 111 tests OK、`scripts\pre_push_smoke_brief.cmd` 679 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`，GitHub Actions run `26436526412` Ubuntu / `windows-2025-vs2026` / Real DB smoke success。剩餘風險：下一步要定義 `latest_plan_passport` stale/過期規則，避免來源或界域變更後舊 passport 被誤讀成最新計畫。 |
| 14:19 | Web Preview / Event-backed Plan Passport | **CHECKPOINT** | `ee05965` | `26435796485` | Restore web preview plan passports from events | Web Preview 的 crawler asset card/detail 現在可從近期 `crawler_asset_plan_outcome_recorded` structured event 讀回 compact `plan_passport`，並以白名單欄位還原 Candidates、Direct、Review、Adapter、內容待辦與 credential/provider gate 摘要；完整 `providers` 與 resolved plan body 不會回灌到前端狀態。這讓頁面重載後的「下載器」分頁仍能顯示真實後端計畫護照，而不是依賴 JS localStorage 或假狀態。驗證：`scripts\pre_push_smoke_brief.cmd` 676 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26435796485` Ubuntu / `windows-2025-vs2026` / Real DB smoke 全部 success。後續仍需評估哪些 passport 欄位應正式寫入 asset profile，避免長期只依賴近期事件。 |
| 13:18 | Web Preview / Workflow Tabs | **CHECKPOINT** | `261b9eb` | `26433777502` | Add web preview workflow tabs | Web Preview 側欄四個工作區正式啟用：`爬蟲資產` 保留既有界域/資產護照心流；`下載器` 以後端 `plan_outcome` / `plan_passport` 呈現已建立的下載計畫摘要；`匯入審核` 顯示最近一次 plan build 回傳的 Adapter review / content parser 待辦；`事件紀錄` 新增 `/api/events/recent`，只讀 bounded structured event 摘要，不在 Web JS 內重寫下載、匯入或審核規則。瀏覽器驗證時先發現舊 8766 server 未吃到新 route 造成事件分頁 404，改用乾淨 `127.0.0.1:8790` 驗證四分頁，console error/warn 為空，並已清掉本輪測試 server，避免殘留 port 混淆。驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview tests.test_source_patterns tests.test_source_pattern_drafts tests.test_dataset_discovery tests.test_crawler_assets -v` 107 tests OK；`py_compile` OK；Browser 實測 `127.0.0.1:8790` 四個工作區可切換、事件列表可讀 structured event；`scripts\pre_push_smoke_brief.cmd` 675 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 12:49 | Web Preview / Responsive UX | **CHECKPOINT** | `510230c` | `26432906670` | Fix web preview responsive sidebar | 修正 Web Preview 中寬度響應式版面：移除 1260px 以下以 `font-size: 0` 隱藏側欄中文標籤的策略，改成保留可讀 nav，並在 980px 以下把側欄轉為頁首控制區；來源篩選改為多欄與高度限制，避免長清單把主工作區推到第一屏外。同步 GTD、handoff 與 Web Preview UIUX 文件，並新增 static regression 鎖住「不得再藏側欄文字」與篩選高度規則。驗證：Browser 實測 `http://127.0.0.1:8766/`（8765 被占用時自動 port-scan），849x910 viewport 下 nav 文字可見、來源篩選高度受控、搜尋 `stac` 後可見卡片數為 2、console error/warn 為空；`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview tests.test_source_patterns tests.test_source_pattern_drafts tests.test_dataset_discovery tests.test_crawler_assets -v` 106 tests OK；`py_compile` OK；`git diff --check` OK（docs CRLF warning only）；`scripts\pre_push_smoke_brief.cmd` 674 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / Real DB smoke success。 |
| 12:31 | Crawler Asset UI / Plan Passport | **CHECKPOINT** | `56fac3f` | `26432325875` | Show plan passport summaries in Tk crawler assets | Tk Crawler Passport 已接上 compact `plan_passport`：送進下載器後會寫入/還原 `plan_passport` event context，右側 passport 顯示 candidates/direct/review/adapter/content/credential/missing-provider 摘要；完整 resolved plan 仍留在 review/download path。補上壞 event count 容錯，避免舊 event 或外部工具寫入非數字造成 Tk 閃退。驗證：臨時 `PYTHONPYCACHEPREFIX` py_compile OK；`py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview tests.test_crawler_assets -v` 76 tests OK；`git diff --check` OK（CRLF warnings only）；`scripts\pre_push_smoke_brief.cmd` 674 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / Real DB smoke success。下一步評估是否把 compact passport 長期固化到 asset profile，而不要複製 full resolved plan。 |
| 12:09 | Web Preview / Plan Passport Visualization | **CHECKPOINT** | `964a880` | `26431734487` | Render compact plan passport in web preview | Web Preview 右側資產護照現在會在本頁 session 內顯示 compact `plan_passport` 面板；面板只吃後端 payload，呈現 resolved-plan presence、Candidates、Direct、Review、Adapter、內容待辦與 credential/provider gate 摘要，不把完整 resolved plan 複製進 UI state。瀏覽器實測使用 `--port-scan`：8765 被占用時 server 自動改用 `http://127.0.0.1:8766/`，建立 NOAA NCEI plan 後，護照面板顯示 `PLAN PASSPORT`、Candidates 49、Direct 69、Review 0、Adapter 39、內容待辦 39，JSON inspector 同時含 `plan_passport`。驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets -v` 39 tests OK；Browser 操作驗證 OK；`git diff --check` OK（docs CRLF warning）；`scripts\pre_push_smoke_brief.cmd` 672 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / Real DB smoke success。剩餘：把同一份 `plan_passport` 延伸到 Tk，並評估哪些欄位需要進入 asset profile/event-backed persisted passport。 |
| 11:52 | Web Preview / Plan Passport Contract | **CHECKPOINT** | `7dc8d9f` | `26431229108` | Expose compact web plan passport payload | Web Preview 的 `execute=true` plan preview 現在會回傳 compact `plan_passport`，由 `api_launcher/crawler_asset_display.py::crawler_asset_plan_passport_payload()` 產生；payload 包含 asset id、resolved-plan presence、candidate/direct/review/content-review counts、credential/missing-provider counts、bounds 與 next action，但不把完整 `providers` / resolved plan body 複製到 UI state。這把「最近 plan 結果」從單純 badge 推進到可給 Tk/Web/Qt 共用的計畫護照 service contract。測試新增 compact passport regression，確認 full plan body 不會洩入 UI payload。驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_display.py frontends\web\preview_api.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets -v` 39 tests OK；`git diff --check` OK（docs CRLF warning）；`scripts\pre_push_smoke_brief.cmd` 672 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / Real DB smoke success。剩餘：把 `plan_passport` 視覺化到 Web/Tk 卡片護照，並評估哪些欄位要回寫成 asset profile；full resolved plan 仍應留在 review/download path。 |
| 11:35 | Web Preview / Selected Asset Outcome Panels | **CHECKPOINT** | `9bd882a` | `26430696664` | Show web plan outcomes in selected asset panels | Web Preview 的 `latest_plan_outcome` 不再只停在卡片牆小徽章；選中資產的 hero 與右側資產護照現在也會顯示最近計畫短標、summary、direct/review counts 與 content-review badge。修補過程中用 Browser 實測抓到前端刷新缺口：按「建立下載計畫」後卡片徽章會更新，但 hero/passport 沒重畫；已補 `selectedAssetDetail` 快取與 `refreshSelectedAssetOutcomeViews()`，建立 plan 後同步刷新三個視覺面。端口驗證採用避讓策略：先用 HTTP smoke 故意佔住一個 port，確認 server 自動跳到下一個可用 port 並在 `/api/health` 回報 `port_scanned=true`；瀏覽器實測使用 `http://127.0.0.1:8766/`，確認 MarineCadastre 建立計畫後卡片、hero、passport 都顯示「零候選」，console error/warn 為空。驗證：`node --check frontends\web\static\app.js` OK、`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets -v` 37 tests OK、HTTP smoke `asset_count=23` / `html_has_app=true` / `port_scanned=true`、`git diff --check` OK（docs CRLF warning）、`scripts\pre_push_smoke_brief.cmd` 670 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`、GitHub Actions Ubuntu / `windows-2025-vs2026` / Real DB smoke success。後續風險：這仍是 Web Preview 的狀態呈現層，正式 resolved-plan passport / asset profile 狀態仍需後端 service 化。 |
| 11:12 | Web Preview / Event Log Writeback | **CHECKPOINT** | `508cd08` | `26430076680` | Record web preview crawler plan events | 修正 Web Preview 只讀 event log、不寫 event 的不完整閉環。現在 Web Preview 執行 `execute=true` 建立 crawler asset 下載計畫後，會同步寫入 compact `crawler_asset_plan_outcome_recorded` structured event；event 只保存 `asset_id`、`outcome_bucket`、`outcome_label`、download/review counts、`content_review` badge、`resolved_plan_available` 與 `user_next_action`，不把完整 resolved plan 寫進 JSONL。這讓 Web 自己剛建立的計畫結果在重新載入頁面後也能被 `latest_plan_outcome` 讀回，並維持「UI 只呈現後端 payload，不重寫 outcome bucket」的邊界。文件同步更新 Web Preview UIUX、GTD 與 handoff。驗證：臨時 pycache `py_compile` OK、`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets -v` 37 tests OK、`git diff --check` OK（僅 docs CRLF warning）、`scripts\pre_push_smoke_brief.cmd` 670 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`，GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke success。剩餘風險：event 仍只是近期回顯，不是正式資產 profile；resolved plan 本體沒有持久 passport，後續要設計可查、可清理、可審核的後端狀態來源。 |
| 10:59 | Web Preview / Event-backed Plan Badge | **CHECKPOINT** | `8085180` | `26429715564` | Surface recent crawler plan badges in web preview | 讓 Web Preview 卡片牆不只顯示本頁 session 剛建立的 `plan_outcome`，也能從近期 `crawler_asset_plan_outcome_recorded` structured event 讀取 `latest_plan_outcome`，重新載入頁面後仍能看到最近一次後端計畫結果徽章；本頁新建結果仍優先，避免 stale event 蓋掉最新操作。新增 `crawler_asset_plan_event_badge_payload()` 作為 Tk/Web/Qt 共用 display payload bridge，`frontends/web/preview_api.py` 只讀 event log 並輸出卡片契約，`app.js` 只判斷有無 badge，不重寫 outcome bucket 業務分支。文件同步更新 Web Preview UIUX、GTD 與 handoff，提醒不要把跨 session 狀態塞進 JS localStorage；正式持久狀態仍應回到 asset profile / backend service。驗證：臨時 pycache `py_compile` OK、`node --check frontends\web\static\app.js` OK、`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets -v` 36 tests OK、`git diff --check` OK（僅 docs CRLF warning）、`scripts\pre_push_smoke_brief.cmd` 669 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`，GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke success。剩餘風險：目前仍是近期 event-backed badge，不是正式資產 profile；下一步應把 Adapter resolving / resolved-plan passport 做成後端可查的持久狀態。 |
| 10:45 | Web Preview / Plan Outcome Badges | **CHECKPOINT** | `e5ae11f` | `26429314332` | Show web preview plan badges | 讓 Web Preview 卡片牆在本次 session 建立下載計畫後顯示後端 `plan_outcome` 徽章：徽章文字使用 `short_label` / `display_label`，色調使用 `display_tone`，若 `content_review.has_review` 也會顯示內容格式待辦徽章。這個切片只視覺化剛收到的 backend payload，不做 localStorage 或跨 session 持久化；持久化仍留給 event log / asset profile / backend service。同步 Web Preview 文件、GTD 與 handoff，並用 static regression 鎖住 `planBadgeHtml` 與 `.plan-badge`。驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets -v` 35 tests OK；`git diff --check` OK（僅 docs CRLF warning）；`scripts\pre_push_smoke_brief.cmd` 668 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke success。殘餘風險：此徽章仍是 session-local UIUX preview，尚未與 Tk structured event 或持久化 profile 互通；下一步若要跨 UI 共用，應先設計 resolved-plan passport / badge service contract。 |
| 10:32 | Web Preview / Runtime Port Safety | **CHECKPOINT** | `ff1833d` | `26428923961` | Report web preview runtime port | 將 Web Preview 的 port fallback 從只印在終端提升成 `/api/health` runtime contract：server 現在回報實際 `host`、`port`、`url`、原請求 port、`port_scan` 與 `port_scanned`，前端總覽與本機互動紀錄會顯示實際 `host:port`，避免多個 agent / IDE Live Preview / 本地 clone 同時開啟時誤連或互相關閉程序。驗證：`node --check frontends\web\static\app.js` OK；臨時 pycache `py_compile` OK（直接寫 K 槽 pycache 曾遇 Windows access denied，屬雲端碟鎖定風險）；`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets -v` 35 tests OK；實際 HTTP smoke 確認 busy port 會切到下一個 port 並在 `/api/health` 回報；`git diff --check` OK（僅 docs CRLF warning）；`scripts\pre_push_smoke_brief.cmd` 668 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke success。殘餘風險：尚未用 Browser/Live Preview 做視覺截圖比對；若需要展示，請在本地 clone 啟動 Web Preview 並以畫面確認 CSS 密度。 |
| 10:17 | Web Preview / Bounds Form Display | **CHECKPOINT** | `8986fb2` | `26428480422` | Group web preview bounds fields | 將 crawler asset 動態界域表單改成後端 `group_display` 驅動的分組顯示：`api_launcher/crawler_asset_display.py` 統一提供「版本控制、資料集選擇、時間界域、空間界域、擷取上限」等群組文案，Web Preview 只依 schema 渲染 `.bounds-group`，不在 JS 裡推理 source type 或 bounds 規則；同時補上 file-index Web API regression，確認 `version` 與 `version_limit` 分離，避免版本上限再次被誤當作實際版本。驗證：`node --check frontends\web\static\app.js` OK；`py -3 -B -m py_compile api_launcher\crawler_asset_display.py tests\test_web_preview.py` OK；`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets tests.test_tk_dialogs -v` 69 tests OK；`git diff --check` OK（僅 docs CRLF warning）；`scripts\pre_push_smoke_brief.cmd` 667 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke success。殘餘風險：Web 表單分組已視覺化，但尚未用 Browser / IDE Live Preview 做截圖比對；若要進一步調整 UI 密度，請在本地 clone 用 port-scan server 或 IDE Live Preview 驗證。 |
| 09:27 | Crawler Asset Bound Form Contract | **CHECKPOINT** | `ece4c0c` | `26427068177` | Split crawler asset version limit facet | 將 crawler asset 動態界域 schema 中的 `version` 與 `version_limit` 正式拆開：`version` 只代表明確版本字串，`version_limit` 才帶預設 `1` 並控制未指定版本時最多取幾個版本。這避免 Tk/Web 共用 form spec 時，把表單預設值 `1` 誤送成 `selected_versions["*"] = ("1",)`，導致下載計畫被假版本擋掉。同步 GTD、handoff、discovery notes，並新增 regression test `test_blank_version_keeps_limit_without_selecting_fake_version`。驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_bounds.py tests\test_crawler_assets.py` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_source_download tests.test_tk_dialogs tests.test_web_preview -v` 76 tests OK；`git diff --check` OK（僅 docs CRLF warning）；`scripts\pre_push_smoke_brief.cmd` 666 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke success。殘餘風險：Web/Tk 目前仍是自由文字版本輸入；後續 schema probe 或 dataset-specific candidate view 應提供真正版本清單。 |
| 09:16 | Crawler Asset Download Plan | **CHECKPOINT** | `35ea7ae` | `26426757385` | Honor crawler asset version bounds | 修正 crawler asset 動態界域表單中的 `version` 欄位只被當成 `version_limit`、沒有真正選版本的缺口。`source_download_options_from_crawler_asset_payload()` 現在會把 source-level version facet 轉成 `selected_versions["*"]`，`selected_version_options()` 會把 wildcard selector 套到 crawler 回傳的 dataset；使用者輸入 `2025-01-02` 這類版本時，下載計畫會選該版本而不是回落到第一個或最新版。同步 `PROJECT_GTD`、`AGENT_HANDOFF`、`DATASET_DISCOVERY_NOTES`，並新增 regression test 鎖住 source-level version selection。驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_service.py api_launcher\source_download.py tests\test_crawler_assets.py` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_source_download tests.test_dataset_download_plan tests.test_adapter_plan_resolver -v` 103 tests OK；`git diff --check` OK（僅 docs CRLF warning）；`scripts\pre_push_smoke_brief.cmd` 665 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke success。殘餘風險：wildcard selector 是 source-level 表單的橋接策略；未來若 UI 已能針對單一 dataset 顯示版本清單，應改用 dataset_uid/dataset_id keyed selector 覆蓋 wildcard。 |
| 08:58 | Discovery / Detector Robustness | **CHECKPOINT** | `f01fa99` | `26426273093` | Guard source pattern fetcher failures | Source pattern detector 入口加上 guarded fetcher。自訂 fetcher、plugin probe 或測試替身在 timeout、HTTP、JSON 或實作錯誤時會回傳 `None` probe result，安全降級成 `unknown` / review，不會中止整段 discovery；新增 fetcher exception 與 malformed JSON regression tests，並同步 GTD、handoff、discovery notes 的 detector contract。驗證：`py -3 -B -m py_compile api_launcher\crawlers\source_patterns.py tests\test_source_patterns.py` OK；`py -3 -B -m unittest tests.test_source_patterns tests.test_source_pattern_drafts tests.test_dataset_discovery` 67 tests OK；`git diff --check` OK（僅 docs CRLF warning）；`scripts\pre_push_smoke_brief.cmd` 664 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke success。殘餘風險：detector 只收斂 probe failure，不負責產生 warning bucket；較細錯誤說明仍應留給 crawler audit / source profile 層。 |

今日主要開發階段：**Web Preview / Crawler Asset Visualization**。把 `tem/ui-aseat-ui` 的設計節奏收斂成 RRKAL 可用的 Web Preview，而不是照搬 Aseat/Steam 詞彙；新增後端 detail payload 的 `flow_steps`，讓 Web/Tk/Qt 以後能共用「seed -> source pattern -> bounds -> download plan -> review gate」這條流程狀態。此 checkpoint 同時補強 OGC/WMS 與 source pattern draft 測試，維持 crawler/source-pattern 主線的 registry 化方向。

| 時間 | 開發階段 | 狀態 | SHA | Run | Commit | 主要內容 |
| --- | --- | --- | --- | --- | --- | --- |
| 19:18 | Discovery / Crawler Run Handoff | **LOCAL PASS / CI 待查** | `b04f916` | `local pre-push` | Add crawler run record payloads | 新增 `api_launcher/crawler_run_records.py`，讓 crawler listing 與 download-plan build 都輸出 compact `run_record`，包含 stage、status、outcome bucket、候選/下載/review/error/warning/duplicate counts、signature、`next_action`、`record_key`、`storage_lane=structured_event_log` 與 `future_sqlite_table=crawler_run_registry`；Tk/Web/Qt/agent 後續應讀同一份 payload，不再從分散欄位自行推理 run 狀態。同步更新 GTD、handoff 與 discovery notes。驗證：`py -3 -B -m py_compile api_launcher\crawler_run_records.py api_launcher\crawler_asset_service.py tests\test_crawler_assets.py` OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview tests.test_tk_dialogs -v` 89 tests OK；`scripts\pre_push_smoke_brief.cmd` 690 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`。Push 已到 `origin/main`，但 GitHub Actions 仍未為此 SHA enqueue 新 run，延續前一輪 Actions/dispatch 異常，因此本列不標成 CHECKPOINT。 |
| 08:45 | Web Preview / Content Parser Display | **CHECKPOINT** | `4dc5add` | `26425907912` | Show content review badge in web preview | 將 Web Preview 的界域表單狀態列接上後端 `plan_outcome.content_review` badge payload：`index.html` 新增 `contentReviewBadge`，`app.js` 以 `setContentReviewBadge()` 顯示 label/tone/title，`styles.css` 補上不同 tone 的內容格式待辦徽章樣式；使用者按下建立下載計畫後，若 NetCDF/GeoTIFF/ZIP 等下載物仍需 content parser / adapter review，Web Preview 會在表單狀態旁顯示「內容格式待辦」，而不是只藏在任務列或 JSON。同步 GTD、handoff 與 USER_GUIDE。驗證：`node --check frontends\web\static\app.js` OK、`py_compile` OK、`py -3 -B -m unittest tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets` 66 tests OK、`git diff --check` OK（僅 docs CRLF warning）、`scripts\pre_push_smoke_brief.cmd` 662 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：目前是界域表單狀態 badge，尚未把 Adapter resolving 結果回寫成卡片牆 badge / resolved-plan passport 元件。 |
| 02:51 | Crawler Asset UI / Content Parser Display | **CHECKPOINT** | `4cc9ad7` | `26415243175` | Record content review badge events | 將 Tk `crawler_asset_plan_outcome_recorded` structured event 也補上 `content_review` badge payload，讓 event log / handoff / 重開 UI 時不只保存短文字 `content_review_label`，也能保存 label、tone、count、has_review 等機器可讀狀態；事件恢復時會優先讀事件中的 badge label，舊事件仍可從 resolved plan 回推。同步 GTD 與 handoff，並新增 headless 測試驗證事件 context 內容。驗證：`py_compile`、`py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview tests.test_crawler_assets` 66 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 662 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：事件已保存 badge payload，但正式卡片 badge 元件與跨 UI 視覺呈現仍待後續切片。 |
| 02:41 | Crawler Asset UI / Content Parser Display | **CHECKPOINT** | `21348c0` | `26414920109` | Add content review badge payload | 將 `crawler_asset_plan_outcome_payload()` 的 content parser 摘要從單純短字串擴充為 `content_review` badge payload，包含 `display_label`、`display_tone`、`count`、`has_review` 與原始 bucket 清單，同時保留 `content_review_label` 作為相容短文字。這讓 Tk/Web/Qt 後續能直接共用同一份內容 Parser 待辦徽章資料，不必各自推理顏色、數量與是否需要 review。同步 GTD 與 handoff。驗證：`py_compile`、`py -3 -B -m unittest tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets` 65 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 661 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：目前只是後端 badge contract，Web/Tk 卡片樣式與 Qt/QSS 對照仍需後續 UI slice 承接。 |
| 02:32 | Crawler Asset UI / Content Parser Display | **CHECKPOINT** | `e8f32e6` | `26414617925` | Show content parser review in crawler summary | 將爬蟲資產「建立下載計畫」後的 popup 摘要也接上 `content_review_label`：當 resolved plan 內含 NetCDF/GeoTIFF/ZIP 等仍需 content parser review 的項目時，摘要會直接顯示「內容格式待辦：內容 Parser 待辦 1」，避免使用者只在表格或 Passport 才看見後續待辦。同步 GTD 與 handoff，並新增 headless 測試鎖住此提示文字。驗證：`py_compile`、`py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview tests.test_crawler_assets` 65 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 661 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：目前仍是 popup 文字提示，正式 card badge / resolved-plan passport 元件與 Web/Tk/Qt 視覺統一仍待下一輪收斂。 |
| 02:20 | Crawler Asset UI / Content Parser Display | **CHECKPOINT** | `ec34f09` | `26414188591` | Show content parser review on crawler assets | 將 resolved plan 的 content parser 摘要回寫到爬蟲資產表格與 Passport：`crawler_asset_plan_outcome_payload()` 現在會帶出 `adapter_review` 與 `content_review_label`，Tk `crawler_asset_plan_outcomes` 旁新增 `crawler_asset_content_review_outcomes`，事件恢復時也可從 resolved plan 回推「內容 Parser 待辦 1」這類短標籤。這讓使用者在爬蟲資產分頁能看見不是只有 Adapter 待辦數，也能看到下載物內容格式仍需 parser review。同步 GTD 與 handoff。驗證：`py_compile`、`py -3 -B -m unittest tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets tests.test_dataset_download_plan` 82 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 660 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：目前是短文字 badge，正式 Web/Tk/Qt 卡片元件與更完整 resolved-plan passport 仍待下一輪 UIUX 收斂。 |
| 02:10 | Tk Download Plan / Content Parser Display | **CHECKPOINT** | `3828f0f` | `26413843634` | Label content parser status in download plan | 將下載器面板 import 欄位從 raw `manual_review_required` 收斂成共用 display contract：新增 `plan_entry_content_status_payload()`，Tk `import_status_label()` 對 NetCDF/GeoTIFF/ZIP 等需要 parser review 的項目會顯示「內容 Parser 待辦 / Content parser needed」，並附上 source format 與 parser id。這讓使用者在下載計畫面板中就能看出「不是不能下載，而是下載後仍需內容 parser / transform review」。同步 GTD 與 handoff。驗證：`py_compile`、`node --check frontends\web\static\app.js`、`py -3 -B -m unittest tests.test_web_preview tests.test_tk_dialogs tests.test_dataset_download_plan tests.test_adapter_plan_resolver` 114 tests OK、`scripts\pre_push_smoke_brief.cmd` 660 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：crawler asset 卡片 badge / resolved-plan passport 尚未顯示同一組 content parser 摘要。 |
| 01:58 | Web Preview / Content Parser Display | **CHECKPOINT** | `1b63d60` | `26413413499` | Show content parser buckets in web preview | 將 Adapter Review 的 content parser summary 推進到 Web Preview display schema：`adapter_review_display_payload()` 現在輸出 `by_content_review_bucket`、`by_content_parser`、`content_review_buckets` 與 `content_parsers`，Web JS 任務列會顯示「內容格式待辦」，讓瀏覽器預覽可以看到 NetCDF/GeoTIFF/ZIP 等下載物停在 parser review 的原因，而不是只看到一般 Adapter 待辦。同步 GTD 與 handoff。驗證：`node --check frontends\web\static\app.js`、`py_compile`、`py -3 -B -m unittest tests.test_web_preview tests.test_dataset_download_plan tests.test_tk_dialogs` 58 tests OK、`scripts\pre_push_smoke_brief.cmd` 658 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：download plan 面板與 crawler asset 卡片 badge 尚未顯示同一組 content parser 摘要，下一輪應沿用此 display contract。 |
| 01:45 | Web Preview / Content Parser Display | **CHECKPOINT** | `cc8fc6d` | `26412968203` | Surface content parser summary in adapter review | 將 Adapter Review item 與 JSON summary 接上 `content_detection` / `content_parser` 摘要：每筆待辦現在會輸出 `content_source_format`、`content_family`、`content_parser_id`、`content_import_status`、`content_review_bucket` 與 `content_reason`，summary 也新增 `by_content_review_bucket` / `by_content_parser`，Tk detail 可直接看到 NetCDF/GeoTIFF/ZIP 等內容格式與 parser review 下一步。同步 GTD 與 handoff。驗證：臨時 pycache `py_compile`、`py -3 -B -m unittest tests.test_tk_dialogs tests.test_dataset_download_plan tests.test_adapter_plan_resolver tests.test_content_registry` 115 tests OK、`scripts\pre_push_smoke_brief.cmd` 658 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：download plan 面板與 Web Preview 卡片尚未顯示同一組 content parser 摘要，下一輪應沿用這份 JSON contract。 |
| 01:32 | Web Preview / Outcome Display Schema | **CHECKPOINT** | `5862d7f` | `26412488719` | Use shared adapter review labels in Tk | 將 Tk Adapter 待辦表格的 outcome 欄接到共用 `adapter_review_outcome_label()`，表格顯示「來源解析待辦」等人類可讀短標籤，detail 仍保留 raw `outcome_bucket` 供 agent / JSON 比對。同步 GTD 與 handoff，延續 Web/Tk/Qt 共用 display schema 的收斂方向。驗證：臨時 pycache `py_compile`、`py -3 -B -m unittest tests.test_tk_dialogs tests.test_web_preview` 40 tests OK、`scripts\pre_push_smoke_brief.cmd` 658 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：Adapter resolving 結果仍未回寫到爬蟲資產卡片 badge / 待辦徽章，下一輪可先做 backend event/payload，再接 UI。 |
| 01:20 | Web Preview / Outcome Display Schema | **CHECKPOINT** | `c43115d` | `26412103842` | Use shared outcome labels in Tk crawler assets | 將 Tk 爬蟲資產表格短狀態接到共用 `api_launcher/crawler_asset_display.py`。`crawler_asset_plan_outcome_payload()` 現在輸出 `short_label`，Tk `crawler_asset_plan_outcome_label()` 只作薄包裝，不再自行維護 `ready_to_download`、`partial_review_required`、`review_required`、`zero_candidates`、`blocked` 分支文案；Web Preview test 也鎖住同一份 `short_label` contract。同步更新 GTD、handoff 與 Web Preview UIUX 文件。驗證：`node --check frontends\web\static\app.js`、臨時 pycache `py_compile`、`py -3 -B -m unittest tests.test_web_preview tests.test_tk_dialogs tests.test_crawler_assets` 62 tests OK、`scripts\pre_push_smoke_brief.cmd` 658 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：Adapter resolving 結果尚未回寫成卡片 badge / 待辦徽章，下一輪可沿用同一份 outcome/tone contract。 |
| 01:04 | Web Preview / Outcome Display Schema | **CHECKPOINT** | `39380bc` | `26411538939` | Share crawler asset outcome display schema | 將爬蟲資產的 plan outcome 與 Adapter review 摘要也收斂進 `api_launcher/crawler_asset_display.py`。Web Preview API 現在回傳 `plan_outcome` 與 `adapter_review` payload，包含 `outcome_bucket`、`display_label`、`display_tone`、`summary`、`next_action_label` 與 Adapter review `by_outcome` 顯示分組；`frontends/web/static/app.js` 只呈現後端 payload，不自行推理業務分支。同步 `WEB_PREVIEW_UIUX`、GTD 與 handoff，並新增 Web Preview tests 鎖住 shared display schema。驗證：`node --check frontends\web\static\app.js`、臨時 pycache `py_compile`、`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets tests.test_tk_dialogs tests.test_dataset_download_plan` 80 tests OK、`scripts\pre_push_smoke_brief.cmd` 658 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：Tk 爬蟲資產分頁仍有自己的 outcome label helper，下一步應逐步改用同一份 display schema 並把 Adapter resolving 結果回寫成卡片 badge。 |
| 00:49 | Web Preview / Shared Display Schema | **CHECKPOINT** | `b71c28f` | `26410994517` | Share crawler asset display schema | 新增 `api_launcher/crawler_asset_display.py`，把 Web Preview 先前分散在 `preview_api.py` 與 `app.js` 的流程條、capability 顯示文字、界域欄位顯示文字收斂成後端共用 schema。`crawler_asset_detail` 現在回傳 `flow_steps`、capability `display_label`、bound field `display_label` / `display_help`，Web 端只優先呈現後端 payload，JS 對照表降為 fallback。這讓未來 Tk/Qt 可共用同一套顯示語彙，不必重寫 crawler readiness 判斷。驗證：`node --check frontends\web\static\app.js`、臨時 pycache `py_compile`、`py -3 -B -m unittest tests.test_web_preview tests.test_crawler_assets tests.test_source_patterns tests.test_source_pattern_drafts` 61 tests OK、`scripts\pre_push_smoke_brief.cmd` 656 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：Tk 尚未改用 display schema，下一步應接 plan outcome / adapter review 視覺化並逐步讓 Tk crawler asset 分頁共用此 schema。 |
| 00:33 | Web Preview / Crawler Asset Visualization | **CHECKPOINT** | `a23d1f9` | `26410424306` | Add web preview flow visualization | 新增 `frontends/web/` stdlib Web Preview、`frontends/shared/ui_tokens.json`、`scripts/run_web_preview.cmd` 與 `docs/WEB_PREVIEW_UIUX.zh-TW.md`，將爬蟲資產清單、資產護照、界域表單、下載計畫預覽、後端 JSON 與新 `flow_steps` 視覺化接到 `api_launcher` service。Web 端以 RRKAL 詞彙顯示 `爬蟲資產`、`資產護照`、`後端流程狀態`、`界域輸入`，並用 `field_id` / `capability_id` 映射乾淨的繁中 UI label，避免後端舊 mojibake 字串外漏。補強 `tests/test_web_preview.py`，並延伸 source pattern / source draft / OGC WMS / crawler asset 測試；本機 `scripts\pre_push_smoke_brief.cmd` 通過 656 tests / 4 skipped，MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。剩餘風險：Web Preview 尚是 UIUX lab，不是正式生產前端；下一步應把 `flow_steps` / label mapping 抽成 shared display schema，讓 Tk/Qt 也能共用。 |

### 2026-05-25

目前開發階段：**Discovery / Crawler Backend Hardening**。主線從昨天晚間的 source pattern detector 與 crawler asset contract，推進到常見資料入口範式的穩定化：補強 ERDDAP/STAC/OGC WMS/HTML file index 的 detector 與 source draft 行為，讓 HTML index 可 bounded crawl 巢狀頁面並抽出更多資料檔案候選；接著把下載後內容格式判斷收斂進 content parser registry 與 adapter plan resolver，讓 GeoTIFF/COG/GeoPackage/Shapefile ZIP/FlatGeobuf/PMTiles/MBTiles、GRIB/GRIB2、CDF/HDF/HDF5、SQLite/DB snapshot 等非表格資產先進 parser review，不再被假裝成可直接匯入的 CSV/JSON。今日所有列出的 checkpoint 均已由 GitHub Actions 驗證成功；尚未 push/CI 驗證的後續工作不列為 checkpoint。

| 時間 | 開發階段 | 標記 | SHA | Run | 原始標題 | 中文說明 |
| --- | --- | --- | --- | --- | --- | --- |
| 22:32 | Crawler Asset UI / Event Persistence | **CHECKPOINT** | `7679dce` | `26405679698` | Persist crawler asset plan outcomes through events | 將爬蟲資產送進下載器後的可視狀態提升成 structured event：Tk 會寫入 `crawler_asset_plan_outcome_recorded`，內容包含 `asset_id`、`outcome_bucket`、`outcome_label`、`review_queue_count`、`resolved_plan` 與 `user_next_action`。爬蟲資產分頁啟動時會讀最近事件，恢復表格短狀態與 Crawler Passport 的 resolved plan 待辦入口，讓結果不只存在記憶體裡。驗證：temp `PYTHONPYCACHEPREFIX` `py_compile` OK；`py -B -m unittest tests.test_tk_dialogs tests.test_crawler_assets` 51 tests OK；`py -B -m unittest discover -s tests` 642 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 642 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。剩餘風險：目前恢復的是最近事件與 resolved plan 檔案，正式長期履歷仍應收斂到 crawler asset profile / run registry。 |
| 22:21 | Crawler Asset UI / Adapter Review Routing | **CHECKPOINT** | `20bf82d` | `26405221159` | Open crawler asset adapter review queue from passport | 將 Crawler Passport 補成可接續操作的入口：爬蟲資產送進下載器後，Tk 會在同一輪 UI session 保留該 asset 的 resolved plan；右側新增 `開本次 Adapter 待辦`，可直接打開剛才那一次 resolved plan 裡的 adapter review items。這避免使用者只看到 `待 Adapter` 標籤卻不知道下一步在哪裡，也避免 Tk 自己重寫 adapter review 解析邏輯；實際清單仍交給既有 `AdapterReviewDialog` 與 `api_launcher.adapter_review`。驗證：temp `PYTHONPYCACHEPREFIX` `py_compile` OK；`py -B -m unittest tests.test_tk_dialogs tests.test_crawler_assets` 50 tests OK；`py -B -m unittest discover -s tests` 641 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 641 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。剩餘風險：目前 resolved plan 履歷仍只存在 UI session；下一步要把同一結果收斂到 profile/event-log，並做卡片 badge。 |
| 22:07 | Crawler Asset UI / Outcome Routing | **CHECKPOINT** | `9eace3d` | `26404630480` | Show crawler asset plan outcomes in UI | 將爬蟲資產「送進下載器」後的結果留在可操作 UI 裡：表格「下一步」欄與右側 Crawler Passport 會在同一輪 UI session 中保留 `已加入`、`已加入 / 待辦`、`待 Adapter`、`零候選` 或 blocked reason，讓使用者回到爬蟲資產分頁時仍能看出剛剛發生什麼，而不是只靠一次性彈窗。這一層仍只顯示後端 `outcome_bucket` 的短標籤，長期跨 session 履歷要等 profile / event log 收斂。驗證：`git diff --check` OK；temp `PYTHONPYCACHEPREFIX` `py_compile` OK；`py -B -m unittest tests.test_tk_dialogs tests.test_crawler_assets` 48 tests OK；`py -B -m unittest discover -s tests` 639 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 639 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。剩餘風險：這是 session-level UI 回饋，不是跨啟動持久事件；下一步適合做卡片 badge、Adapter review 入口與 event-log persistence。 |
| 21:49 | Crawler Asset UI / Outcome Routing | **CHECKPOINT** | `425c0db` | `26403845203` | Polish crawler asset plan outcomes | 將爬蟲資產送進下載器後的結果收斂成後端 `outcome_bucket` / `user_next_action`，包含 `ready_to_download`、`partial_review_required`、`review_required`、`zero_candidates`、`empty_plan`、`blocked`。Tk UI 不再自己解析 resolved plan，而是依後端狀態顯示可直接下載、部分待辦、需 Adapter review、零候選或被封存/停用的下一步提示；成功訊息會指向下載器的開始 / 暫停隊列，待辦訊息會指向 Adapter review 或界域調整。驗證：`py -B -m py_compile api_launcher\crawler_asset_service.py frontends\tk\crawler_asset_workflows.py tests\test_crawler_assets.py tests\test_tk_dialogs.py` OK；`py -B -m unittest tests.test_crawler_assets tests.test_tk_dialogs` 46 tests OK；`py -B -m unittest discover -s tests` 637 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 637 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。剩餘風險：目前仍是訊息層狀態收斂，下一輪要把同一組 bucket 顯示成爬蟲卡片 badge 與更直覺的 Adapter review 入口。 |
| 21:32 | Crawler Asset Download Plan | **CHECKPOINT** | `53a2432` | `26403131319` | Connect crawler assets to download plans | 將爬蟲資產界域 payload 正式接進下載計畫 service：`build_crawler_asset_download_plan()` 會讀取 crawler asset source/profile，擋下 disabled/archived source，把 `CrawlerAssetBoundPayload` 轉成 `SourceDownloadBounds` 後交給既有 `build_source_download_plan()`；Tk「送進下載器」現在會背景建立 `state/crawler_asset_plans/*.original.json` 與 `*.resolved.json`，只把 resolved plan 中可直接下載的項目加入下載器，adapter review 項目保留為待辦。驗證：`py -B -m py_compile api_launcher\crawler_asset_service.py frontends\tk\crawler_asset_workflows.py` OK；`py -B -m unittest tests.test_crawler_assets tests.test_source_download tests.test_tk_dialogs` 51 tests OK；`py -B -m unittest discover -s tests` 634 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 634 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。剩餘風險：UI 已能把 direct entries 送入下載器，但 review-required / blocked reason 還需要更清楚的卡片徽章與待辦引導。 |
| 20:27 | Crawler Query Robustness | **CHECKPOINT** | `734deb1` | `26400477554` | Preserve fragments in crawler query URLs | 修正共用 crawler query builder `search_endpoint_url()`：改用 `urllib.parse` 寫回 query，避免來源 URL 帶 `#fragment` 時把新增查詢參數接到 fragment 後面，造成 CKAN、CMR、Dataverse、GBIF、OGC、OpenAlex、Zenodo、WMS 等共用 helper 的 crawler 形成錯誤 endpoint。新增 `tests/test_crawler_fetch.py`，覆蓋 fragment、既有 query、空參數三種行為；同步 `PROJECT_GTD` 與 discovery notes，要求 crawler URL 組裝走 helper/normalizer，不手寫字串相加。驗證：`tests.test_crawler_fetch tests.test_dataset_discovery` 35 tests OK；完整 `unittest discover -s tests` 632 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 632 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 20:16 | Source Pattern CLI Contract | **CHECKPOINT** | `fe8d453` | `26400037216` | Use backend source pattern confidence default in CLI | 將 CLI `--source-draft-detector-min-confidence` 的預設值改為引用後端 `DEFAULT_PATTERN_MINIMUM_CONFIDENCE`，讓命令列、Tk source draft 表單與 detector service 共用同一個人工 review 門檻，不再由 CLI 硬寫 `0.35`。既有 CLI source draft regression test 也確認未指定參數時會把後端預設值傳給 detector。同步 `PROJECT_GTD` 與 discovery notes。驗證：`tests.test_discovery_drafts tests.test_source_pattern_drafts` 27 tests OK；完整 `unittest discover -s tests` 629 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 629 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 20:03 | Source Pattern UI Contract | **CHECKPOINT** | `4f93af3` | `26399553226` | Use backend source pattern confidence default in Tk | 將 Tk source draft dialog 與 crawler asset workflow 的最低信心門檻改為引用後端 `DEFAULT_PATTERN_MINIMUM_CONFIDENCE`，移除 UI 端硬寫 `0.35` 的漂移風險。Tk 仍只負責收集輸入與呼叫服務，detector contract 由 `api_launcher.crawlers.source_patterns` 維護。新增 headless Tk workflow regression test，確認使用者未填 minimum confidence 時會送出後端預設值。驗證：`tests.test_tk_dialogs tests.test_source_pattern_drafts` 36 tests OK；完整 `unittest discover -s tests` 629 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 629 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 19:50 | Source Draft Safety | **CHECKPOINT** | `62c1574` | `26398990857` | Guard low-confidence source drafts | 強化 `source_pattern_drafts.py` 的寫入前安全邊界：即使注入 detector 回傳非 unknown 且帶 `source_type_hint`，只要 confidence 低於 `DEFAULT_PATTERN_MINIMUM_CONFIDENCE`，仍會停在 review，不會寫成 local source draft。這避免未來測試替身、外部 detector 或 plugin adapter 繞過 detector 的 unknown fallback。同步 `PROJECT_GTD` 與 discovery notes，新增低信心 detector regression test。驗證：`tests.test_source_pattern_drafts tests.test_source_patterns` 28 tests OK；完整 `unittest discover -s tests` 628 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 628 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 19:38 | Source Pattern Contract | **CHECKPOINT** | `5733891` | `26398548754` | Centralize source pattern unknown threshold | 將 source pattern detector 的 unknown fallback 與最低信心門檻集中成 `UNKNOWN_PATTERN_ID` 與 `DEFAULT_PATTERN_MINIMUM_CONFIDENCE`，並讓 source draft writer 與 regression tests 共用同一契約，避免後續 adapter/UI 各自硬寫 `"unknown"` 或 `0.35`。同步 `PROJECT_GTD` 與 discovery notes，明確規定未知/低信心 URL 應停在 review，不應被硬猜成可下載來源。驗證：`tests.test_source_patterns tests.test_source_pattern_drafts` 27 tests OK；完整 `unittest discover -s tests` 627 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 627 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 19:22 | Source Type Registry | **CHECKPOINT** | `dcbee41` | `26397913391` | Centralize file index source type checks | 新增 `api_launcher/crawlers/source_type_registry.py`，把 HTML/file-index 類來源判斷集中成 `source_type_is_file_index()` / `source_uses_file_index()`，並讓 bounds facet、crawler asset capability 與 source pattern draft 共用同一判斷；後續若新增 FTP-like 或 directory-listing 來源，不需要在 UI/capability 層散寫 `source_type == "html_file_index"`。同步 `PROJECT_GTD` 與 discovery notes，並補 source draft / crawler asset regression assertions。驗證：`tests.test_source_pattern_drafts tests.test_crawler_assets` 26 tests OK；完整 `unittest discover -s tests` 627 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 627 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 19:08 | Download Plan Contract | **CHECKPOINT** | `490c75a` | `26397393782` | Centralize plan source type gates | 將 `api_launcher/plans.py` 中 CMR collection 與 DataCite/OpenAlex research metadata 的 adapter-review 安全擋板收斂成 `CMR_COLLECTION_*`、`RESEARCH_METADATA_*` registry/set 與具名 helper，避免日後新增來源時把 `if source_type == ...` 分支散回 resolver 或 UI。同步 `PROJECT_GTD` 與 discovery notes，並新增 regression test 鎖住 CMR、DOI/OpenAlex host/source/native format 判斷。驗證：`tests.test_dataset_download_plan` 18 tests OK；完整 `unittest discover -s tests` 627 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 627 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 18:55 | Crawler Asset UX Contract | **CHECKPOINT** | `6994a61` | `26396895131` | Centralize crawler asset surface labels | 將 crawler asset 的入口表面標籤收斂到 `SOURCE_SURFACE_LABELS` registry，避免 UI 卡片/passport 自己用 source_type 分支猜測入口形態；file index 仍標示為 `file_index`，WMS capabilities 現在標示為 `map_service`，未知 API-like endpoint 保留 endpoint shape fallback。同步 GTD 與 discovery notes，並補上 WMS source surface regression test。驗證：crawler asset targeted tests 16 OK；`py_compile` OK；完整 `unittest discover -s tests` 626 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 626 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 18:45 | Dataset Seed Coverage | **CHECKPOINT** | `2623dc4` | `26396504766` | Treat WMS capabilities as seed entry listings | 修正 seed coverage 對 `ogc_wms_capabilities` 的能力判斷：WMS capabilities XML 已有 crawler handler，且本身是可列 layer 的入口清單，因此歸入 entry-listing seed capability。這避免展示報告與 crawler asset 卡片把 WMS 誤標為 `add_or_repair_supported_crawler_handler`。同步 GTD 與 discovery notes，並新增 `source_seed_coverage()` regression test。驗證：crawler asset/source pattern/discovery targeted tests 74 OK；`py_compile` OK；完整 `unittest discover -s tests` 625 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 625 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 18:32 | Crawler Asset Bounds | **CHECKPOINT** | `b23f135` | `26396017212` | Centralize crawler asset bound facets | 將 crawler asset 的 source_type -> bounds facets 分派從 `bounds_facets_for_source()` 的 if 鏈收斂到 `SOURCE_BOUND_FACETS` registry，讓 Tk/Qt/CLI 產生動態界域表單時共用同一份後端契約；`file_url_regex` 仍會覆蓋成 file-index facets。補上 `ogc_wms_capabilities` 的 collection / bbox / time / format / limit 界域，避免已接 crawler 的 WMS 入口退回只有 `limit`。同步 GTD 與 discovery notes，並新增 registry coverage regression test。驗證：crawler asset/source pattern/discovery targeted tests 73 OK；`py_compile` OK；完整 `unittest discover -s tests` 624 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 624 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 18:11 | Discovery / Source Draft Normalization | **CHECKPOINT** | `cbb9deb` | `26395213453` | Centralize source draft type inference | 將 provider candidate 轉成 source draft 時的 URL 推斷從長串 `if` 分支收斂到 `SOURCE_TYPE_INFERENCE_RULES` registry；`infer_source_type()` 只套用已支援 crawler 的保守 predicate，無法辨識的 URL 仍留在 review，不用 NASA/NOAA 或單一平台名稱硬猜 source type。同步 GTD 與 discovery notes，要求新增來源時補 inference rule、endpoint normalizer 與 unknown/failure regression test。驗證：targeted discovery/source-pattern tests 74 OK；`py_compile` OK；`git diff --check` OK；完整 `unittest discover -s tests` 623 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 623 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 18:01 | Discovery / Source Draft Normalization | **CHECKPOINT** | `c31dc41` | `26394798311` | Centralize source draft endpoint normalizers | 將 source draft endpoint 正規化從 `source_type` if 鏈收斂到 `SOURCE_ENDPOINT_NORMALIZERS` registry；每個 normalizer 只處理自己範式的 path/query 清理，並由 registry coverage 測試確認 normalizer 僅登記已接 crawler 的 `SUPPORTED_DATASET_SOURCE_TYPES`。`html_file_index` 不再被誤當 source type 正規化，未知 URL 維持 review 邊界。同步 GTD 與 discovery notes，明確要求後續新增來源時沿用 normalizer registry。驗證：targeted discovery/source-pattern tests 73 OK；`py_compile` OK；`git diff --check` OK；完整 `unittest discover -s tests` 622 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 622 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 15:04 | Discovery / Source Draft Normalization | **CHECKPOINT** | `972c5e3` | `26387995380` | Normalize CKAN and Socrata source drafts | 修正 detector 成功後 source draft endpoint 仍停在入口頁/深層頁的缺口：CKAN 首頁、dataset 頁或 `package_show` URL 現在會正規化到同 host 的 `/api/3/action/package_search`；Socrata portal/resource URL 會正規化到 `https://api.us.socrata.com/api/catalog/v1?domains=<portal-host>`，讓後續 crawler audit 讀到 Discovery/Catalog `results` payload，而不是誤打 landing/resource 頁。補上 normalization 與 detected-source draft regression tests，並同步 `PROJECT_GTD` / `DATASET_DISCOVERY_NOTES`。驗證：targeted discovery/source-pattern tests 71 tests OK；`py_compile` OK；`git diff --check` OK（僅 docs CRLF warning）；完整 `unittest discover -s tests` 620 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 620 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 14:45 | Discovery / OGC Crawler | **CHECKPOINT** | `a05eabd` | `26387305406` | Support OGC collections source drafts | 補上 OGC API base URL 到 `/collections` 的 source draft 正規化，讓 detector 判成 `ogc_api_records` 後不會停在 root endpoint；`ogc_records` crawler 現在可解析 `/collections` payload，為每個 collection 產生 reviewable candidate、保留 extent/links metadata，並把主要 `api_url` 指向 collection `items` rel 或 fallback `/collections/{id}/items`。同時修正 normalization/crawler 內行尾註解吞掉賦值的隱性風險，補上 OGC collections payload 與 source draft regression tests。驗證：targeted 138 tests OK、完整 unittest 616 tests / 4 skipped OK、`py_compile` OK、`git diff --check` OK、CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 14:21 | Discovery / Detector Robustness | **CHECKPOINT** | `da9fa10` | `26386542149` | Dedupe OGC detector evidence | 修正 OGC detector 多 endpoint probe 造成的 evidence 重複灌分風險。新測試覆蓋 root 與 `/conformance` 都回傳相同 conformance evidence、`/collections` 另外提供 collections evidence 的情境；detector 會先用去重後 evidence 判斷是否停止，避免尚未探到 `/collections` 就和 STAC 打平而被誤判。驗證：targeted 54 tests OK，CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 14:15 | Discovery / OGC Detector | **CHECKPOINT** | `4846490` | `26386360139` | Probe OGC API conformance endpoints | 補強 OGC API detector：除了使用者貼上的 URL，也 bounded probe `conformance`、`collections` 以及 origin fallback 的同名端點，讓 root endpoint 沒有完整 JSON、但標準 `/conformance` 與 `/collections` 存在的 OGC API Features / Records 入口仍能辨識成 `ogc_api_records`。若只有單獨 `collections` 欄位、沒有 OGC/OpenGIS conformance evidence，仍保持低信心 `unknown`，避免誤判一般 catalog 或 STAC-like payload。驗證：targeted 53 tests OK，CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 14:07 | Content Parser / Naming Hardening | **CHECKPOINT** | `083a6df` | `26386110012` | Clarify geospatial asset format registry | 將 content registry 內部常數從 raster-only 命名修正為 geospatial asset 命名，讓 GeoTIFF/COG、Shapefile、GeoPackage、FlatGeobuf、PMTiles、MBTiles 共用同一個 `geospatial_asset` review 邊界時，不會誤導後續 agent 以為這裡只處理 raster。同步調整 review reason，明確寫出 raster/vector/tile 都應先成為 manifest 或 renderer-ready asset。驗證：targeted 65 tests OK，CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 14:02 | Test / Resolver Contract | **CHECKPOINT** | `a768f81` | `26385938719` | Cover GIS archive resolver contracts | 補上 adapter plan resolver 的 GIS archive/tile 行為契約測試，鎖住 `.shp.zip`、`.fgb`、`.pmtiles`、`.mbtiles` 從 CKAN/DCAT 類 resource metadata 進 direct plan 後，必須保留 `source_format`、目標副檔名與 `geospatial_asset_review` / `content_parser_required`，不可被誤當成可直接 curated import 的表格。驗證：targeted 91 tests OK，CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 13:55 | Content Parser / Geospatial | **CHECKPOINT** | `6bccce9` | `26385727093` | Support GIS archive and tile asset links | 補上 Shapefile ZIP、FlatGeobuf、PMTiles、MBTiles 這批常見 GIS archive/tile asset 支援：HTML detector/source draft 可辨識 `.shp.zip`、`.fgb`、`.pmtiles`、`.mbtiles`，direct download eligibility 接受 `.fgb`、`.pmtiles`、`.mbtiles`，resource resolver 與 content registry 會正規化成 `shapefile`、`flatgeobuf`、`pmtiles`、`mbtiles` 並停在 `geospatial_asset_review`，不自動 curated import。驗證：targeted 90 tests OK、`scripts\pre_push_smoke_brief.cmd` 611 tests / 4 skipped + MVP demo smoke `row_count=3`，CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 11:59 | Content Parser / Database Snapshot | **CHECKPOINT** | `7fbfd9a` | `26382438059` | Route database snapshots through parser review | 將 `.db`、`.sqlite`、`.sqlite3` 這類資料庫快照辨識為可下載 raw artifact，但下載後路由到 `database_snapshot_review`，保留 source URL、格式與 parser review 狀態，不把外部資料庫檔直接當成 curated table 匯入。 |
| 11:49 | Content Parser / Scientific Grid | **CHECKPOINT** | `daca822` | `26382172324` | Support CDF and HDF5 crawler asset links | 補強 CDF/HDF/HDF5/H5 連結辨識、direct eligibility、format normalization 與 content parser review，讓科學陣列資料可進 plan/review 流程，但仍不自動解析或匯入。 |
| 11:37 | Content Parser / Scientific Grid | **CHECKPOINT** | `3ae733a` | `26381854086` | Support GRIB2 crawler asset links | 補上 GRIB/GRIB2/GRB2 從 detector、source draft、direct download eligibility、content registry 到 adapter resolver 的支援，天氣/氣象格點資料會被標成 scientific grid review。 |
| 11:26 | Adapter Resolver | **CHECKPOINT** | `c92646f` | `26381573507` | Normalize URL suffixes for resource resolution | 強化 URL suffix 推論與 compound suffix normalization，讓 resolver 即使缺少 media type / format metadata，也能從檔名安全推斷 source format。 |
| 11:18 | Discovery / HTML Pattern | **CHECKPOINT** | `880cde0` | `26381353429` | Share HTML data file pattern vocabulary | 把 HTML file index detector 與 source draft 的資料副檔名 vocabulary 合併，避免 detector 看得到的格式在 draft/audit 端漏掉。 |
| 11:09 | Discovery / HTML Source Draft | **CHECKPOINT** | `ed4855e` | `26381124089` | Align HTML source drafts with geospatial file links | 讓 HTML source draft 的 follow-up audit regex 同步支援 geospatial / scientific / archive 格式，source draft 能把更多真實資料檔候選交給 crawler audit。 |
| 11:02 | Download Eligibility / Geospatial | **CHECKPOINT** | `e842679` | `26380954416` | Treat GeoPackage URLs as downloadable assets | 將 GeoPackage URL 視為可下載 geospatial asset，並保持下載後進 geospatial parser review，不走 CSV/JSON importer。 |
| 10:53 | Discovery / HTML Pattern | **CHECKPOINT** | `411a21b` | `26380711784` | Detect compound HTML index data links | HTML file index detector 開始辨識 compound extensions，例如 `.geojson.gz`、`.tar.gz`、`.csv.zst`，降低壓縮/複合資料檔被漏抓的機率。 |
| 10:42 | Content Parser / Geospatial | **CHECKPOINT** | `7d99e71` | `26380431949` | Recognize GeoPackage media types | Content registry 補 GeoPackage MIME / format 判斷，讓 media type 或副檔名任一線索都能落到 geospatial parser review。 |
| 10:35 | Discovery / HTML Source Draft | **CHECKPOINT** | `cfb96f5` | `26380247707` | Broaden HTML index draft file matching | 擴大 HTML source draft 的檔案 regex，支援更多壓縮、科學與 GIS 資產副檔名，讓後續 crawler audit 有足夠候選。 |
| 10:28 | Adapter Resolver / Geospatial | **CHECKPOINT** | `a37e73c` | `26380072877` | Promote geospatial assets to parser review plans | Adapter resolver 可把 GeoTIFF/COG/GeoPackage 等 geospatial direct assets 推入 download plan，但 import status 仍標示 parser review required。 |
| 10:20 | Content Parser / Geospatial | **CHECKPOINT** | `6e93020` | `26379867289` | Recognize geospatial content media types | Content registry 補 geospatial MIME / format normalization，建立 `geospatial_asset_review` 入口，避免 raster/vector 資產被誤當表格。 |
| 10:13 | Discovery / HTML Crawler | **CHECKPOINT** | `1a84655` | `26379682913` | Traverse nested HTML index pages | HTML file index crawler 支援 bounded nested traversal，搭配 same-origin、seen set、max pages 等限制，讓舊式目錄頁可往下一層找資料檔。 |
| 10:08 | Discovery / HTML Crawler | **CHECKPOINT** | `fbaaf3e` | `26379543771` | Follow bounded HTML index pages | HTML file index crawler 從單頁抽連結前進到 bounded full crawl 模式，補上 page limit 與安全 guard，避免無界網站探索。 |
| 09:57 | Discovery / OGC WMS | **CHECKPOINT** | `fce7967` | `26379287898` | Preserve WMS service metadata | WMS capabilities parser 保留 service title / metadata URL 等資訊，候選 dataset 不再只剩 layer id。 |
| 09:51 | Discovery / OGC WMS | **CHECKPOINT** | `a237821` | `26379124943` | Avoid duplicate WMS detector capabilities probes | 修正 WMS detector 對 capabilities URL 的重複 probe，降低不必要網路請求與重複 evidence。 |
| 09:43 | Discovery / OGC WMS | **CHECKPOINT** | `c5e49ff` | `26378925918` | Handle uppercase WMS capabilities queries | WMS detector 能處理大小寫混用的 `SERVICE` / `REQUEST` query，讓真實入口 URL 不因大小寫差異被判 unknown。 |
| 09:38 | Discovery / OGC WMS | **CHECKPOINT** | `77a1e55` | `26378795499` | Prefer WMS GetMap resources for layer candidates | WMS layer candidate 優先保存可用的 GetMap resource URL，讓後續 plan/review 有更接近實際資產的 evidence。 |
| 09:32 | Discovery / ERDDAP | **CHECKPOINT** | `72e340d` | `26378640023` | Normalize ERDDAP detector probes | ERDDAP detector probe 正規化，能從 deeper dataset URL 回到站台 root 探測 `info/index.json`。 |
| 09:30 | Discovery / ERDDAP | **CHECKPOINT** | `b605c09` | `26378585684` | Probe ERDDAP info from site root | ERDDAP detector 增加 site-root info probe，避免只在 dataset URL 上探測而漏判。 |
| 09:24 | Discovery / ERDDAP | **CHECKPOINT** | `b0ea24e` | `26378428029` | Normalize ERDDAP deep source URLs | ERDDAP source draft 能把 `/griddap/`、`/tabledap/` 等 deep URL 正規化到可 audit 的 allDatasets endpoint。 |
| 09:17 | Discovery / STAC | **CHECKPOINT** | `cc73074` | `26378266034` | Accept STAC collections endpoints in detector | STAC detector 接受 `/collections` endpoint 作為 STAC evidence，不再只依賴 root catalog 的 `stac_version`。 |
| 09:11 | Discovery / OGC WMS | **CHECKPOINT** | `7de569b` | `26378120876` | Add OGC WMS crawler support | 新增 OGC WMS capabilities parser/crawler，能從 WMS XML capabilities 產生 layer dataset candidates，補上 OGC WMS 這條常見 GIS 來源範式。 |
| 09:01 | Discovery / Source Pattern | **CHECKPOINT** | `9fa2592` | `26377877861` | Probe source pattern APIs from origin URLs | Source pattern detector 會從 origin URL 推導常見 API probe endpoint，不再只看使用者輸入的完整 URL 字串。 |
| 08:54 | Discovery / HTML Source Draft | **CHECKPOINT** | `16b278c` | `26377717492` | Connect HTML source drafts to file index crawling | HTML source draft 可接回 file index crawling，讓 detector 判成 HTML index 後能自動產生後續 audit 所需 regex/source config。 |
| 08:48 | Discovery / OGC WMS | **CHECKPOINT** | `94954e8` | `26377574124` | Recognize WMS capabilities as OGC sources | Source pattern detector 能把 WMS GetCapabilities XML 判成 OGC/WMS source，而非 generic HTML/XML 或 unknown。 |
| 08:43 | Discovery / Source Draft Safety | **CHECKPOINT** | `2bd6fd4` | `26377439451` | Guard source pattern draft URLs | Source draft creation 加入 URL guard，拒絕 credential-bearing、非 HTTP(S) 或不適合 promotion 的 URL，避免把危險入口寫進 local source config。 |
| 08:37 | Discovery / Source Pattern Tests | **CHECKPOINT** | `54f086d` | `26377306364` | Harden source pattern detector coverage | 補強 CKAN/Socrata/OGC/ambiguous/unknown 等 detector 測試，讓通用蟲不只在 happy path 上通過。 |
| 08:30 | Adapter Resolver / Content Detection | **CHECKPOINT** | `602cb50` | `26377145294` | Add content detection to resolved plan entries | Adapter resolver direct entry 會附上 `content_detection` / `content_parser`，UI/agent 不必重新猜 source format、parser id、review bucket 或 import status。 |
| 08:20 | Content Parser Registry | **CHECKPOINT** | `191a2cc` | `26376895930` | Add content parser capability registry | 新增 `api_launcher/content_registry.py` 作為下載後內容格式分流層，將 CSV/JSON 類表格格式、ZIP/Archive transform review、NetCDF/HDF/Zarr/GeoTIFF/Parquet/PDF/XML/unknown 等分配到明確 import 或 review bucket。 |

## 2026-05-17 至 2026-05-24 回補流水帳

### 2026-05-24

目前開發階段：**MVP Hardening / Tk UI Decoupling / Backend-Core Hardening / Showcase Readiness / Rapid Delivery Recovery**。這一段先完成 `launcher_ui.py` thin shell 收束，再用小切片進入後端解耦；目前已完成多個 CLI compatibility slices，並開始把 `adapter_plan_resolver.py` 的來源專屬 resolver 逐步移到獨立模組，保持舊入口與測試 patch 點相容。中午展示線已新增可重跑的 seed 覆蓋稽核，用來對外說明目前入口爬蟲與本機輸出路徑狀態；展示急改壓力測試已回收成可追蹤展示資產、地端 clone 測試規則與後續正式化任務。

| 時間 | 開發階段 | 狀態 | SHA | Run | 原始提交訊息 | 中文說明 |
| --- | --- | --- | --- | --- | --- | --- |
| 20:40 | Discovery / Crawler / Docs / Workflow | **CHECKPOINT** | `ebc368c` | `26361516298` | Add source pattern URL drafts | 將來源介面 detector 接成可操作的 local source draft 流程：新增 `api_launcher/source_pattern_drafts.py`，CLI `--write-source-draft-from-url URL` 可把 URL 經 STAC/CKAN/ERDDAP/Socrata/OGC/CMR/HTML detector 轉成 ignored `dataset_discovery_sources.local.json` 草稿，summary 保留 `source_pattern_detection`、`audit_source_ids`、`next_action` 與 crawler audit 指令；未知或 unsupported pattern 仍留在 review，不假裝可下載。同步新增 `docs/AGENT_START_HERE.zh-TW.md` 作為 agent 最短入口地圖，固定權威順序、K 槽參考邊界、Sciverse 低優先級 literature-discovery 定位，以及「功能切片完成前要做文檔失真檢查」規則。驗證：`py_compile` OK；`test_discovery_drafts` 10 tests OK；`test_source_patterns` 4 tests OK；`scripts\pre_push_smoke_brief.cmd` 569 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 20:17 | Docs / Workflow | **CHECKPOINT** | `f7838c9` | `26361016216` | Record concept sample governance rules | 將 K 槽教材與 CODE_KM 的使用方式收斂成治理規則：handoff 新增「K 槽概念樣本庫與 CODE_KM 心法」，明確規定教材只作為概念樣本庫，不直接搬碼；金融/風控、GIS/3D、數學/機械、Pandas、Fluent Python/PyMOTW 各自映射到 RRKAL 的 time-series、renderer、bounds、import cleaning、adapter/CLI/streaming 架構；CODE_KM 則映射到 source/file provenance、checksum、pipeline run state、rights/review gate、local metadata index 與 cleaner/reference skill 分工。GTD 補後續任務：把 CODE_KM 式 status gate 映射到 crawler/source/asset 狀態，並整理 run registry 欄位。驗證：`git diff --check` OK（僅 CRLF warning）；CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 20:08 | Discovery / Crawler | **CHECKPOINT** | `c367ec7` | `26360834138` | Add crawler source pattern detector | 新增來源介面 detector 與 crawler asset 界域表單骨架：`source_patterns.py` 以 evidence/confidence 辨識 STAC、CKAN、ERDDAP、Socrata、OGC、CMR、HTML file index、unknown，並避免非 CMR URL 被固定 NASA probe 污染；新增 frontend-neutral bounds form contract 與 Tk `CrawlerAssetBoundDialog`，讓 crawler asset 可由 `bounds_schema` 動態產生界域輸入並先保存 payload，再交給後續 `build_download_plan` 串接；文件同步記錄「來源介面類型」與「內容格式 parser registry」分層，以及 K 槽爬蟲教材只能作為技術參考、不可直接複製站點腳本。驗證：`py_compile` OK；`tests.test_source_patterns tests.test_crawler_assets tests.test_tk_dialogs` 40 tests OK；`git diff --check` OK（僅 CRLF warning）；CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 19:29 | Docs / Workflow / UIUX | **CHECKPOINT** | `decbbb5` | `26360009914` | Formalize UI UX reference contract | 新增 `docs/UI_UX_DEVELOPMENT_CONTRACT.zh-TW.md`，把 UI/UX 討論從聊天比喻收斂成可驗收契約：參照軟體只萃取互動精神，不自動沿用命名；中大型 UI 變更需先定義操作對象、入口、觸發方式、狀態變化、後端服務、錯誤狀態與驗收方式。同步更新 GTD、handoff、docs index、repo skill 與 OpenSpec `development-workflow`，明確把 Foxy/Steam/tem 這類案例視為行為參考而非正式命名。驗證：`git diff --check` OK（僅 CRLF warning），`npx @fission-ai/openspec validate --all --no-interactive` OK，CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 18:41 | Discovery / Crawler UX | **CHECKPOINT** | `f0261cf` | `26359010215` | Add crawler asset settings entrypoint | 新增 Tk 爬蟲資產 profile 設定入口：`frontends/tk/crawler_asset_profile_dialog.py` 只收集 credential profile、API key env var、帳號提示、排程、限流、重試、seed scope、Logo/favicon/授權備註等 reference，實際保存仍走 `update_crawler_asset_profile()`，避免 raw secret 進入設定檔。Tk 爬蟲分頁新增明確「爬蟲設定」按鈕；下載器清單新增雙擊啟動選中 plan item，明確把雙擊語意保留給「資源清單 -> 下載隊列」，不拿來開設定。驗證：相關 `py_compile` OK，`tests.test_tk_dialogs tests.test_crawler_assets` 33 tests OK，`git diff --check` OK（僅 CRLF warning），CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 18:19 | Discovery / Crawler | **CHECKPOINT** | `fbc584e` | `26358598480` | Formalize crawler asset profile health | 將爬蟲資產 profile 與 health 收斂成後端契約：`CrawlerAssetProfile` 新增 credential profile、API key env var、帳號提示、排程、限流、重試、seed scope、Logo/favicon/授權備註欄位，並提供 `update_crawler_asset_profile()`；`api_launcher/crawler_asset_health.py` 統一輸出 `status_code`、emoji、reason、warning codes 與 `next_action`，Tk 爬蟲分頁先改吃 health 狀態。驗證：`py_compile` OK，`tests.test_crawler_assets tests.test_tk_dialogs` 31 tests OK，`git diff --check` OK（僅 docs CRLF warning），CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 17:45 | Discovery / Crawler | **CHECKPOINT** | `14c83de` | `26357919188` | Formalize crawler asset bounds schema | 將爬蟲資產的界域 facets 提升為 `api_launcher/crawler_asset_bounds.py` 的前端中立 schema，涵蓋 TimeBounds、SpatialBounds、ColumnBounds、VersionBounds、LimitBounds、AuthBounds 等群組，並讓 capability payload 帶 `bounds_schema`，Tk passport 可顯示界域結構。驗證：crawler asset/source-download/Tk dialog targeted tests OK，`py_compile` OK，`git diff --check` OK，CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 17:32 | Discovery / Crawler | **CHECKPOINT** | `a84f631` | `26357665624` | Formalize crawler asset capability contracts | 將 crawler asset 三能力槽正式化為 `fetch_metadata`、`list_datasets`、`build_download_plan`，每槽帶 input/output contract、credential mode、terms risk、error buckets、rate-limit policy 與 bounds facets；舊 `download_selected` 保留為 UI 相容別名。驗證：`tests.test_crawler_assets` OK，相關 py_compile / diff check OK，CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 14:32 | Rapid Delivery / Showcase Recovery | **CHECKPOINT** | `180e94d` | `26354090501` | Track showcase pressure-test artifacts | 回收中午展示急改 fork：`3d4f30c` 補上 Tk showcase seed coverage action 並修正 `detail_panel_workflows.py` 的 Tk widget import 邊界，`2df34ab` 清理 `APIkeys_collection.py` 相容 wrapper，確保 Tk UI 可取得 `active_download_policy()`，`180e94d` 解除 `state/showcase/` 的整體忽略並提交展示稿、PPT、樣本 CSV、seed coverage、截圖、header/sample payload 與 `SHOWCASE_RETROSPECTIVE.zh-TW.md`。同時在 skill / handoff / GTD 記錄快速交付與防禦性編程分流、K 槽主工作區 + C 槽地端 clone 測試規則，以及展示資料夾只追蹤可分享資產、繼續忽略 `pptx_deps`、本機驗證輸出、SQLite 與 log。驗證包含 K 槽 `py_compile` OK、Tk/launcher/showcase targeted 47 tests OK、K 槽 GUI 啟動 stderr 空白、C 槽 GitHub clone + 展示區複製地端測試 81 tests OK、GitHub Actions run `26354090501` Ubuntu / `windows-2025-vs2026` / real DB smoke 全部 success。這個 checkpoint 的重點不是新增願景，而是把壓力測試學到的真實交付條件回灌成專案規則與可重跑資產。 |
| 09:02 | Showcase Readiness | **CHECKPOINT** | `24eec20` | `26348085315` | Add reusable dataset seed showcase audit | 新增中午展示 / 進度說明用的 dataset seed coverage 稽核：`--dataset-discovery-seed-coverage-json` 可輸出機器可讀 JSON，`--write-dataset-seed-coverage` / `--write-dataset-seed-coverage-md` 可在 ignored `state/showcase/` 寫出展示 JSON/Markdown，`--dataset-discovery-complete-seed` 可讓平常採樣的 source 走有頁數上限的完整 seed 嘗試。一般使用者預設下載位置改到系統 Downloads 下的 `RuRuKa Asset Launcher/downloads`，curated SQLite 預設為 `RuRuKa Asset Launcher/curated_imports.db`；CI/開發仍可用 `--downloads-root` 與 `--import-sqlite-db` 覆寫。修正 `Path.home()` 在無 home 測試環境下的 fallback，避免 CLI parse_args 直接中斷。驗證包含 `py_compile` OK、targeted 40 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 514 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 08:05 | Backend/Core Hardening | **CHECKPOINT** | `9d30994` | `26347045573` | Split resource format helpers | 完成 adapter plan resolver 的 resource format 小切片，新增 `api_launcher/adapter_plan_resolvers/resource_formats.py`，將 `normalize_resource_format(...)` 與 `source_format_from_url(...)` 的 format/mediaType normalization、compound suffix 推論、URL 副檔名推論移出主檔。主檔保留同名 wrapper，`source_format_for_resource(...)` 的既有呼叫與測試 patch 點不變。這次不改 `source_format_for_resource(...)` 的 metadata hint 優先序、不改 direct-resource eligibility、不改 resource size guard。驗證包含 `py_compile` OK、adapter/download-plan targeted 63 tests OK、`git diff --check` OK、`cmd /c scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 07:52 | Backend/Core Hardening | **CHECKPOINT** | `2c7dc29` | `26346812516` | Split metadata guard helpers | 完成 adapter plan resolver 的 metadata guard 小切片，新增 `api_launcher/adapter_plan_resolvers/metadata_guards.py`，將 CMR metadata/data link 判斷、CMR rel token normalization、CMR API metadata URL 判斷、OGC API Records metadata rel 判斷、entry marker 判斷與 truthy helper 從主檔移出。主檔保留 `resource_is_ogc_records_metadata_link(...)`、`resource_is_cmr_metadata_link(...)`、`entry_is_cmr_candidate(...)`、`resource_link_rels(...)`、`cmr_link_rel_is_data(...)`、`cmr_link_rel_is_metadata(...)`、`cmr_link_rel_token(...)`、`resource_value_is_truthy(...)`、`resource_url_is_cmr_api_metadata(...)`、`entry_is_ogc_records_candidate(...)` wrapper，維持既有測試 patch 點與呼叫點相容。這次不改直接下載格式判斷、不改 CMR granule asset resolver、不改 OGC Records direct data-link resolver。驗證包含 `py_compile` OK、adapter/download-plan targeted 63 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 07:40 | Backend/Core Hardening | **CHECKPOINT** | `48bdbfb` | `26346575874` | Split NCEI access data resolver | 完成 adapter plan resolver 的 NCEI Access Data bounded sample 小切片，新增 `api_launcher/adapter_plan_resolvers/ncei_access_data.py`，將 `/access/services/data/v1` 查詢的 canonical query、dataset/date/spatial guard、7 天上限判斷、CSV/JSON format normalization、bounded sample entry 產生與 Access Data URL 排除 guard 移出主檔。主檔保留 `ncei_bounded_access_data_entry(...)`、`ncei_bounded_access_data_url(...)`、`ncei_access_data_candidate_urls(...)`、`bounded_ncei_access_data_url(...)`、`canonical_ncei_access_data_query(...)`、`ncei_access_data_bounds(...)`、`ncei_access_data_is_safely_bounded(...)`、`resource_is_ncei_access_data_url(...)` wrapper，並由 wrapper 傳入主檔 `first_text`、query/date helper、resource helper 與既有 max-days 常數。這次不改 NCEI Search metadata sample、不改 NCEI data-file lookup、不改 importer 或下載計畫行為。`adapter_plan_resolver.py` 從 1404 行降到 1272 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、adapter/download-plan targeted 63 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 07:28 | Backend/Core Hardening | **CHECKPOINT** | `c5819a3` | `26346356148` | Split NCEI data-file resolver | 完成 adapter plan resolver 的 NCEI Search data-file lookup 小切片，新增 `api_launcher/adapter_plan_resolvers/ncei_search_data_file.py`，將 `dataset + stations/bbox/locationIds` 有界查詢下的 `limit=1&offset=0` metadata lookup、NOAA host guard、`/data/...` 檔案 URL 正規化、payload resource 轉換與小於 100MB 的 direct file promotion 移出主檔。主檔保留 `ncei_search_data_file_entries(...)`、`ncei_search_data_file_lookup_url(...)`、`bounded_ncei_search_data_file_lookup_url(...)`、`ncei_data_file_lookup_query(...)`、`ncei_search_data_file_bounds(...)`、`ncei_search_data_file_lookup_is_bounded(...)`、`ncei_search_data_file_resources(...)`、`ncei_search_data_file_resource(...)`、`ncei_search_data_file_url(...)`、`ncei_noaa_host(...)` wrapper，並由 wrapper 傳入主檔 `fetch_json`、resource helper 與 format helper，維持既有測試 patch 點相容。這次不改 NCEI Search metadata sample、不改 NCEI Access Data 日期/空間 guard、不改 importer 或下載計畫行為。`adapter_plan_resolver.py` 從 1511 行降到 1404 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、adapter/download-plan targeted 63 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 07:12 | Backend/Core Hardening | **CHECKPOINT** | `16844f3` | `26346069849` | Split NCEI search plan resolver | 完成 adapter plan resolver 的 NCEI Search bounded metadata 小切片，新增 `api_launcher/adapter_plan_resolvers/ncei_search.py`，將 `/search/v1/datasets` / `/search/v1/data` 轉成 `limit=25&offset=0` JSON metadata sample 的 resolver、候選 URL 收集、dataset/data sibling endpoint 推導與 limited query 生成移出主檔。主檔保留 `ncei_bounded_search_entry(...)`、`ncei_bounded_search_url(...)`、`entry_is_ncei_search_candidate(...)`、`ncei_candidate_urls(...)`、`bounded_ncei_search_url(...)`、`ncei_search_sibling_endpoint(...)`、`ncei_limited_query(...)` wrapper；這次不改 NCEI data-file live lookup、不改 NCEI Access Data 日期/空間 guard、不改 importer 或下載計畫行為。`adapter_plan_resolver.py` 從 1667 行降到 1511 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、adapter/download-plan targeted 63 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 07:01 | Backend/Core Hardening | **CHECKPOINT** | `ea0c3f4` | `26345862852` | Split ERDDAP plan resolver | 完成 adapter plan resolver 的 ERDDAP bounded sample 小切片，新增 `api_launcher/adapter_plan_resolvers/erddap.py`，將 ERDDAP tabledap/griddap protocol URL 正規化、`info/{dataset}/index.json` 維度/變數解析、bounded CSV sample URL 生成與 sample plan entry 產生邏輯移出主檔。`api_launcher/adapter_plan_resolver.py` 保留 `erddap_bounded_sample_entry(...)`、`erddap_protocol_url(...)`、`absolute_erddap_url(...)`、`erddap_info_url(...)`、`erddap_info_dimensions_and_variables(...)`、`erddap_sample_csv_url(...)`、`tabledap_sample_variables(...)` wrapper，並持續從主檔傳入 `fetch_json`，維持既有測試 patch 點相容；這次不改 ERDDAP sample policy、不改 importer 或下載計畫行為。`adapter_plan_resolver.py` 從 1805 行降到 1667 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、adapter/download-plan targeted 63 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 06:50 | Backend/Core Hardening | **CHECKPOINT** | `13372f3` | `26345647862` | Split STAC plan resolver | 完成 adapter plan resolver 的 STAC bounded item metadata 小切片，新增 `api_launcher/adapter_plan_resolvers/stac.py`，將 STAC collection -> `items?limit=1` GeoJSON metadata sample 的 resolver、items URL 推導、link mapping 與 `rel=items` guard 移出主檔。`api_launcher/adapter_plan_resolver.py` 保留 `stac_bounded_item_search_entry(...)`、`entry_is_stac_collection(...)`、`stac_item_search_url(...)`、`stac_link_mappings(...)`、`stac_items_endpoint_from_url(...)`、`bounded_stac_items_url(...)`、`resource_is_stac_items_link(...)` wrapper，維持舊 helper 名稱相容；這次不改 STAC bounded policy、不改 importer 或下載計畫行為。`adapter_plan_resolver.py` 從 2123 行降到 1805 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、adapter/download-plan targeted 63 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 06:37 | Backend/Core Hardening | **CHECKPOINT** | `6a72338` | `26345396457` | Split CMR collection plan resolver | 完成 adapter plan resolver 的 CMR collection bounded granule metadata 小切片，新增 `api_launcher/adapter_plan_resolvers/cmr_collection.py`，將 CMR collection metadata -> `granules.json?collection_concept_id=...&page_size=1` 的有界樣本計畫邏輯移出主檔。`api_launcher/adapter_plan_resolver.py` 保留 `cmr_bounded_granule_search_entry(...)`、`cmr_bounded_granule_search_url(...)`、`entry_is_cmr_collection(...)`、`cmr_candidate_urls(...)`、`bounded_cmr_granules_url(...)` wrapper，維持 helper 名稱相容；這次不改 CMR granule asset link resolver、不改 importer 或下載計畫行為。`adapter_plan_resolver.py` 從 2256 行降到 2123 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、adapter/download-plan targeted 63 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 06:27 | Backend/Core Hardening | **CHECKPOINT** | `2ea3582` | `26345187964` | Split CMR granule asset resolver | 完成 adapter plan resolver 的 CMR granule asset 小切片，新增 `api_launcher/adapter_plan_resolvers/cmr_asset.py`，將 CMR granule metadata lookup、concept URL 正規化、links payload 解析、data link 篩選、metadata link 排除與 size guard 前的 direct-entry 轉換移出主檔。`api_launcher/adapter_plan_resolver.py` 保留 `cmr_granule_asset_link_entries(...)` 與相關 helper wrapper，維持既有 `fetch_json` patch 點與 helper 名稱相容；這次不改 CMR collection bounded granule search、不改 CMR link rel 判斷、不改 importer 或下載計畫行為。`adapter_plan_resolver.py` 從 2380 行降到 2256 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、adapter/download-plan targeted 63 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 06:15 | Product Positioning | **CHECKPOINT** | `cf71897` | `26344950123` | Record scientific data gatekeeper positioning | 將最新產品願景收斂進 `docs/PRODUCT_POSITIONING.zh-TW.md`，把成熟形態定義為 Scientific Data Gatekeeper：Headless SDK Core、CLI / automation layer、多端 View layer 三層，並明確標示這是中長期產品收斂方向，不代表 headless service、Qt Pro、plugin market 已完成，也不能反過來成為當前 MVP blocker。這次與 Socrata resolver split 同一 push 驗證，CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 06:15 | Backend/Core Hardening | **CHECKPOINT** | `e9e7fcc` | `26344950123` | Split Socrata plan resolver | 完成 adapter plan resolver 的下一個來源專屬拆分切片，新增 `api_launcher/adapter_plan_resolvers/socrata.py`，將 Socrata/SODA resource/API view/domain metadata 轉成 `$limit=25` bounded sample 的邏輯移出主檔。`api_launcher/adapter_plan_resolver.py` 保留 `socrata_bounded_sample_entry(...)`、`socrata_sample_url(...)`、`socrata_candidate_urls(...)` 與既有 helper 匯出相容面；這次不改 Socrata bounded sample policy、不改 MVP demo flow、不改 importer 或下載計畫行為。`adapter_plan_resolver.py` 從 2503 行降到 2380 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、adapter/MVP demo/download-plan targeted 67 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 05:59 | Backend/Core Hardening | **CHECKPOINT** | `5a24083` | `26344642445` | Split CKAN plan resolver | 延續 adapter plan resolver 來源專屬拆分，新增 `api_launcher/adapter_plan_resolvers/ckan.py`，將 CKAN `package_search/package_show -> resources -> direct entries` 的二階段 resolver 移出主檔。`api_launcher/adapter_plan_resolver.py` 仍保留 `ckan_package_show_resource_entries(...)` wrapper，維持既有 CLI 與測試 patch `fetch_json` 的相容性；這次不改 CKAN URL 轉換、resource size guard、direct entry 規則或 importer 行為。`adapter_plan_resolver.py` 從 2572 行降到 2503 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、`tests.test_adapter_plan_resolver` 46 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 05:46 | Backend/Core Hardening | **CHECKPOINT** | `d281e16` | `26344386999` | Split DataCite and Dataverse plan resolvers | 完成 adapter plan resolver 的第一個來源專屬拆分切片，新增 `api_launcher/adapter_plan_resolvers/` package，將 DataCite DOI contentUrl resolver 與 Dataverse latest-version file resolver 搬到 `datacite.py`、`dataverse.py`。`api_launcher/adapter_plan_resolver.py` 保留同名相容 wrapper，讓既有 CLI 與測試仍可 patch `adapter_plan_resolver.fetch_json`；這次不改 resolver policy、size guard、direct entry 規則或 importer 行為。`adapter_plan_resolver.py` 從 2874 行降到 2572 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、`tests.test_adapter_plan_resolver` 46 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 05:24 | Backend/Core Hardening | **CHECKPOINT** | `065b5f4` | `26343923135` | Split manifest import CLI workflow | 完成後端第五個小切片，把 `--import-csv-manifest`、`--import-verified-csv-manifests`、`--import-json-manifest`、`--import-verified-json-manifests` 的 CLI 外殼搬到 `api_launcher/cli_manifest_import.py`。新模組負責 manifest 讀取、manual local-file synthetic provider guard、CSV/JSON 單檔與 registry batch 匯入調度，以及既有人類可讀輸出；`core.py` 只保留四個 `*_cli(...)` 調度點，CSV/JSON importer 本體不變，`cli_flags.py` 改用 `manifest_import_command_active(args)`。`core.py` 從 1665 行降到 1462 行。驗證包含 `py_compile` OK、csv/json/manual targeted 19 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 05:10 | Backend/Core Hardening | **CHECKPOINT** | `b676907` | `26343640464` | Split manual import CLI workflow | 完成後端第四個小切片，把 `--write-local-file-manifest`、`--import-local-file` 與 `--manual-import-json` 的 CLI 外殼搬到 `api_launcher/cli_manual_import.py`。新模組負責本機檔案 manifest 產生、synthetic `manual_local_files` provider guard、raw manifest asset 登錄、CSV/JSON importer 調度、agent-readable JSON payload 與 guided unsupported-format 邊界；`core.py` 只保留 `write_local_file_manifest_cli(...)`、`import_local_file_cli(...)` 與 `validate_manual_import_args(...)` 調度點。這個切片不改 `api_launcher/manual_import.py` 的格式判斷與 provenance review，不改 CSV/JSON importer 的 SQLite 寫入規則，也不碰 plan-driven download/import；`core.py` 從 1798 行降到 1665 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、manual/csv/json import targeted 19 tests OK、`git diff --check` / cached check OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 04:59 | Backend/Core Hardening | **CHECKPOINT** | `98a42b2` | `26343407952` | Split download plan CLI workflow | 完成後端第三個小切片，把 `--run-download-plan` / `--run-download-plan-json` / `--import-supported-plan-results` 的 CLI 外殼搬到 `api_launcher/cli_download_plan.py`。`core.py` 只保留 `run_download_plan_cli(self.args, self.repository, log_event)` 調度點，實際下載、manifest、匯入與 skip/next_action 邏輯仍走既有 `api_launcher/ingestion_pipeline.py`，因此沒有改 direct download、adapter-required skip、plan-driven import、table rename/replace policy 或 structured event schema。`cli_flags.py` 改用 `download_plan_command_active(args)`，並保留舊版 JSON / 匯入附屬旗標單獨出現時不走預設 init/seed 的相容行為；`core.py` 從 1852 行降到 1798 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、download/ingestion/repair targeted 26 tests OK、`git diff --check` / cached check OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 04:48 | Backend/Core Hardening | **CHECKPOINT** | `3e0d4ce` | `26343188615` | Split database repair CLI workflow | 完成後端第二個小切片，把 database repair 的三種 CLI 執行路徑：`--reimport-missing-sqlite-table`、`--unmanage-database-asset`、`--write-database-repair-sql`，以及 JSON payload 與 `database_repair_completed` structured event，搬到 `api_launcher/cli_database_repair.py`。`core.py` 只保留 `run_database_repairs(self.args, self.repository, self.db_path, log_event)` 調度點；`cli_flags.py` 改用 `database_repair_command_active(args)`，並保留舊版 `--database-repair-json` 單獨出現時不走預設 init/seed 的行為。這個切片不改 `api_launcher/database_repair.py` 的 guard、不改 SQLite reimport / registry-only unmanage / SQL dry-run 安全邊界，也不碰 real DB 執行；`core.py` 從 1946 行降到 1852 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、database/data-store targeted 79 tests OK、`git diff --check` / cached check OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 04:34 | Backend/Core Hardening | **CHECKPOINT** | `dc7523d` | `26342897778` | Split yfinance CLI workflow | 啟動後端小切片解耦，把 yfinance CLI 旗標、command-active 判斷、demo/live plan、storage review、storage handoff 執行流程搬到 `api_launcher/cli_yfinance.py`。`core.py` 只保留 `add_yfinance_args(parser)` 與 `run_yfinance_cli(args)` 兩個調度點，不再直接 import yfinance 常數或 writer；`cli_flags.py` 也改成呼叫 `yfinance_command_active(args)`，保留所有既有 `--write-yfinance-*` flag 與輸出文案相容。這個切片不改 `api_launcher/adapters/yfinance.py` 的資料契約、不碰 Yahoo live guard、不改下載/匯入/資料庫 backend；`core.py` 從 2094 行降到 1946 行。驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、yfinance/Tk targeted 39 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 04:14 | MVP Hardening | **CHECKPOINT** | `2b579de` | `26342472725` | Split Tk source action workflows | 完成 Tk UI 主檔解耦收束，把 Treeview row click/double-click/context menu、library action menu、manifest repair context、star toggle、metadata self-check、納管/解除納管/受控卸載、下載計畫自檢、metadata crawl worker 與官方文件開啟流程搬到 `frontends/tk/source_action_workflows.py`。這個切片只搬移既有 UI dispatch 與 registry guard，不改 crawler、download/import、repair、install/uninstall backend 策略；targeted tests 捕捉到 `frontends.tk.launcher_ui.messagebox` 相容匯出後已補回，`launcher_ui.py` 從 578 行降到 202 行。新增 mixin importability guard；驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、Tk/UI targeted 62 tests OK、`git diff --check` / cached check OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 03:56 | MVP Hardening | **CHECKPOINT** | `46e4610` | `26342101754` | Split Tk lifecycle workflows | 延續 Tk UI 解耦，把主視窗浮出/focus、topmost 釋放、UI ready 訊息、本機 catalog 初始化、共用 `_connect()`、關閉時取消下載 worker、啟動環境檢查回報抽到 `frontends/tk/app_lifecycle_workflows.py`，並把表格欄寬偏好的讀寫/正規化搬進 `frontends/tk/table_interaction_workflows.py`。這個切片只移動啟動/關閉與欄寬偏好 helper，沒有改 row action、爬蟲、安裝、解除納管、卸載、修復或下載 backend；`launcher_ui.py` 從 709 行降到 578 行。新增 mixin importability guard；驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、Tk/UI targeted 62 tests OK、`git diff --check` / cached check OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 03:38 | MVP Hardening | **CHECKPOINT** | `78792d3` | `26341730759` | Split Tk window layout workflows | 延續 Tk UI 解耦，把主視窗樣式、選單列與第一層 layout 組裝的 `_setup_style`、`_build_menu_bar`、`_build_layout` 搬到 `frontends/tk/window_layout_workflows.py`。這個切片只連接既有 command callback，沒有改下載、匯入、修復、OAuth、爬蟲或資料庫 backend；targeted tests 捕捉到 `PROJECT_ROOT` 相容匯出被移除的問題後已補回，`launcher_ui.py` 從 947 行降到 709 行。新增 mixin importability guard；驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、Tk/UI targeted 62 tests OK、`git diff --check` / cached check OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 00:19 | MVP Hardening | **CHECKPOINT** | `50724d2` | `26337627361` | Split Tk detail panel workflows | 延續 Tk UI 解耦，把右側細節抽屜的 `_build_detail_panel`、`update_detail_panel`、provider 描述 fallback、preview/AI summary 文字框 setter 搬到 `frontends/tk/detail_panel_workflows.py`。這個切片只移動顯示與 widget 建置，沒有改安裝、解除納管、移除、修復、爬蟲或下載 backend；`launcher_ui.py` 從 1119 行降到 947 行。新增 mixin importability guard；驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、Tk/UI targeted 62 tests OK、`git diff --check` / cached check OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 00:06 | MVP Hardening | **CHECKPOINT** | `7337f36` | `26337375378` | Split Tk table data workflows | 延續 Tk UI 解耦，把下載計畫面板建置、收合/展開狀態搬到 `frontends/tk/download_plan_panel_workflows.py`，並把 provider/dataset 表格的 `reload_data`、filter、render、dataset row helper 搬到 `frontends/tk/table_data_workflows.py`。主視窗仍保留 menu/layout/detail 與 row action、修復、卸載、爬蟲等較高風險控制流程；`launcher_ui.py` 從 1389 行降到 1119 行。新增 mixin importability guard；驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、Tk/UI targeted 62 tests OK、`git diff --check` / cached check OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |

### 2026-05-23

目前開發階段：**MVP Hardening / Library UI / Discovery Audit / Tk UI Decoupling**。這一段把對外產品名從歷史相容名 `APIkeys_collection` 收斂為 `RuRuKa Asset Launcher`，短稱 `RRKAL`；`RRK` 作為未來品牌字根保留在產品定位中，但既有 repo、package、CLI wrapper 與產物檔名暫不做破壞式批量改名。同時把 Steam-like library action 的 `status_badge`、provider/source discovery review JSON、provider 候選審核面板與本機 ignored seed 寫入、crawler audit `next_action`、頂層 `audit_summary` 與 local discovery promotion dry-run 審核入口接到 Tk UI，並開始把 `launcher_ui.py` 的純 helper、view-model、啟動錯誤處理、桌面檔案定位、UI 靜態設定、狀態/修復文案與可獨立封裝的 dialog class/workflow 逐步拆出，讓使用者能直接看懂可加入計畫、可修復、可受控移除、要先審哪些 provider 候選、要修哪個 crawler source，以及哪些本機 discovery 草稿能安全進入正式 catalog。

| 時間 | 開發階段 | 狀態 | SHA | Run | 原始提交訊息 | 中文說明 |
| --- | --- | --- | --- | --- | --- | --- |
| 15:45 | Discovery / Socrata Source Draft Hardening | **CHECKPOINT** | `15ed86a` | `26389482453` | Normalize Socrata catalog draft queries | 鞏固 Socrata catalog endpoint 正規化：已是 `api.us.socrata.com/api/catalog/v1` 的 canonical URL 會只保留必要的 `domains=<portal-host>`，移除 detector probe 或 pagination 留下的 `limit/offset`、搜尋字串與 fragment，避免 local source draft 被一次性 probe 參數污染；Socrata crawler/resolver 仍負責執行時加入 bounded limit 與 resource-level 查詢。同步更新 `PROJECT_GTD` 與 `DATASET_DISCOVERY_NOTES`，明確記錄 Socrata 是「保留 domains、丟掉其他 probe query」的小例外。驗證：targeted discovery/source-pattern tests 72 tests OK；`py_compile` OK；`git diff --check` 無錯誤但保留 docs CRLF warning；`scripts\pre_push_smoke_brief.cmd` 621 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 15:33 | Discovery / Source Draft Hardening | **CHECKPOINT** | `cec6dfe` | `26389032248` | Harden search source draft endpoints | 鞏固搜尋型 source draft endpoint 正規化：STAC、CKAN、GBIF、Dataverse、Zenodo、DataCite、OpenAlex、NCEI、ERDDAP 這類 catalog/search endpoint 會先移除 detector probe 留下的 query 與 fragment，再交給 crawler/resolver 自行加入 bounded query，避免 `rows/limit/q/search/page_size` 等參數重複或被錯接到 path；同步更新 `PROJECT_GTD` 與 `DATASET_DISCOVERY_NOTES`，把「source draft 只保存乾淨入口，界域與查詢參數由 crawler/resolver 加入」寫成協作邊界。驗證：targeted discovery/source-pattern tests 72 tests OK；`py_compile` OK；完整 `unittest discover -s tests` 621 tests / 4 skipped OK；`scripts\pre_push_smoke_brief.cmd` 621 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 23:50 | MVP Hardening | **CHECKPOINT** | `ed16370` | `26337041547` | Split Tk sidebar and layout workflows | 延續 Tk UI 解耦，把側欄分類 / provider favicon 支援搬到 `frontends/tk/sidebar_workflows.py`，把細節抽屜動畫、responsive layout、表格欄寬重新配置搬到 `frontends/tk/responsive_layout_workflows.py`，把 Treeview 欄寬調整、游標、搜尋 placeholder、分類切換事件搬到 `frontends/tk/table_interaction_workflows.py`；`launcher_ui.py` 從 1628 行再降到 1389 行，且仍保留主視窗/menu/layout/table/detail 資料狀態作為下一輪安全拆分邊界。新增 mixin importability guard；驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、Tk/UI targeted 62 tests OK、`git diff --check` / cached check OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，以及 CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 23:32 | MVP Hardening | **CHECKPOINT** | `bdad2db` | `26336615256` | Split provider settings Tk workflows | 第二十三輪 Tk 主檔安全解耦：把新增/編輯 provider、provider row 轉回 `core.Provider`、資料庫工具/設定入口、integration config 開啟、文件開啟、Developer CLI、語言/AI/data-store/startup/event/Gemini 設定 dialog 入口抽成 `frontends/tk/provider_settings_workflows.py` 的 `ProviderSettingsWorkflowMixin`。主檔保留 menu/button wiring 與 table/detail lifecycle；`PROJECT_ROOT` 仍保留在 `launcher_ui.py` 作為既有 import 相容面，`launcher_ui.py` 降到 1628 行。新增 importability guard；驗證：`py_compile` OK；Tk/UI 相關 62 tests OK；`git diff --check` / cached check OK；`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI 首輪 Windows checkout credential transient failure，未進測試，rerun 後 Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 23:16 | MVP Hardening | **CHECKPOINT** | `ae8ba54` | `26336316057` | Split AI summary Tk workflows | 第二十二輪 Tk 主檔安全解耦：把啟動時本機 AI API key 載入、AI profile label/login 狀態、credential 檢查、provider summary 背景 worker 與 repository 回寫抽成 `frontends/tk/ai_summary_workflows.py` 的 `AiSummaryWorkflowMixin`。主檔只保留 AI 說明按鈕與顯示區連接；OAuth workflow/dialog 仍透過同一組 profile 狀態 helper，startup 不觸發瀏覽器登入或 OAuth 視窗，`launcher_ui.py` 降到 1736 行。新增 importability guard；驗證：`py_compile` OK；Tk/UI 相關 62 tests OK；`git diff --check` / cached check OK；`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 23:01 | MVP Hardening | **CHECKPOINT** | `365d937` | `26335997043` | Split discovery Tk workflows | 第二十一輪 Tk 主檔安全解耦：把 provider candidate review panel、provider discovery worker/message、crawler audit summary/issue helpers、dataset candidate discovery worker 與本機 discovery dry-run audit 抽成 `frontends/tk/discovery_workflows.py` 的 `DiscoveryWorkflowMixin`。主檔仍保留 menu/button 連接與主要表格/detail widget lifecycle；provider/dataset discovery backend、review JSON、`audit_summary` 與 event log 行為不變，`launcher_ui.py` 降到 1861 行。新增 importability guard；驗證：`py_compile` OK；Tk/discovery 相關 77 tests OK；`git diff --check` / cached check OK；`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 22:47 | MVP Hardening | **CHECKPOINT** | `77ad953` | `26335685129` | Split download plan Tk workflows | 第二十輪 Tk 主檔安全解耦：把 provider/dataset version 加入下載計畫、plan key、選取項目、plan entry 組裝、Adapter plan 解析、resolved plan 匯入、Adapter 待辦面板與下載計畫匯出抽成 `frontends/tk/plan_workflows.py` 的 `PlanWorkflowMixin`。主檔仍保留表格 click/context menu、detail drawer 與 provider 編輯等視窗生命週期；下載 queue、匯入 pipeline、Adapter resolver backend 行為不變，`launcher_ui.py` 降到 2391 行。新增 importability guard；驗證：`py_compile` OK；Tk/plan/adapter/pipeline 相關 112 tests OK；`git diff --check` / cached check OK；`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 21:52 | MVP Hardening | **CHECKPOINT** | `d47a8fc` | `26334490851` | Split import Tk workflows | 第十九輪 Tk 主檔安全解耦：把下載後匯入、同名資料表策略、本機 CSV/JSON/JSONL/GeoJSON 檔匯入、匯入狀態標籤與 manifest 健康檢查文案抽成 `frontends/tk/import_workflows.py` 的 `ImportWorkflowMixin`。主檔仍保留 widget 生命週期與計畫資料模型，backend importer、manifest、SQLite 安全策略與 `run_existing_download_import_slice()` 行為不變，`launcher_ui.py` 降到 2771 行。新增 importability guard 並修正 mock patch 路徑。驗證：`py_compile` OK；Tk/import/pipeline 相關 66 tests OK；`git diff --check` / cached check OK；第一次 `scripts\pre_push_smoke_brief.cmd` 在 510 tests OK 後遇到 Windows 暫時性 `APIkeys_collection.py` permission denied，隨後 `py -3 APIkeys_collection.py --summary` 成功且重跑 pre-push 通過：510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 21:29 | MVP Hardening | **CHECKPOINT** | `fd895ec` | `26333973925` | Split download Tk workflows | 第十八輪 Tk 主檔安全解耦：把下載計畫面板、直接下載啟動、skip guidance、download queue 狀態控制、progress callback、完成後 manifest/file asset 註冊流程抽成 `frontends/tk/download_workflows.py` 的 `DownloadWorkflowMixin`。主檔保留計畫資料模型與匯入流程，下載 queue / downloader / manifest backend 行為不變，`launcher_ui.py` 降到 3181 行。新增 importability guard，驗證：`py_compile` OK；Tk/download/repair 相關 73 tests OK；`git diff --check` / cached check OK；`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。第一次 `git push` 曾因 Git 暫時讀不到 current working directory 失敗，改用 `git -C K:\APIkeys_collection push origin main` 成功，未改動工作樹內容。 |
| 21:16 | MVP Hardening | **CHECKPOINT** | `784b1af` | `26333703204` | Split OAuth Tk workflows | 第十七輪 Tk 主檔安全解耦：把 OAuth Client ID 設定、Google browser login、本機 callback server、device-code/QR login、API key session 與 OAuth token 儲存/啟用流程抽成 `frontends/tk/oauth_workflows.py` 的 `OAuthWorkflowMixin`。主檔只保留整合選單入口與繼承 mixin；Google OAuth 仍維持中期/開發者功能邊界，沒有新增 live credential probing、背景登入或產品級 OAuth broker，主檔降到 3495 行。新增 importability guard，驗證：`py_compile` OK；Tk/OAuth 相關 72 tests OK；`git diff --check` / cached check OK；`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 21:04 | MVP Hardening | **CHECKPOINT** | `789cd05` | `26333458027` | Split repair Tk workflows | 第十六輪 Tk 主檔安全解耦：把修復 / 驗證資產面板從 `launcher_ui.py` 抽成 `frontends/tk/repair_workflows.py` 的 `RepairWorkflowMixin`，集中下載 manifest 掃描、database asset self-check、重新排下載、SQLite table reimport、MySQL/PostgreSQL dry-run SQL、connection metadata 編輯與 registry-only stop tracking 等高風險修復 UI 流程。主檔只保留選單/按鈕入口與繼承 mixin，修復守門規則、event log、DB repair backend 與 download queue 邊界不變，主檔降到 4188 行。新增 importability guard，驗證：`py_compile` OK；完整 Tk split suite 62 tests OK；`git diff --check` / cached check OK；`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 20:46 | MVP Hardening | **CHECKPOINT** | `a601942` | `26333075944` | Split MVP demo Tk workflows | 第十五輪 Tk 主檔安全解耦：把 MVP Demo Flow 建立、canonical MVP Demo Smoke 啟動、背景 worker 與結果 callback 抽成 `frontends/tk/mvp_demo_workflows.py` 的 `MvpDemoWorkflowMixin`。`launcher_ui.py` 只保留工具選單 callback、主視窗狀態與繼承 mixin；demo backend、下載/匯入 pipeline、隔離 demo DB 與 event log 邊界不變，主檔降到 4821 行。新增 importability guard，驗證：temp `PYTHONPYCACHEPREFIX` `py_compile` OK；相關 Tk/MVP helper 47 tests OK；完整 Tk split suite 62 tests OK；`git diff --check` / cached check OK；`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 17:45 | MVP Hardening | **CHECKPOINT** | `78268ac` | `26329582129` | Split yfinance Tk workflows | 第十四輪 Tk 主檔安全解耦：把 yfinance 離線 demo plan、guarded live plan 與 storage review dry-run 三個工具選單 workflow 抽成 `frontends/tk/yfinance_workflows.py` 的 `YfinanceWorkflowMixin`。`launcher_ui.py` 只保留選單 callback 與繼承 mixin，yfinance 的 opt-in、review-only、metadata-only 邊界維持不變；主檔降到 4932 行。新增 importability guard，驗證：temp `PYTHONPYCACHEPREFIX` `py_compile` OK；相關 Tk/yfinance helper 47 tests OK；完整 Tk split suite 62 tests OK；`git diff --check` / cached check OK；`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 17:27 | MVP Hardening | **CHECKPOINT** | `2827056` | `26329248495` | Split provider candidate review dialog | 繼續 Tk UI 解耦，把 provider 候選審核 Toplevel、表格/detail pane、開來源/開文件、寫入 ignored local provider seed 與 dataset source draft 的 callback 移入 `ProviderCandidateReviewDialog` class；`launcher_ui.py` 只保留讀取 `state/provider_candidates.ui.json` 與開啟 dialog 的入口，行數降到 5187。這個切片保持 review-only 邊界：candidate 不會直接寫正式 catalog，source draft 仍需 local discovery audit 後才可 promotion。測試更新為檢查 dialog static helper 與 importability；驗證包含 temp `PYTHONPYCACHEPREFIX` `py_compile` OK、`tests.test_tk_dialogs tests.test_launcher_ui` 42 tests OK、完整 Tk split suite 62 tests OK、`git diff --check` OK、`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`，CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 16:06 | MVP Hardening | **CHECKPOINT** | `dd715e6` | `26327691144` | Split dataset candidate review dialog | 第十二輪 Tk 主檔安全解耦：把資料集候選審核 Toplevel 抽成 `DatasetCandidateReviewDialog` class，讓狀態篩選、候選表格 row values、crawler evidence 詳情、標記可用/拒絕、加入下載計畫與開啟來源連結集中到 `frontends/tk/dialogs.py`。主 UI 只保留 `open_dataset_candidate_review_panel()` 委派，審核仍只改 registry 的 `candidate_status`，不下載、不匯入、不改來源資料；`launcher_ui.py` 降到約 5075 行。新增 headless regression 鎖住資料集候選 row/detail 形狀，避免後續解耦弄斷 crawler candidate review 的人類/agent 判讀。驗證：以 temp `PYTHONPYCACHEPREFIX` 跑 `py_compile` 相關 UI/dialog/test 檔 OK；`tests.test_tk_dialogs tests.test_launcher_ui tests.test_tk_ui_labels tests.test_tk_ui_config tests.test_tk_desktop_integration tests.test_tk_ui_helpers tests.test_tk_provider_models` 62 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 510 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 15:48 | MVP Hardening | **CHECKPOINT** | `76f8ca7` | `26327340186` | Split adapter review dialog | 第十一輪 Tk 主檔安全解耦：把 Adapter 待辦 review-only 視窗抽成 `AdapterReviewDialog` class，讓待辦表格、row values、key/value 詳情、來源/landing URL 開啟與「解析可下載 resources」按鈕編排集中到 `frontends/tk/dialogs.py`。主 UI 仍負責檢查下載計畫是否為空、呼叫 `adapter_review_items()` 產生 review item，以及委派既有 resolver 入口，避免解耦時改到 adapter plan resolution 或下載/匯入流程；`launcher_ui.py` 降到約 5277 行。新增 headless regression 鎖住 Adapter review 表格欄位與詳情文字形狀，方便 agent/handoff 接力穩定判讀。驗證：`py_compile` 相關 UI/dialog/test 檔 OK；`tests.test_tk_dialogs tests.test_launcher_ui tests.test_tk_ui_labels tests.test_tk_ui_config tests.test_tk_desktop_integration tests.test_tk_ui_helpers tests.test_tk_provider_models` 61 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 509 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 15:33 | MVP Hardening | **CHECKPOINT** | `1ec684d` | `26327044900` | Split import table policy dialog | 第十輪 Tk 主檔安全解耦：把匯入支援下載結果前的「既有資料表處理方式」Toplevel 抽成 `ImportExistingTablePolicyDialog` class，讓 rename/skip/replace radiobutton、replace 二次確認與 result 回傳集中到 `frontends/tk/dialogs.py`。主 UI 只保留 `ask_import_existing_table_policy()` 委派，實際匯入 pipeline、table overwrite/rename 行為與偏好保存仍走既有方法，避免解耦時改到資料庫寫入邊界；`launcher_ui.py` 降到約 5362 行。新增 headless regression 鎖住同名資料表策略 value `rename/skip/replace`，防止 UI 文案調整影響 pipeline contract。驗證：`py_compile` 相關 UI/dialog/test 檔 OK；`tests.test_tk_dialogs tests.test_launcher_ui tests.test_tk_ui_labels tests.test_tk_ui_config tests.test_tk_desktop_integration tests.test_tk_ui_helpers tests.test_tk_provider_models` 60 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 508 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 15:07 | MVP Hardening | **CHECKPOINT** | `6805ea9` | `26326541203` | Split Google Gemini settings dialog | 第九輪 Tk 主檔安全解耦：把 `open_google_gemini_settings()` 抽成 `GoogleGeminiSettingsDialog` class，讓 Google/Gemini 連線說明、帳號支援表格、MVP API key 主按鈕與中期 OAuth/QR 按鈕編排集中到 `frontends/tk/dialogs.py`。主 UI 只保留開窗委派，實際 OAuth browser/device-code、API key 儲存與 credential 操作仍留在既有方法，避免解耦時改到登入安全邊界；`launcher_ui.py` 降到約 5427 行。新增 headless regression 鎖住 account provider row values；這一刀仍不搬 OAuth flow 實作、repair panel、下載/匯入 backend 或資料庫 repair backend。驗證：`py_compile` 相關 UI/dialog/test 檔 OK；`tests.test_tk_dialogs tests.test_launcher_ui tests.test_tk_ui_labels tests.test_tk_ui_config tests.test_tk_desktop_integration tests.test_tk_ui_helpers tests.test_tk_provider_models` 59 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 507 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 14:13 | MVP Hardening | **CHECKPOINT** | `61cd963` | `26325491675` | Split AI model settings dialog | 第八輪 Tk 主檔安全解耦：把 `open_ai_model_settings()` 抽成 `AiModelSettingsDialog` class，讓 AI profile 表格、選用勾選欄、OAuth/API key 按鈕調度與 profile row rendering 集中到 `frontends/tk/dialogs.py`。主 UI 仍保留實際 OAuth、API key session 與 credential 相關方法，避免一次搬動登入/憑證流程；`launcher_ui.py` 降到約 5520 行。新增 headless regression 鎖住 AI profile row values 與 active profile 勾選欄位；這一刀仍不搬 Google/Gemini 連線細節、OAuth device/browser flow、API key 儲存實作、repair panel 或下載/匯入 backend。驗證：`py_compile` 相關 UI/dialog/test 檔 OK；`tests.test_tk_dialogs tests.test_launcher_ui tests.test_tk_ui_labels tests.test_tk_ui_config tests.test_tk_desktop_integration tests.test_tk_ui_helpers tests.test_tk_provider_models` 58 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 506 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 14:01 | MVP Hardening | **CHECKPOINT** | `59c6101` | `26325267395` | Split Tk utility dialogs | 第七輪 Tk 主檔安全解耦：把開發者 CLI、介面語言設定、啟動環境檢查與最近事件紀錄四個 utility Toplevel 抽成 `DeveloperCliDialog`、`UiLanguageSettingsDialog`、`StartupEnvironmentChecksDialog`、`RecentEventLogsDialog` class。`ApiCollectionUi` 只保留開窗委派；CLI command parsing、語言 label/code 還原、startup check table 與 event JSONL table 的 widget lifecycle 集中到 `frontends/tk/dialogs.py`。新增 headless regression 鎖住 quoted CLI argv、語言 label/code mapping 與事件表格欄位順序；`launcher_ui.py` 降到約 5604 行，仍不搬 repair panel、AI/OAuth flow、下載/匯入 backend 或資料庫 repair backend。驗證：`py_compile` 相關 UI/dialog/test 檔 OK；`tests.test_tk_dialogs tests.test_launcher_ui tests.test_tk_ui_labels tests.test_tk_ui_config tests.test_tk_desktop_integration tests.test_tk_ui_helpers tests.test_tk_provider_models` 57 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 505 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 13:44 | MVP Hardening | **CHECKPOINT** | `01991a8` | `26324931499` | Split data-store settings dialog | 第六輪 Tk 主檔安全解耦：把 `open_data_store_connection_settings()` 內的完整資料儲存設定視窗抽成 `DataStoreConnectionSettingsDialog` class，集中管理 profile table、測試連線、寫出 env 範本、設定 active profile、事件紀錄與錯誤提示。`ApiCollectionUi` 只保留 `DataStoreConnectionSettingsDialog(self)` 委派，並持續提供 `tr`、`status_var`、`open_integration_config_file` 與 `data_store_next_action_message` 這些窄 UI 介面；`launcher_ui.py` 降到約 5730 行。新增 regression 鎖住 class 匯入與 active profile label 使用主 UI 翻譯 callback；這一刀仍不搬 repair panel 的資料庫資產流程、不碰下載/匯入 backend。驗證：`py_compile` 相關 UI/dialog/test 檔 OK；`tests.test_tk_dialogs tests.test_launcher_ui tests.test_tk_ui_labels tests.test_tk_ui_config tests.test_tk_desktop_integration tests.test_tk_ui_helpers tests.test_tk_provider_models` 54 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 502 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 13:32 | MVP Hardening | **CHECKPOINT** | `5cf1c2a` | `26324695216` | Split Tk dialog classes | 第五輪 Tk 主檔安全解耦：依照「善用 class 封裝」方向，把已經有清楚生命週期與狀態邊界的 `ProviderEditorDialog`、`DatabaseClientSettingsDialog` 搬到 `frontends/tk/dialogs.py`，主檔只保留開窗與消費結果的呼叫點。新增 `tests/test_tk_dialogs.py`，在不啟動 Tk 視窗的情況下鎖住 dialog class 匯入、資料庫工具 profile label 與 selected profile id 配對。`launcher_ui.py` 降到約 5852 行；這一刀沒有搬下載 queue、背景 thread、repair/import backend 或一般 Toplevel workflow。驗證：`py_compile` 相關 UI/dialog/test 檔 OK；`tests.test_tk_dialogs tests.test_launcher_ui tests.test_tk_ui_labels tests.test_tk_ui_config tests.test_tk_desktop_integration tests.test_tk_ui_helpers tests.test_tk_provider_models` 53 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 501 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 13:16 | MVP Hardening | **CHECKPOINT** | `e635b43` | `26324382409` | Split Tk UI status labels | 第四輪 Tk 主檔安全解耦：把下載資格標籤/原因、下載修復標籤、資料庫修復標籤/說明、data-store `next_action` 訊息與 crawler `next_action` 訊息移到 `frontends/tk/ui_labels.py`，`ApiCollectionUi` 內保留同名 thin wrapper，讓後續 UI controller 可以重用同一套狀態文案而不必回到 6000+ 行主檔尋找字串。新增 `tests/test_tk_ui_labels.py` 鎖住 API key 後綴、本地化 fallback、SQL dry-run 邊界、data-store env 修復提示與 crawler parser 修復提示；這一刀仍不搬 dialog、thread、Tk widget lifecycle 或下載/匯入 backend 呼叫。驗證：`py_compile` 相關 UI/label/test 檔 OK；`tests.test_tk_ui_labels tests.test_launcher_ui tests.test_tk_ui_config tests.test_tk_desktop_integration tests.test_tk_ui_helpers tests.test_tk_provider_models` 50 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 498 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 12:48 | MVP Hardening | **CHECKPOINT** | `d389440` | `26323851448` | Split Tk UI config constants | 第三輪 Tk 主檔安全解耦：把產品顯示名、相容檔名、UI 語言、色票、表格欄位、responsive layout 參數、下載修復可 requeue 狀態與 `configured_ui_language()` 移到 `frontends/tk/ui_config.py`，並補 `tests/test_tk_ui_config.py` 鎖住語言 fallback、品牌/檔名相容性與 action 欄位位置。`launcher_ui.py` 仍只讀同一批常數，不搬 dialog、thread、widget lifecycle 或下載/匯入 backend 呼叫。驗證：`py_compile` 相關 UI/config/test 檔 OK；`tests.test_tk_ui_config tests.test_launcher_ui tests.test_tk_desktop_integration tests.test_tk_ui_helpers tests.test_tk_provider_models` 44 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 492 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 12:32 | MVP Hardening | **CHECKPOINT** | `20df3d8` | `26323548216` | Split Tk startup and desktop helpers | 第二輪 Tk 主檔安全解耦：把 Tk 啟動失敗修復提示與視窗生命週期 `TclError` suppressor 移到 `frontends/tk/startup_helpers.py`，把跨平台檔案管理器定位 helper 移到 `frontends/tk/desktop_integration.py`，並補 `tests/test_tk_desktop_integration.py` 鎖住 Windows explorer `/select`、macOS `open -R`、Linux 父資料夾 URI fallback。這一刀保持原本 suppressor 行為，不搬 dialog、thread、widget lifecycle 或下載/匯入 backend 呼叫。驗證：`py_compile` 相關 UI/helper/test 檔 OK；`tests.test_launcher_ui tests.test_tk_desktop_integration tests.test_tk_ui_helpers tests.test_tk_provider_models` 39 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 487 tests / 4 skipped + MVP demo smoke `stage=download_import_completed`、`row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 12:14 | MVP Hardening | **CHECKPOINT** | `5221160` | `26323194045` | Split Tk UI helper boundaries | `frontends/tk/launcher_ui.py` 第一輪高風險解耦完成：純 UI helper / MVP demo 訊息 / manual import 訊息 / yfinance path parser 移到 `frontends/tk/ui_helpers.py`，Provider table view-model 移到 `frontends/tk/provider_models.py`，並新增 `tests/test_tk_ui_helpers.py`、`tests/test_tk_provider_models.py`，讓後續測試可以不把所有純邏輯都綁在 6000+ 行 Tk 主檔上；這一刀沒有搬 dialog、thread、Tk widget lifecycle 或下載/匯入 backend 呼叫。`launcher_ui.py` 約降到 6243 行，`PROJECT_GTD` 與 `WORKSPACE_LAYOUT` 已補 UI 拆分規則。驗證：`py_compile` with temp pycache OK；`tests.test_launcher_ui tests.test_tk_ui_helpers tests.test_tk_provider_models` 36 tests OK；`git diff --check` OK；`scripts\pre_push_smoke_brief.cmd` 484 tests / 4 skipped + MVP demo smoke `row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 11:51 | MVP Hardening | **CHECKPOINT** | `01e20d0` | `26322747363` | Expose source draft audit handoff | Provider candidate source draft summary 現在帶 `next_action`、`audit_command` 與 `audit_source_ids`，CLI 會留下 `provider_candidate_source_drafts_written` structured event；`--handoff-report` / `--handoff-report-json` 會顯示最近一次 source draft 寫入時間、ignored local source 路徑、draft/skipped 數量與下一步 dry-run audit 指令。這讓 Mac 或 heartbeat agent 接力時能直接跑 local discovery audit，不會誤以為 source draft 已經是正式 catalog。同步 USER_GUIDE、DATASET_DISCOVERY_NOTES、GTD 與 repo/local skill。驗證：`tests.test_discovery_drafts tests.test_handoff tests.test_discovery tests.test_discovery_promotion` 27 tests、`py_compile`、`git diff --check`、`scripts\pre_push_smoke_brief.cmd` 483 tests / 4 skipped + MVP demo smoke `row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 11:30 | MVP Hardening | **CHECKPOINT** | `7a39951` | `26322334358` | Add provider candidate source draft CLI | Provider candidate review JSON 現在可透過 CLI/agent `--write-provider-candidate-source-drafts` 批次轉成 ignored local dataset discovery source 草稿，並可輸出 machine-readable summary、用 provider id 過濾候選；未知 landing page 或無法證明 supported crawler type/endpoint 的項目會留在 skipped/review，不會誤寫正式 catalog。這補齊了 Tk「寫入 source 草稿」之外的無 UI 接力路徑，Mac 或 heartbeat agent 可用同一份 review JSON 重建本機 source drafts，再跑 local discovery audit / crawler audit / promotion guard。同步更新 USER_GUIDE、DATASET_DISCOVERY_NOTES、GTD 與 repo/local skill。驗證：`tests.test_discovery_drafts tests.test_discovery tests.test_discovery_promotion tests.test_launcher_ui` 50 tests、`git diff --check`、`scripts\pre_push_smoke_brief.cmd` 482 tests / 4 skipped + MVP demo smoke `row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 08:41 | MVP Hardening | **CHECKPOINT** | `e68dfc8` | `26318718206` | Write provider candidates to source drafts | Provider 候選審核面板新增「寫入 source 草稿」動作，透過新的 `api_launcher.discovery_drafts` 把已明確標出或可保守推導 supported crawler type/endpoint 的候選轉成 ignored local dataset discovery source；未知 landing page、缺必要欄位、無法判斷 crawler type 的候選會繼續留在 review，不會假裝可爬。Tk UI 仍不寫正式 catalog、不安裝、不探測 credential；後續必須跑本機 discovery 草稿審核 / crawler audit / promotion guard。同步 USER_GUIDE、GTD、AGENT_HANDOFF、DATASET_DISCOVERY_NOTES 與 repo/local skill。驗證：`tests.test_discovery_drafts tests.test_launcher_ui tests.test_discovery tests.test_discovery_promotion` 48 tests、`git diff --check`、`scripts\pre_push_smoke_brief.cmd` 480 tests / 4 skipped + MVP demo smoke `row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 08:09 | MVP Hardening | **CHECKPOINT** | `5f533aa` | `26317936755` | Write provider candidates to local seeds | Tk provider candidate review panel 新增「寫入本機 seed」動作，可把選取候選轉成 `ProviderSeed` 並寫入 ignored local provider discovery seed config；此動作會保留 provider id、名稱、owner、categories、scope、source/docs/api/signup/auth type，並在缺少必要欄位時拒絕寫入。這仍不寫正式 catalog、不安裝、不探測 credential；後續必須再跑本機 discovery 草稿審核 / crawler audit / promotion guard。同步 USER_GUIDE、GTD、AGENT_HANDOFF 與 repo/local skill。驗證：`tests.test_launcher_ui tests.test_discovery` 41 tests、`git diff --check`、`scripts\pre_push_smoke_brief.cmd` 475 tests / 4 skipped + MVP demo smoke `row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 03:47 | MVP Hardening | **CHECKPOINT** | `ed0a532` | `26308617244` | Add provider candidate review panel | Tk `資料庫 > 審核 provider 候選` 新增 review-only 面板，讀取 `state/provider_candidates.ui.json`，用表格顯示 provider id、名稱、confidence、auth type、文件 URL，右側顯示 source/docs/API/signup/evidence 與「尚未納管、未安裝、未取得 API key」提醒，並提供開來源、開文件、開 review JSON 的按鈕。這補齊 provider discovery UI 產出 JSON 之後的一般使用者審核入口，但仍不寫正式 catalog。同步 USER_GUIDE、GTD 與 AGENT_HANDOFF。驗證：`tests.test_launcher_ui tests.test_discovery` 39 tests、`git diff --check`、`scripts\pre_push_smoke_brief.cmd` 473 tests / 4 skipped + MVP demo smoke `row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 03:37 | MVP Hardening | **CHECKPOINT** | `c86d7a1` | `26308196408` | Add provider discovery review UI | Tk `資料庫 > 發現 provider 候選` 新增 provider/source discovery review 入口：背景讀 built-in + local provider discovery seeds、跳過已存在 provider，輸出 `state/provider_candidates.ui.json` metadata-only review JSON，彈窗預覽 candidate id 與 confidence，並明確標示不寫正式 catalog、不安裝、不抓 API key 或登入內容。同步 USER_GUIDE、GTD、AGENT_HANDOFF 與 repo/local skill。驗證：`tests.test_launcher_ui tests.test_discovery` 38 tests、`git diff --check`、`scripts\pre_push_smoke_brief.cmd` 472 tests / 4 skipped + MVP demo smoke `row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 03:27 | MVP Hardening | **CHECKPOINT** | `d87df2f` | `26307734066` | Add local discovery audit UI dry run | Tk `資料庫 > 審核本機 discovery 草稿` 新增 local discovery promotion dry-run 入口：只審核 ignored local provider/source config，不直接寫正式 catalog；完成後寫出 `state/local_discovery_audit.ui.json`，彈窗會標明 dry-run、列出可提升/略過數量，並重用 crawler `audit_summary` 顯示整體狀態、warning 分組、下一步與略過來源。同步 USER_GUIDE、GTD、AGENT_HANDOFF 與 repo/local skill。驗證：`tests.test_launcher_ui tests.test_discovery_promotion` 32 tests、`git diff --check`、`scripts\pre_push_smoke_brief.cmd` 471 tests / 4 skipped + MVP demo smoke `row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 03:17 | MVP Hardening | **CHECKPOINT** | `404959d` | `26307284740` | Show crawler audit summary in Tk UI | Tk `資料庫 > 發現資料集候選` 的 warning dialog 不再只列逐 source warning；現在會先顯示後端頂層 `audit_summary`，包含整體狀態、source/candidate/problem source 數量、總體下一步、warning 分組、next-action 分組與優先檢查來源，再列 bounded 逐來源明細。這讓一般使用者不用開 JSON 也能知道是 0 筆、重複、metadata mapping 或 source overlap 問題。同步 USER_GUIDE、GTD、AGENT_HANDOFF 與 repo/local skill。驗證：`tests.test_launcher_ui tests.test_dataset_discovery tests.test_discovery_promotion` 54 tests、`git diff --check`、`scripts\pre_push_smoke_brief.cmd` 470 tests / 4 skipped + MVP demo smoke `row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 03:01 | MVP Hardening | **CHECKPOINT** | `56e7f40` | `26306555252` | Stabilize crawler audit dedupe order | 修正 `61d3bef` 暴露的並行 crawler 去重不穩定問題：crawler 仍並行抓取，但去重歸屬改成先收集結果，再依 catalog source 設定順序處理，避免 Ubuntu/Windows 因 `as_completed` 完成順序不同而把 duplicate warning 歸到不同 source。驗證：`py_compile api_launcher\crawlers\orchestrator.py tests\test_dataset_discovery.py`、`tests.test_dataset_discovery tests.test_discovery_promotion` 25 tests、`git diff --check`、`scripts\pre_push_smoke_brief.cmd` 469 tests / 4 skipped + MVP demo smoke `row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 02:56 | Discovery / Crawler | **CI 失敗** | `61d3bef` | `26306303037` | Summarize crawler audit outcomes | 新增 crawler `audit_summary`，讓 `--discover-dataset-candidates` 與 local discovery promotion audit JSON 在頂層輸出 `status`、`by_status`、`by_warning_code`、`by_next_action`、`problem_sources`，agent/UI 可先讀摘要再回到 `source_results` 查細節；同步更新 USER_GUIDE、DATASET_DISCOVERY_NOTES、GTD、handoff 與 launcher skill。Local targeted tests 與 pre-push smoke 通過，但 Ubuntu CI 在 full suite 中失敗，原因是並行 crawler 去重使用 `as_completed` 完成順序，duplicate problem source 可能在 `source_a` / `source_b` 間浮動；後續 `56e7f40` 已將去重歸屬改為 catalog source 順序並修回綠燈。 |
| 02:37 | MVP Hardening | **CHECKPOINT** | `a666249` | `26305468261` | Add structured download repair outcomes | 下載 manifest repair suggestion 增加 `outcome_bucket`、`next_action`、`adapter_id`、`review_hint`，讓 `--verify-downloads-json`、event log、library action repair stream 可以分辨 `requeue_ready`、`manifest_parse_error`、`source_url_missing`、`adapter_source_missing`、`adapter_source_not_requeueable`、`provider_id_missing` 等狀態。有 adapter metadata 且不能安全 requeue 時改導向 adapter review/resolver，不會假裝自動修復。測試補 `tests.test_repair` adapter/no-source/non-HTTP cases；驗證：以 `PYTHONPYCACHEPREFIX` 避開 Windows pycache lock 後 py_compile，`tests.test_repair tests.test_library_actions tests.test_handoff` 35 tests，`git diff --check`，`scripts\pre_push_smoke_brief.cmd` 469 tests / 4 skipped + MVP demo smoke `row_count=3`，CI Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 02:20 | MVP Hardening | **CHECKPOINT** | `d1c6d21` | `26304716621` | Show localized library action badges | 讓 Steam-like 資料資產右鍵動作真正露出可讀狀態：`api_launcher/library_actions.py` 新增 `status_badge` 的 zh-TW/en-US 短標籤與 menu-label 選項，Tk 右鍵選單會顯示「可加入計畫」、「可重排修復」、「可受控移除」等 badge；同時更新 USER_GUIDE、GTD、AGENT_HANDOFF，並補 `tests.test_library_actions` 覆蓋本地化 badge 與 disabled reason 合併輸出。驗證：`py -B -m py_compile api_launcher\library_actions.py frontends\tk\launcher_ui.py tests\test_library_actions.py`、`py -B -m unittest tests.test_library_actions tests.test_launcher_ui -v` 41 tests、`git diff --check`、`scripts\pre_push_smoke_brief.cmd` 467 tests / 4 skipped + MVP demo smoke `row_count=3`；CI Ubuntu、`windows-2025-vs2026`、real DB smoke 全部 success。 |
| 01:29 | Product Naming / MVP Hardening | **CHECKPOINT** | `3d3b35d` | `26302421754` | Brand as RuRuKa Asset Launcher | 將對外產品名統一為 `RuRuKa Asset Launcher`，短稱 `RRKAL`；Tk 視窗標題、UI ready 訊息、Google callback 頁標題、handoff / heartbeat 報告標題、README、產品定位、架構文件、GTD、使用者文件、repo/local skill 與測試全部同步。`Aseat` 拼字已收斂為標準 `Asset` / `Crawler Asset` 用語；產品定位明確寫入 `RRK` 可作為未來品牌字根，但 `APIkeys_collection` 仍保留為 repo、Python package、CLI 入口與歷史相容名稱。GitHub repo description 也更新為 RRKAL 描述。驗證：`git diff --check`、`py_compile APIkeys_collection.py APIkeys_collection_ui.py frontends\tk\launcher_ui.py api_launcher\handoff.py api_launcher\heartbeat.py`、`tests.test_handoff tests.test_heartbeat tests.test_launcher_ui` 49 tests、`scripts\pre_push_smoke_brief.cmd` 465 tests / 4 skipped + MVP demo smoke `row_count=3`、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |

### 2026-05-22

目前開發階段：**MVP Hardening**（canonical MVP Demo closure 已完成，接下來把 demo 能力收束成一般使用者也能順手操作、遇錯有引導、狀態可回溯的流程。）

主線：完成 repo-wide 繁中維護註解工程，把註解規則落到所有 `.py` 檔，同步 GTD 狀態，定義 `tem/` 本機暫存區的 Git 與交接規則，補上可重複 MVP Demo Flow，抽出下載/匯入 pipeline slice，讓 UI 共用 demo 與匯入邊界，把 yfinance 收斂成可選非官方金融資料源、離線時間序列 Demo plan、明確 opt-in live CSV plan、保留天數、查詢視窗、儲存目標 metadata、storage review dry-run、Tk 工具入口與人類/DBA handoff，並讓 Tk 審查流程一次輸出 review JSON / dry-run SQL / handoff Markdown，將語言套件支援庫收斂成 canonical source metadata / access surface 關聯表，把 MySQL/PostgreSQL missing table 的 dry-run SQL 修復引導接到 CLI 與 Tk UI，補上 data-store env 範本輸出、agent-readable 連線測試 JSON 指引、Tk 下一步修復提示、本機作用中 data-store profile 選擇與 handoff 報告摘要，新增使用者自備本機 CSV/JSON 檔的 manifest/provenance、CLI 與 Tk SQLite 匯入閉環，補上手動匯入 agent-readable JSON payload、provenance review 摘要、Tk 成功對話框短版來源審查與不支援格式轉檔/adapter review 引導，把「一輪對話可多個實質 checkpoint」的推進規則固化到 handoff/skill，並以 `MVP Readiness` 將 canonical MVP Demo closure 與 post-MVP hardening 明確切開。

| 時間 | 開發階段 | 標記 | SHA | Run | 原始標題 | 中文說明 |
| --- | --- | --- | --- | --- | --- | --- |
| 23:54 | Docs / Workflow | **CHECKPOINT** | `33d5bae` | `26297982209` | Update GitHub owner references | GitHub 帳號 / repo owner 已由 `YanAnnLu` 改為 `kagamihara-rururka`，本機 `origin` 已更新為 `https://github.com/kagamihara-rururka/APIkeys_collection.git`。同步更新 repo/local launcher skill、heartbeat `REPO_SLUG`、manual import docs URL、AGENT_HANDOFF、GIT_HANDOFF、PROJECT_STATE 與 failure modes 內的 `gh run list/watch` 固定 repo slug；歷史脈絡保留舊名但明確標示已改名。驗證：`gh repo view kagamihara-rururka/APIkeys_collection`、`git ls-remote`、`gh api user --jq .login`、`py_compile api_launcher\heartbeat.py api_launcher\manual_import.py`、`tests.test_heartbeat tests.test_handoff tests.test_manual_import` 30 tests、`git diff --check`、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 23:38 | Docs / Workflow | **CHECKPOINT** | `695edb3` | `26297180294` | Document OpenSpec language policy | 回答並固化 OpenSpec 語言規則：OpenSpec 不限英文，capability id、change id、CLI flag、路徑、`Requirement:` / `Scenario:` 等工具標記維持英文/ASCII；需求意圖、驗收標準、風險、交接提醒可用繁中或雙語。同步 `development-workflow` spec、DEVELOPMENT_WORKFLOW_OPEN_SPEC 與 AGENT_HANDOFF，避免未來 agent 誤以為 OpenSpec 一律只能英文。驗證：`cmd /c npx.cmd -y @fission-ai/openspec@latest validate --all --no-interactive` 1 spec passed、`git diff --check`、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 23:32 | Docs / Workflow | **CHECKPOINT** | `69744ac` | `26296920268` | Document portable OpenSpec workflow | 將目前專業化開發模式整理成可移植工作流骨架：GTD、handoff、development log、docs index、OpenSpec workspace、project skill、本地 smoke checks 與 checkpoint reporting rules；同時明確移植到其他 repo 時要刪掉 APIkeys_collection 專用的 crawler/provider/database/renderer 規則。新增 Windows `scripts/check_ui_tooling.cmd` / `.ps1`，可輸出人類版與 JSON 版 UI tooling 盤點，確認本機 Spectra 2.3.1 可用、Qt Creator 尚未安裝或不在 PATH、Conda `py3_12_13` 尚無 PySide6、`metal_trade_312` 屬於 macOS 工作站。同步 DEVELOPMENT_WORKFLOW_OPEN_SPEC、SETUP、PROJECT_GTD、AGENT_HANDOFF、OpenSpec spec 與 repo/local skill。驗證：`check_ui_tooling` human/JSON、PowerShell AST parse、OpenSpec validate、`git diff --check`、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 22:54 | MVP Hardening | **CHECKPOINT** | `e43edae` | `26294997403` | Surface crawler next actions in Tk UI | Tk `資料庫 > 發現資料集候選` 現在會把 crawler audit 的 `next_action` 轉成繁中下一步提示，完成狀態列與 warning dialog 會直接告訴使用者要審來源、修搜尋詞/parser、調整最低預期、檢查去重/id mapping，或修 metadata mapping，而不是只列 warning 文字。新增 `crawler_next_action_label()` 與 bounded `crawler_audit_issue_lines()`，保留後端 `warning_codes` / `next_action` 為單一事實來源，並同步 USER_GUIDE、DATASET_DISCOVERY_NOTES、PROJECT_GTD 與 AGENT_HANDOFF。驗證：暫存 pycache `py_compile frontends\tk\launcher_ui.py tests\test_launcher_ui.py`、`tests.test_launcher_ui tests.test_dataset_discovery tests.test_discovery_promotion` 53 tests、`git diff --check`、`scripts\pre_push_smoke_brief.cmd` 465 tests / 4 skipped + MVP demo smoke `row_count=3`、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 22:38 | Docs / Workflow | **CHECKPOINT** | `19088e8` | `26294166838` | Add brief pre-push smoke output | 部署長輸出降噪工作流：新增 `scripts/pre_push_smoke_brief.cmd` / `.ps1` 與 `scripts/summarize_command_log.ps1`，完整 pre-push smoke log 會寫到 `state/logs/pre_push_smoke_*.log`，對話中只輸出關鍵狀態、失敗、traceback、unittest 與 MVP smoke 行，降低 agent token 消耗但保留 raw log 證據。同步 GIT_HANDOFF、DEVELOPMENT_WORKFLOW_OPEN_SPEC、AGENT_HANDOFF、PROJECT_GTD 與 repo/local skill。也評估 `@samuelfaj/distill@1.5.2`：Windows npm 安裝後會尋找尚未發布的 `@samuelfaj/distill-win32-x64`，因此不列為專案依賴，且已移除不可執行的全域安裝；未來只有 `distill.cmd --version` 成功時才可作為 saved log 的可選後處理。驗證：實際 `.\scripts\pre_push_smoke_brief.cmd` 通過，輸出摘要且完整 log 落地；`git diff --check` OK；CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 22:16 | MVP Hardening | **CHECKPOINT** | `27bcc49` | `26293038205` | Add crawler audit next actions | Crawler audit 現在不只列 warning 文字，也輸出 source-level `warning_codes` 與 `next_action`；`zero_candidates`、`below_min_candidates`、`duplicate_heavy_output`、`all_candidates_duplicate`、`candidate_metadata_issue` 會路由到修搜尋詞/parser、調整最低預期、檢查去重/id mapping 或修 metadata mapping。`--discover-dataset-candidates` JSON、local discovery promotion audit JSON 與 repo/local skill 都同步採用這套狀態碼，並新增 duplicate-heavy output 防線，避免「有抓到資料但重複異常偏高」被誤判為成功。同步 DATASET_DISCOVERY_NOTES、USER_GUIDE、PROJECT_GTD 與 AGENT_HANDOFF。驗證：`py_compile` 受影響 crawler/CLI/promotion/tests、`tests.test_dataset_discovery tests.test_discovery_promotion` 25 tests、`git diff --check`、完整 `scripts\pre_push_smoke.cmd` 463 tests / 4 skipped + MVP demo smoke `row_count=3`、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 21:18 | MVP Hardening | **CHECKPOINT** | `3440311` | `26290151732` | Guide Tk startup failures and tag log phases | Tk UI 啟動最早期若 `Tk()` 因 `init.tcl`、Tcl/Tk runtime 或 display 問題失敗，現在會寫入 `ui_tk_startup_failed` event、在 stderr 印出繁中修復建議並回傳 `2`，避免使用者只看到堆疊或視窗無聲消失。同步 SETUP、USER_GUIDE、GTD 與 handoff。開發日誌也回補 `開發階段` 欄位，所有既有 row 都標示粗粒度階段，並把此規則同步到 DOCS_INDEX、handoff、repo/local skill。驗證：暫存 pycache `py_compile frontends\tk\launcher_ui.py tests\test_launcher_ui.py`、`tests.test_launcher_ui` 26 tests、開發日誌表格欄位檢查、`git diff --check`、完整 `scripts\pre_push_smoke.cmd` 462 tests / 4 skipped + MVP demo smoke `row_count=3`、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 20:58 | MVP Hardening | **CHECKPOINT** | `b6ff2ff` | `26289185846` | Add one-click MVP demo smoke UI | Tk UI 新增 `工具/更多 > 一鍵驗證 MVP Demo Flow`，讓一般使用者或展示者不必手動跑多段 CLI，就能在背景使用隔離 `state/mvp_demo/launcher.sqlite` 執行 canonical 離線 `download -> manifest -> SQLite import` smoke。完成後 UI 會顯示 stage、table、row_count、下載/匯入統計與 artifact 路徑；失敗時會引導使用者查看最近事件紀錄、修復/驗證資產面板，並附上可在 PowerShell 重跑的 CLI 指令。同步 USER_GUIDE、USER_MANUAL、GTD 與 handoff。驗證：`py_compile APIkeys_collection.py APIkeys_collection_ui.py frontends\tk\launcher_ui.py api_launcher\core.py tests\test_launcher_ui.py`、`tests.test_mvp_demo_flow tests.test_ingestion_pipeline tests.test_launcher_ui` 32 tests、完整 `unittest discover -s tests` 460 tests / 4 skipped、實際 `--run-mvp-demo-smoke-json` 回報 `stage=download_import_completed` 且 `row_count=3`、`git diff --check`、完整 `scripts\pre_push_smoke.cmd` 460 tests / 4 skipped + MVP demo smoke `row_count=3`、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 20:08 | MVP Demo Closure | **CHECKPOINT** | `480e22c` | `26286893742` | Add MVP readiness to handoff | `--handoff-report` / `--handoff-report-json` 新增 `MVP Readiness` / `mvp_readiness`，把最近一次 canonical `--run-mvp-demo-smoke-json` 的 `stage`、`succeeded`、table 與 `row_count` 轉成 `ready_for_mvp_demo` 或 blocker 清單。成功條件明確定義為 `download_import_completed`、`succeeded=true`、`row_count > 0`，讓 canonical MVP Demo closure 可回報 0%，而 GTD 內的 crawler/UI/database/renderer/lakehouse hardening 不再被誤算成 demo closure blocker。同步 USER_GUIDE、AGENT_HANDOFF、GTD 與 repo/local skill。驗證：`tests.test_handoff` 11 tests、`py_compile api_launcher/handoff.py tests/test_handoff.py`、實際 `--run-mvp-demo-smoke-json` 回報 `row_count=3`、`--handoff-report-json` 回報 `mvp_readiness.status=ready_for_mvp_demo`、`git diff --check`、完整 `scripts\pre_push_smoke.cmd` 457 tests / 4 skipped + MVP demo smoke `row_count=3`、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 19:49 | MVP Demo Closure | **CHECKPOINT** | `164b907` | `26286042831` | Add MVP smoke to handoff resume checks | `--handoff-report` 的 Suggested Resume Checks 現在直接包含 `py APIkeys_collection.py --db state/mvp_demo/launcher.sqlite --init-db --seed --run-mvp-demo-smoke-json state/mvp_demo/flow.json`，讓接手者不用翻 USER_GUIDE 就能重跑 canonical 離線 MVP 閉環。同步 AGENT_HANDOFF 與 GTD，並補 handoff regression。驗證：`tests.test_handoff` 9 tests、`py_compile api_launcher/handoff.py tests/test_handoff.py`、`git diff --check`、`scripts\pre_push_smoke.cmd` 455 tests / 4 skipped + MVP demo smoke `row_count=3`、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 19:40 | CI / Dev Workflow | **CHECKPOINT** | `cd555b5` | `26285637021` | Fix Windows UTF-8 CLI smoke | 修復前一筆 Windows CI CLI smoke 失敗：`--run-mvp-demo-smoke-json` 會輸出含中文欄位的 JSON，在 `windows-2025-vs2026` runner 預設 cp1252 stdout 下觸發 `UnicodeEncodeError`。`main()` 現在啟動時會將 stdout/stderr reconfigure 為 UTF-8 並以 replace fallback 保護舊終端；CI 與 pre-push smoke 也設定 `PYTHONIOENCODING=utf-8`。本機用 `PYTHONIOENCODING=cp1252` 模擬後，MVP smoke 可正常輸出；完整 `scripts\pre_push_smoke.cmd` 455 tests / 4 skipped 並通過 MVP demo smoke；CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 19:34 | MVP Demo Closure | **CI 失敗** | `a6e342b` | `26285399718` | Gate pushes on offline MVP smoke | 將離線 MVP demo smoke 加進 `scripts/pre_push_smoke.ps1` 與 GitHub Actions CLI smoke，讓本地 push gate 與 CI 都實際驗證 `download -> manifest -> SQLite import`。本地完整 pre-push smoke 通過，但 Windows GitHub runner 的 stdout 編碼為 cp1252，列印含中文 `acceptance_zh_TW` 的 JSON 時失敗；Ubuntu 與 real DB smoke 均成功。此失敗確認了 CI gate 有價值，也暴露 CLI UTF-8 輸出 guard 缺口；下一筆 `cd555b5` 已修復。 |
| 19:21 | MVP Demo Closure | **CHECKPOINT** | `c59d10c` | `26284841398` | Surface MVP demo smoke in handoff | `--run-mvp-demo-smoke-json` 完成後寫入的 `mvp_demo_smoke_completed` structured event 現在會被 `--handoff-report` / `--handoff-report-json` 摘出，列出最近一次 MVP demo smoke 的時間、`stage`、`succeeded`、table 與 `row_count`，讓下一位 agent 不必重跑 smoke 或解析 raw logs 就能知道 canonical MVP 閉環最近是否成功。同步 USER_GUIDE、GTD、AGENT_HANDOFF 與 repo/local skill。驗證：`tests.test_handoff` 9 tests、`py_compile api_launcher/handoff.py tests/test_handoff.py`、`git diff --check`、`scripts\pre_push_smoke.cmd` 455 tests / 4 skipped、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 19:11 | MVP Demo Closure | **CHECKPOINT** | `070e6e0` | `26284395811` | Add offline MVP demo smoke JSON | 新增 `--run-mvp-demo-smoke-json`，讓 agent 或 release smoke 能用單一 CLI 重寫 canonical MVP Demo artifacts，直接跑離線 `download -> manifest -> SQLite import`，並輸出純 JSON 摘要，包含 `stage=download_import_completed`、`succeeded=true`、`table_name`、`row_count=3` 與 download/import 統計。`api_launcher.mvp_demo` 新增 `run_mvp_demo_offline_smoke()` 與 SQLite row-count 驗收，flow commands 也加入一鍵 smoke 步驟；USER_GUIDE、USER_MANUAL、GTD、handoff 與 repo/local skill 同步更新。驗證：`tests.test_mvp_demo_flow` 4 tests、`tests.test_mvp_demo_flow tests.test_ingestion_pipeline` 8 tests、實際 temp CLI smoke、`py_compile`、`git diff --check`、`scripts\pre_push_smoke.cmd` 454 tests / 4 skipped、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 18:32 | Agent Handoff / Observability | **CHECKPOINT** | `efb8d57` | `26282730551` | Emit handoff report JSON snapshot | 新增 `--handoff-report-json`，讓 heartbeat 或外部 agent 可直接從 stdout 取得與 Markdown `--handoff-report` 同源的 JSON snapshot，包含 git 狀態、catalog 摘要、verification summary、open GTD focus、recent logs、portal intake/local discovery 摘要；JSON mode 會抑制 `[db]`、`[seed]` 與 `[handoff]` 人類提示，避免 parser 被文字污染。同步更新 USER_GUIDE、AGENT_HANDOFF、GTD 與 repo/local skill。驗證：`tests.test_handoff` 8 tests、`py_compile api_launcher/core.py api_launcher/handoff.py api_launcher/cli_flags.py tests/test_handoff.py`、`git diff --check`、`scripts\pre_push_smoke.cmd` 453 tests / 4 skipped、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 18:21 | Download / Import | **CHECKPOINT** | `b660caf` | `26282215732` | Log download plan execution in handoff | `--run-download-plan` 完成後現在會寫入 `download_plan_executed` structured event，context 包含 input plan、stage、next_action、entry/submitted/completed/skipped、skip_summary、import counts 與 error_count；`--handoff-report` 會固定列出最近一次下載計畫執行的 plan、stage、counts、skip_summary 與下一步，讓接力 agent 不必重跑下載或解析文字輸出。同步更新 USER_GUIDE、AGENT_HANDOFF、GTD 與 repo/local skill。驗證：`tests.test_download_plan_runner` + `tests.test_handoff` 17 tests、`py_compile api_launcher/core.py api_launcher/handoff.py tests/test_download_plan_runner.py tests/test_handoff.py`、`git diff --check`、`scripts\pre_push_smoke.cmd` 451 tests / 4 skipped、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 18:04 | Download / Import | **CHECKPOINT** | `7e02842` | `26281487056` | Emit download plan JSON summary | `--run-download-plan` 新增 `--run-download-plan-json`，讓下載/匯入 pipeline 可輸出純 JSON 摘要，包含 `stage`、`next_action`、`result.skip_summary`、下載/匯入統計與 `errors`；JSON stdout 模式會保留 init/seed 行為但抑制 `[db]`/`[seed]` 人類提示，避免 agent 解析失敗。同步更新 `USER_GUIDE`、`AGENT_HANDOFF`、GTD 與 repo/local skill，並補 CLI regression。驗證：`tests.test_download_plan_runner` + `tests.test_ingestion_pipeline` 15 tests、`py_compile api_launcher/core.py api_launcher/cli_flags.py tests/test_download_plan_runner.py`、`git diff --check`、`scripts\pre_push_smoke.cmd` 450 tests / 4 skipped、CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 17:47 | Adapter / Resolver | **CHECKPOINT** | `eb60997` | `26280723173` | Emit adapter plan resolution JSON summary | `--resolve-adapter-plan` 現在可加 `--resolve-adapter-plan-json`，讓 stdout 輸出 agent-readable resolver summary JSON，包含 input/output plan、entry_count、direct_entries_added、resolved/unresolved、warning_count、warnings 與 resolved plan summary；預設人類輸出仍保持原本 `[adapter-resolve] wrote ...` 形狀。同步 USER_GUIDE、handoff、GTD 與 repo/local skill，讓 heartbeat/agent 不必解析人類字串。驗證包含 `tests.test_adapter_plan_resolver` 46 tests、`py_compile api_launcher/core.py api_launcher/cli_flags.py tests/test_adapter_plan_resolver.py`、`git diff --check`、完整 `scripts\pre_push_smoke.cmd`（449 tests / 4 skipped、summary）；CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 17:34 | Adapter / Resolver | **CHECKPOINT** | `d5589a3` | `26280159333` | Surface adapter plan resolution in handoff | `--handoff-report` 的 Verification Timestamps 現在會固定列出最近一次 `adapter_plan_resolved` 的時間、resolved plan 輸出路徑，以及 direct/resolved/unresolved/warning 統計，讓接力者不用翻 raw logs 就能判斷 Adapter 計畫最近是否已解析過。同步 handoff 與 GTD。驗證包含 `tests.test_handoff` + `tests.test_adapter_plan_resolver` 50 tests、`py_compile api_launcher/handoff.py tests/test_handoff.py`、`--handoff-report state\handoff_smoke.md`、`git diff --check`、完整 `scripts\pre_push_smoke.cmd`（448 tests / 4 skipped、summary）；CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 17:23 | Adapter / Resolver | **CHECKPOINT** | `84edf1c` | `26279627272` | Log adapter plan resolution | `--resolve-adapter-plan` 成功寫出 resolved plan 時會記錄 `adapter_plan_resolved` structured event，context 包含 input/output plan、entry_count、output_entry_count、resolved/unresolved review entries、direct_entries_added 與 warning_count。這補上 Adapter review JSON export 之外的下一段 resolver 可觀測性，讓接力者可以從 logs 判斷最近是否已做過 bounded plan rewrite。同步 handoff 與 observability GTD。驗證包含 `tests.test_adapter_plan_resolver` + `tests.test_handoff` 49 tests、`py_compile api_launcher/core.py tests/test_adapter_plan_resolver.py`、`git diff --check`、完整 `scripts\pre_push_smoke.cmd`（447 tests / 4 skipped、summary）；CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 17:14 | Adapter / Resolver | **CHECKPOINT** | `ecd5802` | `26279227181` | Surface adapter review exports in handoff | `--handoff-report` 的 Verification Timestamps 現在會固定列出最近一次 `adapter_review_json_written` 的時間、輸出路徑與 outcome summary，不只把事件藏在 Recent Logs。這讓下一位 agent 或 heartbeat 一讀交接報告就能定位最近產出的 Adapter 待辦 JSON 檔。同步 GTD。驗證包含 `tests.test_handoff` + `tests.test_dataset_download_plan` 21 tests、`py_compile api_launcher/handoff.py api_launcher/core.py tests/test_handoff.py tests/test_dataset_download_plan.py`、`git diff --check`、完整 `scripts\pre_push_smoke.cmd`（447 tests / 4 skipped、summary）；CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 17:05 | Adapter / Resolver | **CHECKPOINT** | `16165c9` | `26278789580` | Log adapter review JSON exports | `--write-adapter-review-json` 現在成功寫出檔案時會記錄 `adapter_review_json_written` structured event，context 包含 input plan、output path、item_count 與 `by_outcome`，讓 `--show-logs` / handoff report / agent 接力可以知道最近產生過哪份 Adapter 待辦交接檔。同步 observability GTD 與 handoff。驗證包含 `tests.test_dataset_download_plan` 17 tests、`py_compile api_launcher/core.py tests/test_dataset_download_plan.py`、`git diff --check`、完整 `scripts\pre_push_smoke.cmd`（446 tests / 4 skipped、summary）；CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 16:53 | MVP Demo Closure | **CHECKPOINT** | `523a55d` | `26278235384` | Add adapter review JSON to MVP demo flow | `--write-mvp-demo-flow` 現在會同步產生 `socrata_311.adapter_review.json`，並在 flow commands 的 Adapter 待辦步驟加入 `--write-adapter-review-json`，讓 canonical MVP demo 不只有人類 review plan，也能提供 heartbeat/agent 可直接讀取的 outcome summary。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 repo/local skill。驗證包含 `py_compile api_launcher/mvp_demo.py tests/test_mvp_demo_flow.py`、`tests.test_mvp_demo_flow` + `tests.test_ingestion_pipeline` 7 tests、`git diff --check`、完整 `scripts\pre_push_smoke.cmd`（446 tests / 4 skipped、summary）；CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 16:39 | Adapter / Resolver | **CHECKPOINT** | `dbd2ead` | `26277622971` | Write adapter review JSON handoff | Adapter 待辦現在除了 stdout JSON，也可用 `--write-adapter-review-json PATH` 把同一份 agent-readable payload 寫成檔案，方便 heartbeat、外部 agent 或接力腳本不用從人類輸出擷取大型 JSON。新 flag 必須搭配 `--adapter-review-plan PATH`，只寫 review payload 與短摘要，不 resolve、不下載、不匯入；repo/local skill、GTD、handoff 與 USER_GUIDE 同步更新。驗證包含 `py_compile api_launcher/core.py api_launcher/cli_flags.py tests/test_dataset_download_plan.py`、`tests.test_dataset_download_plan` 17 tests、`git diff --check`、完整 `scripts\pre_push_smoke.cmd`（446 tests / 4 skipped、summary）；CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 16:26 | Adapter / Resolver | **CHECKPOINT** | `62db28e` | `26277047384` | Test adapter review JSON CLI outcomes | 補上 `--adapter-review-plan ... --adapter-review-json` 的 CLI regression，確認 stdout 可解析為 agent-readable JSON，且 `summary.by_outcome` 與 item `outcome_bucket` 會保留 `downloaded_payload_transform`。這保護後續 CLI subcommand / core.py 拆分時不要只保留 human output、卻弄斷 agent 自動化入口。驗證包含 `tests.test_dataset_download_plan` 16 tests、`git diff --check`、完整 `scripts\pre_push_smoke.cmd`（445 tests / 4 skipped、summary）；CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠。 |
| 16:14 | CI / Dev Workflow | **CHECKPOINT** | `dbc3547` | `26276537859` | Pin Windows CI to VS 2026 image | GitHub Actions 的一般測試矩陣從 `windows-latest` 改成明確的 `windows-2025-vs2026`，提前在 GitHub 即將把 `windows-latest` 導向的 Windows Server 2025 / Visual Studio 2026 image 上跑測試，避免未來隱式切換才發現差異。同步 GIT_HANDOFF、GTD 與 repo/local skill。驗證包含 `git diff --check`、完整 `scripts\pre_push_smoke.cmd`（444 tests / 4 skipped、summary）；CI Ubuntu、`windows-2025-vs2026` 與 real DB smoke 全綠，且不再出現 `windows-latest` migration notice。 |
| 16:04 | Adapter / Resolver | **CHECKPOINT** | `df36c31` | `26276085727` | Classify adapter review outcomes | Adapter 待辦現在會在每筆 item 補 `outcome_bucket`，並在 agent-readable JSON summary 增加 `by_outcome`，把需要來源解析的 `source_resolution_required`、已可下載但需要解壓/轉換的 `downloaded_payload_transform`、以及 import adapter/transform 類問題分開。CLI `--adapter-review-plan` 會印出 `outcome=...`，Tk `Adapter 待辦` 表格與 detail 也會顯示結果分類，讓人類與 agent 不必只靠 free-text reason 或 UI 字串判斷下一步。同步 USER_GUIDE、GTD、handoff 與 repo/local skill。驗證包含 `py_compile api_launcher/adapter_review.py api_launcher/core.py frontends/tk/launcher_ui.py`、15 個 dataset download plan / adapter review 目標測試、`git diff --check`、完整 `scripts\pre_push_smoke.cmd`（444 tests / 4 skipped、summary）；CI Ubuntu、Windows 與 real DB smoke 全綠。 |
| 15:52 | MVP Hardening | **CHECKPOINT** | `b8ecd5d` | `26275562555` | Add library action status badges | `api_launcher.library_actions` 的每個 action 現在都有短版 `status_badge`，例如 `ready_to_plan`、`update_available`、`repair_requeue_ready`、`missing_render_assets`、`guarded_uninstall_ready`。CLI human output 會印出 `badge=...`，agent-readable JSON 也會帶同一欄位，讓 UI/agent 能顯示狀態或 routing，而不用從英文 `reason` 重新推斷；實際執行仍以 `enabled`、`risk`、ownership metadata 與 guarded CLI 參數為準。同步 USER_GUIDE、TECHNICAL_OVERVIEW、GTD、handoff 與 repo/local skill。驗證包含 `py_compile api_launcher/library_actions.py api_launcher/core.py`、11 個 library action 目標測試、`git diff --check`、完整 `scripts\pre_push_smoke.cmd`（443 tests / 4 skipped、summary）；CI Ubuntu、Windows 與 real DB smoke 全綠。 |
| 15:39 | Download / Import | **CHECKPOINT** | `45ca772` | `26275010791` | Guide Tk import skips | Tk `匯入可支援下載結果` 現在若同一份 plan 同時有可匯入項目與略過項目，確認框會列出略過原因預覽，並提示先處理 Adapter 待辦、解析 Adapter 計畫、下載或 manifest 健康狀態，避免只顯示 skipped 數量。新增 `import_skipped_detail_message()` 與 fake UI regression，確認預覽會限制長度、保留略過原因，且混合 supported/adapter-required plan 的匯入確認框會顯示原因。同步 USER_GUIDE、GTD、handoff，並依使用者授權修正 Spectra/OpenSpec 規則：Agent 可主動用自動化處理文書/規格/checkpoint 整理，測試與 CI 紀錄是接力證據，不是額外卡點。驗證包含 21 個 Tk 目標測試、`py_compile frontends/tk/launcher_ui.py`、`git diff --check`、完整 `scripts\pre_push_smoke.cmd`（442 tests / 4 skipped、summary）；CI Ubuntu、Windows 與 real DB smoke 全綠。 |
| 15:25 | CI / Dev Workflow | **CHECKPOINT** | `013ce8a` | `26274410345` | Check pending push diff in pre-push smoke | 強化本地 pre-push smoke 的 diff 檢查範圍：除了 worktree 與 staged diff，也會在有 upstream 時檢查 `origin/main..HEAD` 這類待推送 commit diff，避免 pre-push hook 在乾淨工作區下漏掉已提交但尚未推送的 whitespace 問題。同步 GIT_HANDOFF、OpenSpec/Spectra 流程、handoff 與 repo/local Codex skill。驗證包含 `pre_push_smoke.ps1` PowerShell AST 語法檢查、`pre_push_smoke.cmd -SkipTests/-SkipSummary`、完整 `pre_push_smoke.cmd`（worktree/staged/pending-push 三段輸出、440 tests / 4 skipped、summary）；CI Ubuntu、Windows 與 real DB smoke 全綠。 |
| 15:09 | CI / Dev Workflow | **CHECKPOINT** | `40137ac` | `26273759018` | Add local pre-push smoke workflow | 新增本地預檢 workflow：`scripts/pre_push_smoke.cmd` / `.ps1` 可在 push 前跑 `git diff --check`、核心 `py_compile`、完整 `unittest discover -s tests` 與 `--summary`，並固定 temp pycache 以避開 Windows/RaiDrive 鎖檔；`scripts/install_pre_push_hook.cmd` / `.ps1` 可選擇把同一條檢查安裝成該 clone 的 `.git/hooks/pre-push`，不強迫提交 hook。同步 GIT_HANDOFF、OpenSpec/Spectra 流程、GTD、handoff、repo/local Codex skill；同時依使用者授權修正文案，允許 Agent 主動用 Spectra/OpenSpec 自動化代辦行政文書與規格套用，但每個實質 checkpoint 仍保留測試與 CI 紀錄。驗證包含 PowerShell AST 語法檢查、`pre_push_smoke.cmd -SkipTests`、完整 `pre_push_smoke.cmd`（440 tests / 4 skipped、summary、diff check）；CI Ubuntu、Windows 與 real DB smoke 全綠。 |
| 14:53 | Download / Import | **CHECKPOINT** | `5594a12` | `26273129273` | Guide partial Tk download skips | Tk 下載計畫現在不只在「全部都不能 direct download」時提示 Adapter 下一步；若同一份計畫已有部分 direct download 成功排隊、但另有 adapter-required / metadata / selector 項目被略過，也會彈出「部分項目未啟動下載」提示，說明已啟動的下載會繼續排隊，被略過的項目仍要走 Adapter 待辦或解析 Adapter 計畫。新增 `download_skip_next_action_message()` 與 fake UI regression，確認 mixed direct + adapter plan 只送出 direct job 並保留 skipped 指引；同步 USER_GUIDE、GTD、handoff。驗證包含 temp pycache `py_compile`、19 個 Tk 目標測試、`--summary`、`git diff --check`、本機 `py -B -m unittest discover -s tests` 440 tests / 4 skipped；CI Ubuntu、Windows 與 real DB smoke 全綠。 |
| 14:40 | Download / Import | **CHECKPOINT** | `e4bd8d2` | `26272614161` | Keep next action for partial download skips | 下載/匯入 pipeline 現在即使有部分 direct plan 成功、但同一份 plan 仍有 adapter-only 或非 direct 項目被略過，也會保留 blocked `next_action`，避免 CLI、UI 或後續 agent 把「部分成功」誤判成「整份計畫已完成」。新增 mixed direct + adapter regression，確認 `completed=1`、`skipped=1` 時仍輸出 adapter review 指引。驗證包含 temp pycache 的 `py_compile`、`tests.test_ingestion_pipeline`、`--summary`、`git diff --check`、本機 `py -B -m unittest discover -s tests` 438 tests / 4 skipped；CI Ubuntu、Windows 與 real DB smoke 全綠。 |
| 14:31 | Download / Import | **CHECKPOINT** | `91ae773` | `26272293420` | Guide Tk manual import format errors | Tk 手動匯入不支援格式時，現在會直接顯示後端已準備好的修復指引，不再在對話框前面加 `ValueError:` 這類工程例外字樣。新增 `local_file_import_error_message()` 並補 UI worker regression，確認 Excel/未知格式會提示「請先轉成支援格式，或留在 adapter/manual review，不要硬塞進 SQLite」。驗證包含 temp pycache 的 `py_compile`、17 個 Tk 目標測試、`--summary`、`git diff --check`、本機 `py -B -m unittest discover -s tests` 437 tests / 4 skipped；CI Ubuntu、Windows 與 real DB smoke 全綠。 |
| 14:20 | Download / Import | **CHECKPOINT** | `e94ef7a` | `26271895829` | Localize Tk download plan labels | 收束 Tk 下載計畫區的可見文案：初始計數從「0 個資料源」改成「0 個項目」，更符合 provider / dataset / version 都可進 plan 的現況；移除、清空、匯出計畫按鈕與 cart 欄名改用 `self.tr(...)`，讓介面語言切換不會留下硬編碼中文。驗證包含 `py_compile`、15 個 Tk 目標測試、`--summary`、`git diff --check`、本機 `py -B -m unittest discover -s tests` 435 tests / 4 skipped；CI Ubuntu、Windows 與 real DB smoke 全綠。 |
| 14:12 | CI / Dev Workflow | **CHECKPOINT** | `a5ec662` | `26271612538` | Make tests importable for unittest discovery | 新增 `tests/__init__.py`，讓 `python -m unittest discover -s tests` 在本機 Windows/K: 工作區、GitHub Actions 與其他 Python 3.13 環境都能把 `tests` 視為可匯入起點。這修掉本輪本機完整測試第一次被 discovery 啟動器卡住的問題；驗證包含 `py_compile tests/__init__.py`、本機 `py -B -m unittest discover -s tests` 435 tests / 4 skipped、`git diff --check`；CI Ubuntu、Windows 與 real DB smoke 全綠。 |
| 14:04 | Download / Import | **CHECKPOINT** | `1f2f0b7` | `26271344774` | Add collapsible Tk download plan panel | Tk 下載計畫面板新增「收合下載計畫 / 展開下載計畫」控制；收合時保留 header、計畫名稱、項目數與主要動作，只隱藏 cart/job tables，且不停止背景下載 queue 或改變 plan item state。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 local launcher skill；驗證包含 `py_compile`、Tk 目標測試、`--summary`、`git diff --check`、435 tests / 4 skipped 的完整模組測試；CI Ubuntu、Windows 與 real DB smoke 全綠。注意：本機 `unittest discover -s tests` 在 Python 3.13 會因 `tests/` 無 `__init__.py` 被判定不可匯入，這次用所有 `tests.test_*` 模組清單跑完整集合。 |
| 13:49 | Download / Import | **CHECKPOINT** | `d166544` | `26270881542` | Guide unsupported manual import formats | 改善手動本機檔案不支援格式的失敗引導。`api_launcher.manual_import` 現在拒絕 SQL、Excel、Parquet、Shapefile、NetCDF/HDF、ZIP/TAR 原始包等非 CSV/JSON 類格式時，會明確列出目前支援格式，並提示先轉成支援的 CSV/JSON 類檔案，或留在 adapter/manual review，不要硬塞進 SQLite。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 local skills；本機驗證包含 `py_compile`、22 個 manual/Tk 目標測試、433 個 unittest、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 13:38 | Download / Import | **CHECKPOINT** | `206026c` | `26270531326` | Show manual import provenance in Tk | 將手動本機檔案的 `provenance_review` 接到 Tk 成功對話框。UI 匯入完成後除了 table、rows/columns、manifest 與 SQLite path，也會顯示短版「來源審查」：來源類型、格式、安全邊界、不會執行的動作與下一步，讓使用者不用打開 manifest JSON 就能知道來源與授權仍需人工確認。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 local launcher skill；本機驗證包含 `py_compile`、21 個 Tk/manual 目標測試、432 個 unittest、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 13:28 | Download / Import | **CHECKPOINT** | `04f6276` | `26270185973` | Add provenance review for manual imports | 替手動本機檔案 manifest 增加 `metadata.provenance_review`，用中文固定記錄來源標籤、格式標籤、trust boundary、可安全執行的操作、被禁止的操作、授權/再散布 caveat 與下一步資料庫自檢建議。`--manual-import-json` 也會把這段 review 帶給 agent，讓自動化工具與初學使用者都能看懂「本機檔案只驗 checksum/匯入結果，不代表原始來源或授權已驗證」。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 local skills；本機驗證包含 `py_compile`、30 個 manual/CSV/JSON/UI 目標測試、431 個 unittest、CLI JSON smoke、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 13:17 | MVP Foundation | **CHECKPOINT** | `1b0eeef` | `26269844823` | Add agent JSON for manual local imports | 替手動本機檔案 manifest/匯入流程新增 `--manual-import-json`。`--write-local-file-manifest ... --manual-import-json` 與 `--import-local-file ... --manual-import-json` 現在會輸出單一 agent-readable JSON payload，包含 manifest、raw asset id、匯入 table、列數、欄位、schema fingerprint 與下一步 `--self-check-databases --self-check-databases-json` 建議；單獨使用 `--manual-import-json` 會報錯，避免 agent 收不到結果。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 local skills；本機驗證包含 `py_compile`、manual/CSV/JSON/UI 目標測試、430 個 unittest、CLI JSON smoke、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 13:01 | Download / Import | **CHECKPOINT** | `0f71e38` | `26269394868` | Add Tk local file import entry | 將手動本機檔案匯入接到 Tk UI：`資料庫 > 匯入本機 CSV/JSON 檔` 與「更多 > 匯入本機 CSV/JSON 檔」會開檔案選擇器，只處理使用者選的一個 CSV/JSON 類檔案，可輸入 table 名稱；UI 會先呼叫 `api_launcher.manual_import` 寫 sidecar manifest、登記 `manual_local_files` raw file asset，再重用既有 CSV/JSON importer 匯入 `state/curated_imports.sqlite`。若 table 已存在，UI 以 `unique_table_name()` 自動改名，例如 `weather_2`，不覆蓋、不刪檔、不掃資料夾。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 repo/local skill；本機驗證包含 `py_compile`、27 個 UI/manual/CSV/JSON 目標測試、428 個 unittest、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 12:46 | Download / Import | **CHECKPOINT** | `bc39603` | `26268962086` | Add local file manual import manifest flow | 新增 `api_launcher/manual_import.py` 與 CLI `--write-local-file-manifest` / `--import-local-file`，讓使用者自備本機 CSV/CSV.GZ/JSON/JSON.GZ/JSONL/NDJSON/GeoJSON 檔可以先建立 sidecar manifest、記錄 `file://` provenance、checksum 與 `source_format`，再用既有 CSV/JSON manifest importer 匯入 SQLite。預設 synthetic provider `manual_local_files` 會自動建立並登記 raw file asset；若指定真實 provider，要求該 provider 已存在以保護 provenance。此路徑不掃資料夾、不移動或刪除來源檔、不把本機檔當成可重排網路下載，也不覆蓋 table，除非使用者明確傳 replace。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 repo/local skill；本機驗證包含 `py_compile`、15 個 manual/CSV/JSON 目標測試、427 個 unittest、CLI 本機 CSV 匯入 smoke、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 12:24 | Agent Handoff / Observability | **CHECKPOINT** | `17cb2cb` | `26268307704` | Include data store profile in handoff report | 將目前作用中 data-store profile 帶進 `--handoff-report`，報告會列出 active profile、engine、store kind、必要 env var 名稱、一般測試指令、JSON 測試指令與 env 範本指令。handoff 只讀設定並列出命令，不跑連線測試、不觸發網路、不輸出 secret。同步 USER_GUIDE；本機驗證包含 `py_compile`、37 個 handoff/integration/data-store 目標測試、handoff smoke、422 個 unittest、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 12:16 | Database / Repair | **CHECKPOINT** | `a2f7296` | `26268094137` | Add active data store profile selection | 新增本機作用中 data-store profile 選擇：CLI `--set-active-data-store-profile PROFILE_ID` 會把 profile id 寫入 ignored `launcher_integrations.local.json`；Tk `整合 > 資料儲存連線` 會顯示目前作用中 profile，並可用「設為作用中」更新。這只保存 profile id，不保存密碼、不測連線、不修改 registry；後續 MySQL/SQLite/PostgreSQL UI/agent 流程可有明確預設 profile。同步 config example、USER_GUIDE、SETUP、TECHNICAL_OVERVIEW、GTD、handoff 與 repo/local skill；本機驗證包含 `py_compile`、45 個 integration/data-store/UI 目標測試、421 個 unittest、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 12:03 | Database / Repair | **CHECKPOINT** | `405f72a` | `26267753946` | Guide data store connection fixes in Tk | 將 data-store 連線測試的 backend `next_action` 接回 Tk `整合 > 資料儲存連線` dialog。UI 測試選取 profile 後，缺 env 會提示先按「寫出 env 範本」並本機填值，缺 optional driver 會提示只在專案環境安裝 driver，連線錯誤會提示檢查 host、port、database、帳號權限、網路與 driver 相容性；此變更只改善引導，不保存密碼、不連線寫庫、不修改 registry。同步 USER_GUIDE；本機驗證包含 `py_compile`、42 個 data-store/UI 目標測試、418 個 unittest、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 11:55 | Database / Repair | **CHECKPOINT** | `db604ae` | `26267530847` | Add data store connection JSON guidance | 新增 `--test-data-store-json`，讓 data-store 連線測試可輸出 agent-readable JSON，包含 profile、engine、status、details 與 `next_action`。缺環境變數時 `next_action` 指向 `--write-data-store-env-template ... --data-store-env-template-profile ...`；缺 optional driver 時只建議在專案環境安裝 driver；unsupported/error 也會給保守檢查方向。同步 USER_GUIDE、TECHNICAL_OVERVIEW、GTD、handoff 與 repo/local skill；本機驗證包含 `py_compile`、41 個 data-store/UI 目標測試、417 個 unittest、CLI JSON smoke、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 11:44 | Database / Repair | **CHECKPOINT** | `b10d2a9` | `26267195418` | Add data store env template export | 新增 data-store `.env` 範本輸出閉環：CLI 可用 `--write-data-store-env-template PATH --data-store-env-template-profile PROFILE_ID` 寫出 MySQL/PostgreSQL/SQLite 等 profile 的必要與選用環境變數清單；Tk `整合 > 資料儲存連線` 新增「寫出 env 範本」按鈕，輸出到 ignored `state/data_store_env_templates/`。範本只列變數名稱與空值，不保存、不顯示、不提交任何密碼或 token；同時同步 USER_GUIDE、SETUP、TECHNICAL_OVERVIEW、GTD、handoff 與 repo/local skill。本機驗證包含 `py_compile`、37 個 data-store/UI 目標測試、413 個 unittest、CLI env-template smoke、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 11:26 | Provider / yfinance | **CHECKPOINT** | `ba0b2e1` | `26266674074` | Add yfinance storage handoff to Tk review | 將 yfinance storage review 的 Tk dialog 擴充為一次產出 review JSON、必要時的 `.dry_run.sql` 與人類 / DBA 審查用 handoff Markdown。新增共用路徑 helper `yfinance_project_path_from_ui_text()`，讓 plan/review/handoff 三個輸出欄位都用同一個 project-root 基準，並在 UI 事件紀錄中加入 `handoff_path`、`dry_run=true` 與 `will_write_database=false`。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 repo/local skill；本機驗證 `PYTHONPYCACHEPREFIX` 下的 `py_compile`、31 個 yfinance/UI 目標測試、408 個 unittest、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 11:15 | Provider / yfinance | **CHECKPOINT** | `996cbce` | `26266329621` | Add yfinance storage handoff report | 新增 `--write-yfinance-storage-handoff ... --yfinance-storage-handoff-review ...`，可把 yfinance storage review JSON 轉成給人類 / DBA 審查的 Markdown。handoff 會列出 review JSON、target、engine、table、CSV 來源、symbols、dry-run SQL、execution guard、審查清單與下一步邊界，並拒絕非 `dry_run=true` 的 review payload；它仍只寫文件，不連線、不建表、不匯入、不代表批准執行。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 repo/local skill；本機驗證 `PYTHONPYCACHEPREFIX` 下的 `py_compile`、30 個 yfinance/UI 目標測試、407 個 unittest、CLI handoff smoke、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 10:59 | Provider / yfinance | **CHECKPOINT** | `89f637a` | `26265849867` | Add yfinance storage review UI entry | 把 yfinance storage review dry-run 接到 Tk `工具 > 產生 yfinance 儲存審查 dry-run`，UI 可讀取既有 plan 路徑、指定 review 輸出與審查目標，呼叫共用 adapter 產生 review JSON 與必要時的 `.dry_run.sql`。新增 `yfinance_storage_review_paths_from_ui()` helper，固定相對路徑基準並拒絕空路徑；事件紀錄明確寫入 `dry_run=true`、`will_write_database=false`。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 repo/local skill，提醒後續 agent 這仍是人工/DBA 審查交接，不會連線、建表、匯入、背景抓取或接 crawler。驗證包含 `PYTHONPYCACHEPREFIX` 下的 `py_compile`、27 個 yfinance/UI 目標測試、404 個 unittest、CLI yfinance review smoke、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 10:40 | Provider / yfinance | **CHECKPOINT** | `8d44e94` | `26265281226` | Add yfinance storage review dry-run | 替既有 yfinance plan 增加 `--write-yfinance-storage-review` 審查入口，可讀取 `--yfinance-storage-review-plan`，輸出 storage review JSON，並依 plan 內 storage target 或 `--yfinance-storage-review-target` 產生 MySQL、TimescaleDB/PostgreSQL、ClickHouse、Parquet/DuckDB 的 dry-run SQL/命令草稿。審查檔明確標記 `dry_run=true`、`will_write_database=false`、`requires_user_review=true`，launcher 不會連線、不會建表、不會匯入，也不會把 storage target metadata 當成自動執行。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 repo/local skill；本機驗證 `py_compile`、25 個 yfinance/UI 目標測試、402 個 unittest、CLI smoke、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 10:12 | Docs / Workflow | **CHECKPOINT** | `0f2e706` | `26264425447` | Document multi-checkpoint development turns | 依使用者要求，把「一輪對話可包含多個實質 checkpoint commit」固化到 handoff 與 repo/local launcher skill。規則改為：同一 MVP 主題下可連續推進多個相鄰小切片，但每個 commit 仍須可審查、可驗證、可由 CI 回溯；開發日誌要記錄每個已推送且驗證成功的實質 feature/fix/workflow/docs checkpoint，但 log-only commit 不再記錄，避免日誌遞迴。本輪為 workflow/docs checkpoint，`git diff --check` 通過；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 10:07 | Provider / yfinance | **CHECKPOINT** | `83d69a1` | `26264291720` | Add yfinance storage target metadata | 替 yfinance opt-in live CSV plan 加入 metadata-only storage target profiles：CLI 新增 `--yfinance-storage-target`，Tk live dialog 新增「儲存目標」下拉選單，plan/source/dataset/time_series_contract 可記錄 `auto`、`sqlite_mvp_table`、`mysql_timeseries_table`、`parquet_duckdb_archive`、`timescaledb_hypertable`、`clickhouse_ohlcv_table` 等目標建議。這些 target 只作資料護照與後續匯出/匯入規劃，不會自動寫 MySQL、Parquet、TimescaleDB、ClickHouse，不會背景刷新、排程、下載或匯入。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 repo/local skill；本機驗證 `py_compile`、22 個 yfinance/UI 目標測試、399 個 unittest、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 09:18 | Provider / yfinance | **CHECKPOINT** | `36fa6c2` | `26262760724` | Add yfinance query window presets | 替 yfinance opt-in live CSV plan 加入 chart-friendly query window presets：CLI 新增 `--yfinance-query-window`，Tk live dialog 新增「查詢視窗」下拉選單，可把 `intraday_5d_5m`、`daily_1mo`、`daily_6mo`、`weekly_1y` 轉成 `period`/`interval`、`chart_profile` 與 `storage_hint` metadata。`period`/`interval` 手動覆寫會記為 `manual_override`；query-window 只作 chart/storage metadata，不會自動刷新、下載、匯入、排程或 crawler live call。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 repo/local skill；本機驗證 `py_compile`、20 個 yfinance/UI 目標測試、397 個 unittest、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 08:15 | Provider / yfinance | **CHECKPOINT** | `f81847a` | `26260743271` | Add yfinance retention metadata controls | 替 yfinance opt-in live CSV plan 加入 `--yfinance-retention-days`，CLI/Tk UI 可把保留天數寫入 plan/source/dataset metadata 與 time-series contract；保留政策只作本機快取治理提示，不會自動刪檔、背景刷新、下載、匯入或 crawler live call。同步 USER_GUIDE、USER_MANUAL、GTD、handoff 與 repo/local skill；本機驗證 `py_compile`、17 個 yfinance/UI 目標測試、394 個 unittest、`--summary` 與 `git diff --check`；CI Ubuntu、Windows、real DB smoke 全部成功。 |
| 04:53 | Provider / yfinance | **CHECKPOINT** | `f31b481` | `26252519723` | Add yfinance plan actions to Tk UI | 把 yfinance 的離線 demo plan 與明確 opt-in live CSV plan 接到 Tk `工具` 選單。UI 只建立 plan 並加入下載計畫，後續仍由使用者按「開始」與「匯入」；live path 會要求 symbol/period/interval 與 unofficial personal/research 確認框，不會自動下載、背景排程、接 crawler 或在 CI 打 Yahoo。同步更新 USER_GUIDE、USER_MANUAL、GTD、handoff 與 repo/local skill；本機驗證包含 `py_compile`、16 個 yfinance/UI 針對性測試、393 個 unittest、`--summary` smoke 與 `git diff --check`，CI 的 Ubuntu、Windows 與 real DB smoke 全數成功。 |
| 04:29 | Docs / Workflow | **CHECKPOINT** | `ecfaa2b` | `26251323191` | Document data source access surfaces | 將 yfinance 延伸出的概念收斂成 `canonical source` metadata 模型：語言套件、REST、CLI、toolbox 與 database driver 不再各自變成 provider/source，而是以 `來源 / 語言或 runtime / 套件或工具名稱` 的關聯表掛到同一個背後資料庫/API。文件明確要求多語言 wrapper 取聯集合併，API key 不因公開套件而公開，adapter 的價值是把 JSON/CSV/DataFrame/SDMX/OData/paged/archive 等輸出轉成 manifest、checksum、schema/provenance、可匯入資料庫與可修復 registry record。同步更新 platform concepts、discovery notes、GTD、handoff 與 repo/local skill；本輪為文件/skill checkpoint，`git diff --check` 通過，CI 的 Ubuntu、Windows 與 real DB smoke 全數成功。 |
| 04:11 | Provider / yfinance | **CHECKPOINT** | `1b11b57` | `26250412357` | Add opt-in yfinance live plan | 新增 `--write-yfinance-live-plan` 與 `--yfinance-acknowledge-unofficial`，讓使用者可明確 opt-in 呼叫本機選用 `yfinance`，把 live OHLCV 結果寫成本機 CSV，再產生 file-backed download/import plan；此路徑保留非官方、personal/research-only 警告，不把 yfinance 做成 hard dependency，不接 crawler/background/CI live call。`api_launcher/adapters/yfinance.py` 補上 period/interval 驗證、time-series contract、CSV 欄位正規化、fake fetcher 測試入口與 plan metadata；CLI、USER_GUIDE、USER_MANUAL、架構/關聯圖、GTD、handoff 與 skill 同步更新。驗證包含 `py_compile`、391 個 unittest、yfinance adapter fixture/mock 測試、`git diff --check`，CI 的 Ubuntu、Windows 與 real DB smoke 全數成功。 |
| 03:41 | Database / Repair | **CHECKPOINT** | `10e3ea7` | `26248941703` | Add Tk SQL repair dry-run action | 將 MySQL/PostgreSQL manifest-backed missing table 的 dry-run SQL 產生接到 Tk `工具 > 修復 / 驗證資產` 的資料庫分頁，新增「產生 dry-run SQL」按鈕、輸出路徑顯示、成功事件紀錄與防誤用提示；路徑正規化抽成 `database_repair_sql_path_for_asset()`，讓 CLI/UI 共用同一個檔名 guard。此動作仍只寫 `state/database_repair/*.dry_run.sql`，不連線、不執行 SQL、不修改 registry。同步更新 USER_GUIDE、failure modes、TECH_STACK、GTD、handoff 與 repo/local skill；本機驗證包含 `py_compile`、52 個 database repair/self-check/UI 測試、387 個 unittest、`git diff --check`，CI 的 Ubuntu、Windows 與 real DB smoke 全數成功。 |
| 03:22 | Database / Repair | **CHECKPOINT** | `5b81689` | `26248005108` | Add SQL repair dry-run generation | 新增 `--write-database-repair-sql ASSET_ID`，讓 manifest-backed missing MySQL/PostgreSQL table 可產生 `state/database_repair/*.dry_run.sql` 供人工審核；此路徑只寫 SQL 檔，不連線、不執行 DDL/DML、不修改遠端資料庫或 registry。`--self-check-databases-json` 也會在安全條件成立時標示 `sql_dry_run_available=true`。同步更新 USER_GUIDE、failure modes、GTD、handoff 與 repo/local skill；本機驗證包含 `py_compile`、47 個 database repair/self-check 測試、384 個 unittest、`git diff --check`，CI 的 Ubuntu、Windows 與 real DB smoke 全數成功。 |
| 03:03 | Provider / yfinance | **CHECKPOINT** | `e4bd0a6` | `26247067553` | Add optional yfinance demo adapter | 新增 `Yahoo Finance via yfinance` provider seed、`YFinanceMarketDataAdapter` query-template contract，以及 `--write-yfinance-demo-plan` 離線 OHLCV CSV fixture plan。這條金融時間序列 Demo 可用既有 direct downloader 產生 manifest 並匯入 SQLite，保留 `event_time`、`symbol`、`received_at`、`ingest_run_id`、`revision` 等欄位；同時明確不把 `yfinance` 做成 hard dependency、不在 CI 打 Yahoo、不把 Yahoo Finance 資料視為商用可再散布來源。同步更新 catalog、使用手冊、架構/關聯圖、GTD、handoff 與 skill；本機驗證包含 25 個針對性測試、381 個 unittest、adapter discovery smoke、yfinance 離線下載匯入 smoke、`py_compile` 與 `git diff --check`，CI 的 Ubuntu、Windows 與 real DB smoke 全數成功。 |
| 02:38 | MVP Demo Closure | **CHECKPOINT** | `40ddb4b` | `26245778018` | Add UI MVP demo flow entry | 讓 Tk UI 新增 `工具 > 產生 MVP Demo Flow` 與上方 `更多 > 產生 MVP Demo Flow`，直接呼叫共用的 `api_launcher.mvp_demo`，寫出 `state/mvp_demo/*` 並把離線 direct plan 加入下方下載計畫；UI 不自動下載或匯入，仍由使用者明確按「開始」與「匯入」。同步更新使用手冊、交接、GTD 與 skill，並把 `Yahoo Finance via yfinance` 記為後續可選、非官方、personal/research 用途的金融 adapter 候選；本機驗證包含 5 個針對性測試、375 個 unittest、`py_compile` 與 `git diff --check`，CI 的 Ubuntu、Windows 與 real DB smoke 全數成功。 |
| 02:22 | MVP Pipeline | **CHECKPOINT** | `da0f4d8` | `26244973496` | Route UI plan imports through pipeline slice | 讓 Tk UI 的 `匯入可支援下載結果` 改走 `api_launcher/ingestion_pipeline.py` 的 `run_existing_download_import_slice()`，把已下載 manifest 驗證、downloaded asset 登錄、SQLite 匯入、逐項狀態與 rename table 規則收進共用 service，不再在 `launcher_ui.py` 內重寫一套匯入 loop；本機驗證包含 15 個針對性測試、375 個 unittest、`py_compile` 與 `git diff --check`，CI 的 Ubuntu、Windows 與 real DB smoke 全數成功。 |
| 01:55 | MVP Pipeline | **CHECKPOINT** | `ffa9f87` | `26243591715` | Add download import pipeline slice | 新增 `api_launcher/ingestion_pipeline.py` 作為 direct plan 的下載/匯入流程切片，讓 `core.py --run-download-plan` 透過同一個 service 執行下載、manifest 登錄、支援格式匯入、blocked next_action、stage 分類與 CLI 摘要渲染；本機驗證包含 15 個針對性測試、374 個 unittest、`py_compile` 與 `git diff --check`，CI 的 Ubuntu、Windows 與 real DB smoke 全數成功。 |
| 01:36 | MVP Demo Closure | **CHECKPOINT** | `73c2220` | `26242596720` | Add canonical MVP demo flow | 新增 `--write-mvp-demo-flow` 與 `api_launcher/mvp_demo.py`，可產生固定 flow manifest、Socrata adapter review plan、離線 JSON fixture 與離線 direct plan；本機驗證包含 adapter 解析、離線下載、manifest 驗證、SQLite 匯入、372 個 unittest、`py_compile` 與 `git diff --check`，CI 的 Windows、Ubuntu 與 real DB smoke 全數成功。 |
| 01:05 | Download / Import | **CHECKPOINT** | `7e7387f` | `26240992737` | Guide blocked download plans | 收束下載計畫的失敗閉環：`--run-download-plan` 現在會輸出 `skip_summary` 與 `next_action`，Tk UI 在沒有可直接下載項目時會引導使用者前往 Adapter 待辦或解析 Adapter 計畫。 |
| 00:50 | Discovery / Crawler | **CHECKPOINT** | `cacbffe` | `26240183046` | Document crawler asset concept | 將「爬蟲資產 / Crawler Asset」定位寫入平台概念、產品定位、探索筆記與架構文件，明確把它視為受治理的資料取得能力，而不是立刻新增大 registry。 |
| 00:37 | Docs / Workflow | **CHECKPOINT** | `04a4644` | `26239536548` | Document local tem staging folder | 將 `tem/` 的用途寫入 `WORKSPACE_LAYOUT`、`AGENT_HANDOFF` 與 `DOCS_INDEX`：它是本機暫存區、已被 Git 忽略，團隊協作者與 CI 看不到內容；若暫存素材要變成正式資產，必須提升到 canonical docs/source 後再提交。 |
| 00:34 | Docs / Workflow | **CHECKPOINT** | `8b61c2c` | `26239356382` | Ignore local tem staging folder | 將 `/tem/` 加入 `.gitignore`，並同步 repo/local Codex skill，明確禁止未經使用者要求就 `git add`、commit 或 push `tem/` 內容，也禁止讓正式流程依賴 `tem/` 路徑。 |
| 00:07 | Docs / Workflow | **CHECKPOINT** | `1b8f8db` | `26237944941` | Add repo-wide Chinese maintainer comments | 完成全 repo Python 註解工程：正式模組、Tk UI、scripts、renderer helper 與 tests 都補上繁中維護/測試意圖註解；本機驗證包含註解覆蓋檢查、AST 語法解析、`git diff --check` 與 368 個 unittest，CI 的 Ubuntu、Windows 與 real DB smoke 也成功。 |

### 2026-05-21

主線：補 heartbeat automation 與 Codex runner，串 library repair actions，收斂 Tk 啟動可見性，補程式關聯圖、Demo/使用者手冊、Mermaid 繁中規則、README.zh-TW、文件整理規則，並修正 resolver 對壓縮 JSON/GeoJSON 類資源格式的交接。

| 時間 | 開發階段 | 標記 | SHA | Run | 原始標題 | 中文說明 |
| --- | --- | --- | --- | --- | --- | --- |
| 22:40 | MVP Foundation | **CHECKPOINT** | `1c6e2b0` | `26233013484` | Preserve compressed JSON resource formats | 保留壓縮 JSON/GeoJSON/NDJSON 類 resource format，讓 `json.gz`、`jsonl.gz`、`ndjson.gz`、`geojson.gz` 等既有 importer 支援格式能正確進入下載與匯入計畫。 |
| 21:57 | Docs / Workflow | **CHECKPOINT** | `da35451` | `26230599073` | Consolidate docs refactor workflow | 收攏文件整理流程與繁中入口，並同步 repo/local Codex skill 的文件重構規則。 |
| 21:30 | Docs / Workflow | **CHECKPOINT** | `98c1ba0` | `26229130535` | Document Mermaid flow conventions | 把繁中 Mermaid 圖說規則文件化，要求繁中 `.md` 的可見節點與箭頭以中文為主。 |
| 21:27 | Docs / Workflow | **CHECKPOINT** | `a525a33` | `26228924048` | Map program flows and demo documentation | 補上程式關聯圖與 Demo/操作文件，讓模組調度、Demo 閉環與使用路徑更容易接手。 |
| 21:13 | MVP Hardening | **CHECKPOINT** | `8236fac` | `26228172868` | Polish Tk startup presentation | 收斂 Tk UI 啟動後的顯示、置前與 focus 行為，改善 IDE 或背景 shell 啟動時看不到視窗的問題。 |
| 08:47 | CI / Dev Workflow | **CHECKPOINT** | `dde1eed` | `26198656901` | Connect library repair actions to suggestions | 把 library repair actions 接到既有修復建議，讓 UI/agent 不必重建修復判斷邏輯。 |
| 08:18 | Agent Handoff / Observability | **CHECKPOINT** | `69dd22e` | `26197671241` | Add scheduled Codex heartbeat runner | 新增可由排程呼叫本機 Codex CLI 的 runner 入口，讓安全檢查通過後能自動推進下一步。 |
| 08:07 | Agent Handoff / Observability | **CHECKPOINT** | `5a6c539` | `26197318657` | Add heartbeat command wrappers | 新增 Windows 友善的 heartbeat `.cmd` wrapper，避免 PowerShell Execution Policy 擋住 `.ps1` 入口。 |
| 00:21 | Agent Handoff / Observability | **CHECKPOINT** | `c2f426a` | `26175532616` | Fix heartbeat planner JSON serialization | 修正 heartbeat planner 產生 JSON 時的循環參照問題，讓 plan payload 可被 CI 與外部工具穩定讀取。 |
| 00:15 | Agent Handoff / Observability | **CI 失敗** | `102994f` | `26175194234` | Add heartbeat automation entrypoints | 新增 heartbeat automation 的 CLI 與接力入口，作為排程推進的第一版實作；這筆當時 CI 失敗，後續修正 planner JSON 序列化。 |

### 2026-05-20

主線：強化 resolver resource metadata 判讀、UI import policy、archive import 格式、database repair UI/CLI、real MySQL/PostgreSQL smoke、event logs、handoff report 與資料資產平台文檔。

| 時間 | 開發階段 | 標記 | SHA | Run | 原始標題 | 中文說明 |
| --- | --- | --- | --- | --- | --- | --- |
| 23:30 | Repair / Asset Health | **CHECKPOINT** | `86ad74e` | `26172676367` | Log UI repair requeue events | 讓 Tk repair panel 的 requeue 操作寫入 event log，記錄 queued/blocked/failed 等結果。 |
| 23:10 | Download / Import | **CHECKPOINT** | `a929633` | `26171557234` | Log download manifest verification summaries | 讓下載 manifest verification 寫入結構化 event log，方便 handoff 與 UI/agent 追蹤檔案健康。 |
| 23:00 | Agent Handoff / Observability | **CHECKPOINT** | `b961e57` | `26171027033` | Enrich handoff report progress context | 擴充 handoff report 的進度上下文，加入 GTD focus、manifest/asset/verification timestamps 等資訊。 |
| 22:33 | MVP Foundation | **CHECKPOINT** | `d0e940d` | `26169451239` | Ignore Spectra local artifacts | 把 Spectra/OpenSpec GUI 的本機 artifact 加入忽略規則，避免開發工具產物被誤提交。 |
| 22:17 | Database / Repair | **CHECKPOINT** | `dc985fa` | `26168517850` | Add database registry repair CLI | 新增 database registry repair CLI，讓 agent 可執行 stop-tracking 或 guarded reimport 並輸出 JSON。 |
| 21:32 | Agent Handoff / Observability | **CHECKPOINT** | `3fdb7b0` | `26165912750` | Update handoff for agent transfer | 更新 agent handoff，記錄當前驗證、CI 與下一步，支援跨機器/跨 Agent 接力。 |
| 21:21 | Database / Repair | **CHECKPOINT** | `12419bb` | `26165313576` | Cover real DB schema drift smoke | 讓 real DB smoke 覆蓋 schema drift，透過受控 ALTER 驗證 fingerprint drift 能被偵測。 |
| 15:39 | MVP Foundation | **CHECKPOINT** | `cfe6999` | `26148529063` | Cover registry-backed real DB self-check | 讓 real DB smoke 覆蓋 registry-backed table self-check，確認 present/missing 狀態。 |
| 15:27 | Database / Repair | **CHECKPOINT** | `908b15a` | `26148007327` | Run real database smoke in CI | 在 GitHub Actions 加入 disposable MySQL/PostgreSQL service containers，讓真實 driver smoke 可在 CI 跑。 |
| 15:18 | Database / Repair | **CHECKPOINT** | `ffbd07f` | `26147585557` | Add opt-in real database smoke tests | 新增 opt-in 真實 MySQL/PostgreSQL smoke tests，預設跳過，只有設定 env vars 與 driver 時才連線。 |
| 15:08 | Database / Repair | **CHECKPOINT** | `0862d25` | `26147163057` | Mark safe database repair suggestions | 讓 database repair suggestion 精準標記哪些情況可以自動修復，降低誤操作風險。 |
| 14:58 | Download / Import | **CHECKPOINT** | `08c6dc6` | `26146744743` | Cover tar geojson archive imports | 補上 TAR.GZ 內 GeoJSON.GZ 匯入測試，確認 archive transform 能處理 GIS JSON 類資料。 |
| 14:44 | Download / Import | **CHECKPOINT** | `510e2b4` | `26146150793` | Expand archive import member formats | 擴充 ZIP/TAR archive importer 可抽出的 member 格式，讓壓縮包能接既有 SQLite 匯入路徑。 |
| 14:29 | MVP Foundation | **CHECKPOINT** | `8d6333a` | `26145565066` | Preserve compound source formats | 保存 compound source format，例如 csv.gz、jsonl、geojson.gz、zip、tar.gz，避免 provenance 被簡化成 unknown。 |
| 14:16 | Database / Repair | **CHECKPOINT** | `9435894` | `26145046980` | Expand guarded SQLite reimport formats | 擴充 SQLite guarded reimport 支援格式，納入更多 CSV/JSON/JSONL/GeoJSON 變體。 |
| 14:06 | Agent Handoff / Observability | **CHECKPOINT** | `81a26ee` | `26144697993` | Update handoff after import preference CI | 在 UI 匯入偏好測試通過後更新 handoff，保存驗證結果與接手狀態。 |
| 14:04 | Download / Import | **CHECKPOINT** | `c903db4` | `26144606542` | Remember UI import policy preference | 讓 UI 記住匯入策略偏好，減少使用者每次重選的摩擦。 |
| 13:50 | Agent Handoff / Observability | **CHECKPOINT** | `65cb251` | `26144124360` | Update handoff after import policy CI | 在 import policy CI 通過後更新 handoff，讓下一位 Agent 知道該切片已驗證。 |
| 13:48 | Download / Import | **CHECKPOINT** | `aca3de5` | `26144029986` | Add UI import policy selection | 讓 UI 可選擇匯入策略，為 keep/rename/replace 類操作提供明確入口。 |
| 08:49 | MVP Foundation | **CHECKPOINT** | `27dbce9` | `26134376662` | Add safe rename policy for plan imports | 新增 plan import 的安全改名策略，遇到既有 table 時建立新表而不是覆蓋。 |
| 08:19 | MVP Foundation | **CHECKPOINT** | `b9167a3` | `26133399043` | Install numpy for dev test coverage | 在開發測試環境補上 numpy，讓 renderer-light 或相關測試覆蓋可執行。 |
| 08:01 | Adapter / Resolver | **CHECKPOINT** | `9a79a6f` | `26132764678` | Read JSON-LD graph resource metadata | 讓 resolver 能讀取 JSON-LD `@graph` 包裝中的 resource metadata，避免漏掉嵌套下載線索。 |
| 07:47 | Adapter / Resolver | **CHECKPOINT** | `1231457` | `26132254701` | Support namespaced DCAT resource metadata | 支援帶 namespace 的 DCAT/schema.org resource metadata，例如 `dcat:downloadURL` 與 `schema:contentUrl`。 |
| 04:24 | Adapter / Resolver | **CHECKPOINT** | `077989c` | `26123107780` | Read distribution metadata in adapter resolver | 讓 adapter resolver 讀取 distribution 類 metadata，以便從資料目錄中找出可審核下載資源。 |
| 02:12 | CI / Dev Workflow | **CHECKPOINT** | `d8c6b18` | `26116211720` | Update CI actions for Node 24 | 更新 GitHub Actions 官方 action 版本，消除 Node 24 相關相容性風險。 |
| 02:06 | Docs / Workflow | **CHECKPOINT** | `91cd635` | `26115924978` | Add APIkeys collection Codex skills | 新增 APIkeys_collection 專案 Codex skills，讓開發與操作工作有固定接手規則。 |
| 01:41 | Download / Import | **CHECKPOINT** | `1f6f90d` | `26114595344` | Recognize common resource download metadata keys | 擴充 resolver 對常見下載 metadata 欄位的辨識，例如 downloadURL、contentUrl、fileUrl 等。 |
| 01:30 | Download / Import | **CHECKPOINT** | `672bfa4` | `26114036790` | Skip existing tables on plan import reruns | 讓 plan-driven import 重跑時預設跳過已存在 table，避免把重跑視為失敗或覆蓋資料。 |
| 01:13 | Concept / Roadmap | **CHECKPOINT** | `79cd466` | `26113102560` | Document data asset platform concepts | 補充資料資產平台概念，記錄 Discovery Tool、lakehouse/K8S、Render Studio、ML asset 與 connector 路線。 |
| 00:43 | Database / Repair | **CHECKPOINT** | `8a0d422` | `26111524365` | Reimport missing SQLite tables from manifests | 讓缺失的 SQLite table 可從健康 manifest 重新匯入，並維持不覆蓋既有表的安全 guard。 |
| 00:28 | Database / Repair | **CHECKPOINT** | `a7f19a9` | `26110723509` | Let repair UI stop tracking database assets | 讓修復 UI 可 registry-only 停止追蹤指定 database/table asset，不執行 SQL 或刪檔。 |
| 00:12 | Database / Repair | **CHECKPOINT** | `b3363e0` | `26109863663` | Let repair UI adjust database asset profiles | 讓修復 UI 可調整 database/table asset 的 profile/schema metadata，為多資料庫自檢做準備。 |
| 00:01 | Adapter / Resolver | **CHECKPOINT** | `4f2aafe` | `26109254273` | Resolve DataCite DOI content URLs narrowly | 讓 DataCite DOI 只在明確 `contentUrl` 且格式/大小安全時轉成 direct plan，避免把 DOI landing page 當檔案下載。 |

### 2026-05-19

主線：重構 crawler 模組，補 pagination/fetch/query helper，擴充 DataCite、OGC API Records、Socrata、OpenAlex、STAC、CMR、NCEI、Dataverse 等 crawler/resolver，並導入 OpenSpec workflow。

| 時間 | 開發階段 | 標記 | SHA | Run | 原始標題 | 中文說明 |
| --- | --- | --- | --- | --- | --- | --- |
| 23:43 | Adapter / Resolver | **CHECKPOINT** | `c5954c1` | `26108195045` | Resolve bounded NCEI search data files | 讓 NCEI Search data file 只在 dataset 加站點/空間條件且小於上限時提升為 direct file。 |
| 23:28 | Adapter / Resolver | **CHECKPOINT** | `13972f9` | `26107326025` | Resolve CMR granule data links narrowly | 讓 CMR granule data/download/enclosure links 只在格式與大小安全時提升為 direct asset。 |
| 23:06 | Adapter / Resolver | **CHECKPOINT** | `3f1eed7` | `26106052749` | Handle DataCite contentUrl resources | 讓 DataCite crawler 交出的 `contentUrl` resources 可被 generic resolver 安全判讀。 |
| 22:48 | Adapter / Resolver | **CHECKPOINT** | `7e30ff0` | `26104948281` | Guard CMR metadata links in adapter resolver | 在 CMR resolver 中保護 metadata/service/browse/opendap links，避免誤當 direct asset。 |
| 16:41 | Adapter / Resolver | **CHECKPOINT** | `d342677` | `26086226444` | Keep OGC metadata links in adapter review | 讓 OGC metadata/navigation links 留在 adapter review，只有明確 data/download link 才可能提升。 |
| 16:23 | Discovery / Crawler | **CHECKPOINT** | `005edd2` | `26085383980` | Keep OGC broker links out of primary API URLs | 避免把 OGC/WIS2 broker 或 notification link 當成 primary HTTP API URL。 |
| 16:07 | Discovery / Crawler | **CHECKPOINT** | `a455e47` | `26084647609` | Add WMO WIS2 OGC records source | 新增 WMO WIS2 Global Discovery Catalogue 作為 OGC API Records 真實 smoke source。 |
| 15:45 | Discovery / Crawler | **CHECKPOINT** | `95fd110` | `26083601605` | Support CMR feed entry JSON imports | 讓 JSON importer 支援 NASA CMR `feed.entry` 結構，能把 granule metadata 匯入 SQLite。 |
| 15:10 | Adapter / Resolver | **CHECKPOINT** | `478a261` | `26082086854` | Add bounded CMR granule sample resolver | 新增 CMR collection -> `page_size=1` granule metadata sample resolver。 |
| 14:32 | Docs / Workflow | **CHECKPOINT** | `20e0d79` | `26080506526` | Adopt OpenSpec workflow tooling | 導入 OpenSpec workflow tooling，為中大型變更建立 proposal/tasks/acceptance 的工作流。 |
| 13:25 | Adapter / Resolver | **CHECKPOINT** | `b164194` | `26078095042` | Add Dataverse candidate file resolver | 新增 Dataverse candidate file resolver，對 persistent id 做單次 latest-version lookup 並挑安全小檔。 |
| 12:14 | Discovery / Crawler | **CHECKPOINT** | `cfcecd8` | `26075781921` | Add OpenAlex dataset discovery crawler | 新增 OpenAlex Works dataset crawler，從研究 metadata 產生 dataset candidates。 |
| 11:36 | Adapter / Resolver | **CHECKPOINT** | `7a33062` | `26074628076` | Add bounded NCEI access data resolver | 新增 NCEI Access Data 有界查詢 resolver，要求 dataset/date/spatial 等條件才產生小樣本。 |
| 11:08 | Discovery / Crawler | **CHECKPOINT** | `e22740d` | `26073749709` | Cover Socrata candidate plan resolution | 補上 Socrata candidate -> bounded plan 的測試/覆蓋，確認不會做 unsafe full-table download。 |
| 10:55 | Agent Handoff / Observability | **CHECKPOINT** | `b54534d` | `26073331125` | Add Socrata catalog dataset crawler | 新增 Socrata catalog crawler，從城市/open data portal 找出 resource candidates。 |
| 09:59 | Discovery / Crawler | **CHECKPOINT** | `2da6882` | `26071556561` | Add OGC API Records dataset crawler | 新增 OGC API Records crawler，支援 WIS2 等 records catalog 的 metadata candidates。 |
| 09:30 | Agent Handoff / Observability | **CHECKPOINT** | `854af3f` | `26070617504` | Clarify handoff checkpoint reference | 釐清 handoff checkpoint 引用方式，避免下一位 Agent 依賴過期聊天紀錄。 |
| 09:27 | Agent Handoff / Observability | **CHECKPOINT** | `919f24b` | `26070538931` | Update handoff checkpoint | 更新 handoff checkpoint，記錄當前 crawler/resolver 進度與驗證狀態。 |
| 08:34 | Adapter / Resolver | **CHECKPOINT** | `d0b3ca8` | `26068790700` | Add NCEI bounded search resolver | 新增 NOAA/NCEI Search bounded resolver，把 search candidate 轉成小型 metadata sample。 |
| 08:17 | Adapter / Resolver | **CHECKPOINT** | `199b94e` | `26068208282` | Add Socrata bounded sample resolver | 新增 Socrata/SODA bounded sample resolver，把 resource/API metadata 轉成 `$limit=25` 小樣本。 |
| 07:40 | Adapter / Resolver | **CHECKPOINT** | `c1b1719` | `26066899140` | Add CKAN bounded package resolver | 新增 CKAN package_show bounded resolver，只做單次 metadata lookup 並提升安全 direct resources。 |
| 07:27 | Discovery / Crawler | **CHECKPOINT** | `01f6ae0` | `26066406408` | Centralize crawler source registry | 集中 crawler source registry，讓 supported source types 與 dispatcher 對齊。 |
| 07:11 | Discovery / Crawler | **CHECKPOINT** | `ece3043` | `26065821569` | Add DataCite dataset crawler | 新增 DataCite DOI search crawler，產生 metadata-first dataset candidates。 |
| 06:56 | Discovery / Crawler | **CHECKPOINT** | `0d0120a` | `26065239194` | Move crawler source fetch flows | 把 source-level fetch/parse flows 移出 dispatcher，讓 `dataset_sources.py` 更接近純註冊表。 |
| 06:44 | Discovery / Crawler | **CHECKPOINT** | `c7dd6c6` | `26064775791` | Extract crawler query URL helpers | 拆出 crawler query URL builder helper，讓 source-specific 查詢 URL 組裝集中管理。 |
| 04:21 | Discovery / Crawler | **CHECKPOINT** | `81285fd` | `26058221654` | Update crawler pagination docs | 更新 crawler pagination 文件，記錄各 source crawler 的分頁責任與安全邊界。 |
| 04:18 | Discovery / Crawler | **CHECKPOINT** | `4fae862` | `26058098589` | Extract NCEI pagination flow | 把 NOAA/NCEI pagination flow 拆出，讓 Search API full crawl 有清楚上限與去重規則。 |
| 04:11 | Discovery / Crawler | **CHECKPOINT** | `7244209` | `26057753176` | Extract Zenodo pagination flow | 把 Zenodo records pagination flow 拆出，讓 Zenodo crawler 的分頁與解析分工清楚。 |
| 04:05 | Discovery / Crawler | **CHECKPOINT** | `3b05a2b` | `26057447812` | Extract Dataverse pagination flow | 把 Dataverse pagination flow 拆出，讓 Dataverse search crawler 可獨立演進。 |
| 04:00 | Discovery / Crawler | **CHECKPOINT** | `509025e` | `26057191504` | Extract GBIF pagination flow | 把 GBIF dataset search pagination flow 拆出，降低 GBIF crawler 與 dispatcher 耦合。 |
| 03:54 | Discovery / Crawler | **CHECKPOINT** | `eedbcb6` | `26056912411` | Extract CKAN pagination flow | 把 CKAN pagination flow 拆出，統一 package_search 類 source 的分頁處理。 |
| 03:49 | Discovery / Crawler | **CHECKPOINT** | `8beb6d6` | `26056658366` | Extract CMR pagination flow | 把 NASA CMR pagination flow 拆出，讓 CMR collection/granule 抓取更容易維護。 |
| 03:43 | Discovery / Crawler | **CHECKPOINT** | `e77f873` | `26056377268` | Extract STAC pagination flow | 把 STAC pagination flow 拆出，讓 STAC crawler 的多頁抓取邏輯獨立可測。 |
| 03:34 | Discovery / Crawler | **CHECKPOINT** | `23ce25a` | `26055903367` | Extract crawler pagination helpers | 把 full-crawl page cap 與候選去重 append helper 拆出，統一各 crawler 的分頁邊界。 |
| 03:28 | Discovery / Crawler | **CHECKPOINT** | `92d41e0` | `26055610460` | Extract crawler fetch helpers | 把 crawler 共用 HTTP fetch、JSON validation 與 URL helper 拆出，降低 source crawler 重複碼。 |

### 2026-05-18

主線：把下載結果推進到 CSV/JSON SQLite 匯入，記錄 resident/mobile/P2P/Hadoop/K8S 路線，建立 crawler-first dataset discovery、candidate review、plan import、adapter review queue 與 bounded resolver。

| 時間 | 開發階段 | 標記 | SHA | Run | 原始標題 | 中文說明 |
| --- | --- | --- | --- | --- | --- | --- |
| 20:33 | Discovery / Crawler | **CHECKPOINT** | `545a2f0` | `26033730560` | Extract NCEI crawler parser | 拆出 NCEI crawler parser，讓 NOAA/NCEI Search payload parsing 可獨立測試。 |
| 20:23 | Discovery / Crawler | **CHECKPOINT** | `8e758fc` | `26033258410` | Extract HTML index crawler parser | 拆出 HTML index crawler parser，讓檔案索引頁解析與其他 crawler 分離。 |
| 20:17 | Discovery / Crawler | **CHECKPOINT** | `928d730` | `26032973671` | Extract Zenodo crawler parser | 拆出 Zenodo crawler parser，讓 Zenodo records payload parsing 可獨立維護。 |
| 20:11 | Discovery / Crawler | **CHECKPOINT** | `46dd25f` | `26032682072` | Extract Dataverse crawler parser | 拆出 Dataverse crawler parser，讓 Dataverse search payload parsing 可獨立維護。 |
| 20:05 | Discovery / Crawler | **CHECKPOINT** | `133782c` | `26032390536` | Extract GBIF crawler parser | 拆出 GBIF crawler parser，讓 GBIF dataset payload parsing 可獨立維護。 |
| 19:57 | Discovery / Crawler | **CHECKPOINT** | `a55eb32` | `26032040223` | Extract CMR crawler parser | 拆出 CMR crawler parser，讓 NASA CMR collection payload parsing 可獨立測試。 |
| 19:51 | Discovery / Crawler | **CHECKPOINT** | `b5cbfe4` | `26031746771` | Extract ERDDAP crawler parser | 拆出 ERDDAP crawler parser，讓 allDatasets payload parsing 可獨立維護。 |
| 19:45 | Discovery / Crawler | **CHECKPOINT** | `b3a1e21` | `26031476832` | Extract CKAN crawler parser | 拆出 CKAN crawler parser，讓 package_search payload parsing 可獨立測試。 |
| 19:38 | Discovery / Crawler | **CHECKPOINT** | `3b619ec` | `26031151955` | Extract STAC crawler parser | 拆出 STAC crawler parser，讓 STAC payload parsing 可獨立測試。 |
| 19:28 | Discovery / Crawler | **CHECKPOINT** | `0689340` | `26030671026` | Split crawler shared types | 拆出 crawler shared types，讓 DatasetDiscoverySource/DatasetCandidate 從 dispatcher 分離。 |
| 19:10 | Agent Handoff / Observability | **CHECKPOINT** | `f693923` | `26029840457` | Organize documentation handoff map | 整理 documentation handoff map，建立文件索引與接手閱讀順序。 |
| 18:58 | Adapter / Resolver | **CHECKPOINT** | `ff540bd` | `26029319639` | Add bounded STAC item resolver | 新增 STAC collection -> `limit=1` item-search GeoJSON sample resolver。 |
| 18:30 | Agent Handoff / Observability | **CHECKPOINT** | `4554612` | `26027998403` | Add workspace inventory and handoff summaries | 新增 workspace inventory 與 handoff summaries，幫助整理檔案與跨 Agent 接力。 |
| 16:51 | MVP Foundation | **CHECKPOINT** | `6b2a745` | `26023315124` | Promote audited local discovery sources to catalog | 把 crawler audit 通過且無 warning 的 local discovery sources 提升到正式 catalog。 |
| 16:20 | MVP Foundation | **CHECKPOINT** | `741de74` | `26021879309` | Promote portal intake drafts to local discovery config | 把通過基本檢查的 portal intake drafts 提升到 ignored local discovery config。 |
| 15:27 | MVP Foundation | **CHECKPOINT** | `dd65a2b` | `26019610366` | Parse portal intake sheet into review payloads | 把 portal intake Markdown 表解析成 review JSON，分出 provider seed、dataset source 與 adapter backlog。 |
| 15:05 | MVP Foundation | **CHECKPOINT** | `16354f6` | `26018721608` | Add portal intake sheet and bounded repository resources | 新增 portal intake 表與 bounded repository resource 記錄，讓團隊可提交候選入口。 |
| 14:17 | Discovery / Crawler | **CHECKPOINT** | `afd3df5` | `26016912397` | Expand dataset discovery sources and repository crawlers | 擴充 dataset discovery sources 與 repository crawlers，增加正式 catalog 中可爬入口。 |
| 13:49 | Adapter / Resolver | **CHECKPOINT** | `c181974` | `26015980242` | Resolve ERDDAP candidates to bounded sample plans | 讓 ERDDAP candidates 可解析成 bounded sample CSV plan，用小樣本測試下載/匯入閉環。 |
| 13:19 | MVP Hardening | **CHECKPOINT** | `38c44ea` | `26015031497` | Improve launcher drawer and dataset discovery visibility | 改善 launcher detail drawer 與 dataset discovery 可見性，讓 crawler-imported dataset rows 更容易被看見。 |
| 12:38 | Adapter / Resolver | **CHECKPOINT** | `09d9098` | `26013846029` | Resolve direct links from adapter plans | 讓 adapter plan resolver 處理更多 direct link 欄位與 metadata 包裝。 |
| 12:31 | Adapter / Resolver | **CHECKPOINT** | `a4cfd18` | `26013618401` | Expose adapter plan resolver in Tk UI | 把 adapter plan resolver 暴露到 Tk UI，讓使用者可從介面解析待辦計畫。 |
| 12:18 | Adapter / Resolver | **CHECKPOINT** | `92880f9` | `26013242152` | Resolve direct resources from adapter plans | 讓 adapter plan resolver 從 resources metadata 中找出安全 direct resources。 |
| 11:55 | Download / Import | **CHECKPOINT** | `909c5f5` | `26012625890` | Import supported files from archives | 新增 bounded archive transform，可從 ZIP/TAR 抽第一個支援檔案再接 SQLite importer。 |
| 11:43 | Adapter / Resolver | **CHECKPOINT** | `234f237` | `26012296875` | Add adapter review queue for plans | 新增 Adapter review queue，讓 CLI/Tk 可列出需要 adapter 的 plan items。 |
| 11:26 | Adapter / Resolver | **CHECKPOINT** | `517b10f` | `26011837192` | Add adapter review hints to dataset plans | 在 non-direct 或需轉換的 plan entries 加入 adapter_review handoff hints。 |
| 11:15 | Download / Import | **CHECKPOINT** | `355722a` | `26011548780` | Avoid UI import table overwrite | 避免 UI 匯入覆蓋既有 table，預設以安全改名建立新表。 |
| 11:05 | Download / Import | **CHECKPOINT** | `1345e9b` | `26011302588` | Show UI import readiness for plan items | 在 UI cart/job table 顯示 import readiness、狀態與 target table hints。 |
| 10:28 | Download / Import | **CHECKPOINT** | `83e98a5` | `26010301302` | Add guided UI import for plan results | 新增 UI guided import，讓使用者從下載 job/table 狀態進行驗證後匯入。 |
| 10:12 | MVP Hardening | **CHECKPOINT** | `913ca95` | `26009883696` | Support dataset-version UI plan items | 讓 Tk cart 支援 dataset/version plan items，避免同一 provider 多 dataset 互相覆蓋。 |
| 09:52 | Download / Import | **CHECKPOINT** | `2dadf51` | `26009360013` | Import supported download plan results | 讓 `--run-download-plan` 可在驗證後匯入支援的 CSV/JSON/GeoJSON 結果。 |
| 09:39 | Discovery / Crawler | **CHECKPOINT** | `e189b0c` | `26008990109` | Export crawler candidates as dataset plans | 讓 crawler candidates 可輸出為 dataset-version download/import plan。 |
| 09:05 | Discovery / Crawler | **CHECKPOINT** | `eb91ec4` | `26008124730` | Add concurrent dataset crawler auditing | 新增並行 dataset crawler auditing，並對 0 筆/過低/metadata 不完整結果產生 warning。 |
| 08:12 | MVP Hardening | **CHECKPOINT** | `68fe616` | `26006798788` | Organize launcher modules and docs | 整理 launcher 模組與文件，讓 API launcher core、UI 與 docs 分工更清楚。 |
| 07:53 | Docs / Workflow | **CHECKPOINT** | `863ed0a` | `26006389536` | Add dataset candidate review workflow | 新增 dataset candidate review workflow，讓 crawler candidates 可被批准、拒絕或加入計畫。 |
| 06:47 | MVP Hardening | **CHECKPOINT** | `1418445` | `26004971266` | Close Gemini description integration loop | 打通 Gemini provider description integration loop，讓保存的 API key 可供摘要生成使用。 |
| 06:06 | MVP Hardening | **CHECKPOINT** | `2d9a155` | `26004061132` | Reject invalid Google OAuth client IDs | 拒絕格式不合法的 Google OAuth client id，避免重複觸發 invalid_client。 |
| 05:55 | MVP Hardening | **CHECKPOINT** | `e1cfbaa` | `26003811483` | Clarify Google OAuth setup flow | 釐清 Google OAuth setup flow，避免一般使用者被要求貼 developer client id。 |
| 05:48 | MVP Hardening | **CHECKPOINT** | `6e353bd` | `26003673538` | Add browser-based Google OAuth login | 新增 browser-based Google OAuth login 草案，後續因正式 OAuth app 需求而收斂。 |
| 05:19 | MVP Hardening | **CHECKPOINT** | `1b86544` | `26003014490` | Add user-facing Google QR login setup | 新增使用者面向的 Google QR login setup 草案，後續收斂為中期/開發者路線。 |
| 05:07 | Discovery / Crawler | **CHECKPOINT** | `aaf0ab1` | `26002746161` | Expand crawler-driven dataset sources | 擴充 dataset discovery sources，增加更多 metadata catalog 入口。 |
| 01:20 | Discovery / Crawler | **CHECKPOINT** | `d7bfef5` | `25997562969` | Add crawler-driven dataset discovery | 新增 crawler-driven dataset discovery 主線，讓 provider/source 能產生 reviewable dataset candidates。 |
| 00:43 | Concept / Roadmap | **CHECKPOINT** | `79707ba` | `25996713812` | Reserve Hadoop and Kubernetes pipeline ports | 預留 Hadoop/HDFS/Hive/Spark 與 K8S orchestration 對接契約，不納入當前 MVP 依賴。 |
| 00:31 | Database / Repair | **CHECKPOINT** | `a844fd0` | `25996450926` | Import verified JSON manifests to SQLite | 新增從健康 JSON/JSONL/GeoJSON manifest 匯入 curated SQLite table 的路徑。 |
| 00:23 | Concept / Roadmap | **CHECKPOINT** | `b14b458` | `25996247479` | Record P2P dataset node concept | 記錄 P2P dataset node 概念，限定 public dataset 且需授權與 checksum guard。 |
| 00:17 | Concept / Roadmap | **CHECKPOINT** | `7b4e8c0` | `25996119286` | Record mobile companion app direction | 記錄 mobile companion app 方向，未來手機只做安全遠端控制與狀態查看。 |
| 00:13 | Concept / Roadmap | **CHECKPOINT** | `05dc758` | `25996017684` | Record resident desktop app roadmap | 記錄 resident desktop app 路線，規劃未來 system tray/menu bar 與長駐 worker。 |
| 00:08 | Download / Import | **CHECKPOINT** | `5d78925` | `25995903858` | Batch import verified CSV manifests | 新增批次匯入健康 CSV manifests 的 CLI，預設跳過不支援、不健康或已匯入項目。 |
| 00:02 | Database / Repair | **CHECKPOINT** | `3ef4269` | `25995759465` | Import verified CSV manifests to SQLite | 新增從健康 CSV/CSV.GZ manifest 匯入 curated SQLite table 的路徑。 |

### 2026-05-17

主線：建立跨平台 CI 與 SQLite/Windows 修復守則，補上 manifest/repair/data-store 自檢、Tk repair panel、AI/Gemini 入口、下載 manifest 與直接下載計畫執行。

| 時間 | 開發階段 | 標記 | SHA | Run | 原始標題 | 中文說明 |
| --- | --- | --- | --- | --- | --- | --- |
| 23:54 | Download / Import | **CHECKPOINT** | `a9c0117` | `25995569527` | Run direct download plan entries | 讓 CLI 可執行 direct download plan entries，驗證 sidecar manifest 並註冊健康 payload。 |
| 23:44 | Adapter / Resolver | **CHECKPOINT** | `75a9d54` | `25995328804` | Export adapter dataset download plans | 讓 adapter-discovered dataset versions 可輸出為 dataset download plans。 |
| 23:24 | MVP Foundation | **CHECKPOINT** | `12dd16b` | `25994869232` | Register verified downloads as file assets | 把健康下載 manifest 註冊為 managed file assets，閉合 direct download -> registry ownership 路徑。 |
| 23:15 | MVP Foundation | **CHECKPOINT** | `370cd98` | `25994644779` | Classify realtime dataset updates | 分類 realtime/append-only dataset update 策略，避免把時間序列資料當靜態版本跳過。 |
| 23:07 | Database / Repair | **CHECKPOINT** | `6ccbb68` | `25994470084` | Add per-asset data store profiles | 新增 per-asset data store profile 欄位，讓不同 database/table asset 可指定不同連線設定。 |
| 22:44 | Repair / Asset Health | **CHECKPOINT** | `915dd44` | `25993937240` | Add JSON download repair report | 新增 JSON download repair report，讓 agent/UI 可讀取 manifest issues 與 requeue plan。 |
| 22:37 | MVP Foundation | **CHECKPOINT** | `6d9aee1` | `25993760759` | Reuse verified HTTP downloads | 讓 HTTP downloader 在 target file 與 sidecar manifest 已驗證時重用檔案，避免重複下載。 |
| 22:16 | MVP Hardening | **CHECKPOINT** | `f33bd6e` | `25993288423` | Refresh user-facing launcher docs | 更新使用者面向 launcher 文件，讓 UI/操作流程說明更貼近現況。 |
| 22:11 | MVP Hardening | **CHECKPOINT** | `61a4478` | `25993177475` | Add AI profile QR login and provider icons | 新增 AI profile QR login 草案與 provider icons，後續收斂為更清楚的 AI/profile UX。 |
| 21:19 | MVP Hardening | **CHECKPOINT** | `34e1556` | `25992000198` | Show resize cursor for Tk table columns | 在 Tk table columns hover 時顯示 resize cursor，讓手動調欄寬更直覺。 |
| 21:12 | MVP Hardening | **CHECKPOINT** | `b3dd868` | `25991847511` | Improve Tk detail drawer layout | 改善 Tk detail drawer layout，讓詳細資訊區更可讀且不壓縮主列表。 |
| 20:58 | Renderer / Roadmap | **CHECKPOINT** | `3267287` | `25991520833` | Handle cross-platform Unreal paths on macOS | 處理 macOS 上看到 Windows Unreal path 時的跨平台警告/忽略邏輯，避免 startup 阻塞。 |
| 20:47 | Database / Repair | **CHECKPOINT** | `8f96316` | `25991283090` | Localize repair UI and show database issues | 本地化 repair UI，並在 UI 顯示 database issues 與修復建議。 |
| 20:31 | Database / Repair | **CHECKPOINT** | `ffbfb7c` | `25990910893` | Add database self-check repair suggestions | 新增 database self-check repair suggestions，讓問題有穩定 suggestion IDs 與修復方向。 |
| 20:21 | CI / Dev Workflow | **CHECKPOINT** | `c690b27` | `25990701428` | Document CI and SQLite handoff safeguards | 補充 CI 與 SQLite handoff safeguards，提醒後續使用 contextlib.closing 等安全做法。 |
| 20:18 | CI / Dev Workflow | **CHECKPOINT** | `6d9612f` | `25990628206` | Close SQLite probes for Windows CI | 修正短生命週期 SQLite probes，確實關閉 connection，解掉 Windows CI 檔案鎖。 |
| 20:07 | Database / Repair | **CI 失敗** | `4bb5b18` | `25990396833` | Verify SQL table assets from install ownership | 嘗試依 install ownership 驗證 SQL table assets；這筆當時 CI 失敗，後續由 SQLite probe 修正收斂。 |
| 19:59 | Database / Repair | **CI 失敗** | `3fea8d4` | `25990224293` | Add SQL information schema introspection helpers | 嘗試新增 SQL information_schema helpers；這筆當時 CI 失敗，後續修復 Windows SQLite 檔案鎖問題。 |
| 19:53 | Database / Repair | **CI 失敗** | `179d1e4` | `25990089504` | Add SQLite table asset self-checks | 嘗試新增 SQLite table asset self-checks；這筆當時 CI 失敗，後續由 connection close 修復。 |
| 19:45 | Database / Repair | **CI 失敗** | `39722f3` | `25989914834` | Detect SQLite database schema drift | 嘗試偵測 SQLite database schema drift；這筆當時 CI 失敗，後續修正 SQLite probe 關閉方式。 |
| 19:39 | Repair / Asset Health | **CI 失敗** | `73e7e98` | `25989799370` | Add repair and data-store self-check workflows | 嘗試新增 repair 與 data-store self-check workflows；這筆當時 CI 失敗，後續集中修復 SQLite 連線/Windows 檔案鎖。 |
| 17:29 | Repair / Asset Health | **CHECKPOINT** | `625a910` | `25987126632` | Add Tk manifest repair panel | 新增 Tk manifest repair panel，讓使用者可在 UI 檢查 manifest 與重新排下載。 |
| 17:25 | MVP Hardening | **CHECKPOINT** | `e439210` | `25987046110` | Share library action helpers with Tk UI logs | 讓 Tk UI logs 共用 library action helpers，避免 UI 另寫 action policy。 |
| 17:18 | Agent Handoff / Observability | **CHECKPOINT** | `b18fa87` | `25986903749` | Add cross-agent handoff card | 新增跨 Agent handoff card，固定接手時要讀的狀態、雷點與驗證流程。 |
| 17:12 | Database / Repair | **CHECKPOINT** | `0d51022` | `25986787195` | Consolidate data store connection structure | 收攏 data store connection 結構，建立 profile/test-result/env-var mapping 的單一契約。 |
| 16:55 | MVP Hardening | **CHECKPOINT** | `e063ff3` | `25986434233` | Enable Gemini summary invocation path | 打開 Gemini summary invocation path，讓 AI provider description 能被實際呼叫。 |
| 16:48 | MVP Hardening | **CHECKPOINT** | `ffb20c3` | `25986286696` | Add account link and AI prompt contracts | 新增 account link 與 AI prompt contracts，定義摘要生成與帳號/credential 邊界。 |
| 16:43 | MVP Hardening | **CHECKPOINT** | `139a712` | `25986179526` | Refine Tk launcher and Gemini login skeleton | 調整 Tk launcher 與 Gemini login skeleton，改善 UI 結構與 AI 摘要入口。 |
| 16:29 | MVP Foundation | **CHECKPOINT** | `01b5d81` | `25985891413` | Add Steam-like library action skeleton | 新增 Steam-like library action skeleton，定義 install/update/repair/open/render 等 action 方向。 |
| 16:24 | Renderer / Roadmap | **CHECKPOINT** | `12f63aa` | `25985796665` | Add frontend contract inspection hooks | 新增 frontend contract inspection hooks，讓前端/renderer contract 可被檢查。 |
| 16:17 | Renderer / Roadmap | **CHECKPOINT** | `c880ae5` | `25985640519` | Add renderer frontend pipeline skeleton | 新增 renderer frontend pipeline skeleton，建立 Taichi/Unreal/Cesium 類 renderer bridge 概念。 |
| 15:33 | Renderer / Roadmap | **CHECKPOINT** | `5a06622` | `25984763083` | Add Unreal bridge planning skeleton | 新增 Unreal bridge planning skeleton，記錄未來虛擬地球/frontend 對接方向。 |
| 15:21 | Download / Import | **CHECKPOINT** | `54de4ec` | `25984516437` | Add manifest listing CLI | 新增 manifest listing CLI，讓下載/manifest 狀態可由命令列檢視。 |
| 15:19 | CI / Dev Workflow | **CHECKPOINT** | `304af0e` | `25984470572` | Update project state after manifest and CI work | 在 manifest 與 CI 工作後更新 project state，保存當時進度與驗證結果。 |
| 15:16 | Database / Repair | **CHECKPOINT** | `b2e0a2f` | `25984424650` | Make path tests independent of local SQLite state | 讓 path tests 不依賴本機 SQLite 狀態，修復跨平台 CI 不穩定因素。 |
| 15:15 | MVP Foundation | **CI 失敗** | `ec4dcd2` | `25984393356` | Add Chinese cross-platform setup notes | 新增中文跨平台 setup notes；這筆當時 CI 失敗，後續與 CI/path 修復一起收斂。 |
| 15:13 | CI / Dev Workflow | **CI 失敗** | `a6669f5` | `25984353375` | Add cross-platform CI workflow | 嘗試新增跨平台 CI workflow；這筆當時 CI 失敗，後續由路徑測試與 SQLite 連線關閉修復。 |

## 目前已知風險

- 本機 `git log` 在 2026-05-20 附近仍會碰到缺失 object `aca3de5bb67649e291223106e927a144fe403a9a`，這與先前 handoff 記錄的雲端同步損傷吻合；本次回補改以 GitHub Actions push run list 作為時間線來源。
- 先前未追蹤的 `APIkeys_collection (1).py` 重複副本已移除；目前不再列為待處理風險。
- `docs/PROJECT_STATE.md` 是長篇歷史狀態快照；最新狀態應以本開發日誌、`PROJECT_GTD.md`、`AGENT_HANDOFF.zh-TW.md`、`git log` 可讀範圍與 GitHub Actions 為準。

## 2026-05-27 Architecture / Docs checkpoint

| 時間 | 分類 | 狀態 | SHA | Run | Commit | 摘要 |
| --- | --- | --- | --- | --- | --- | --- |
| 10:46 | Discovery / Seed Registry | **CHECKPOINT** | `0bb9cf5` | `26487560257` | Add seed remote pagination contract | `CrawlerAssetListingResult` 新增 `remote_pagination_status`、`remote_exhausted`、`remote_next_page_token`，並輸出不含 raw token 的 `remote_pagination` payload；`seed_enumeration` 新增 `completion_confidence`，把 `local_limit_only` 與 `remote_reported_exhausted` 分開，避免 UI 把本機 `max_results` 上限誤認為遠端已完整枚舉。驗證：`py -3 -B -m py_compile api_launcher\crawler_asset_service.py tests\test_crawler_assets.py` OK；`py -3 -B -m unittest tests.test_crawler_assets -v` 37 tests OK；`py -3 -B -m unittest tests.test_crawler_assets tests.test_web_preview tests.test_tk_dialogs -v` 110 tests OK；`scripts\pre_push_smoke_brief.cmd` 746 tests / 4 skipped + MVP demo smoke `download_import_completed` / `row_count=3`；GitHub Actions run `26487560257` Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
| 10:32 | Architecture / Docs | **CHECKPOINT** | `8a2fd41` | `26487098990` | Record declarative architecture decision | 新增 `docs/DECLARATIVE_ARCHITECTURE_DECISION.zh-TW.md`，固定「宣告式架構是第二階段收斂方向，不是第一階段全面重寫理由」；第一階段仍維持 `seed -> crawler -> candidate -> plan -> download -> import -> UI`，現有 Python adapter / service / registry 先保持可測、可用、可交付。同步更新 GTD、handoff 與 docs index。驗證：`git -C K:\APIkeys_collection diff --check` OK（僅既有 CRLF warning）；GitHub Actions run `26487098990` Ubuntu / `windows-2025-vs2026` / real DB smoke success。 |
